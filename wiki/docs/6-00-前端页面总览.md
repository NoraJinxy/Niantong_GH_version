# 6-00 前端页面总览

> 本页参考旧 `4-00 前端页面总览`，是 v2 的路由、组件、API 客户端和页面状态入口。

<div class="elys-meta" markdown>

定位
: 前端路由、组件、API 调用映射

事实源
: `frontend/elys-web/src/router/index.ts` · `frontend/elys-web/src/views/` · `frontend/elys-web/src/api/`

更新
: 2026-05-24 11:35:00 +08:00

</div>

## 一句话现状

前端使用 Vue 3、Pinia、Vue Router、Axios 和 LiteGraph.js。登录、Dashboard、研究项（已容器化：工作流·运行 / 结果 / 数据 / 概览 为研究项内部 tab）、数据集管理（`/datasets`）主线已接后端核心链路；`/observe/waveform` 波形详情与 `/admin/withdrawals` 撤回审核也已接后端；其余观察子页、统计、作图、机器学习、管理后台多数为静态预览。正式主站域名为 `elysbrain.site`，首页底部展示备案号 `粤ICP备2026062729号`。

## 1. 路由分组

| 分组 | 路由 | 状态 |
|---|---|---|
| 展示与认证 | `/` · `/login` | 展示页与登录已实现 |
| 研究项与数据 | `/dashboard` · `/studies` · `/studies/:studyId/{data,pipeline,results}` · `/datasets`（alias `/import`） | 已接后端 |
| 工作流 / 结果 | 研究项容器内 tab（`/studies/:studyId/pipeline` · `.../results`）；旧顶层 `/pipeline` · `/results`（带 `?studyId`）升级重定向，旧 `.../workflow` 已无兜底 | 核心接入 |
| 预处理 | `/preprocess` → `/ica` · `/ica` | `/ica` 已接真实 ICA 成分审阅 API（地形图 / 时序 / 频谱；从工作流 ICA Apply 节点带 job 上下文跳入可提交剔除） |
| 观察 | `/observe` · `/observe/erp` · `/observe/psd` · `/observe/tfr` · `/observe/microstate` · `/observe/connectivity` · `/observe/source` · `/observe/waveform` | `/observe/waveform` 已接真实 timeseries API，其余仅前端预览 |
| 输出与智能 | `/statistics` · `/figures` · `/ml` | 仅前端预览 |
| 管理 | `/admin` · `/admin/withdrawals` · `/admin/publicizations` | `/admin` 静态；`/admin/withdrawals` 撤回审核、`/admin/publicizations` 转公开审核均已接后端 |

## 2. 路由总表

| 路径 | name | 组件 | requiresAuth | 状态 |
|---|---|---|:---:|---|
| `/` | `Index` | `Index.vue` | — | 展示页 |
| `/login` | `Login` | `Login.vue` | — | <span class="elys-badge elys-badge--done">已接</span> |
| `/dashboard` | `Dashboard` | `Dashboard.vue` | ✓ | <span class="elys-badge elys-badge--done">已接</span> |
| `/studies` | `Studies` | `StudiesPage.vue` | ✓ | <span class="elys-badge elys-badge--done">已接（master-detail：左列表 + 右概览 + 进入工作区）</span> |
| `/studies/:studyId` | — | `StudyLayout.vue` | ✓ | <span class="elys-badge elys-badge--done">研究项工作区（3-tab，默认重定向 pipeline）</span> |
| `/studies/:studyId/data` | `StudyData` | `StudyDetailPage.vue` | ✓ | <span class="elys-badge elys-badge--done">数据 tab</span> |
| `/studies/:studyId/pipeline` | `StudyPipeline` | `PipelinePage.vue` | ✓ | <span class="elys-badge elys-badge--wip">工作流 tab</span> |
| `/studies/:studyId/results` | `StudyResults` | `ResultsPage.vue` | ✓ | <span class="elys-badge elys-badge--done">结果 tab</span> |
| `/datasets`（alias `/import`） | `Datasets` | `DatasetsPage.vue` | ✓ | <span class="elys-badge elys-badge--done">已接（数据集管理主页；详情 3-tab：数据文件〔默认〕/ 上传 / 发布与共享；含撤回审核入口）</span> |
| `/pipeline` · `/results` | — | （重定向到容器对应 tab） | ✓ | <span class="elys-badge elys-badge--done">兼容重定向</span> |
| `/preprocess` | — | 重定向 `/ica` | ✓ | <span class="elys-badge elys-badge--planned">仅前端</span> |
| `/ica` | `Ica` | `IcaPage.vue` | ✓ | <span class="elys-badge elys-badge--done">已接真实 ICA 成分 API（地形图 / 时序 / 频谱；带 `studyId`+`study_output_id` 进 live，再带 `executionId`/`jobId`/`decisionVersion` 可提交剔除）</span> |
| `/observe` | `Observe` | `StaticWorkbenchPage.vue` | ✓ | <span class="elys-badge elys-badge--planned">仅前端</span> |
| `/observe/erp` | `ObserveErp` | `ErpPage.vue` | ✓ | <span class="elys-badge elys-badge--planned">仅前端</span> |
| `/observe/psd` | `ObservePsd` | `PsdPage.vue` | ✓ | <span class="elys-badge elys-badge--planned">仅前端</span> |
| `/observe/tfr` | `ObserveTfr` | `TfrPage.vue` | ✓ | <span class="elys-badge elys-badge--planned">仅前端</span> |
| `/observe/microstate` | `ObserveMicrostate` | `MicrostatePage.vue` | ✓ | <span class="elys-badge elys-badge--planned">仅前端</span> |
| `/observe/connectivity` | `ObserveConnectivity` | `ConnectivityPage.vue` | ✓ | <span class="elys-badge elys-badge--planned">仅前端</span> |
| `/observe/source` | `ObserveSource` | `SourcePage.vue` | ✓ | <span class="elys-badge elys-badge--planned">仅前端</span> |
| `/observe/waveform` | `ObserveWaveform` | `WaveformPage.vue` | ✓ | <span class="elys-badge elys-badge--done">已接真实 timeseries API（从 Pipeline 页跳入）</span> |
| `/statistics` | `Statistics` | `StatisticsPage.vue` | ✓ | <span class="elys-badge elys-badge--planned">仅前端</span> |
| `/figures` | `Figures` | `FiguresPage.vue` | ✓ | <span class="elys-badge elys-badge--planned">仅前端</span> |
| `/ml` | `MachineLearning` | `MlPage.vue` | ✓ | <span class="elys-badge elys-badge--planned">仅前端</span> |
| `/admin` | `Admin` | `StaticWorkbenchPage.vue` | ✓ | <span class="elys-badge elys-badge--planned">静态部分</span> |
| `/admin/withdrawals` | `AdminWithdrawals` | `AdminWithdrawalsPage.vue` | ✓ | <span class="elys-badge elys-badge--done">撤回审核已接后端</span> |
| `/admin/publicizations` | `AdminPublicizations` | `AdminPublicizationsPage.vue` | ✓ | <span class="elys-badge elys-badge--done">转公开审核已接后端（镜像撤回审核）</span> |
| `/:pathMatch(.*)*` | — | 重定向 `/` | — | 兜底 |

完整 router 配置见 `frontend/elys-web/src/router/index.ts`。

## 3. 登录守卫

`router.beforeEach` 会从 `auth` store 恢复 localStorage：

- 访问需要登录的页面且未认证时跳转 `/login`。
- 已登录访问 `/login` 时跳转 `/dashboard`。
- 其它情况正常放行。

## 4. 组件复用

| 组件 | 用途 |
|---|---|
| `WorkbenchShell` | 工作台壳，包含顶部导航；全局侧栏可按页面关闭 |
| `BidsUploadPanel` | Dataset-first 上传组件，宿主页是 `DatasetsPage.vue`；接收 `studyId`、`datasetAssetId`、`mountName` 等上下文，并导入到 Dataset working version |
| `LoadDataPanel` | LoadData 数据选择面板 |
| `ObserveTabs` | 观察页面 tabs |
| `TechnicalFold` | 技术细节折叠容器，props `title` / `hint` / `defaultOpen`（见 6-05 §8） |
| `IconLine` | 带图标的列表行 |
| `AppIcon` | 全站图标组件 |

> `src/components/` 实际文件：`WorkbenchShell.vue` · `BidsUploadPanel.vue` · `LoadDataPanel.vue` · `ObserveTabs.vue` · `TechnicalFold.vue` · `IconLine.vue` · `AppIcon.vue`。`Index.vue` 是 view 不是组件。

## 5. API 客户端

| 前端文件 | 后端能力 | 对应模块 |
|---|---|---|
| `api/client.ts` | Axios 客户端、401 refresh 拦截 | M0 |
| `api/auth.ts` | `/api/v1/auth/*` | 账号权限 |
| `api/dashboard.ts` | `/api/v1/dashboard/summary` | 首页聚合摘要 |
| `api/studies.ts` | `/api/v1/studies/*`（含 `/studies/trash`） | 研究项管理 |
| `api/datasetAssets.ts` | `/api/v1/dataset-assets/*`，且内含 `recordingApi`（`/studies/{id}/recordings*`） | Dataset Asset、bootstrap、采集记录 |
| `api/datasetVersions.ts` | `/api/v1/dataset-versions/*`、`/dataset-withdrawals/*`、`/dataset-publicizations/*` | 发版、撤回审核、转公开审核（admin） |
| `api/datasets.ts` | `/api/v1/studies/{id}/datasets/mounts*` 与导入 | 挂载与数据导入 |
| `api/pipelines.ts` | `/api/v1/pipeline/*` 与研究项工作流接口 | 工作流 |

`api/pipelines.ts` 当前已经接入标准 Execution 创建入口 `/studies/{study_id}/pipelines/{pipeline_id}/executions`，并封装 Execution manifest、Execution lineage 和 StudyOutput 系列接口（`listStudyOutputs / getStudyOutput / updateStudyOutput / batchUpdateStudyOutputs / runStudyOutputCleanup / previewStudyOutput / downloadStudyOutput`）。

## 5.1 Dataset 导入前端口径

当前口径是 Dataset-first：用户先创建或选择 Dataset，系统创建或选择配套 Study，并通过 `study_dataset_mounts` 挂载；上传组件拿到 `studyId/studyId`、`datasetAssetId`、`mountName` 后再调用兼容上传接口。

当前已接入（主数据页是 `DatasetsPage.vue`，路由 `/datasets`，alias `/import`；不存在 `ImportPage.vue`）：

- `DatasetsPage.vue` 默认创建 Dataset Asset，并调用 `datasetAssetApi.bootstrap()` 自动创建配套 Study 和 mount。
- `DatasetsPage.vue` 以两阶段展示 Dataset 导入：步骤 1 准备 Dataset 目标，步骤 2 上传 EEG 原始数据；上传组件初始态可见，用户可先选择 BrainVision/EDF/BDF 文件，待 Dataset target 就绪后再提交。admin 可见「撤回审核」入口跳 `/admin/withdrawals`。
- `/datasets?study_id=...` 或 `?study_id=...` 保留 Study-first 快捷入口，但仍要求用户明确 Dataset Asset 和 mount。
- `BidsUploadPanel.vue` 的标题、说明和上传参数已经改为 Dataset working version 语义，调用 `datasetApi.upload()` 时提交 `dataset_asset_id` 和 `mount_name`。
- 页面不展示服务器绝对路径，导入结果优先展示 original upload、Raw BIDS、canonical FIF 和 dataset_files。

仍需 staging 验收：真实浏览器大文件上传、真实后端登录态、真实 MNE 转换、真实 Celery/任务进度，以及 Dataset-first 页面在生产 API 下的异常提示。

## 5.2 Pipeline 页面接入现状

`PipelinePage.vue` 当前承担 Pipeline 编辑器和 Execution detail 的 MVP 主界面：

- Pipeline 保存会携带 `expected_version`。
- Execution 创建对话框提供 `execution_mode` 和 `save_policy`。
- 运行按钮会根据 Pipeline 状态禁用不允许的 Execution。
- LoadData 表格可显示 Dataset Asset 和 mount 信息；后端预览已支持 active mount 下的外部 Dataset。
- Execution detail 提供 `结果 / 节点 / 任务 / 输入 / 摘要 / Manifest / Lineage` 分栏，默认进结果。
- 结果行支持 inline 改名 / 行内 tag 编辑 / 状态切换；底部清理 cached 按钮调用 `runStudyOutputCleanup()`。

## 5.3 Results 跨 Execution 浏览页

`ResultsPage.vue` 是新增的跨 Execution 结果浏览主页，详见 [3-45 §8](3-45-StudyOutput.md)：

- 选择 Study → 进入结果列表（默认按 `created_at` 降序）。
- 顶部 chip 多维筛选：`data_type / node_types / bids_subject_ids / sessions / tasks / conditions / tags / retention_statuses / run_ids`。
- 表格列：`display_name / data_type / subject / session / task / condition / retention / tags / run / 创建时间`。
- 行内动作：固定 / 设为正式 / 隐藏 / 下载；批量动作：批量改 retention、批量加标签、批量隐藏、清理 cached。
- 详情抽屉：来源链可视化（`upstream_dataset_ids` → `upstream_recording_ids`）+ inline 改名 / 改描述 / 改标签。
- 调用 `listStudyOutputs / batchUpdateStudyOutputs / updateStudyOutput / runStudyOutputCleanup`。

## 6. 全局状态

| Store | 字段 | 持久化 |
|---|---|---|
| `stores/auth.ts` | `access` · `refresh` · `user` · `isAuthenticated` | localStorage |

未来可补 `studies`、`pipelines`、`tasks` 等 store，分别缓存当前研究项、工作流状态和异步任务进度。

## 7. 工作台导航

页面用 `WorkbenchShell` 组件，由 `data/workbenchPages.ts` 维护顶部导航和可选侧栏导航。Dashboard、Dataset 管理、Study 管理、Pipeline/Execution 主工作页关闭全局侧栏；Pipeline 页只保留自身节点库/工作流选择栏，避免双左栏。

| nav | 内容 | 说明 |
|---|---|---|
| `topNavItems` | 工作台、数据集、研究项、工作流、结果、观察、统计、作图、机器学习（9 项） | 顶部主要入口（`data/workbenchPages.ts`） |
| `sideNavGroups` | 四对象主线、预处理与分析预览、观察与出图（3 组） | 工作台内侧栏入口 |

静态预览入口应保持“预览”或“规划”语义，避免误导为后端能力已上线。

## 7.1 首页备案展示

正式域名 `elysbrain.site` 已备案，备案号为 `粤ICP备2026062729号`。`Index.vue` 首页底部应长期显示备案号，并链接到 `https://beian.miit.gov.cn/`。如果后续新增独立营销页或文档站落在 `elysbrain.site` 子域名下，也应在对应页面底部保留备案展示。

## 8. 维护规则

| 改动 | 必须同步 |
|---|---|
| 新增或修改路由 | 本页路由表、导航配置 |
| 改 API 调用 | 对应模块页、API 总表、页面状态说明 |
| 改工作台导航 | 本页、`workbenchPages.ts`、对应页面入口 |
| 改全局状态 | 本页 Store 表与相关页面验收 |

## 9. 子页文档（v2 重编）

| 文档 | 涵盖页面 |
|---|---|
| [6-01 设计原则与 UI 约定](6-01-设计原则与UI约定.md) | 全局设计准则、技术栈约定、CSS 规范 |
| [6-10 展示与认证](6-10-展示与认证.md) | `/`（首页）· `/login`（登录） |
| [6-20 研究项与数据管理](6-20-研究项与数据管理.md) | `/dashboard` · `/studies` · `/studies/:id` · `/datasets`（alias `/import`） |
| [6-30 工作流与执行](6-30-工作流与执行.md) | `/pipeline` · `/results` |
| [6-40 预处理与观察](6-40-预处理与观察.md) | `/ica` · `/observe` 及 6 个观察子页 |
| [6-50 输出与智能](6-50-输出与智能.md) | `/statistics` · `/figures` · `/ml` · `/admin` |

## 10. 相关页面

- [3-45 输出（StudyOutput）](3-45-StudyOutput.md)
- [5-00 研究管理总览](5-00-研究管理总览.md)
- [5-30 Execution 管理](5-30-Execution管理.md)
- [8-00 功能模块总览](8-00-功能模块总览.md)
- [1-00 需求分析总览](1-00-需求分析总览.md)
