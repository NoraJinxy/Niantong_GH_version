# 2-80 代码工程结构

> 仓库目录布局、各文件职责、构建与依赖清单。配合 [2-20 服务分层](2-20-服务分层设计.md) 看："2-20 是分层逻辑，2-80 是物理位置"。

<div class="elys-meta" markdown>

定位
: 仓库目录 · 文件职责 · 依赖与工具链

代码根
: `elys_project/`

更新
: 2026-05-21 13:48:30 +08:00

</div>

## 1. 仓库总览

<div class="elys-tree">
elys_project/
├── <b>README.md</b>            <i>v1 简介 + 一键部署 + 排查命令</i>
├── <b>backend/</b>             FastAPI · MNE · SQLAlchemy
│   ├── app/
│   └── requirements.txt
├── <b>frontend/</b>            Vue 3 · TypeScript
│   └── elys-web/
├── <b>database/</b>
│   └── init.sql             14 张表 + 索引 + 角色权限种子
├── <b>deploy/</b>              一键部署脚本
│   ├── deploy_remote.ps1    <em>Windows 主机入口</em>
│   └── deploy.sh            服务器侧实际执行
└── <b>docs/</b>
    └── BIDS_DATA_IMPORT_AND_VERSIONING.md    <i>导入与版本说明</i>
</div>

## 2. 后端结构

<div class="elys-tree">
backend/
├── <b>requirements.txt</b>                <em>所有 Python 依赖</em>
└── app/
    ├── <b>main.py</b>                     FastAPI app + CORS + UUID 编码 + /health
    ├── <b>config.py</b>                   Settings（pydantic-settings）
    ├── <b>database.py</b>                 engine + SessionLocal + get_db()
    ├── routers/
    │   ├── auth.py                    login / refresh / me / logout
    │   ├── studies.py                研究项 CRUD + 成员 + 垃圾箱 + 硬删除
    │   ├── datasets.py                列表 + 同步导入
    │   └── pipelines.py               节点 spec + 工作流 CRUD + 校验 + 运行 + LoadData
    ├── schemas/
    │   ├── auth.py     study.py     dataset.py     pipeline.py
    ├── services/
    │   └── study_access.py          require_study_read / write
    ├── models/
    │   ├── user.py                    User · Role · Permission · UserRole · RolePermission
    │   ├── role.py                    （角色关联表）
    │   └── study.py                 Study · StudyMember · Subject · Dataset · DatasetUpload · PipelineDefinition · PipelineExecution · AuditEvent
    ├── pipeline/
    │   ├── registry.py                NodeRegistry
    │   ├── load_data.py               resolve_load_data_selection
    │   └── nodes/                     10 个 NodeSpec JSON
    │       ├── eeg_data_load.json
    │       ├── eeg_filter_fir.json
    │       ├── eeg_filter_butterworth.json
    │       ├── eeg_preproc_resample.json
    │       ├── eeg_preproc_rereference.json
    │       ├── eeg_ica_compute.json
    │       ├── eeg_ica_apply.json
    │       ├── eeg_epoch_segment.json
    │       ├── eeg_analysis_erp.json
    │       └── eeg_output_save_result.json
    └── utils/
        └── security.py                hash_password · verify_password · JWT
</div>

## 3. 前端结构

<div class="elys-tree">
frontend/elys-web/
├── <b>package.json</b>                    Vue 3 · Vite · Pinia · Vue Router · Axios · LiteGraph.js
├── vite.config.ts                     <i>Vite 构建配置</i>
├── tsconfig.json
├── index.html                         <em>入口</em>
├── public/
└── src/
    ├── main.ts                        Vue 3 app + Pinia + Router 挂载
    ├── App.vue                        全局壳
    ├── style.css                      全站基础样式
    ├── router/index.ts                18 条路由 + 登录守卫
    ├── stores/
    │   └── auth.ts                    Pinia · token / user / localStorage
    ├── api/
    │   ├── client.ts                  axios 实例 + 401 自动 refresh 拦截
    │   ├── auth.ts                    api/v1/auth/*
    │   ├── studies.ts                api/v1/studies 全部
    │   ├── datasets.ts                api/v1/studies/{id}/datasets
    │   └── pipelines.ts               api/v1/pipeline/* + api/v1/studies/{id}/pipelines/*
    ├── views/                         <em>18 个页面</em>
    │   ├── Index.vue Login.vue Dashboard.vue
    │   ├── StudiesPage.vue StudyDetailPage.vue ImportPage.vue
    │   ├── PipelinePage.vue           工作流编辑器（4-08）
    │   ├── IcaPage.vue StaticWorkbenchPage.vue
    │   ├── ErpPage.vue PsdPage.vue TfrPage.vue
    │   ├── ConnectivityPage.vue MicrostatePage.vue SourcePage.vue
    │   └── StatisticsPage.vue FiguresPage.vue MlPage.vue
    ├── components/
    │   ├── WorkbenchShell.vue         工作台壳
    │   ├── BidsUploadPanel.vue        导入组件
    │   ├── ObserveTabs.vue            观察 tabs
    │   └── AppIcon.vue                SVG 图标
    ├── types/index.ts                 全部 TS 类型
    └── data/workbenchPages.ts         静态工作台元信息
</div>

## 4. 后端依赖

```text
fastapi==0.109.0          后端框架
uvicorn[standard]==0.27.0 ASGI 服务器
sqlalchemy==2.0.25        ORM
psycopg2-binary==2.9.9    PostgreSQL 驱动
python-jose==3.3.0        JWT 编解码
bcrypt==4.2.1             密码 hash
python-multipart==0.0.6   表单上传
pydantic==2.5.3           数据校验
pydantic-settings==2.1.0  配置
numpy==1.26.4
scipy==1.14.1
mne==1.7.1                EEG IO + 算法
```

## 5. 前端依赖

| 类型 | 包 | 版本 |
|---|---|---|
| 框架 | `vue` | `^3.4.0` |
| 路由 | `vue-router` | `^4.3.0` |
| 状态 | `pinia` | `^2.1.0` |
| HTTP | `axios` | `^1.6.0` |
| 工作流画布 | `litegraph.js` | `^0.7.18` |
| 构建 | `vite` | `^5.4.0` |
| 类型 | `typescript` · `vue-tsc` | `^5.3` · `^2.0` |

未引入但拟引入：`ECharts` / `uPlot` / `Plotly.js`（观察页）· `mermaid`（前端图）等。

## 6. 数据库与脚本

```text
database/
└── init.sql           表 + 索引 + 角色权限种子 + 测试账号

deploy/
├── deploy_remote.ps1  Windows 一键部署到入口 + 计算服务器
└── deploy.sh          服务器侧 bash 实际执行
```

## 7. 启动与构建

| 任务 | 命令 |
|---|---|
| 后端 dev | `uvicorn app.main:app --reload --host 127.0.0.1 --port 8000` |
| 前端 dev | `cd frontend/elys-web && npm run dev` |
| 前端 build | `cd frontend/elys-web && npm run build`（输出 `dist/`） |
| 后端依赖 | `pip install -r backend/requirements.txt` |
| 前端依赖 | `cd frontend/elys-web && npm install` |
| 一键部署 | `pwsh elys_project/deploy/deploy_remote.ps1` |
| 文档站点 | `pwsh wiki/deploy_docs.ps1` |

## 8. 当前缺失项

| 项 | 状态 | 备注 |
|---|---|---|
| `tests/` 单元 / 集成 | <span class="elys-badge elys-badge--planned">未建</span> | 验证机制依赖手工冒烟 |
| 数据库版本 | <span class="elys-badge elys-badge--done">已确定</span> | 单一事实源 `database/schema/` 6 SQL 文件 + DROP/重建升级；不用 Alembic |
| CI 流水线 | <span class="elys-badge elys-badge--planned">未建</span> | 手动跑部署脚本 |
| `pre-commit` 钩子 | <span class="elys-badge elys-badge--planned">未建</span> | 无 lint / format 强制 |
| 容器化（Dockerfile） | <span class="elys-badge elys-badge--planned">未建</span> | 当前直接 systemd |

## 9. 相关页面

- [2-00 系统架构总览](2-00-系统架构总览.md)
- [2-20 服务分层设计](2-20-服务分层设计.md)
- [3-00 数据库设计总览](3-00-数据库设计总览.md)
- [6-00 前端页面总览](6-00-前端页面总览.md)
- [5-00 研究管理总览](5-00-研究管理总览.md)
