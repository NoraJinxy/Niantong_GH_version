# 9-02 2026 年 6 月更新

> 2026 年 6 月的详细变更记录，按"日期 · 主题"倒序排列。标题级周概览见 [9-00 周报总览](9-00-更新日志.md)。

<div class="elys-meta" markdown>

定位
: 月度详细更新记录（2026 年 6 月）

更新
: 2026-06-19 +08:00

</div>

## 2026-06-19 · feat(observe): 三观察页地形图「双击放大查看全部」

**背景**：时域 / 频域 / 时频三页底部都有一条**地形图条**（`TopoStrip`，横向滚动的小卡片，每张是某个条件 / 频段 / 游标点的头皮分布插值图）。卡片只有 ~132×96px，多条件时挤成一排得左右拖，医生想并排比对「哪个条件在哪块脑区强」时看不清、也分不清哪张是哪张。用户要求：双击弹出一个独立大图，把这组地形图整体铺开、标签打清楚，风格沿用三观察页。

**改动**（只落共用组件 `components/observe/TopoStrip.vue`，三页 `<TopoStrip>` 用法零改动即同时获得）：

- **双击条带任意处**（或点条带右上角新增的「⤢ 放大」药丸按钮）→ 弹大图。复用全站通用弹窗外壳 `common/Modal.vue`（`position: fixed` 居中 + 点遮罩 / 按 Esc 关，与全站 9 个弹窗同款），不用 Teleport（避开既知的 Teleport + keep-alive 崩 Vue 渲染器坑）；经查观察页布局祖先无 `transform`/`filter`，fixed 正常锚到视口。
- 弹窗里把**当前这组**地形图铺成自适应网格（`minmax(230px, 1fr)`），每张放大到 230px 高、**按条件名（`cell.label`）打标签** + 顶部条件色点，头部带标题 / 副标题（同条带 `subtitle`）+ 色阶条 + 单位——「哪张是哪个条件」一眼可辨。
- **绘制零重写**：小图与大图共用同一套 `drawCell`（薄板样条插值色面 + 头罩 / 鼻耳 + 电极点）、同一份 montage 插值矩阵缓存（`kernelCache`）、同一条色阶（`barLo/barHi` + 当前色板 LUT）。重构点仅是把原先写死的命中表 `hitMap` 抽成「按目标传入的 store」，让条带与弹窗各持一套 canvas / 命中表、互不串扰；`drawCell` 按 canvas 实际 `clientWidth/clientHeight` 出更高 backing 分辨率，放大后依旧锐利。
- 弹窗开着时仍**实时跟随**：游标模式移动、数据切换都经 `renderAll` 一并重绘弹窗（hover 仍可读电极名 + 值，单位跟随 `props.unit`，顺手修掉原 hover 写死 `µV` 对 PSD/dB 不对的小问题）。

**校验**：`vue-tsc --noEmit` 全仓 0 错。真机观感（弹窗定位 / 网格 / 大图清晰度）待云端部署后验。

---

## 2026-06-19 · feat(pipeline): 节点卡片改「只读摘要 + 点击跳检查器编辑」（医生视角精简）

**背景**：用户拍板节点卡片设计原则——卡片是「这步在做什么」的一眼**只读摘要**（方法学句子），呈现为主、编辑在右侧检查器；受众是医生 / 心理学家，不是每个节点都要呈现 + 交互一堆参数。（经历「只读贴纸太寡淡 → 全交互 widget 一堆 litegraph 原生控件 bug + 太工程化 → 拉回只读为主」的来回。）

**改动**（`PipelinePage.applyNodeWidgets`）：
- **全卡只读**（自绘 `elys_readonly`：标签灰 + 值深加粗，去尾零 `.0` / 带单位 / 截断；combo 显中文档位、开关显「开 / 关」）。卡上**不再有任何内联输入**。
- **编辑统一归右栏**：点节点上任意行（只读事实 / 「摘要 + 编辑 ›」胶囊）= 选中节点 + 打开检查器 + 滚到对应 `data-param` 字段并**聚焦输入框**。（曾短暂给阈值类数字做内联步进，但「点数字输入精确值」弹 litegraph 原生 prompt 太丑、用户要求去掉，故撤掉全部内联输入 + `NODE_CARD_EDITABLE` 白名单。）
- **复杂参数**（通道 / 条件 / 数据集）→「摘要 + 编辑 ›」胶囊跳检查器。
- **LoadData 专属**：撤掉三个数据筛选控件，改只读——选 1~2 个显**文件名**（`recordingDisplayName` 取 fif/source 路径 basename）、更多显「N 个文件」、没选显「未选择数据 / 点击设置 →」。

**校验**：`vue-tsc` 0 错 + 全 pipeline 39 用例过。真机观感待重新部署。

---

## 2026-06-18 · feat(数据集导入): 转 FIF 时自动绑定电极位置（montage）

**背景**：电极位置（montage / 通道在头皮上的坐标）原本只能靠流水线里的「通道定位（Ch Loc Assign）」节点指派——每条 pipeline 都要手接一个节点，否则下游 Bad Channels 球面样条插值、地形图、ICA 成分图全因缺坐标而失败。但电极位置本质是**采集时的设备元数据**、不是分析决策：BIDS 把它放数据集级 sidecar（`electrodes.tsv` / `coordsystem.json`），EEGLAB 开局就把 chanlocs 灌进数据集。所以正确的位置是**导入那一刻**，而非流水线步骤。

**改动**：新增 `app/engine/preprocess/montage_autobind.py`，在 `dataset_imports.generate_canonical_fif` 转 FIF 时（`raw.save` 之前、设备元数据通道重标 misc 之后）自动绑定：

- **数据驱动识别厂家帽**：不硬编码「bdf=BioSemi」之类判断，而是拿「数据 EEG 通道名 ↔ 每顶 MNE 内置帽通道名」的**实际重叠**打分。BioSemi 的 `A1..H32`、EGI 的 `E1..E256` 天然只会和 `biosemi*` / `GSN-HydroCel*` 帽高度重叠，标准 10-20 命名只会和 `standard_1020/1005` 重叠，识别自然分流。
- **得分 =(命中道数, 精度)**，精度 = 命中 / 帽电极数，避免「64 导数据套上 256 帽」的虚胖匹配；同进程跨多文件用 `lru_cache` 复用已建帽对象。
- **三档降级**：认出厂家/标准帽 → 套（必要时把通道名对齐到帽的标准写法再 `set_montage`）；认不出（命中 < 3，多是 `Ch1/Ch2` 这类无信息命名）→ 不绑、`applied=False`，留待用户手动上传电极文件；文件自带坐标（如部分 BrainVision）→ 保留不覆盖。
- **绝不阻断导入**：模块内吞掉所有异常，绑不上就记 `error`、raw 原样（无坐标）继续；绑定明细写进 `provenance.validation.montageAutobind`（绑了哪顶帽、命中几道、改了哪些通道名）做溯源。

**影响**：

- canonical FIF「**出生即带坐标**」——下游所有节点 / 观察页地形图天生有坐标，**医生 / 心理 persona 无需再认识「通道定位」这个步骤**。
- 三个测试脚本（`01_erp_basic` / `02_erd_ers` / `03_resting_state`）即可安全删掉 Ch Loc 节点，Bad Channels 维持 `interpolate` 也能找到坐标（解掉「删 Ch Loc → Bad Channels 插值缺坐标硬失败」的连带死结）。
- 「通道定位」流水线节点不删除，保留为**专家想中途重指派 / 上传自定义电极文件**的可选项（厚薄两层：默认链路免它，专家仍可手动覆盖）。

**待做**：手动覆盖路径（数据集详情页选标准帽 / 上传自定义文件作用到导入产物）仍走现有 `DatasetMontage` 节点级消费，尚未下沉到导入/加载层；EGI `EEG 1` 这类非 `E1` 导出命名暂不自动映射、落手动上传。

**验证**：后端 `py_compile` 过；新增 `tests/test_montage_autobind.py` 5 用例（BioSemi/EGI/标准/无信息/自带坐标五路分流），因依赖云端锁定的 mne 1.7.1，**真机需部署 + 重新导入数据**后跑测试脚本确认。

## 2026-06-18 · fix(observe): 三观察页未选数据集显示真名 + 修双 sub- 前缀

**背景**：用户反馈时域图左栏「数据集」选择器里，未勾选的数据集只显示「数据集 2」而非真名，且已选中项的被试名是双前缀 `sub-sub-093`。

**根因**：
- 数据集名只能从「已加载的绘图响应」（`tsMap`/`psdMap`/`tfrMap` 的 `subject` + `condition`/`segment_label`）取；而页面为避免一墙「加载中」，**默认只加载第 1 个数据集** → 其余未加载的拿不到名字、退回 `数据集 N`。**三页（时域/频域/时频）同病**。
- `sub-${subject}` 双前缀：后端 view 响应的 `subject` 字段值已是完整 BIDS 实体 `sub-093`，前端又拼一个 `sub-` → `sub-sub-093`。

**改动**：
- 新增 `composables/observe/outputLabels.ts` 的 `loadOutputLabels(studyId, outputIds, into)`：挂载时（多产物）并发对所有 `study_output_id` 取**轻量元数据**（`pipelineApi.getStudyOutput` → `bids_subject_id`/`condition`/`display_name`，不含重绘图数据）→ 写进 `labelCache`。于是选择器在加载重数据**之前**就显示「被试 · 条件」（取不到的项静默退回 `数据集 N`，best-effort；已有值不覆盖）。**入口无关**——直接 URL 进也生效，不依赖 PipelinePage 传名。
- `observeUtils.fmtSubject(s)`：归一化被试前缀，已含 `sub-` 不再重复（`sub-093` 保持 / `093`→`sub-093`）。三页 `segLabel` 与时域 load 的 4 处 `sub-${...}` 全改用它。
- 三页（`WaveformPage`/`PsdPage`/`TfrPage`）`onMounted` 各加一行 `loadOutputLabels` 调用。

**校验**：`vue-tsc --noEmit` 全绿。真机待云端验收（顺带确认该数据集下 `getStudyOutput.bids_subject_id` 确为期望被试号）。

---

## 2026-06-18 · fix(pipeline): 节点就地控件一批修复（黑块 / 该藏没藏 / 等宽收高 / 单位 / 滑块 / 长 label）

**背景**：Tier 1 就地控件部署后，用户截图反馈一堆毛病，按严重度逐条修。

- **复杂参数「黑块」**：litegraph 原生 button widget 背景色写死 `#222` + 文字深色 → 黑底黑字全废。改自绘「摘要 + 编辑 ›」浅底圆角控件（`pushSummaryWidget`，走 litegraph 非内置 type 的 `w.draw / w.mouse` 钩子），点击仍跳右侧检查器对应区。
- **`visible_when` / `advanced` 没生效**（带通误显「工频」、陷波误显「低/高截止」）：**真根因在后端**——`NodeProperty` 响应模型（`schemas/pipeline.py`）漏声明 `advanced` 与 `visible_when`，Pydantic 据此把这两字段从 `/pipeline/nodes` 响应里过滤掉，前端永远收不到（截图里"高级参数没显示"只是它们排第 5+ 位被 cap-4 截掉、非真过滤）。补回两字段即可；前端 `planNodeWidgets` 过滤逻辑本就正确（单测能过）。
- **节点等宽 + 收高**：`sizeNodeForWidgets` 改为所有节点统一 `NODE_CARD_WIDTH` 宽、高度=端口区+控件区贴合收紧，消除控件下方大片空白。
- **数字补单位**：数字控件标签后缀带单位（低截止 Hz / Tmin s）。尾零 `.0` 仍在（litegraph 数字控件原生 `toFixed`，留待自绘数字控件）。
- **滑块去进度条观感**：有界数字改用步进器渲染，不再用 litegraph slider 的填充块。
- **长 label 溢出**：label / combo 选项值截断加省略号（combo 另建「截断 label → 真值」表保证写回正确）。

**未做（下一轮）**：端口与参数视觉分区、信息密度精简（#9/#10）；已保存旧布局 + 高节点的重叠需点「整理布局」或加重叠检测；`.0` 尾零。

**校验**：`py_compile` + `vue-tsc` 0 错 + 全 pipeline 39 用例过。真机观感待**重新部署**（含后端，`visible_when` 是后端字段）。

---

## 2026-06-18 · feat(pipeline/chanloc): 通道定位节点厂家电极帽预设 + 数据集级自定义 montage 上传

**背景**：「通道定位」节点（`eeg/preproc/channel_location`，给数据写电极坐标，供地形图 / ICA 成分图 / 坏导插值用）原本只有三个内置电极帽：auto（按通道数选 10-20/10-05）、standard_1020、standard_1005。用户希望①补几家典型厂家放大器的电极帽预设供选；②能上传自己的电极位置文件。

**领域要点**：地形图坐标在观察页出图时由 `montage_layout.channel_positions_2d` 现算，缺内嵌 montage 会**按通道名兜底匹配标准帽**。所以标准 10-20 命名的设备（含 Emotiv）本就能自动定位、不需要预设；预设真正救的是**用厂家私有命名**的设备——BioSemi 的 `A1..A32/B1..`、EGI 的 `E1..E257`，名字对不上 10-20，不喂对应 montage 摆不出位置。

**改动 · 预设（小，但有个必须一起修的坑）**：

- 节点 JSON `montage` select 选项扩充：BioSemi 64/128/256、EGI HydroCel 64/128/256/257、EGI_256、EasyCap M1/M10（选项值用 MNE 内置帽**精确名**，大小写敏感，如 `GSN-HydroCel-64_1.0`、`EGI_256`、`easycap-M1`）。
- **修 `_resolve_montage_name`**（`engine/preprocess/channel_location.py`）：原逻辑只认 `standard_1005/1020`，**其余任何值一律掉进 auto 分支**——即光往 JSON 加选项也跑不通；且它先 `.strip().lower()`，会把大小写敏感的厂家帽名（`EGI_256`→`egi_256`）洗坏。改为 auto 按通道数选、其余**原样透传** + `mne.channels.get_builtin_montages()` 成员校验（认不出给明确报错而非静默降级）。

**改动 · 自定义上传（数据集资产级）**：

- **存储归属拍板为数据集资产级**（贴 BIDS：电极位置本属于数据集）。新表 `dataset_montages`（`database/schema/03_datasets.sql` + ORM `models/study.py`）：`dataset_asset_id` 外键 CASCADE、name / file_format / storage_uri / relative_path / n_electrodes / sha256；物理存 `DATASETS_STORAGE_ROOT/{asset_id}/montages/{id}.{ext}`，并入 lifespan 重置表清单。
- **新路由 `routers/dataset_montages.py`**（注册进 `main.py` + routers `__init__`）：`POST/GET/DELETE /dataset-assets/{asset_id}/montages`（上传/列举/删除，走数据服务器，文件落 storage）+ `GET /studies/{study_id}/montages`（按研究项挂载的资产聚合，给节点选择器）。上传即用 `mne.channels.read_custom_montage` 试解析校验——解析不了 = 不是有效电极文件，删盘 + 422；支持 .elc/.sfp/.bvef/.tsv/.csv/.txt/.loc 等。
- **节点新增 `montage=custom` + `montage_picker` 参数**（`required` + `visible_when montage=custom`）。执行器 `dispatcher._execute_channel_location` 用 `context.db` 按 `custom_montage_file_id` 解析出物理路径，**闭包**喂给 `run_channel_location(..., custom_montage_path=)` 走 `read_custom_montage`——保持引擎函数纯净，也不把服务器路径塞进参数哈希（哈希仍认 file_id）。
- **前端**：新组件 `DatasetMontagePanel.vue` 挂数据集详情页「数据文件」tab 顶部（上传/列举/删除）；`PipelinePage` 加 `montage_picker` 渲染分支（下拉本研究项已上传电极文件 + ↻ 刷新 + 空态引导）；`datasetAssetApi` 加 upload/list/delete/listStudy 四方法；`nodeWidgetPlan` 把 `montage_picker` 归复杂类型（画布出摘要按钮）；类型 `DatasetMontage`。

**影响页面**：Pipeline 编辑器（通道定位节点参数）、数据集详情页（数据文件 tab）。

**状态**：后端 `py_compile` + 节点 JSON 校验通过；前端 `vue-tsc` 0 错。**真机待云端部署验证**——含 DROP 重建以建 `dataset_montages` 表；MNE 内置帽精确名（尤其 `GSN-HydroCel-64_1.0`）需在云端 mne 1.7.1 用 `get_builtin_montages()` 复核。

## 2026-06-18 · refactor(observe): 三观察页命名统一 + 时域页接入共享 composable / CSS（统一设计 Tier 0）

**背景**：时域(Waveform)、频域(PSD)、时频(TFR)三张观察图共享一批 composable（useMultiSelect / usePalette / useCursorState / useFacetGrid）与外壳 CSS（observePage.css）。审查发现这是一次「做了一半的重构」——这些 composable 当初是**从时域页提炼**出来的（多个文件注释写着「移植自 WaveformDetailPage」），却只接到了较新的 PSD / TFR 两页，唯独没回迁源页：时域页一直内联手搓着选择 / 调色 / 游标三套逻辑（约 120 行）+ 独占 247 行 scoped `.wf-*`，成了三套逻辑的「化石副本」。本轮把三页复用收口成统一设计（Tier 0）。

**改动**：

- **新建 `composables/observe/observeUtils.ts`**：`useQueryString`（查询参数读取，因依赖 useRoute 做成 composable）/ `round` / `toNum` / `shortId` / `clampInt`——三页逐字重复的工具收口为单一来源，三页删本地定义改 import。
- **时域页接入三个共享 composable**（与 PSD/TFR 同源）：选择 → `useMultiSelect`（`segSel` / `chanSel`，删内联 `onSegClick`/`onChanClick`/`selectAll`/`selectNone`/`*Anchor`）；配色 → `usePalette`（`colorAt`，删内联 `currentPalette`/`palette`/`paletteContinuous`/`paletteGroups`/`selectPalette`）；游标三态 → `useCursorState`（删内联 `cursorReadout`/`cursorLocked`/`lockedReadout`/`displayReadout`/`cursorState*`/`onCursor`/`onLock`/`onUnlock`）。**关键技巧**：挡路的 `cursorX`（驱动实时地形图）本质就是 `computed(() => displayReadout.value?.x ?? null)`，接入后自然成立、无需把 composable 泛化（行为与 PSD 一致）；页内独有的 hover 高亮（`effectiveFocus`/`toggleLock`/`onLineHover`）保留。
- **CSS 迁移**：时域页弃用 247 行 scoped `.wf-*`，全量改 `observePage.css` 的 `.ov-*`（单一外壳来源，该文件本就是从时域抠出来的祖版、~85% 类 1:1 等价）；时域独有类（view-only 滤波 `ov-grid2`/`ov-apply`/`ov-view-tag`/`ov-sbar-filter`、段步进 `ov-seg-stepper`/`ov-seg-idx`、明细表分列着色 `ov-dt-*`）补进 observePage.css；顺带 `.ov-hover-val` 补回 `16px/700`（游标读数大数字醒目，PSD/TFR 同步受益）。
- **命名对齐**（三页范式统一到抽象最彻底的 PSD）：通道选择集 `selected`→`selectedChans`、有序通道 `orderedSel`→`orderedChans`（消除「selected 是段还是通道」歧义）、主段 PSD `primary`→`primarySeg`；地形图模式枚举 `topoMode` 时域 `'mean'|'live'` → `'window'|'cursor'`（与 TFR 同口径——原来同名属性不同枚举值是暗坑）。
- **文件改名**：`WaveformDetailPage.vue` → `WaveformPage.vue`（去掉无意义的 `Detail`，三页统一 `<Kind>Page`），`router/index.ts` 动态 import 同步；路由 path/name（`/observe/waveform` · `ObserveWaveform`）已规范不动。
- **清死代码**：TFR scoped 里从 PSD 复制残留的 `.psd-bandpill`（TFR 无频段药丸）、PSD `collapsed.band` 未用键。

**刻意保留的差异（领域语义不同，硬统一=假统一）**：TFR 的 2D 游标（`{t,f}`，`useCursorState` 仅支持 1D）与恒矩阵分面（不套 `useFacetGrid` 的叠加维度模型）；TFR 热图色阶体系（`heatmapColor` cmap）vs 1D 曲线色板（`channelColor`）；时域专属的取数窗 / 幅度缩放 / view-only 滤波；各页右栏统计本体（时域峰谷潜伏 vs PSD 的 IAF/频段相对功率 vs TFR 的 2D ROI）。

**未做（留作后续）**：主数据对象命名 `ts`(时域) / `primaryMeta`(TFR) → `primaryTs` / `primaryTfr`（`ts` 子串遍布全文、纯字面量替换风险高、收益低，暂缓）；Tier 1 共享抽取（`useObserveExport` 收口三页 PNG/CSV/剪贴板导出、`useFullscreen`、两个 Canvas 的交互骨架 `useCanvasZoomPan`/`useCursorThrottle`/`useExportCanvas`）；取数层统一（让 PSD/TFR 也走 `plotCache` 三级缓存，现仅时域页用）。

**校验**：分四次提交（observeUtils + PSD/TFR 命名 → 时域 composable + CSS → 文件改名 → 文档），每次 `vue-tsc --noEmit` 全绿。本地无运行环境，画布交互 / 视觉布局待云端部署验收。

---

## 2026-06-18 · feat(pipeline): 工作流节点就地可编辑控件（Tier 1 · litegraph widgets）

**背景**：节点卡片标题栏以下是空白、参数只能点开右侧检查器看（看图认不出每步在干嘛）。改为在卡片上按「常用 1~3 项（封顶 4）」就地放可交互控件，套既有扁平浅色皮肤。参照 litegraph 原生 widget 能力（核实其全局换肤常量 `WIDGET_*` + `slider/number/combo/toggle/button` 类型齐全），但 Niantong 的节点编辑器同样只点开侧面板编辑、节点本体一样空，无现成方案可抄。

**改动**：

- 新增纯函数 `nodeWidgetPlan.planNodeWidgets`（`spec.properties` + 当前 `params` → 控件描述列表）：跳过 `advanced` 与 `text`（文本走检查器、避开 litegraph 浏览器 prompt）、按 `visible_when` 过滤、封顶 4。`select`→下拉(combo) / `number`(无界)→步进 / `number`(有界 min&max)→滑块 / `boolean`→开关；复杂类型（通道 / 事件 / 标签 / 数据集）→「摘要 + 编辑 ›」按钮。
- `PipelinePage.applyNodeWidgets`：清空后按规划 `addWidget`，按控件数调高卡片；在 `createLiteGraphNode` / `updateLiteGraphNode` 各调一次（建节点 / 改参数时重建）。
- `onNodeWidgetEdited`：widget 改值写回 `node.properties` + `scheduleLiteGraphSync(true)`（同步 definition + 标脏），与检查器双向同步；仅当改的是 `visible_when` 控制项才重建控件（避免拖滑块每帧重建、打断拖拽）。
- combo「存真值显中文」：用 option 的 `label` 数组显示 / 循环，回调里 `label→value` 反查写回 `params`（litegraph combo 对 Object values 会把 value 变成索引号，故不能直接用映射对象）。
- 复杂项按钮 `focusInspectorParam`：选中节点 + 打开检查器 + 滚到对应 `data-param` 字段并短暂高亮（决策：跳右侧检查器对应区）。
- widget 主题沿用既有浅色（`WIDGET_*`），行高 20→24 配 28px 端口行；滑块单色填充（Tier 1 全站一种主色，逐类别色 / 自绘控件留 Tier 2）。

**决策（与用户拍板）**：①节点放常用 1~3 项、检查器留全量 / 高级 / 复杂项；②复杂项点击跳检查器对应区；③封顶 4；④先 Tier 1；⑤文本不放节点。

**校验**：`vue-tsc` 0 错；新增 `nodeWidgetPlan.spec.ts` 6 用例 + 全 pipeline 39 用例通过。画布交互观感（点击 / 拖拽 / HiDPI / 与端口不重叠）待云端部署验收。

---

## 2026-06-18 · fix/feat(observe): 三观察页（时域/频域/时频）多产物体验统一

**背景**：把两个多产物观察体验问题，在时域(Waveform/ERP)、频域(PSD)、时频(TFR)三页一次拉齐。

**① 双击节点 = 看本节点产物（撤销跨节点自动发现）**

- TFR 双击打开时频页，原来是「发送端 `openNodeWaveform` 只送该节点首个产物 `saved[0]` + 接收端 `TfrPage.discoverSiblings()` 把整个研究项下所有 TFR 产物全捞进数据集列表」（即 06-16「自动发现同研究项其它 TFR 产物」那版）→ 双击某节点却看到别节点的产物、分不清在看谁产的；而 ERP/PSD 早已是节点级。
- 改为与 ERP/PSD 一致：`PipelinePage.openNodeWaveform` 补 `tfrs` 分支，把**本节点**全部 TFR 产物一并送出（多 condition→多数据集对比、单 condition→单数据集）；`TfrPage` 移除 `discoverSiblings`，数据集列表 = URL 带的 = 本节点产物。后端产物本就记 `produced_by_node_id`（哪个节点产的），节点级有据可依。

**② 数据集按「被试 · 条件」命名**

- 多被试节点产出 N 被试 × M 条件个产物，列表原来只按 `condition` 标 → 6 个 `clench_fist` 全长一样、分不出被试。
- 后端 `tfr_view` / `psd_view` / `timeseries`（在 `build_timeseries` 装配点一处注入）各增返 `subject`(=`bids_subject_id`) + `display_name`；三页 `segLabel` 统一显示「`sub-H01D01B01 · clench_fist`」、缺被试退化 `display_name`。时域页顺手把 `segOptions`（勾选清单）收口到 `segLabel`，免清单/图例两套标签漂移。

**③ 双击多产物节点默认只选第 1 个**

- 原来默认勾选 URL 带的全部产物，双击一个 6 被试 × 2 条件的节点 = 开页即甩十几路云端 TFR 读、糊一墙「加载中」（每格 = 一次独立请求、render 上限 `MAX_CELLS=16`）。
- 三页默认改为只选第 1 个，其余列出待勾、要对比再手动加。**已知连带**：未勾选的数据集名暂显「数据集 N」（名字随数据加载才到）；如需未选也显全名，需另加「轻量元数据预取」（三页统一的后续，未做）。

**校验**：后端 `py_compile`（`tfr_view` / `psd_view` / `timeseries`）+ 前端 `vue-tsc --noEmit` 全 0 错。落 3 提交：`87ff966`（TFR 节点级）/ `477cd7f`（TFR 命名 + 默认只选）/ `f5fae6d`（PSD/时域对齐）。真机待部署后双击 TFR / PSD / ERP 节点验收三页一致。

---

## 2026-06-18 · feat(pipeline): 工作流节点自动蛇形布局 + 适应屏幕

**背景**：脚本（`elys_scripts`）程序化建的 pipeline 把节点坐标全钉在同一行（`y=80`、x 每个 +320），9 个节点拉成近 2900px 的横线，画布一屏只看得到前 3~4 个；而原画布「居中」按钮只 `centerOnNode` 单个节点、没有「缩放到全部可见」，这类工作流永远看不全、也很难看。

**改动**：

- **`litegraphUtils.ts` 新增纯函数 `computeSnakeLayout`**：把「多为线性、偶有末端分叉」的 DAG 排成蛇形折行网格（boustrophedon / 牛耕式）——先用 Kahn 拓扑算每个节点的「最长路径层深」，保证它排在所有上游之后（末端 ERP/TFR/PSD 同层分叉会聚在一起）；再按列数折行、奇数行反向，使「上一行末」与「下一行首」上下相邻、连线最短。列数按画布宽高比自适应（`cols=√(aspect·n·gapY/gapX)`），让网格接近画布形状、缩放后最舒服。
- **`PipelinePage.vue` 新增三个视图函数**：`fitGraphToView`（按全部节点包围盒缩放 + 平移到可视区，镜像 LiteGraph `centerOnNode` 的坐标变换 `画布像素=(图坐标+offset)×scale`，缩放夹在 `ds.min/max_scale` 内）、`autoArrangeGraph`（蛇形重排 + 适应）、加载工作流时 `normalizeGraphViewOnLoad` 对**退化布局**（单行 / 单列）**自动整理一次**（不标脏，保存时才落库；非退化布局只缩放适应）。
- **画布按钮**「居中」改为「整理布局 / 适应」，右键菜单同步补「整理布局 / 适应屏幕」项。
- 脚本侧不动：前端对任意来源的工作流都生效，旧脚本无需改坐标或重跑。

**校验**：`vue-tsc --noEmit` 0 错；新增 `litegraphUtils.spec.ts` 5 组用例（空图 / 折行相邻 / 层深排序压过数组原序 / margin 偏移 / 悬空连线容错）全过。真机布局观感待部署验收。

**同日跟进（蛇形 → 打字机式）**：部署后用户反馈「方向看不清」。根因——litegraph 节点端口固定（入左 / 出右），蛇形让偶数行右→左，那一行节点「出口在右却要连左边的下一个」，连线逆着端口往回兜，流向不可读。`computeSnakeLayout` 改名 `computeFlowLayout` 并去掉奇数行反向：**每行一律左→右**，行尾换到下一行行首（像打字机 / 段落换行），每节点出→入恒向右、流向一眼可辨，换行处仅一条「回扫线」。另把 `autoArrangeGraph` 的行列间距从固定 `NODE_GAP_X/Y` 改为**按画布上节点实际最大宽高算**——加 widget 后节点变高，固定行距会上下重叠；现节点越高 gapY 越大 → 列数越多、行数越少、回扫线越少。spec 同步改为打字机式断言。

---

## 2026-06-18 · perf(pipeline): 运行「卡一下」优化 —— 轮询渐进退避 + worker 探测缓存

**背景**：全员命中缓存的工作流点「运行」仍要卡几秒，而节点执行本身只占 ~200ms。卡顿来自「点运行 → 看到完成」链路的周边开销，非节点计算。

**两处主因 + 改动**：

- **前端轮询第一拍干等 1.5s**（`useRunExecution.ts`）：派发后 `startRunPolling` 用 `setTimeout(tick, 1500)` 起步，哪怕 worker 200ms 跑完，前端也要到 1.5s 那拍才查状态。改为**立即首拍 + 渐进退避**：首拍 0ms（顺带让画布即时显示「运行中」）、前几拍 300 / 700ms、之后回落到常规 `EXECUTION_POLL_INTERVAL_MS=1500`。缓存命中的运行 0.3–0.6s 即可见完成；长任务（如新算 TFR 16s）仍走常规间隔、不高频压后端。
- **auto 模式每次运行都 ping Celery worker**（`pipelines.py` 的 `celery_workers_available`）：用于决定走 inline 还是异步，worker 在线时是 broker 往返、离线时要等满 `CELERY_WORKER_PING_TIMEOUT_SECONDS=0.5`。加**短 TTL 可用性缓存**（新增配置 `CELERY_WORKER_PING_CACHE_TTL_SECONDS=5`，模块级、每进程一份）：TTL 内的连续运行复用上次探测、跳过 ping。worker 中途挂掉最多 TTL 秒后下次探测自愈——比写死 `PIPELINE_EXECUTION_MODE=celery`（worker 一挂任务会无限卡队列）安全；TTL 设 0 即关闭、恢复每次 ping。

**未动（仅诊断备忘）**：`_ensure_execution_input_snapshots` 每次运行重建输入快照、终态 `generate_execution_manifest`（5 组查询 + 写盘）也是每次固定开销，可后续按需优化。

**校验**：`vue-tsc --noEmit`（`useRunExecution.ts` 0 错）+ `py_compile`（`config` / `pipelines`）通过。时序待真机部署后体感验收（缓存命中的运行点击应明显更跟手）。

---

## 2026-06-18 · feat(数据集导入): 批量上传自动区分被试编号（subject）避免互撞

**背景**：用户一次选 6 个 BDF 一起导入，6 行的 subject/task/session/run 被自动填成完全一样的一套（`01 / rest / 空 / 空`）→ 它们彼此的 BIDS 四元组相同，自己跟自己撞、触发「『01·rest』已经有数据了」的判重弹窗。根因不在弹窗，而在自动填写太弱。

**根因**：`BidsUploadPanel.vue` 的 `inferEntities` 只认 BIDS `sub-XX` 文件名标签，认不出时**兜底硬填 `01`**——一批没带标签的文件（如 econ BDF）于是全填 `01`。

**修复**（纯前端，`BidsUploadPanel.vue`）：把单文件级的推断升级为「批级」消歧。

- `inferEntities` 去掉 `|| '01'` 兜底，认不出就留空，交给批级逻辑统一编号。
- 新增 `assignBatchSubjects(groups)`，在 `classifyFiles` 末尾对一整批跑两步：
  - **第一步·按文件名差异抠编号**：`deriveDistinctTokens` 对一批匿名文件的相对路径取公共前缀 / 公共后缀并剥掉，留中间「真正不一样」的那段当编号（`P01..P06`→`01..06`、`A/rec.bdf`/`B/rec.bdf`→`A`/`B`、纯数字补零对齐）；仅当每个都非空且互不相同才采纳。
  - **第二步·顺序去重兜底**：抠不出来（如中文名 sanitize 后为空、或完全同名同路径）就顺延分配 `01、02…`，且任何与「库里已有」或「本批在前」相同的四元组都跳过该号——保证既不自撞、也尽量不撞已导入数据。
- **避让已有数据**：新增 `loadExistingRecordings`（best-effort 调 `datasetApi.list` 拉当前数据集已有 recordings 的四元组，按目标缓存、上传成功后刷新；拿不到就只保证本批内部不冲突，后端仍兜底判重）。
- 唯一性口径 `(subject/session/task/run)` 与后端 `DATASET_EXISTS` 一致；自动改动后页脚提示「已按文件名自动区分被试编号…如不准确可在下方逐个修改」，用户可随时手改。

**设计取舍**：批量 EEG 上传最常见是「一文件一被试」，故消歧默认落在 **subject** 维（用户在截图里也是圈的 subject 列）；若实际是同一被试的多轮次，自动编号会偏，用户在表格里改即可——属「大致模糊」的便利，非强约束。

**校验**：`vue-tsc --noEmit` EXIT:0；另抽纯逻辑（公共前后缀剥离 / 顺序兜底 / 四元组避让）跑 8 组用例（数字编号、同名不同目录、完全无法区分、中文名、避开库里已有、explicit+匿名混合、跨位数补零）全 PASS。真机待部署后按「一次选多个无 BIDS 标签文件 → subject 列各不相同、不再弹判重」验收。

---

## 2026-06-18 · fix(pipeline): 上游命中缓存导致紧邻下游节点必然重跑一次

**背景**：LoadData → Epoch → TFR 流水线，参数全程未改、重复运行第 2 次时，Epoch 命中缓存（43ms），但下游 TFR 仍重跑 22s（按理也应命中）。

**根因**：缓存命中判定靠节点指纹 `node_hash`（= 节点类型 + 后端函数 + 参数指纹 + **输入指纹 `input_hash`**）。`input_hash`（`app/pipeline/hash.py`）为每份上游产物算签名，且**同时哈希 `data_infos` 与 `artifacts` 两个列表**。问题：同一份上游产物，「新鲜计算」（dispatcher 经 `StudyOutputSummary.to_dict()` 生成）与「命中缓存恢复」（`cache.py` 重建）两条路径，在若干「溯源 / 来源 / 表示层」字段上并不逐字段一致 —— 而旧签名把这些字段算了进去，于是两条路径签名不同 → 紧邻下游 `input_hash` 漂移 → `node_hash` 漂移 → 查不到候选缓存 → miss、白跑一次。

实测两个漂移点（缺一不可，初版只堵了第一个、未解决）：

- `_data_info_signature` 含 `processing`：缓存恢复时 `cache.py._restore_value` 往其注入 `cached = True`（新鲜运行没有）。
- `_artifact_signature` 含 `artifact_type` / `source_dataset_id`：`to_dict()` 带（`artifact_type` 恒为 `"derivative"`、`source_dataset_id` 取上游 recording），而 `cache.py._register_artifact_references` 重建时**两字段都没填**。

链条特征：**只要某节点命中缓存，它的紧邻下游就必然 miss 一次**。Epoch 能命中，是因其上游 LoadData 是 source 节点、永远新鲜跑、签名稳定；TFR miss，是因其上游 Epoch 命中了缓存。

**修复**（`hash.py`，两个签名函数）：把签名收敛成**只认「内容身份」**—— 内容哈希（`content_hash`/`sha256`/`checksum`）+ 存储定位（`storage_*`/`logical_path`/`fif_path`）+ `artifact_id` + `data_type`。一律排除溯源 / 表示层字段（`processing`、`artifact_type`、`source_dataset_id`）。这样不论上游本次是新鲜算还是缓存恢复，签名都一致，从根上消除「这一整类表示层漂移」；又不会误命中（内容不同 → `sha256` 必不同）。比打地鼠式逐字段补齐更稳：哪怕日后还藏第三个溯源字段，它也已不在签名里。

**回归测试**（`tests/test_node_hash.py`）：`test_input_hash_ignores_cached_marker_in_processing`（data_info.processing 漂移）+ `test_input_hash_ignores_artifact_provenance_drift`（artifact 的 artifact_type/source_dataset_id 漂移）—— fresh 与 cached 上游 `input_hash` 须一致；另两条 `*_still_changes_on_*`/`*_content_change` 保证真实内容变化仍改签名（不引入误命中）。顺带把过时的 `test_cache_config_change` 改成断言「cache 配置不参与 node_hash」（与现实现一致）。

**影响**：仅 `hash.py` 两个签名函数收敛。重部署会全量重置（清库清盘），故部署后第一次跑重建指纹、第二次起稳定命中，无历史脏 hash 残留。

**校验**：`py_compile`（hash）+ `tests/test_node_hash.py` 全 19 条通过。真机待部署后按「连跑两次、第二次 TFR 应显示『缓存』」验收。

---

## 2026-06-18 · fix(#39): 三个观察页 PNG 导出重绘逻辑修复

**背景（issue #39）**：PSD、Waveform 页点击 ⬇ 导出时，下载的 PNG 是 facet grid 小格的 canvas 放大版——该 canvas 可能是 portrait/方形（例如 300×400px），导致：tick 极稀（仅 0 / 500 / 1000ms）、比例与浏览器看到的宽图完全不同、轴标签被 `denseAxes` / `hideXLabels` 隐藏。

**根因**：`exportCell` 通过 `querySelector('canvas')` 拿的是屏幕上那个小 canvas，再做双线性放大，结果只是「更大的错误图」而非「正确图」。

**修复方案**（与 TFR 页 `HeatmapCanvas.getExportCanvas()` 同思路）：

| 文件 | 改动 |
|---|---|
| `TimeCourseCanvas.vue` | `buildOpts` 增 `exportMode?: bool`：强制 `dense=false`、`hideX/Y=false`；`drawLegend` 增 `forceShow` 参数；新增 `getExportCanvas()`：在 `position:fixed;top:-9999px` 隐藏 div 以 `⌈2400/DPR⌉ × ⌈2400/DPR×0.4⌉`（始终→物理 2400px）重建 uPlot，skip `drawLocked`，copy canvas 返回；`defineExpose({ getExportCanvas })` |
| `WaveformDetailPage.vue` | 新增 `cellTimeCourseRefs: any[]`；`:ref` 回调绑定到每格 TimeCourseCanvas；按钮 `@click` 传 `ci`；`exportCell(e, title, ci)` 优先走 `getExportCanvas()`，降级双线性放大 |
| `PsdPage.vue` | 同上，selector 从 `.wf-cell` 改 `.ov-cell` |

**效果**：
- 导出图为横版（2400×960 物理像素），tick 密度与浏览器全宽视图一致，轴标签完整显示，图例强制呈现。
- TFR（HeatmapCanvas）上轮已修，本次收尾 PSD + Waveform。

**校验**：`vue-tsc --noEmit` EXIT:0。

---

## 2026-06-18 · feat(observe): 频域/时域图例可交互——游标读数行 hover/click 锁定高亮（issue #40）

**背景**：PSD/时域观察页多通道叠线图，曲线颜色难以与读数列表对应，悬停时线变亮但用户不知道是哪根、也不知道亮的目的是什么（issue #40）。

**方案**：游标读数列表 = 交互式图例，并与右栏「◎ 焦点」开关联动（B 方案）——**焦点关 = 纯读数**（鼠标移动只刷新读数、图线零强调）；**焦点开 = 互动图例**（hover 临时高亮、click 锁定）。点击任意读数行/明细行在焦点关时一键开焦点并锁定该线（选项①，免去先找开关）。

> 当日先实现「无条件高亮」首版（hover 即强调），用户实测发现两个问题：①「悬停档 2.6/0.7」与「锁定档 3.0/0.35」线宽不一致（根因 `highlightLocked = !!lockedHighlight` 误把"是否有锁定"当成"当前线是否锁定"，导致已锁时 hover 别的行也走锁定档）；②未启用焦点时鼠标移动也强调，与右栏游标读数信息重复、噪音大。遂改为下述焦点门控终态。

**核心变更**：

`TimeCourseCanvas.vue`：
- `line-hover` emit：overlay 模式下 `setCursor` 钩子实时比较游标 Y 与各序列当前 Y（`valToPos` CSS 坐标系），取最近序列名 emit；去重（`lastLineHover`）避免每帧重复触发；`mouseleave` / `cursor(null)` 时自动 emit `''`
- 线宽**归一为单档**（目标 2.6 / 其余 0.7，与 `buildOpts` 初次渲染一致）：消除「悬停 vs 锁定」不一致；hover 与锁定的区别改由右栏行 `●` 标记 + 持久性表达，不再用线宽强弱。（首版的 `highlightLocked` prop 及其 watch 一并移除）

`PsdPage.vue` / `WaveformDetailPage.vue`（相同模式）：
- 三层状态：`hoveredHighlight`（瞬态，行 hover + 图线 hover）→ `lockedHighlight`（持久，行 click / 明细表 click）→ 自动缺省（PSD `focusChannel` α 最强 / 时域 `focusStat` 峰值线，两页对齐）
- `effectiveFocus = focusEnabled ? (hovered || locked || 自动缺省) : ''`——**焦点门控**：关焦点直接返回 `''`，图上零强调；统一驱动 `:highlight` 与 `is-focus` 子图边框 + 明细行
- `onLineHover()` / 行 `@mouseenter` 均 `if (focusEnabled)` 门控；`toggleLock()` 实现选项①（关焦点→开焦点+锁定；开焦点→切换锁定）；`watch(focusEnabled)` 关闭时清空 hovered/locked 回纯读数态
- 读数行 `cursor: pointer` + `@mouseenter`/`@mouseleave`/`@click`；锁定行右侧 `●` 标记；焦点开且有强调时其余行降 32% 透明（`is-dim`，hover 即点亮）

`observePage.css`（PSD 页）/ WaveformDetailPage 内联样式各加：`.is-hl` 粗体、`.is-dim` 半透、`.is-locked ●`、行 hover 背景。

---

## 2026-06-18 · fix(observe): 三观察页统计结果「复制」按钮在 HTTP 环境下静默失效

**背景（issue #43）**：时域（ERP）、频域（PSD）、时频（TFR）三个观察页的右栏「📋 复制」按钮点击后没有任何反应。

**根因**：三页的 `copyStats()` 都用 `navigator.clipboard?.writeText(text).then(...)` ——在非 HTTPS 环境（云端调试 HTTP 服务器）下，`navigator.clipboard` 为 `undefined`，可选链短路为 `undefined`，后面的 `.then()` 永远不执行，`copied` 状态不变，按钮静默无反应。

**改动**（三文件同源改动）：

- `WaveformDetailPage.vue` / `TfrPage.vue` / `PsdPage.vue` 的 `copyStats()` 函数：
  - 先判断 `navigator.clipboard` 是否可用；可用时走 Clipboard API（HTTPS/localhost）。
  - 不可用时降级到 `document.execCommand('copy')` textarea 注入方式（HTTP 环境兼容）。
  - 两条路径都触发 `showCopied()`，给出「✓ 已复制」1.2 秒反馈。

**校验**：`vue-tsc --noEmit` 通过，无新类型错误。

---

## 2026-06-18 · fix(PSD): IAF 标记改为按 condition 各自计算

**背景（issue #44）**：PSD 观察页在多条件分面时（如 Stimulus/S 5 vs Stimulus/S 3 各占一格），所有图的 IAF 竖线都落在同一位置（如 10.7 Hz），原因是 `psdMarkers` 直接取全局 `readoutStat`（跨所有 condition α 最强的那一行），同一个 marker 传给每一格。

**改动**：

- `useFacetGrid.ts`：`FacetCell` 接口补 `segs: number[]` 字段（该格涉及的段索引），cells 构建时 `out.push` 附上 `cellSegs`。
- `PsdPage.vue`：`psdMarkers` computed 改为函数 `cellPsdMarkers(cellSegs)`：
  - 单 seg 格（seg 是 grid 因素，即每格 = 一个 condition）：在 `statsRows` 里查 `seg === cellSegs[0] && chan === readoutChan`，用该条件自己的 α 峰频画标记。
  - 多 seg 叠加格（overlay = seg，所有条件叠在一张图）：降级到全局 readout stat（保持原逻辑）。
- 模板 `:markers="psdMarkers"` → `:markers="cellPsdMarkers(cell.segs)"`。

**效果**：Stimulus/S 5 和 Stimulus/S 3 若 α 峰位置不同，现在会各自标出各自的 IAF；位置相同则视觉上保持一致，不再是"强行同一位置"。

**校验**：`vue-tsc --noEmit` 通过，无新类型错误。

## 2026-06-17 · 数据集维护中心三栏滚动统一（中间表格补内部滚动 + 表头/分组钉头 + 三栏共享高度变量）

**背景**：数据集详情「数据文件」tab（数据维护中心 `DatasetMaintenanceTab`）是左·被试导航 / 中·记录表格 / 右·记录抽屉三栏。左右两栏 06-16 已做 sticky 钉视口 + 自身内部滚动，唯独中间 `.dmt-main` 是 `overflow:hidden`、无高度上限——记录一多表格一直往下长，把整个 `.page`（文档流滚动）撑高，于是**整页滚起来**：工具栏、「仅看有问题」筛选、右栏选中记录全跟着卷出屏幕。

**改动**（纯 CSS，集中在 `views/DatasetsPage.css` 的 `.dmt-*`）：

- **中间表格补内部滚动**：`.dmt-main` 由 `overflow:hidden` 改 `max-height: calc(100vh - var(--header-h) - var(--dmt-chrome))` + `overflow:auto`。高度绑视口（非写死像素，大屏自动更长）；记录少时 `max-height` 不触发、表格缩到内容高、不出滚动条。
- **表头钉顶**：`.dmt-table thead th` 加 `position:sticky; top:0; z-index:2` + 实色背景，滚到第 80 行也不丢「会话/任务/轮次/状态」列头。
- **分组小标题二级钉头**：`.dmt-group td` 加 `position:sticky; top:29px; z-index:1`，滚某被试时其名字（`sub-ADMU2848` 等）常驻表头下方；`z-index` 低于表头，相遇被表头盖住（`top` 与表头实测高 ≈30px 联动，动了表头字号/内边距才需跟调）。
- **三栏统一高度上限**：把原散在三处的常量（中 264 / 左右 28）收成 `.dmt-body` 上的单一变量 `--dmt-chrome: 264px`，三栏 `max-height` 共用——三栏**同一个底边**，左右两栏也从「内容多时垂到屏外要翻页」改成「缩到视口内自己滚」。左右两栏 06-16 的 `position:sticky` 安全网保留（只换高度公式），万一 `--dmt-chrome` 估偏致整页仍滚一点，左右栏照样钉在视口里不消失。

**两个可调旋钮**：`--dmt-chrome`（三栏底边整体高低，部署后嫌矮调小、钻屏外调大，一处管三栏）；`.dmt-group td { top }`（分组钉头贴表头位置）。`--dmt-chrome` 的 264 ≈ 顶栏外页头/标签/工具栏那一摞估高，本机无数据无法精确量，待真数据眼验微调。

**校验**：纯 CSS、无 TS/JSON 改动，无需 typecheck；本机无运行环境且无多记录数据，「数据量大」场景留到 `s2_deploy_remote.cmd` 部署后随真数据眼验。

**Why**：滚动设计两原则——①滚动区要够长（绑视口高而非固定像素，否则小窗滚动条无意义）；②尽量别让整页滚（整页滚会把工具栏/筛选/选中态卷走，内部滚动保持框架稳定）。两者靠「每栏各自封顶到视口、独立滚动、不在会滚的页里再套会滚的区」化解。此前左右栏已合规、中间栏漏网，本次补齐并把三栏收口到同一高度变量。

## 2026-06-17 · 三观察页统一分面引擎 + 轴范围手势（时域 facet 收口 / PSD·TFR 补 Ctrl+滚轮）

**背景**：时域(ERP)/频域(PSD)/时频(TFR)三张观察页的"选维度→分面→子图"布局，PSD/TFR 已走 `useFacetGrid` 组合式引擎（一个把"选中维度格内叠加、其余拆成行/列子图"的布局逻辑抽出来的可复用函数），唯独时域还留着一份 inline 手写的 facet 计算（`cells`/`facetStyle`/`gridCols`/`cellHide*` 等百余行），是第三份重复实现。同时三页的轴范围调节手势不一致：时域有 Ctrl+滚轮调幅，PSD/TFR 没有。

**改动**：

- **时域 facet 收口到 `useFacetGrid`**：`WaveformDetailPage.vue` 删掉 inline 的 `Cell` 接口 / `cells` / `facetStyle` / `legendCellIndex` / `denseAxes` / `gridCols` / `cellHideX/Y` 及派生的 `effectiveOverlay`/`facetDims`/`rowFactor`/`colFactor`，改为调用 `useFacetGrid({ segs, chans, segCount, overlayDim, segLabel, buildCell })`，本页只保留 `buildCell` 注入数据访问（多段×多通道叠加 + `scaleFor` 单位换算→µV + 按叠加维度着色）。行为零改变，三视图自此同源。
- **统一"轴范围手势"语言**：约定 **平滚=缩放自变量轴**（时域/TFR 时间、PSD 频率）、**Ctrl+滚轮=调度量轴**、**拖拽=选区间**、**双击锁定 / 右键解锁**。
  - PSD 补 Ctrl+滚轮：绕 dB 窗中心收/放 → 写入手动 Y 上下限（`onAmp`，与拖输入框同源），向上滚=窗收窄=曲线起伏放大。
  - TFR 补 Ctrl+滚轮：`HeatmapCanvas` 新增 `amp` 事件（与 `TimeCourseCanvas` 同契约），父层据相对倍率收/放色阶 `zmax`（向上滚=zmax 变小=更饱和）。
  - 三页「操作提示」浮层同步补 Ctrl+滚轮 一行。
  - 时域滚轮缩放回写「时间窗」输入框：此前滚轮只动视觉缩放(`viewX*`)、工具条数字不联动（用户实测发现）。根因是时域时间窗框本是**取数窗**(`reqTmin/Max`，喂后端重取)、与纯前端视觉缩放是两套变量，故单独补一条显示同步 watch（退出缩放回填当前已取窗、再取数由 `load()` 回填）；频域/时频的窗口输入本就是视觉缩放变量、早有回写，无此问题。
- **同批 observe 打磨**：TFR 色彩映射从 3 套扩到 13 套——含**招牌 `elys 风格` 发散色**（团队从 4 个候选比选定稿：冷端入深靛→钴蓝、暖端白→玫→红、深酒红收尾——红蓝为主·无屎黄[不碰金/桃]·深沉，借 Turbo 深度 + Viridis 沉稳，中段贴品牌主蓝 #3F5E8F × 语义红 #B0544C 一脉、与时域/频域曲线色**同家族**；**设为 ERSP 默认**，绝对功率仍默认顺序色 Viridis；比选过的另 3 款已弃）；**全站地形图统一走 elys 配色**（`TopoStrip` 加 `cmap` 入参、默认 `elys`：删掉原内置手写 RdBu LUT[本是 `rdbu` cmap 的重复品]，时域/PSD 不传 cmap 即吃默认 elys，TFR 传当前热图 cmap 以跟随选择；缓存 LUT、cmap 不变不重烤；下标 (t+1)/2·N 对发散映白心、对顺序映低→高皆成立）→ 三页地形图 + TFR 热图色系一体；其余发散 RdBu/RdYlBu/Coolwarm/Spectral/PuOr + 顺序 Viridis/Plasma/Inferno/Magma/Hot/Jet/Turbo，选择器由「发散（有基线校正）/ 顺序（绝对功率）」术语分组的按钮墙改为**渐变色卡下拉**（对标时域/频域「配色」、去抽象术语·靠色卡自解释 + 一句人话提示；`heatmapColor.ts` 重构成关键帧 STOPS 注册表，LUT 与 colorbar CSS 渐变同源生成，状态条色阶图例去「蓝/红」字样改纯 ERD/ERS 以兼容 PuOr 等非蓝红发散色）；删 TFR 通道列表「能量最强」tag（对分析人员信息量低、显刻意）；**时域 Y 轴控件由 ±5/±10/… 对称下拉改为非对称上下限手输 + 「自动」**——ERP 成分本就有正有负（P300 朝上、N200 朝下），单一 ±max 浪费半张图；与 PSD 的 dB 范围输入同款（叠加图直接用非对称 `[lo,hi]`，排列模式仍取两端最大绝对值作对称满量程＝每道泳道本性对称，地形图随主图改用非对称 `[lo,hi]`——见下条「自洽性修正」，TFR 色阶因发散色绕 0 居中故保留单一 ±zmax）。

- **自洽性修正（用户实测「主图改非对称、地形图仍 ±对称」不自洽后补，并据此通查关联细节）**：
  - **时域地形图随主图非对称**：时域地形图是绝对电压、与主图 Y 同一物理量，必须同尺度。`TopoStrip` 加 `domain` 入参，把整条 蓝→白→红 **线性铺满 `[lo,hi]`**（蓝=lo·红=hi·白=窗中点，对标 EEGLAB「色限/color limits」），色阶条标签变 `lo … hi`，时域传 `:domain="effectiveYDomain"`。注：先试过「白钉死 0µV、正负侧各自归一」，但非对称全正窗（如 0–12）会退化成半截白→红、丢掉蓝端不像头图，遂改 EEGLAB 铺满式；对称的「自动」窗白点自然回到 0µV、与旧版逐像素一致。PSD/TFR 地形图是去均值的**相对**量、`barLo/barHi` 退化为 `±vmax`（白居中即 0），不传 domain、零回归。
  - **`HeatmapCanvas` 顺序色判定硬编码 bug**：`seq = cmap === 'viridis'` 是写死的——cmap 从 3 套扩到 12 套后，Plasma/Inferno/Magma/Hot/Jet/Turbo 这 6 个顺序色会被误判成发散、走 ±zmax 映射（颜色错半张）。改为 `IS_SEQUENTIAL[cmap]`，与 `heatmapColor.heatmapLutIndex` 同口径。

**校验**：`npm run typecheck`（vue-tsc）全绿；本机无运行环境，真机行为待 `s2_deploy_remote.cmd` 部署后验。

**Why**：分面逻辑三处手写是历史债，收口到单一引擎后再改分面只动一处；轴手势统一让医生/心理背景用户在三张图间「肌肉记忆」一致——平滚看哪段、Ctrl+滚调灵敏度，三张图到处一样。

## 2026-06-17 · Group 分析节点 P1+P2：Group Merge + Grand Average PSD

**背景**：03_resting_state 测试链（60 EDF，EO/EC 各 30 被试）需要把 30 个单被试 PSD 聚合成 Grand Average。现有 dispatcher 全部是 1-to-1 或 1-to-N；N-to-1 聚合是新路径。

**改动**：

- **`app/engine/group/merge.py`（新建）**：`run_group_merge_psd(input_data_infos, params)` — 遍历所有上游 PSD data_info，用 `resolve_path_reference` 读出各 .npz，`np.stack` 堆叠成 `(n_subjects, n_channels, n_freqs)` 张量，返回 dict 含 `group_psds / freqs / ch_names / sfreq / subjects / label`。
- **`app/engine/group/average.py`（新建）**：`run_group_average_psd(data_info, params)` — 读 group_psd .npz，沿 axis=0 求 `np.mean` + `np.std(ddof=1)/√n` (SEM)，返回 `psds / psds_sem / freqs / ch_names / sfreq / n_subjects / label`。
- **`app/engine/io.py`（扩充）**：新增 `load_psd_npz`、`save_group_psd_npz`、`load_group_psd_npz`、`save_psd_grandavg_npz`、`summarize_group_psd`、`summarize_psd_grandavg` 共 6 个函数；文件格式：numpy .npz，与单被试 PSD 同族，`data_type` 字段区分。
- **`app/pipeline/dispatcher.py`（扩充）**：
  - 注册两个新 handler：`"eeg/group/merge"` → `_execute_group_merge_psd`（N-to-1，**架构新路径**，一次性读全部上游 data_infos、保存 1 个产物）；`"eeg/group/average"` → `_execute_group_average_psd`（1-to-1，读 group_psd、存 psd_grandavg）。
  - 新增导入 `run_group_merge_psd`、`run_group_average_psd`、`load_psd_npz`、`save_group_psd_npz` 等。
- **`nodes/eeg_group_merge.json`（新建）**：input `psd`（cardinality `one_or_many`），output `group_psd`（`one`），属性 `label`。
- **`nodes/eeg_group_average.json`（新建）**：input `group_psd`，output `psd_grandavg`。
- **`elys_scripts/projects/03_resting_state/run.py`（更新）**：在 PSD 节点后追加 `eeg/group/merge`（label=task.upper()）→ `eeg/group/average`，每条 EO/EC pipeline 末尾各出一个 grand average 产物。

**产物 data_type**：`group_psd`（中间张量）、`psd_grandavg`（最终 grand avg ± SEM）。

**下一步**：云端部署验证（`s2_deploy_remote.cmd`），然后 `s3_test.cmd -3` 跑静息态全链。

## 2026-06-16 · Pipeline 缓存策略重构：「无变化重跑」跳过计算

**问题**：pipeline 每次点「运行」全链路重跑，即使数据和参数完全没有改动。根因有二：

1. **枚举 bug**：`eeg_analysis_psd`、`eeg_epoch_reject`、`eeg_epoch_baseline` 三个节点 spec 写了 `compute_cost: "low"`，但 `_COMPUTE_RANK` 字典只认 `"cheap"/"moderate"/"expensive"`，`"low"` 映射 `None`→被视为「缺标签 source 节点」→保守不缓存。
2. **策略混淆**：`is_cache_eligible` 把「产物是否值得长期留存（存储成本权衡）」和「执行前是否查缓存跳过重跑」耦合在一起，STORAGE_WEIGHT=2 的评分使几乎所有节点（含计算密集的 TFR/BadChannels）都判为「不缓存」。

**改动**：

- **Fix 1 — 修枚举 bug**（`nodes/eeg_analysis_psd.json` / `eeg_epoch_reject.json` / `eeg_epoch_baseline.json`）：
  - PSD：`"low"` → `"cheap"`（Welch FFT 快速）
  - Reject Trials：`"low"` → `"moderate"`（autoreject 模式交叉验证可慢）
  - Baseline：`"low"` → `"cheap"`（减均值，纯算数）

- **Fix 2 — 拆分缓存策略**（`cache_policy.py`、`executor.py`）：
  - 新增 `should_lookup_cache(spec)`：执行前查缓存门控，策略宽松——只要不是 `interactive`/`source`/`explosive` 节点就查。
  - 保留 `is_cache_eligible(spec)`：不变，专供 `save_settings` 设 `StudyOutput.cache_eligible` 标志（存储留存决策，评分逻辑不动）。
  - `executor._restore_cached_node_output` 从 `is_cache_eligible` 改用 `should_lookup_cache`。

**效果**：无变化重跑时，13 个节点中 11 个（除 LoadData/ICA Apply 外全部处理节点）会先查历史 job 的 `node_hash` 命中缓存，命中则 status=`"cached"` 直接跳过，不再触发 MNE 计算。ICA Apply 因 `interactive=true` 硬规则维持不查（跳过会绕开 `waiting_user_input` 人工确认）。

**事实源**：`backend/app/pipeline/cache_policy.py`（+`should_lookup_cache`）、`executor.py`（门控改调 `should_lookup_cache`）、三个节点 JSON。

## 2026-06-17 · 重部署自动清中间缓存（lifespan 钩子）

**背景**：调试期每次重部署都可能改了算法逻辑，旧缓存的中间 FIF（滤波 / 坏道 / 分段等）可能已经无效。用户希望「重新部署时全部清除」，但叶节点结果（ERP Average / PSD / TFR，keep=True）应保留。

**实现**（`elys_project/backend/app/main.py`）：

- 添加 `lifespan` 异步上下文管理器 + `_full_reset_on_startup()`，挂载到 `FastAPI(lifespan=lifespan)`。
- Startup 时两步重置：
  1. **清库**：`TRUNCATE TABLE studies, dataset_assets, pipeline_*, recording_*, study_outputs, execution_outputs, async_tasks, ... CASCADE`（26张业务表），保留 `users / roles / permissions / user_roles / role_permissions`（账号不丢）。
  2. **清文件**：删除 `ELYS_STORAGE_ROOT`（`/mnt/elys_data/storage/`）下所有内容，重建 `datasets / studies / trash` 空子目录。
- 结果：每次 `s2_deploy.cmd` 重启后 → 研究项、数据集、Pipeline 历史、产物全清，测试脚本从零重建。
- 异常处理：清除失败只 WARNING，不阻断服务启动。

**事实源**：`backend/app/main.py`（`lifespan`）。

## 2026-06-17 · Pipeline 缓存 Fix 4：_artifacts_for_job 补 execution_outputs 回退

**背景**：content-addressed dedup 发生时（同数据同参数重跑），`StudyOutput` 行的 `produced_by_job_id` 保持指向原始首产 job，但 `execution_outputs` 表里已经有当前 job 的 "reused" 边。`_artifacts_for_job` 此前只查 `produced_by_job_id`，对 dedup'd job 返回空 → `restore_node_output` 不得不迭代 `_candidate_jobs` 所有历史候选（O(n)）才能找到。

**同类型系统检查结论**（在本次 fix 前做了全量审计）：

- 16 个节点 spec 标签全部正确（`compute_cost` / `output_footprint` / `interactive`），无新标签 bug。
- 所有前处理节点（Bandpass / Notch / Ch Loc / Bad Channels / Epoch / Reject Trials）均在两种模式下落盘产出 FIF，无遗漏。
- 运行面板「0个产物」：Bad Channels 成功完成但显示 0，是 dedup 导致当前 job 无 "created" 边（只有 "reused" 边）+ 前端按 `produced_by_job_id` 分组产生的视觉误差；`execution_scoped_outputs` 后端逻辑已正确通过 attribution 覆写 `produced_by_job_id`，可能是 execution_outputs 边写入时机或前端轮询时序的边缘问题，非主链 bug。
- 交替命中（Bad Channels 成功、Epoch 缓存）：正常行为——MNE 运算确定性，下游 node_hash 不变；交替只在 Fix 3a 过渡期（旧 TTL 立即过期 → GC 清盘 → 首跑重建缓存）出现，再跑一次即全命中。

**改动**（`elys_project/backend/app/pipeline/cache.py`）：

- `_artifacts_for_job` 拆成两步：
  1. 直接查 `produced_by_job_id == job_id`（首产节点，O(1)，无 JOIN）
  2. 为空时回退 JOIN `execution_outputs`（via `job_id`，dedup / 缓存命中路径，O(1)）
- 效果：dedup 场景下缓存命中仍然 O(1)，不再需要迭代整个候选列表。

**事实源**：`backend/app/pipeline/cache.py`（`_artifacts_for_job`）。

## 2026-06-16 · Pipeline 缓存 Fix 3：产物实际留存 + 快速校验

Fix 1+2 上线后，重跑仍然卡顿（Bandpass 891ms、Notch 944ms），定位到两个更底层的根因：

**根因 A — 产物立刻过期**：`save_settings.py` 原来用 `is_cache_eligible`（评分门控，只有 ICA Compute 通过）决定 TTL。其余节点 `cache_eligible=False` → `retention_expires_at=now` → GC 下次运行就删掉文件 → `_artifacts_are_valid` 找不到文件 → 缓存 miss，无论 `should_lookup_cache` 说什么都白搭。

**根因 B — 即使命中也慢**：`_artifacts_are_valid` 对每个产物调 `StudyOutputStore.sha256_file(path)`，即把整个 FIF 文件读一遍算 SHA256。500 MB FIF ≈ 1-2 秒/节点，4 个节点 = 4-8 秒额外开销，看起来像没缓存。

**改动**：

- **Fix 3a — 让中间节点产物留 7 天**（`save_settings.py`）：
  - `retention_expires_at` 改用 `should_lookup_cache(spec)` 而非 `is_cache_eligible(spec)` 判断：
    - 所有非 source/interactive/explosive 节点（Bandpass、Notch、ChLoc、BadChannels、TFR、PSD 等）→ `now+7d`
    - source/interactive/explosive → `now`（同原来逻辑）
  - `StudyOutput.cache_eligible` 字段本身不变（保留存储留存语义），只是 TTL 门控从它换成更宽的 `should_lookup_cache`。

- **Fix 3b — 缓存命中校验改用 file_size**（`cache.py._artifacts_are_valid`）：
  - 文件产物：`path.stat().st_size == artifact.file_size` 即视为有效，不再读文件内容。`stat()` 近乎无 I/O，几十微秒。
  - 目录产物（ICA）：保留 SHA256 完整校验（目录无 file_size 字段）。
  - 旧行无 `file_size`（理论上不存在，防御性降级）：仍走 SHA256。

**预期效果**：重跑无变化的 pipeline 时，处理节点逐一 `status=cached`、耗时 <50ms/节点，整条链路秒内完成。

**事实源**：`backend/app/pipeline/save_settings.py`（TTL 门控）、`backend/app/pipeline/cache.py`（快速校验）。

---

## 2026-06-16 · PSD 地形图：自定义频率区间 + 跟随游标实时联动

此前 PSD 观察页地形图只能从预设 δ/θ/α/β/γ 下拉选一个频段。两处缺陷：①无法看任意 Hz 区间（如 10–12 Hz IAF 附近精确窗口）；②鼠标在谱线上移动时地形图不跟动，用户只能静态切段。

**改动逻辑**：后端已返回每通道 `power: number[]`（原始频谱数组，与 `freqs[]` 对齐），因此地形图所需的任意频率功率**前端可直接从已取数据中算出**，无需新端点。在 `topoCells` computed 加三路分支：

- **频段模式**（原有）：沿用 `ch.bands.find(b => b.name === selectedBand)` 取后端已算好的频段均值。
- **自定义模式**：`avgPowerInRange(ch.power, freqs, lo, hi)` 在 [lo, hi] Hz 范围内对 `power[]` 求均值，用户在左栏输入 lo/hi Hz 后按 Enter 或 change 触发（同频窗输入框交互习惯）。
- **游标模式**：`powerAtFreq(ch.power, freqs, cursorHz)` 找最近频率 bin 取单点值，`cursorHz` 来自 `displayReadout.value?.x`（已有的游标读数流，双击锁定）。游标离开谱区或处于 idle 时地形图显示"游标（待移入）"空提示。

左栏 UI 在「自动/联动 Y 轴」切换下方新增第二行三按钮组：`[频段] [自定义] [跟随鼠标]`；选择「频段」才显示原来的频段下拉；选「自定义」显示两个 Hz 输入框；选「跟随鼠标」直接生效。地形图副标题（`topoSubtitle`）随模式切换显示当前频率标注（如「游标 10.5 Hz 相对功率」）；提示文字（`topoModeHint`）也对应更新。

纯前端改动（`PsdPage.vue`），`vue-tsc` 0 错，真机待 `s2_deploy_remote.cmd` 部署验证。

**事实源**：`views/PsdPage.vue`（+`topoSource`/`topoCustom*`/`powerAtFreq`/`avgPowerInRange`/`topoSourceLabel` + UI 新按钮组）。

## 2026-06-16 · 时频观察页（TFR）推翻重做：对齐时域/频域三栏壳 + 新写热图画布

把时频页从「`WorkbenchShell` + `ObserveTabs` 里、数学函数伪造数据的 SVG 热图（live 模式只是套壳显示单通道）」整体推翻，重写成与 PSD 观察页（`PsdPage`）/ 时域看图页同款的 `ov-page` 全屏三栏壳（左选择器 / 中工具条+facet 画布+状态条 / 右临床读数），并新写一个原生 Canvas 热图组件替代 SVG。背景：观察页此前多是纯设计稿（见 06-13 TFR、06-14 PSD 两条），PSD 已先一步转 live，本轮补齐 TFR。

**为什么和时域/频域「形似而神不同」**：时频比时域/频域多一维——时域/频域是 1D 折线（x=时间/频率、y=幅值/功率），而时频的功率是 **(时间, 频率) → 颜色** 的 2D 面。这条决定了一连串差异：① 热图天生**不能像折线那样多通道同图叠加**，每通道（×数据集）只能各占一张面 → 恒为分面、去掉 PSD 的「叠加维度」控件；② 取数从「一次返回多通道」变成**按 (数据集×通道) 并发取单通道**（后端 `build_tfr_heatmap` 本就单通道，复刻 `PsdPage` 的 `Promise.allSettled`、key 多一维，**零后端改动**）；③ 拖拽框选从 1D 区间变 **2D ROI**（时窗×频窗）；④ 游标读数从「各通道@某频率」变「**各图@某 (t,f) 点**」；⑤ PSD 的「Y(dB) 量程」在 TFR 变成「**色阶 ±zmax**」（自动=后端 98 分位、跨格共享保证可比，可手动）。滚轮缩时间轴 / 双击锁定 / 右键解锁的手势与时域/频域保持一致。

**给医生/心理学家的认知锚点**（[6-01](6-01-设计原则与UI约定.md)「显得简单」）：热图即教科书 ERSP——横轴时间、纵轴频率、**红=功率增强(ERS)、蓝=减弱(ERD)、白=无变化**（发散色 RdBu、0 居中，与 `TopoStrip` 地形图色源一致）；t=0 一条红色刺激线钉死事件点；δθαβγ 频段淡参考线分带；默认只开一张「刺激后能量最强」通道的大图、不糊一墙；右栏英雄数字给**峰值 ERD/ERS（值 + 出现的 t,f）**+ 频段功率变化条（刺激后均值、有正负），全是临床能直接读的话、明确不作诊断。绝对功率（无基线校正）自动切 Viridis 顺序色。

**新增前端件**：
- `components/observe/HeatmapCanvas.vue`：原生 Canvas 画时频面 + 冒泡游标/框选/缩放，与时域/PSD 的 `TimeCourseCanvas` 对称（宿主/DPR/事件/受控缩放同套路）。渲染走「原始矩阵(nT×nF)烤进离屏 `ImageData` → 按视窗子矩形 `drawImage` 拉伸 + 双线性插值」得 MNE 风平滑面；底图（面+轴+频段线+刺激线）缓存，鼠标移动只 blit 底图叠十字线、不重算面。
- `components/observe/heatmapColor.ts`：发散 RdBu（有符号功率）/ 顺序 Viridis（绝对功率）两套 LUT + colorbar CSS 渐变 + 单值取色，发散色与 `TopoStrip` 严格同源。

**复用 / 刻意不复用**：复用 `useMultiSelect`（数据集/通道多选）、`usePalette`（卡片描边/圆点的类别色，注意这与「热图色阶」是两回事）、`observePage.css` 三栏壳；**不复用** `useFacetGrid`（它为「选中维度同图叠加」而设，TFR 不叠加，分面退化为 数据集×通道 笛卡尔积，单写更直白）与 `useCursorState`（它存 1D `{x,items}`，TFR 游标是 2D `{t,f}`，本页手写三态）。

**频段地形图（同日补，应用户「没有地形图」反馈）**：补「热图缺空间维」这块——医生看完「α 在 0.5s 去同步」必问「在头皮哪个区」。新增后端端点 `GET /outputs/{id}/tfr/topo`（`tfr_view.build_tfr_topomap`）：读 `-tfr.h5` 全通道、取 (时窗×频窗) 内每通道均值 + **真实 montage 2D 坐标**（h5 带完整 info，`channel_positions_2d(tfr.info, …)`，比 PSD 的 npz 无坐标、靠标准帽兜底更准）。前端复用 PsdPage 同款 `TopoStrip` 条：地形图窗口 = 框选 ROI 时跟随 ROI，否则 选中频段 × 时窗（默认刺激后，跟随时间缩放）；功率**有符号**(ERD<0/ERS>0)直接发散色绕 0 上色（比 PSD 绝对 dB 还干净——PSD 全负要去均值，TFR 仅 baseline=none 的绝对功率才去均值）。多数据集时每数据集一张头皮卡。这是本次**唯一的后端改动**。

**多数据集对比改自动发现（应用户「为什么只能显示一个数据集」反馈）**：原先多数据集并排依赖 URL 预拼多个 `study_output_id`（artifact 预览入口每次只传一个 → 只显示一个，与 PSD 同机制）。改为页面挂载后调 `listStudyOutputs(studyId, {data_types:['tfr']})` **自动发现同研究项下其它 TFR 产物**，追加进左栏「数据集」多选列表（默认选中 URL 带的、其余可勾选并排）；`outputIds` 从 URL 常量改为可增长 ref，选择变化经既有 watch 增量取数（热图 + 地形图）。免手动拼 URL；这套自动发现亦可回头反哺 PSD（PSD 仍依赖 URL 多 id）。

**频段维度先做后撤（同日，按用户反馈）**：曾把频率轴刻度/网格改按 δθαβγ 边界(1/4/8/13/30/80)打 + 选中频段琥珀高亮，意在合并「频段参考线」与「网格线」。用户验后判定**太丑、不通用**——δθαβγ 写死，但数据频率范围未必覆盖全（如只有 α/β 时硬塞 5 段），且把坐标轴搞乱了。**遂直接移除整个「频段」交互维度**：删左栏频段 pills + 热图上的频段线/名/选中高亮，**频率轴回归普通均匀刻度**（niceTicks，与时间轴同风格）；右栏「频段功率变化」条改为**只列后端实际返回的频段**（`tfr.bands` 已跳过窗外频段，α/β-only 数据就只显示两条，不再硬塞 0）；地形图取数窗口从「选中频段×时窗」改为「**当前频窗×时窗**」（想聚焦某频段用频窗输入框缩范围或框选 ROI，更通用）。`drawFreqAxisBands`→`drawFreqAxis`，画布字体顺手从失效的 CSS 变量改回 `monospace`。教训：固定脑电频段当**强制**可视化维度不通用，留给用户用频窗/ROI 自定义范围更稳。

验证：全项目 `vue-tsc` 0 错（含本页新文件，已与并行会话的 PSD 改动合并后复跑）；后端 `tfr_view.py` / `study_outputs.py` `py_compile` 过。本机无运行环境，真实热图渲染（插值面/色阶/2D 游标/ROI 均值/并发多通道取数/峰值 ERD/ERS/地形图坐标/多数据集发现/频率轴均匀刻度·右栏频段条只列实际频段·地形图按频窗）待 `s2_deploy_remote.cmd` 真机部署验证。

**事实源**：前端 `views/TfrPage.vue`（推翻重写）、`components/observe/{HeatmapCanvas.vue,heatmapColor.ts}`（新）、`types/index.ts`（+`StudyOutputTfrTopo`）、`api/pipelines.ts`（+`getStudyOutputTfrTopo`）；后端 `pipeline/tfr_view.py`（+`build_tfr_topomap`）、`routers/study_outputs.py`（+`GET /tfr/topo` 端点）。单通道热图沿用既有 `/tfr` + `build_tfr_heatmap`（未改）。

## 2026-06-16 · 「调整归类」relabel：在线改 BIDS 实体 + 物理重排派生层

补全两层模型里"组织可调"的另一半——让用户在记录抽屉里直接改 `subject/session/task/run`，不必为一个标签错误重新上传。关键认知：BIDS 实体**烧死在物理文件名 + 三张表**（`recordings` / `recording_versions` / `dataset_files` 的路径字段），所以改标签 = 真·重排，不是改个列。

**唯一性已拍板 = Study 级**：schema `uq_recordings_bids_entities` = `(study_id, subject, session, task, run)`（不含 asset），原"待定 Study/Asset 级"按现状定为 Study 级。relabel 的冲突检查同口径（同研究项内撞车则 409、排除自己），见 [[pending-design-decisions]]。

**后端**（`dataset_imports.py` + `dataset_recordings.py` + `schemas/dataset.py`）：
- 新服务 `relabel_recording`：`normalize_bids_label` 规范化新实体（`sub-`/`task-`/`ses-`/`run-` 前缀，带不带都行）→ study 级排重 → **物理重排派生层**：按新实体用 `build_canonical_fif_base_path_for_asset` 重算 canonical FIF + sidecar 路径、`shutil.move` 旧→新（仅移存在的文件）→ 同步 `recordings.fif_path` / 各 `recording_versions` 的 `fif_dir/fif_path/sidecar_paths` / 各 `dataset_files`（仅 `fif*` 派生角色）的 `storage_uri/relative_path/logical_path` → 找/建目标 `Subject` 重指 → 写 `session/task/run`。**原始上传（`original_upload` / sourcedata）一律不碰**（不可改存档）；没生成过 canonical 的记录不无中生有路径。
- 新端点 `PATCH /studies/{id}/recordings/{rid}`（`require_study_write` + `data:write`），事务化、失败 rollback，记 `recording.relabeled` 审计。
- 新 schema `RecordingRelabelRequest`。

**前端**（`useDatasetRecordings` + `DatasetMaintenanceTab` + `api/datasetAssets` + `types`）：抽屉「归类」区加「调整」→ 内联表单改四元组 → `recordingApi.relabel`（PATCH）→ 成功后重载列表 + 该记录版本/文件缓存；撞车显示后端 `RECORDING_LABEL_CONFLICT` 人话提示。一行提示「改标签只重排 BIDS 组织、不动原始数据」。

**未做 / 待真机重点**：旧分析结果在 relabel 后是否提示重跑（同重新上传那条，待拍）。本机无运行环境，**改标签后文件物理是否真挪到新 BIDS 路径、三表路径是否同步、撞车 409、被试改名后 Subject 重指**均待 `s2_deploy_remote.cmd` 真机验。验证：后端 `py_compile`（imports/recordings/schemas）过、前端 `vue-tsc` 0 错。

**事实源**：`backend/app/routers/dataset_imports.py`（`relabel_recording`）、`backend/app/routers/dataset_recordings.py`（PATCH 端点）、`backend/app/schemas/dataset.py`、`frontend/elys-web/src/composables/datasets/useDatasetRecordings.ts`、`frontend/elys-web/src/components/datasets/DatasetMaintenanceTab.vue`、`frontend/elys-web/src/api/datasetAssets.ts`、`frontend/elys-web/src/types/index.ts`。

## 2026-06-16 · 数据管理「两层」落地：记录抽屉重组 + 上传决策换人话

接着上一条上传页重构，和用户深聊"数据管理怎么让用户感知"。结论：用户真正拥有两层——**①「原始数据（你上传的）」像档案，按第几次上传存档、不可删改；②「归类」（BIDS 被试/会话/任务/轮次标签）可随时调整、决定用哪次上传**。系统的"标准化 / 派生 .fif"不是用户层、只是个状态。原抽屉用「两份数据（原始 + 标准化）」「可重建（删了能再生成）」把系统内脏摆给医生看，违背 [6-01](6-01-设计原则与UI约定.md)「显得简单」。

**A. 记录抽屉（`DatasetMaintenanceTab`）重组**：
- 元数据 dl → 「**归类（标签）**」chips（被试 / 会话 / 任务 / 轮次 + 通道·事件），为将来在线改标签留位。
- 「两份数据」→ 「**原始数据（你上传的）· 存档不可改**」的**上传历史列表**：按 `version_seq` 倒序列「第 N 次上传」，`current_version_seq` 那条标「当前使用」、其余「已替换·留作历史」；**当前那次展开列真实源文件名**（`source_files`，让"可溯源"从一个数字变成"我认得的文件"）。下方一句「上传过的原始数据不会被修改或删除」+「重新上传这条记录」（跳「上传」tab）。
- 标准化文件 track → 收敛成一行状态「**分析数据：已整理好，可直接分析**」（或"稍后自动准备"），**不再出现 标准化 / 派生 / 可重建** 等系统说法。
- 数据来源：接 `recordingApi.listVersions`（类型 / 端点本就有、前端没用过），`useDatasetRecordings` 加 `recordingVersionsById` + `loadRecordingVersions`（懒加载、按 seq 倒序、接口不可用时静默退化为「文件桶」兜底显示一条「已上传」），`DatasetRecordingRow` 补 `currentVersionSeq`。

**B. 重新上传决策弹窗（`BidsUploadPanel`）换人话**：检测到记录已存在时的弹窗，从「这条记录已存在，保存为新版本？/ 确认上传新版本」改成「**「sub · task」已经有数据了 → 作为新一次数据上传**」；正文讲清"之前留作历史、分析自动改用这次"；摘要行从 `dataset_id` / `upload-002` 改成「之前已上传 N 次 / 这次作为新一次数据 / 之前的留作历史」；「取消」→「取消（去改标签）」对应"其实是另一条记录"的分支。后端机制未动（upload_seq + 当前指针 + 失败保旧 早就有），纯文案。

**未做（按说定留下一步）**：在线「调整归类」改 BIDS 标签的**写接口**——依赖四元组唯一性规则（待拍板），本轮抽屉只读展示标签、不开放编辑。另：第 2 次上传成为当前后，**基于第 1 次跑出的旧分析结果**如何标记（建议提示重跑而非默默失效），待拍板。

验证：前端 `vue-tsc --noEmit` 0 错。本机无运行环境，上传历史真实渲染（多版本 / 真实文件名）、`listVersions` 云端连通、重新上传决策走通待 `s2_deploy_remote.cmd` 真机验。

**事实源**：`frontend/elys-web/src/components/datasets/DatasetMaintenanceTab.vue`、`frontend/elys-web/src/composables/datasets/useDatasetRecordings.ts`、`frontend/elys-web/src/components/BidsUploadPanel.vue` 及 `DatasetsPage.css`。

## 2026-06-16 · 数据集「上传」页布局重构（去伪按钮 + 单一主线）

用户反馈：上传页按钮太多、不知道点哪个，尤其顶部「继续上传」点了没反应。排查确认它不是死代码，但 `@click` 只调 `scrollToUploadPanel()` 把页面滚到下方上传面板——而该面板本就在同屏可见，于是"毫无效果"；且整页有三个并列的蓝色主按钮（继续上传 / 选择数据 / 导入）在抢视觉焦点，真正的入口（选择数据）反被埋在第 5 层。

按 [6-01 设计原则](6-01-设计原则与UI约定.md) 的渐进披露 / 薄层向导，重排为"选数据 → 导入"一条主线：

- **删顶部「可以上传了 / 继续上传」横条**（`DatasetImportTab.vue`）。它报的目标信息与下方 `BidsUploadPanel` 完全重复，按钮只滚屏。自动准备导入目标的 `onMounted` 静默 bootstrap 保留、用户无感；导入 tab 退化为「bootstrap 提示 + 上传面板」薄壳。
- **拖拽区（选择数据 / 选择文件夹）提到面板最顶**当主入口（`BidsUploadPanel.vue`）。删横条后全页只剩 `选择数据` 与 `导入` 两个 primary，且分处"选"与"提交"两步、不再抢戏。
- **目标信息压成一行**：删「数据集 / 研究项 / 目标状态」三卡（`bids-target-context`），其内容由头部 `uploadTargetSentence`（上传到「X」）+ 页脚"系统正在自动准备"覆盖。
- **三个散落折叠区合并为一个「高级选项」**：原「导入处理过程」说明 + 「BIDS 实体默认值」并入同一 `<details>`，置于文件列表与「导入」按钮之间。

**连带清理**（调试期直接删、不留死代码）：composable `useDatasetImportTarget` 去掉无消费者的 `scrollToUploadPanel` / `uploadPanelRef`（`scrollToUploadSection` 仍用于 bootstrap 后滚动、保留）；`BidsUploadPanel` 去掉随三卡失效的 `uploadStudyName` computed 与 `formatStudyName`；导入 tab 删掉已失效的「关联名称」输入（静默 bootstrap 早已用默认 `primary` 建好挂载、用户来不及改；新建数据集表单仍保留该能力）。死 CSS（`dataset-target-panel*` / `bids-target-context*` / `dataset-mount-field` / `dataset-upload-panel-anchor`）从 `DatasetsPage.css` / `style.css` 同步移除。

验证：前端 `vue-tsc --noEmit` 0 错。本机无运行环境，实际观感（拖拽区置顶 / 高级选项折叠 / 移动端响应式）待 `s2_deploy_remote.cmd` 真机验。

**事实源**：`frontend/elys-web/src/components/datasets/DatasetImportTab.vue`、`frontend/elys-web/src/components/BidsUploadPanel.vue`、`frontend/elys-web/src/composables/datasets/useDatasetImportTarget.ts`、`frontend/elys-web/src/views/DatasetsPage.vue` 及 `DatasetsPage.css` / `style.css`。

## 2026-06-16 · 修 PSD Condition 选择器始终空白

**问题**：Pipeline 编辑器里选中 PSD 节点，Condition 区始终显示「上游加载节点暂无可用事件」，用户无法选 condition；运行时后端报 `PSD.condition is required`。

**根因**：`useEventSelectEditor.ts` 里 `isConditionFromEpoch` 判断只写了 ERP / TFR，漏掉 PSD——导致 `availableEventLabels` computed 对 PSD 节点走空值分支返回 `[]`，选择器永远空。

**修复**（三行）：
- `pipelineConstants.ts`：新增 `export const PSD_NODE_TYPE = 'eeg/analysis/psd'`
- `useEventSelectEditor.ts`：import 加 `PSD_NODE_TYPE`，`isConditionFromEpoch` 加 `|| PSD_NODE_TYPE`

验证：`vue-tsc --noEmit` 0 错，真机待部署。

**事实源**：`frontend/elys-web/src/composables/pipeline/pipelineConstants.ts`、`frontend/elys-web/src/composables/pipeline/useEventSelectEditor.ts`。

## 2026-06-16 · PSD 增 FFT 单段法（SSVEP）+ 频段绝对/相对功率

应用户两点频域需求落地，**避让并行会话正在重写的 PSD 显示层**（`psd_view.py` / `PsdPage.vue` / `types/index.ts` 都是它的活），本次纯落在后端引擎 + 节点 spec。

**A. FFT 单段（SSVEP）**：`method` 加 `fft`。SSVEP（稳态视觉诱发电位）盯单个刺激频率点（如 12Hz 及谐波），而 Welch 分段平均靠牺牲频率分辨率换低方差——恰恰是 SSVEP 最不想要的。`fft` 让 Welch 退化成单段整段周期图（`n_per_seg`=整段、`n_overlap`=0），频率分辨率 = 1/时长拉满。引擎 `_psd_kwargs` 统一构造 kwargs 并上报 `method=fft`；Epochs 用每 epoch 点数、Raw 用整段点数。

**B. 频段绝对 + 相对功率**：引擎 `_band_powers` 在**线性域梯形积分**（∑PSD·Δf）算 δ/θ/α/β/γ 的绝对功率（µV²）+ **相对功率**（频段 ÷ 所分析全谱），跨通道均值经 `summarize_psd` 进产物 preview。补上「相对功率」（跨被试可比、临床报告最常用，此前完全没有，是硬伤）；且用严格积分，修掉显示层 `psd_view._band_stats` 那种「dB 域求平均」的不严谨——显示层是并行会话区、本次不碰，只在引擎给出正确数据源。相对功率必须线性域算（dB 是对数不能相除）。完整 per-channel / 可导出的频段功率，留给将来的 `band_power` 一等节点（psd→analysis_result）。

验证：`py_compile`（psd / io）+ PSD 节点 JSON 校验（method = welch / multitaper / fft，默认 welch）过。本机无运行环境，FFT 单段实跑 + 频段功率数值待 `s2_deploy_remote.cmd` 真机验。

**事实源**：`backend/app/engine/analysis/psd.py`、`backend/app/engine/io.py`、`backend/app/pipeline/nodes/eeg_analysis_psd.json`。

## 2026-06-16 · 新增「试次剔除」节点（Reject Trials，`eeg/epoch/reject`）

接「pipeline 节点设计梳理」里点名的质量缺口——**试次级伪迹剔除**（此前只有坏**通道**处理，没有坏**试次**剔除）。这是对标隔壁 Niantong 的 automark / 坏段盘点里、三大分析最该补的一块：分段后、ERP / 时频 / 频域平均前，把含眼动 / 肌电 / 漂移等大伪迹的整段 epoch 扔掉，否则脏试次会污染平均结果。

**A. 节点设计（一个节点、两种方法）**：`method` 下拉切换，符合 [6-01](6-01-设计原则与UI约定.md)「按科研决策边界拆节点」——「剔坏试次」是同一个科研决策，算法是参数，与 Filter 的 FIR/IIR、ICA 的 fastica/Picard、Bad Channels 的 LOF/RANSAC 同构。
- `threshold`（默认，**零新依赖**）：MNE 内置 `epochs.drop_bad(reject=, flat=)`——任一通道在某 epoch 内峰峰值（max−min）超阈、或低于平坦阈值，剔掉整段（经典 ERP 做法，Luck 2014）。阈值前端以 µV 给、引擎换算成 V。
- `autoreject`（Jas et al. 2017，需库）：交叉验证给每通道自动学阈值，还能先插值少量坏通道再决定剔不剔；更省心但慢、插值需电极坐标（先接 Ch Loc Assign）。

**B. 端口与执行**：`epochs → epochs`（端口类型精确匹配，画布天然接得上 Baseline/ERP/TFR/PSD，无需动 litegraph）。引擎 `analysis/reject.py` 的 `run_reject_trials` 按 `method` 分流、返回 `(cleaned_epochs, meta)`。dispatcher 新增 `_execute_reject_trials`，与 `_execute_baseline` 同构（epochs→epochs、保留上游 condition），复用 `_save_epochs_dataset`——给它加了个可选 `extra_summary` 参数，把剔除溯源（`n_epochs_before/after` / `n_dropped` / `drop_fraction` / 判据）并进 summary，同时进 preview 与 `mne_summary`，**剔除过程非黑箱**。

**C. 依赖**：`requirements.txt` 加 `autoreject==0.4.3`——依赖 mne(≥1.0，有 1.7.1 ✓)/numpy/scipy/scikit-learn，咱们全有且不冲突。引擎对 autoreject 用懒加载（未装时只 `autoreject` 方法失败、`threshold` 不受影响），**云端重部署即 `pip install`**。

**D. 节点参数**：`method`（threshold / autoreject）；`reject_peak_to_peak`（峰峰值阈值 µV，默认 150，`visible_when method=threshold`）、`flat`（平坦阈值 µV，选填，advanced）；`n_interpolate`（autoreject 每 epoch 最多插值通道数，默认 4，`visible_when method=autoreject`）——只露当前算法相关旋钮。

**影响面**：后端执行器 13→14、节点数 14；前端**零改动**（节点面板从后端 registry 自动渲染、归入 `epoch` 分组，端口同为 `epochs` 画布原生连通）。建议接线位置：`Epoch →（Baseline）→ Reject Trials → ERP / TFR / PSD`。

验证：后端 `py_compile`（reject / dispatcher）过、节点 JSON 校验过（`epochs→epochs`、methods=threshold,autoreject）、节点目录计数 14、`registry._validate_spec` 必填字段齐（不对端口类型做枚举）。本机无运行环境，**threshold 实际剔除 + autoreject `pip install` 后跑通**待 `s2_deploy_remote.cmd` 真机验（mne 锁 1.7.1）。

**事实源**：`backend/app/engine/analysis/reject.py`、`backend/app/pipeline/dispatcher.py`、`backend/app/pipeline/nodes/eeg_epoch_reject.json`、`backend/requirements.txt`。

## 2026-06-16 · PSD 接受连续数据（raw→PSD）+ Bad Channels 顺序软护栏（P2 / P4）

接「pipeline 节点设计梳理」一轮分析后落地两条最高性价比修复（scoped 在时域 / 频域 / 时频三类分析内）。

**P2 · PSD 节点开通连续数据输入（静息态频域）**

问题：频域分析 `eeg/analysis/psd` 入口端口此前写死只吃 `epochs`，而 `Epoch` 节点必须有事件才能切分（无事件直接 `No events found` 报错）。结果**静息态 / 无事件数据切不了 Epoch → 到不了 PSD → 频域分析整条路对静息态不可用**。而临床最常见的频域需求（α 峰、各频段功率）恰恰是静息态。MNE 的 `raw.compute_psd()` 本就能直接在连续数据上算。

改动：
- **新端口类型 `spectral_source`**（频谱类输入，语义=「能估谱的东西」），兼容矩阵里同收 `eeg_data` + `epochs`（前端 `pipelineFormatters.ts`、后端 `validator.py` 两份镜像各加一条）。`eeg/analysis/psd.json` 入口 `type` 从 `epochs` 改 `spectral_source`、label 改 `EEG / Epochs`。
- **litegraph 连线放行**：litegraph 0.7.x 的原生 `isValidConnection` 是**字符串相等**判定（13 个现有节点全是精确同类型相连，所以一直没暴露），新端口类型若直接给会被画布在我们的 `portTypesCompatible` 跑之前就拦死。利用 litegraph 支持**逗号分隔类型列表**的特性，`liteGraphPortType('spectral_source')` 展开成具体的 `'epochs,eeg_data'`，让画布拖 `Filter`/`LoadData`/`Epoch`→`PSD` 都原生通过、同时仍挡住 `evoked`/`tfr`。
- **引擎 `run_psd` 按输入分流**：`hasattr(data, "event_id")` 区分（只有 Epochs 有 `event_id`）。Epochs 走原逻辑（按 condition 选 → Welch → 跨 epoch 平均）；连续 Raw 走新增 `_run_psd_continuous`（整段一条谱、无 condition、无 epoch 平均，`RawSpectrum.get_data()` 直接出 (n_channels, n_freqs)）。
- **dispatcher `_execute_psd_output` 加 raw 分支**：新 helper `_data_info_is_epochs`（按 `data_type` + `-epo.fif` 后缀判形态）分流；raw 一进一出、`condition=None`、显式释放 ~500MB raw 防 OOM（对齐 raw 预处理路径）。
- **节点参数**：`condition` 从 `required:true` 改 `required:false`（仅 Epochs 输入需要；Epochs 缺 condition 仍在运行期报错，与 ERP/TFR 同口径）。`save` 把名模板拆成 `name_template_default`（无 condition，连续数据用）+ `name_template_default_split`（带 condition，Epochs 用），`dynamic_tags_always`→`dynamic_tags_when_split`、去掉 `always_per_condition`——这样静息态 PSD 名字不再硬塞一个空 `{condition?}`。

**P4 · Bad Channels 顺序软护栏**

问题：`eeg_data` 阶段内所有预处理（filter / resample / reref / chanloc / bad_channels / ICA）端口同为 `eeg_data→eeg_data`、可任意排序，类型系统无法表达科研顺序约束。`Bad Channels` 用插值或 RANSAC **需要电极坐标**，必须排在 `Ch Loc Assign` 之后，此前只在节点描述里写了句话、接反了要等**运行期才炸**。

改动：`validator.py` 新增 `NODE_ORDER_HINT`（severity=warning）——主链路可达的 `Bad Channels` 节点若 `action=interpolate` 或 `method=ransac`，但上游 BFS 找不到 `Ch Loc Assign`，校验期就给一条中文软提示（不阻断校验，因部分数据导入时已自带 montage）。把「缺坐标」从运行期失败提前到编辑期提示。

**影响面**：频域分析对静息态 / 无事件数据可用（`LoadData`/`Filter`→`PSD` 直连即整段谱）；时域 ERP、时频 TFR 入口与逻辑**未动**（仍只吃 Epochs）。已知小尾巴：保存设置面板（`useSaveSettingsPanel.ts`）的标签**预览**仍按 `always_per_condition` 取值，Epochs-PSD 的 `cond:` 标签预览不再显示（运行期实际仍正确写入），纯预览差异、后续可收口。前端 event_select 在 raw 上游时无事件 chips、降级为空（`condition` 选填不报错）。

验证：后端 `py_compile`（psd / dispatcher / validator）过、PSD 节点 JSON 校验过（`spectral_source` / `condition.required=false`）、`registry` 不对端口类型做枚举校验（新类型可注册）、现有 `test_dispatcher_split` / `test_save_settings` 用的是合成 spec 不读 PSD JSON（不受影响）；前端 `vue-tsc` 0 错。本机无运行环境，**raw→PSD 端到端跑、画布拖 `Filter`→`PSD`、event_select 在 raw 上游降级、保存面板标签预览**均待 `s2_deploy_remote.cmd` 真机验。

**事实源**：`backend/app/engine/analysis/psd.py`、`backend/app/pipeline/dispatcher.py`、`backend/app/pipeline/validator.py`、`backend/app/pipeline/nodes/eeg_analysis_psd.json`、`frontend/elys-web/src/composables/pipeline/pipelineFormatters.ts`、`pipelineConstants.ts`。

## 2026-06-16 · 时域看图页加绘图手势（滚轮缩放 / Ctrl+滚轮调幅 / 操作提示）

补齐对标隔壁 Niantong 盘点里时域查看器仅剩的「绘图手势」缺口（原则仍是抄能力不抄颗粒度）。落在 `TimeCourseCanvas.vue`（手势行为）+ `WaveformDetailPage.vue`（缩放态 + 操作提示 UI）。

**A. 滚轮缩放时间轴**：滚轮绕游标所在时刻缩放 x 轴（上滚放大 / 下滚缩小、缩到全幅自动退回自动量程），对标 EEGLAB / MNE 等查看器的肌肉记忆。`TimeCourseCanvas` 在宿主上挂 `wheel` 监听（`passive:false` 才能 `preventDefault` 阻止页面滚动）。

**B. Ctrl+滚轮调幅度**：overlay（叠加）模式改 y 量程（`yRange = 基准 ÷ ampScale`）、spread（排列）模式改泳道波高（`ampScale` 折进 `effYMax`）；回到 1× 附近自动吸附，避免卡在 1.03× 这类尴尬值。

**C. 缩放态父层广播（关键设计）**：缩放状态不放子图、由父层 `WaveformDetailPage` 持有（`viewXMin` / `viewXMax` / `ampScale`）并作为 props 下发给**所有** `TimeCourseCanvas` 子图——facet 多格对比时各格同窗缩放，**不破坏本日早些做的共享 facet 轴**（否则缩一格、底排刻度就对不上）。子图滚轮只 `emit('zoom'/'amp')`、本格乐观就地 `setScale` 免一帧延迟，兄弟格走 props 回灌；声明式 `range` 函数读 props 保证 `setSize` / `setData` 不把缩放冲掉。**纯前端视觉缩放、不回后端取数**——契合「连续 raw 流式浏览归其它部件、本页不做」的划分。

**D. 操作提示 + 缩放指示**：工具条原「导出 PNG」单行提示升级为 hover 悬浮片，用 `<kbd>` 小键帽列全 6 手势（滚轮 / Ctrl+滚轮 / 拖拽 / 双击 / 右键 / ⬇）；状态条出现 `🔍 视窗 · N×` 指示、点一下一键复位。取新数据 / 换 Y 档 / 切叠加排列自动复位缩放，换通道选择则保留（加根通道不打断正在看的细节）。

**E.（同次提交收口）此前未落库的观察页打磨**：这两个文件接手前已 dirty，提交一并收入其完整当前状态（故 diff 达 809/300 行），含先前未提交的：双击锁定游标 / 右键解锁 + 游标三态、右栏三段式（焦点开关 · 条件对比卡 · 折叠明细表）、期刊 / 色盲配色下拉、叠加维度模型 + 共享 facet 轴、全屏与左 / 右 / 地形栏开关、通道 / 段 Ctrl 加选 · Shift 连选、游标 emit 合帧 + 高亮就地改线宽等性能重构。

验证：`vue-tsc` 0 错；本机无运行环境，手势手感 / facet 同窗缩放 / 复位均待 `s2_deploy_remote.cmd` 真机验。提交 `5945f85`（feat/figure）。

**事实源**：`frontend/elys-web/src/components/observe/TimeCourseCanvas.vue`、`views/WaveformDetailPage.vue`。

## 2026-06-16 · 坏道节点补 RANSAC（pyprep / PREP）检测算法

继 06-15 的自动坏道节点（当时只有 LOF），应用户要求给 `eeg/preproc/bad_channels` 补第二种检测算法 RANSAC——**一个节点、两种算法**（`method` 下拉切换），与 Filter 节点 FIR/IIR、ICA 节点 fastica/Picard 同构，符合 [6-01](6-01-设计原则与UI约定.md)「按科研决策边界拆节点」（找坏道是同一个决策、算法是参数，不为两种算法拆两个节点）。

**A. RANSAC（PREP 流程）**：检测 = pyprep `NoisyChannels.find_bad_by_ransac`——随机抽样「看着正常」的电极、按电极位置互相预测每个通道该长啥样，预测相关性低于 `corr_thresh`（默认 0.75）判坏；比 LOF 激进（标更多）。固定 `random_state=42` 保证同输入同结果可复现。

**B. 接线约束**：RANSAC 靠电极坐标做空间预测，故选 RANSAC 时**和插值一样需要先接 `Ch Loc Assign`**（缺 montage 报中文错）；LOF 不需要坐标。引擎对 pyprep 用懒加载（未装时只 RANSAC 失败、LOF 不受影响）。

**C. 依赖**：`requirements.txt` 加 `pyprep==0.7.1`——它依赖 mne(≥1.3，有 1.7.1 ✓)/numpy/scipy 咱们全有且不冲突，**净增只有 pyprep + psutil**（psutil = 进程/内存小工具），不拉 sklearn / matplotlib；Python ≥3.10（云端 3.11 ✓）。**云端重新部署即 `pip install`**。

**D. 节点参数**：`method` 下拉加 `ransac`；LOF 的 `threshold` / `n_neighbors` 改 `visible_when method=lof`、新增 RANSAC 的 `corr_thresh`（`visible_when method=ransac`）——只露当前算法相关的旋钮。

验证：`py_compile` 过、节点 JSON 校验过（methods=lof,ransac）；本机无运行环境，RANSAC 实际检测待云端 `pip install pyprep` + 真机跑（mne 锁 1.7.1）。

**事实源**：`backend/app/engine/preprocess/bad_channels.py`、`backend/app/pipeline/nodes/eeg_preproc_bad_channels.json`、`backend/requirements.txt`。

## 2026-06-16 · 地形图改薄板样条插值 + 电极中性标记（对标 EEGLAB/MNE）；多通道配色铺满；游标抖动

接上一条时域看图页配色后的几处对标经典软件的修补，落在 `TopoStrip.vue` / `channelColor.ts` / `WaveformDetailPage.vue`。

**A. 地形图色面：IDW → 薄板样条（thin-plate spline）**：`TopoStrip` 原本用 IDW（反距离加权 `1/dist²`，本质是加权平均）插色面，毛病是把场往均值压扁、电极附近起「牛眼」伪影。查了三大软件画法——EEGLAB `topoplot` 默认 `v4`（双调和样条，Sandwell 1987）、FieldTrip `ft_topoplotER` 同样默认 `v4`、MNE `plot_topomap` 默认 `cubic`（Clough–Tocher 分片三次）——共同点是用**穿过电极真值、最小化弯曲能量的样条**，而非加权平均。改 `buildSurface` 为薄板样条 `f(x,y)=a0+ax·x+ay·y+Σ wⱼ·φ(r)`（`φ(r)=r²ln r`，与 v4 双调和同族），自带高斯-约当线性求解器（电极数小、O(n³) 够快、每张图解一次），退化（电极共线）时回退为无色面、只留标记。

**B. 电极改中性标记（不按值填色）**：原本电极点按各自真值填色叠在 IDW 色面上，因色面被压扁 →「点鲜艳、底发灰」对不上。三大软件都把电极画成**中性小标记**（黑/白点、十字），底色负责表达数值、点只标位置。照办：电极改白底 + 深描边小点（红/蓝/近白底色上都看得见），真值移到 hover `title`。A+B 叠加后色面穿过真值、点也不再与底色冲突。

**C.（同批）多通道配色铺满**：`channelColor` 原本超出色板长度就走与色板无关的黄金角彩虹 → 全选 63 通道时换色板「看不出生效」。改为按总条数铺满——连续色板（viridis/parula）插值取样成平滑渐变、离散色板循环复用自身颜色；新增 `continuous` 标记 + `sampleRamp`，`chColor` 传通道总数、`segColor` 传段数。

**D.（同批）地形图游标 ms 抖动**：`跟随游标` 模式下「游标 315ms」位数变化（5 / 315 / 1000）撑动标题列宽 → 右侧地形图卡左右抖。`.topo-cap` 固定宽 92px + 数字 `tabular-nums`。

验证：`vue-tsc` 0 错；本机无运行环境，地形图色面 / 电极标记 / 多通道渐变 / 游标稳定均待 `s2_deploy_remote.cmd` 真机验。

**事实源**：`frontend/elys-web/src/components/observe/TopoStrip.vue`、`composables/observe/channelColor.ts`、`views/WaveformDetailPage.vue`。

**经典软件画法参考**：[EEGLAB topoplot](https://github.com/sccn/eeglab/blob/develop/functions/sigprocfunc/topoplot.m)、[MNE plot_topomap](https://mne.tools/stable/generated/mne.viz.plot_topomap.html)、[FieldTrip ft_topoplotER](https://github.com/fieldtrip/fieldtrip/blob/master/ft_topoplotER.m)。

**E.（同日）期刊色板统一加「风格」（商标考量）**：五套期刊色板显示名统一为「期刊名 + 风格」——`NPG`→`Nature 风格`、`AAAS`→`Science 风格`、`Lancet`→`Lancet 风格`、`NEJM`→`NEJM 风格`、`JAMA`→`JAMA 风格`。决策依据——著作权无虞（名字 / 颜色数值不可版权化）；`Nature`/`Science` 是 Springer Nature / AAAS 的注册商标，但作色板名属**描述性 / 指明性使用**（标「仿其图色风格」、不当产品品牌、不暗示背书），加之二词本身通用、保护范围窄，风险低；加「风格」限定词进一步弱化品牌关联，与上轮去 MATLAB 印记同一「减品牌关联」思路。内部 key 仍为 `npg`/`aaas`、颜色不变。事实源 `composables/observe/channelColor.ts`。

## 2026-06-15 · 新增自动坏道检测 + 插值修复节点 `eeg/preproc/bad_channels`

接「ELYS 有没有自动检测换通道」的盘点（对标 MNE 与隔壁 Niantong），补上 Niantong 对标两个真洞里的第二个——**自动坏道处理**（第一个「通道定位」已于本日早些时候补上）。此前 elys 只被动尊重数据里已有的 `info['bads']`，既不自动找坏道、也不插值修复。

**A. 节点能力（用户只设参数）**：新增 `phase1` 预处理节点 `eeg/preproc/bad_channels`（标题 `Bad Channels`），raw→raw，与 Filter / Resample / Re-reference / Ch Loc 同属积木式预处理。参数：

- `action` 处理方式：`interpolate` 检测并插值修复（默认）/ `mark` 仅标记不改数据；
- `method` 检测算法：`lof`（06-15 时唯一；06-16 起增 `ransac`，见 06-16 条）；
- `threshold` 判定阈值（默认 1.5）、`n_neighbors` 近邻电极数（默认 20，通道少于此值时自动下调）——均 advanced。

**B. 科学性对标 MNE**：

- 检测 = `mne.preprocessing.find_bad_channels_lof`（LOF，Local Outlier Factor 局部离群因子——按每个电极相对邻近电极的"离群程度"打分判坏，是 MNE 1.7 为 EEG 内置的自动坏道法，文献 Kumaravel et al. 2022）；仅在 EEG 通道上算。
- 修复 = `raw.interpolate_bads`（球面样条插值，用周围好电极重建坏电极）；需要电极坐标，故节点须接在 `Ch Loc Assign` 之后，缺 montage（电极位置）时报可操作中文错。
- `reset_bads` 默认 True（插值后通道恢复为正常，MNE 默认语义；关掉则保留坏道标记供下游排除）。

**C. 溯源不黑箱**：`summarize_raw` 本不透出 `bads`，故让引擎回传检测明细（method / threshold / 检测到的坏道列表 / 是否插值），由 dispatcher 并入结果 `preview` 与 `mne_summary`——医生能看到"自动判了哪些坏道、插了没"，而非黑箱改数据（兑现 [6-01](6-01-设计原则与UI约定.md) 节点带 provenance 的约束）。

**D. 落点**：

- 引擎 `app/engine/preprocess/bad_channels.py::run_bad_channels`（raw→raw，返回 `(raw, {"bad_channels": 明细})`）；
- 节点 JSON `app/pipeline/nodes/eeg_preproc_bad_channels.json`（registry 自动加载，前端节点面板零改即现）；
- `dispatcher.py`：import + handler 注册 + `_execute_bad_channels`（按 action 决定派生文件后缀 interp / badchan）+ 给通用 `_execute_raw_preprocess` 加"引擎可回传 `(raw, extra_meta)` 溯源"的向后兼容钩子（其余 4 个预处理节点返回单 raw、不受影响）；
- 依赖 `scikit-learn`（LOF 需要）+ `mne 1.7`（LOF 所在版本）均已在 `requirements.txt`。

后端执行器至此 13 个；坏道处理从 [5-25](5-25-工作流节点路线图.md) 批次 A 的规划项（原拟 `eeg/qc/bad_channels`）落地为 `eeg/preproc/bad_channels`（归入预处理、与同为 raw→raw 的兄弟节点对齐）。

验证：`py_compile`（引擎 + dispatcher）过、节点 JSON 必填字段校验过；本机无运行环境，LOF 检测 / 插值数值与端到端待 `s2_deploy_remote.cmd` 后真机验（mne 锁 1.7.1，本机 1.12.1 不代表云端）。

**事实源**：`backend/app/engine/preprocess/bad_channels.py`、`backend/app/pipeline/nodes/eeg_preproc_bad_channels.json`、`backend/app/pipeline/dispatcher.py`。

## 2026-06-15 · 时域看图页配色扩到 16 套 + 配色选择器改色卡点选 + 工具条/全屏微调

时域看图页（`WaveformDetailPage.vue`）一批体验打磨——曲线配色（多通道 / 多条件叠加时给每条线分配区分色的色板）扩容与选择器重做、工具条按钮归位、全屏聚焦。

**A. 曲线色板 3 → 16 套（`composables/observe/channelColor.ts`）**：原本只有 elys / NPG / Wong，补齐成业内公认的一批，并加元数据 `PALETTE_DEFS`（一处定义 key / 名字 / 分组 / 色卡，下拉直接拿来画预览）。四组——

- **品牌**：`elys 风格`（默认，长在全站 token 上）；
- **期刊配色**：`NPG` / `AAAS` / `Lancet` / `NEJM` / `JAMA`（各大期刊正文图的定性用色，投稿对味）；
- **色盲安全**：`Wong`（Okabe-Ito）/ `Tol Bright`（经红绿色盲校验仍可区分）；
- **通用**：`Tableau 10` / `D3 Category10` / `seaborn deep` / `ColorBrewer Set1` / `Set2`，外加两套**连续色**（从低到高的有序渐变）`parula` / `viridis`。

> 超过色板长度的曲线仍走原有黄金角 HSL 兜底，未动。

**B. 命名去来源印记（商用考量）**：产品下拉里的名字求短、专业、不暴露专有软件牌子——`MATLAB lines` → `Scientific Lines`（连 key 也由 `matlab` 改 `scientific`、代码注释不留 MathWorks 字样）、`matplotlib tab10 / D3` → `D3 Category10`、原「软件预设」组整组并入「通用」（组名里的具体软件名一并消失）。

> **商用结论**：一串颜色数值（RGB 列表）本身一般不构成版权客体、更非专利客体；受版权保护的是各库的源代码，而本项目只用数值、自行实现取色。其中 ColorBrewer = Apache-2.0、seaborn = BSD、viridis = CC0 公共领域，商用明确零负担；期刊 5 套 + `Scientific Lines` + `parula` 属「仅借数值、非开放许可」（来源敏感但风险极低），代码注释已就此标注。「商用版只留全开放许可色板」的洁癖开关经用户拍板**暂不做**。

**C. 配色选择器：原生下拉 → 内联色卡点选行**：原生 `<select>` 看不到颜色，改成念通式点选行——当前条显示「色卡条 + 名字」，点开**就地按组展开**（不浮动，规避左栏 `overflow-y:auto` 把浮层裁掉），每个候选项左边一条实物色卡直接预览。

**D. 工具条右栏开关归位 + 全屏聚焦**：

- 三个布局开关的图标本是空间隐喻（左条 / 底条 / 右条），但「右栏」开关原夹在中间、全屏占了最右。重排末组为 `[全屏] ┃ [底部][右栏]`，让「右栏」开关贴最右边缘、与工具条最左的「左栏」开关左右对称；
- 全屏时隐去顶部信息条（`<header>` 加 `v-show="!isFullscreen"`）——全屏目标是整页，原顶栏白占一条，隐掉让绘图区吃满；退出全屏的按钮在工具条上仍可见。

验证：`vue-tsc` 0 错；本机无运行环境，配色实物 / 点选交互 / 全屏均待 `s2_deploy_remote.cmd` 后真机看。

**事实源**：`frontend/elys-web/src/composables/observe/channelColor.ts`、`views/WaveformDetailPage.vue`。

## 2026-06-15 · 研究项页面去黑话 + 信息架构重排（面向临床用户）

照 6-01「让脑电分析显得简单、专业，而非暴露系统有多少零件」的纲领，对「研究项」页（`StudiesPage.vue` + 右栏 `StudyOverviewTab.vue`）做一次面向临床用户（医生 / 心理 / 神经科学家）的体检。逐条修改清单见 `日志/11_研究项页UX优化260615/`。

**A. 全局去黑话**：前台 `数据资产` → `数据集`（5 处：`StudiesPage` 副标题重写、`DatasetsPage`、`Index.vue`×2、`workbenchPages.ts`）。

**B. 结果状态不暴露「缓存/临时」**：内部三态（`keep` 用户保留 / `cache_eligible` 系统缓存 / `deleted_at` 回收站）对用户收敛成 **保存 / 不保存 / 已删除**——`StudyOverviewTab.retentionStatusLabel`、`ResultsPage.retentionLabel`、共享 `pipelineFormatters.formatArtifactRetention`（含 spec 断言）三处口径统一；删结果详情「技术信息」里的「系统缓存 是/否」行；「更多」菜单「清理缓存 / 临时数据」→「清理未保存的结果」。（`formatJobStatus` 的 `cached→缓存` 是**运行步骤**状态、非结果状态，且在工程向工作流页，按用户「（结果状态）」限定词不动。）

**C. 信息架构：瘦身与归位（叙事顺序保留工程师序）**：经用户确认，右栏卡片顺序仍为 **研究进度 → 工作流与运行 → 结果**（数据→流程→运行→结果）——「建议下一步」决策卡留在研究进度卡底部、「结果」留在工作流与运行卡内，**不做**"决策卡置顶 / 结果前置"的临床序重排（曾实现后按用户要求回退）。其余瘦身 / 归位照做——

- 左栏列表行瘦身：挂载标签 + `数据/工作流/运行` 三计数（同一组数字一屏三遍）收敛成**单枚阶段药丸**（待导入数据 / 待配置流程 / 运行中 / 可继续分析 / 同步中），判定沿用右栏决策主线；
- 删与决策卡重复的详情页头部「进入工作区」（决策卡按钮随状态自适应、更聪明；删后「移入回收站」独占头部、降低误点主操作旁破坏性按钮的风险）；
- 「研究进度」四宫格去掉「权限」格（权限不属于进度）→ 成员数 / 你的角色并入「元信息」；
- `存储配额`（运维/计费概念）从「元信息」正文下沉进「技术信息」折叠。
- 「使用的数据集」不再显示数据集版本标签（默认工作版本裸露成 `vworking`）与「可升级」标记——版本是后台架构、研究概览用户不需要也看不懂，只留「数据集名 + N 名被试」；数据集管理页仍用 `formatVersionLabel` 显示「工作版本」（管版本的地方）。

**D. 顺手术语**：`挂载`→`关联`（研究项页空标签 / 使用的数据集 / 决策文案）、`重放`→`重跑`（运行模式标签，含共享 `pipelineFormatters` / `pipelineConstants`）、研究项页「可查看结果和审计信息」→「…运行详情」；决策文案「数据引用」→「数据」。

**E. 结果数据类型去黑话**：`data_type` 枚举（`evoked` / `tfr` / `psd` / `ica` / `epochs` / `raw` / …）此前直显原始值，新增共享 `formatDataType()`（+spec）映射成大白话——**ERP 波形**（evoked）/ 时频图 / 功率谱 / ICA 成分 / 分段数据 / 原始数据…，未知回退原文。研究概览结果列表 + `ResultsPage` 的类型标签 / 筛选 chip / 详情标签都走它；筛选匹配 / 按类路由 / 下载名 / `dataTypeClass`·`Icon` / 技术折叠枚举仍用原值。保留 `节点`、工作流 `v1`、BIDS 结果名（真实科学标识、非后台架构）。

**刻意不动**：`节点`（用户认为临床可理解）、顶部导航栏（全站问题本轮不碰）、admin / 数据集生命周期的真实 `审计` 语境。

验证：`vue-tsc` 0 错、`vitest` 27 过、前台 `数据资产` 归零；本机无运行环境，真机视觉验收待下次 `s2_deploy_remote.cmd`。

**事实源**：`frontend/elys-web/src/views/StudiesPage.vue`、`views/study/StudyOverviewTab.vue`、`views/ResultsPage.vue`、`composables/pipeline/pipelineFormatters.ts`（+`.spec.ts`）、`pipelineConstants.ts`、`views/Index.vue`、`data/workbenchPages.ts`、`views/DatasetsPage.vue`。

## 2026-06-15 · 新增 execution_outputs 关联表：修「重跑/缓存命中产物显示 0」

**现象**：某节点（如 ERP Average）运行面板显示「成功 · 产物 0」，但分析其实算完了、结果文件也在。

**根因**（两层叠加）：

1. 输出按 content-addressed 去重——`study_outputs` 表对 `(study_id, sha256)` 唯一，一条物理结果只有一行；`produced_by_execution_id` / `produced_by_job_id` 永远指向**最早产出它的那次执行**。
2. 运行面板的「产物」数，是按 `produced_by_execution_id == 本次执行` 统计的。

于是确定性节点**重跑**（同输入→同字节→去重命中复用旧行）或**缓存命中**时，新执行不新建行，旧行又记着旧执行，本次执行就数出 0。`必存`（keep=true）的结果尤其容易踩——旧行永不被清，重跑必撞。

**修法**：新增 `execution_outputs` 多对多关联表（每次「某执行的某 job 产出/复用某 study_output」记一条边，`relation` ∈ {created, reused}），运行面板改从这里统计；`study_outputs.produced_by_*` 不动，保留 canonical 首产者血缘。比改写归属（丢血缘）或并 `job.output_json`（口径割裂）更正：血缘与「本次产物」两个问题各归各表。

- **表**：`database/schema/05_outputs.sql` 加 `execution_outputs`（FK 全 CASCADE / job SET NULL，唯一索引 `(execution_id, job_id, study_output_id)`）；ORM `ExecutionOutput`（`models/study_output.py`）。
- **写路径三处**（覆盖所有产出口）：`study_output_store._register_study_output` 新建行记 `created`、去重命中记 `reused`；`cache._register_artifact_references` 缓存命中记 `reused`。统一走新 `record_execution_output_link()`（get-or-create 幂等）。
- **读路径两端点**：`list_pipeline_execution_study_outputs`（运行面板）与 `get_pipeline_execution`（执行详情）改用 `_pipeline_shared.execution_scoped_outputs()`（按关联表取、按 output 去重、附本次执行的 job 归属）；`study_output_to_response(..., attribution=)` 在「本次执行视角」下用关联边的 (execution_id, job_id) 覆盖序列化，前端按 job 统计/分组的逻辑零改即生效。
- **不动血缘溯源**：`build_pipeline_execution_lineage_response`（lineage DAG）仍按 `produced_by_*`——复用输出在 DAG 里的顶点本就该是它的首产节点。

验证：`py_compile` 全过；本机无 SQLAlchemy/DB，mapper 配置与端到端待云端。**部署须 DROP + 重建库**（`init.sql` 新表才会建出，调试期照常）。

**事实源**：`database/schema/05_outputs.sql`、`backend/app/models/study_output.py`、`app/pipeline/study_output_store.py`、`app/pipeline/cache.py`、`app/routers/_pipeline_shared.py`、`app/routers/pipelines.py`。

## 2026-06-15 · 照 Niantong 对标补强：通道定位节点 + 地形图坐标兜底 + Picard

对标隔壁 Niantong-eeg-analysis（17 注册节点）盘点 elys（11 节点）该补什么，原则=**抄能力不抄颗粒度**（补能解锁真实工作流的，拒绝只为镜像 MNE/EEGLAB 函数而拆的——故不拆 filter 回 FIR/Butterworth、不拆 PSD 回 pwelch/welch）。本轮补两个真洞之一「通道定位」。

「montage」（电极帽 / 通道位置）= 每个电极在头皮上的坐标，画**地形图**（topomap：把某时刻各电极的值按头皮位置铺成色块图）必须先有它。问题：elys 地形图坐标此前只读 FIF 内嵌 montage，数据没带就画不出。

- **地形图坐标兜底**（`timeseries.py`，逻辑抽到新 `montage_layout.py`）：内嵌 montage 取不到时，按通道名归一化（`FP1` / `'EEG Fp1-Ref'` → `Fp1`）匹配 MNE 标准帽 `standard_1005` / `1020` 拿模板坐标（同 EEGLAB 的 look-up standard locations 思路）；非标准名匹配不上则诚实返回空、不硬塞。
- **新增预处理节点 `eeg/preproc/channel_location`（Ch Loc Assign）**：把标准电极位置**写进数据**供下游（地形图 / ICA 成分图 / 将来坏导插值）共用——`montage` 选 auto（≤32 导用 10-20、否则 10-05）/ 10-20 / 10-05，`rename` 自动把通道名规范到标准命名（rename 并入本节点、不单列，连念通也这么做）。引擎 `app/engine/preprocess/channel_location.py`，dispatcher 复用 raw 预处理通道；前端节点面板 spec 驱动自动收录。
- **顺手给 ICA compute 加 Picard 选项**（Picard：比 FastICA 更快更稳的 ICA 算法），补依赖 `python-picard==0.7`；并补 `scikit-learn==1.5.2`——既有的默认 `fastica` 本就需要它（之前 requirements 漏列，用户确认云端要装）。

验证：`py_compile` + 节点注册校验（11→12 节点）+ 合成数据冒烟（`FP1→Fp1` 归一化、9 导定位、EOG 跳过、无 montage 时地形图兜底 9/9）；CRLF 无污染。真机待云端部署。

**事实源**：`backend/app/pipeline/montage_layout.py`、`timeseries.py`、`app/engine/preprocess/channel_location.py`、`app/pipeline/nodes/eeg_preproc_channel_location.json`、`dispatcher.py`、`eeg_ica_compute.json`、`requirements.txt`；参照 `../Niantong-eeg-analysis/backend/channel_position_utils.py`。

## 2026-06-15 · ICA 成分交互审阅做成真实功能（ica_inspect + 端点 + IcaPage 重建）

ICA（独立成分分析：把脑电拆成一组"独立来源"，便于挑出眼动 / 肌电 / 心电等伪迹成分剔除）此前 elys 只能**算**不能**审**——`Compute ICA` / `Apply ICA` 后端是真的，但审阅页 `IcaPage` 是纯设计稿（成分写死、地形图是 CSS 渐变假图、零接口）。这轮做成真实功能，用户拍板"完整版·对标念通"（隔壁 `ica_visualization_utils.py` 作只读参照）。

**后端**：

- 新增 `app/pipeline/ica_inspect.py`：每个成分的可视化数据——**地形图**（把成分在各电极上的权重落到 2D 头皮坐标着色，复用上面 `montage_layout` 投影 = 电极散点，不做插值面，与"插值留出图二期"一致）、**时域波形**、**Welch 频谱**（把信号切段做傅里叶再平均得到的功率谱估计，dB）、**去除前后对比**（某通道上"原始 vs 排除选定成分后"）。纯函数（入参是已载入的 mne ICA + Raw），本地合成 ICA 验证四函数全过。
- 两个 output 级只读端点（仿 `/timeseries`）：`GET /studies/{id}/outputs/{oid}/ica-components`（总览 + 每成分地形图网格）、`GET …/ica-components/{index}?excluded=…`（单成分时域 / 频谱 + 可选去除前后对比）。源 raw 回溯：`Compute ICA` 存盘时把输入 raw 的紧凑引用写进 artifact 的 `preview_json.source_ref`，端点据此 `read_raw_from_data_info` 载入（StudyOutput 无通用 metadata 列，借 preview_json 落地）。

**前端**：`IcaPage` 从 mockup 重建为真实审阅台——路由带 `studyId` + `study_output_id` 即进 live 模式：拉成分网格（真实电极散点地形图）→ 点选出详情面板（时域 / 频谱 / 去除前后对比，随标记动态刷新）→ 逐成分标记剔除 → 若从工作流「ICA Apply」等待节点打开（带 `executionId` / `jobId` / `decisionVersion`），「应用去除」提交剔除决策到已有的 `waiting_user_input` / `decision` 握手；无 job 上下文则只读查看。无参数时给诚实空态（不再伪造）。

验证：后端 `py_compile` + 合成 ICA 冒烟测试；前端 `vue-tsc` 绿。真机待云端部署（本机无后端 / 真实 ICA 数据）。

**事实源**：`backend/app/pipeline/ica_inspect.py`、`montage_layout.py`、`app/routers/study_outputs.py`（ica-components 两端点）、`app/pipeline/dispatcher.py`（ICA compute 记 source_ref）、`frontend/elys-web/src/views/IcaPage.vue`；参照 `../Niantong-eeg-analysis/backend/ica_visualization_utils.py`。

## 2026-06-14 · 观察作图与缓存架构方案落档（日志/10_… 6 篇）+ 时域看图改用 uPlot 宿主渲染（P1 增量①）

「观察作图」指六类观察页（时域 / 频域 / 时频 / 伪迹审核 / 地形图·ICA …）的看图体验。此前这些页多是用数学函数伪造数据的纯设计稿（背景见下面 06-13 TFR 一条）。这轮先把"图怎么画、数据怎么缓存"的整体架构想清楚、落成文档，再动第一刀代码。

**方案落档（`e2fea4e`，`日志/10_观察作图与缓存架构260614/` 共 6 篇、1622 行）**：总览与架构决策、前后台多级缓存机制、信号作图（时域·频域·时频）、伪迹审核、地形图与 ICA、问题清单 + 时域 P1 实施方案。核心做法是把隔壁的 **Niantong-eeg-analysis**（静态 HTML 旧形态产品）当**只读参照**，把它六类观察作图的成熟经验融进 elys（Vue3）。已锁定原则：一期用 **uPlot** 做"探索面"（先把图快速画出来、能交互；uPlot 是专为时间序列优化的轻量 Canvas 图库）、二期再上 FigureSpec + D3 + headless 出图（出版级静态图）；真实技术栈 = uPlot + 原生 Canvas + 原生 SVG + Web Worker；缓存默认开。

**代码 P1 增量①（`889e7f8`）**：把时域看图页 `WaveformDetailPage`（`/observe/waveform`）从手写 SVG 改为 **uPlot Canvas 渲染**——手写 SVG 采样点一多就卡，uPlot 走 Canvas、专治大点量时间序列。新增两件：

- `components/observe/TimeCourseCanvas.vue`：uPlot **宿主组件**（用 `shallowRef` 持实例、避免 Vue 深响应包裹画图对象；`onMounted/onUnmounted` 建 / 销毁；`ResizeObserver` 跟容器自适应；游标读数 `emit` 给父组件）。
- `composables/observe/channelColor.ts`：elys 烙印版**分类色板**（给不同通道分配区分色：主色板用完用黄金角算法兜底，另备 NPG / Wong 两套色盲友好色板）。

沿用原有通道选择 / 时间窗 / 段切换 / 悬停读数，按决策去掉零线；引入 `uplot` npm 依赖。命名「P1 增量①」——缓存层等后续增量另行落地。

验证：`vue-tsc` typecheck 绿；真机渲染待云端。

**事实源**：`日志/10_观察作图与缓存架构260614/`（6 篇方案）；`frontend/elys-web/src/views/WaveformDetailPage.vue`、`components/observe/TimeCourseCanvas.vue`、`composables/observe/channelColor.ts`、`package.json`（uplot 依赖）。

## 2026-06-14 · 4-00 补「对标产品」节：OpenNeuro + fw-heudiconv

用户指定：文件 / 目录管理可对标两款成熟产品，记入 [4-00 文件管理总览](4-00-文件管理总览.md) §8。

- **[OpenNeuro](https://openneuro.org/)**：免费开放的神经影像（MRI / MEG / EEG / iEEG…）数据共享平台，整套基于 BIDS 做组织 / 校验 / 版本化 / DOI 铸造。借鉴点 = 成熟「BIDS 数据仓库」的样子（数据集如何版本化、合规校验、私有→公开共享 + DOI），对应 ELYS 数据集存储布局与「发布≠转公开」生命周期。
- **[fw-heudiconv](https://fw-heudiconv.readthedocs.io/)**：Flywheel 的 HeuDiConv，用可配置「启发式规则」把杂乱原始数据批量整理进 BIDS 目录结构。借鉴点 = 「原始上传 → BIDS 四元组（sub/ses/task/run）」如何规则驱动地映射，对应 ELYS 导入转换（sourcedata → BIDSdata canonical、四元组识别）。

二者是**对标参考、非依赖**：ELYS 借鉴其组织思路（BIDS 为骨架、原始与衍生分层、版本与共享分离），但 EEG canonical 层用 FIF（非 BIDS 认可的 raw 扩展名）、不追求对外 validator 全绿，标准 BIDS 走按需导出。

**事实源**：`wiki/docs/4-00-文件管理总览.md` §8「对标产品」。

## 2026-06-14 · 仓库根工作流 cmd 脚本前缀 step→s（s1–s4）

接 2026-06-13 的「step1–step4」编号改名，进一步把前缀 `step` 缩成单字母 `s`（用户偏好：让这 5 个工作流脚本在仓库根按首字母 `s` 抱团、与其它文件区分、少打几个字）。

| 旧名 | 新名 |
|---|---|
| `step1_buy_ecs.cmd` | `s1_buy_ecs.cmd` |
| `step2_deploy_remote.cmd` | `s2_deploy_remote.cmd` |
| `step3_test1_erp.cmd` | `s3_test1_erp.cmd` |
| `step3_test2_erders.cmd` | `s3_test2_erders.cmd` |
| `step4_release_ecs.cmd` | `s4_release_ecs.cmd` |

`git mv` 保留历史；脚本内部转调的 `deploy/*.ps1`、`elys_scripts/start.cmd`、`step_banner.ps1 -Step N`（步骤号是概念、非文件名）均不变。全仓 14 处**活引用**一次同步：`deploy/{buy_ecs,deploy_remote,release_ecs}.ps1` 的用户可见帮助提示、`AGENTS.md`/`CLAUDE.md`/wiki `2-10`·`2-80`·`7-10`/`elys_scripts/README.md` + `01_erp_basic`·`02_erd_ers` 两份 README/`erders_reference.py`/`日志/8_…`。用**字节级 ASCII 替换**（Latin-1 往返）只换文件名子串、不动周围 UTF-8 中文与 CRLF。顺修 stale bug：`01_erp_basic/README` 原引用不存在的 `step3_elys_debug.cmd` → 正确的 `s3_test1_erp.cmd`。`aliyun-test.env` 的 s2 注释改动因与并行会话未提交的 `COMPUTE_SERVER_IP` 更新同处一文件，留给部署会话一并提交（已于 `2063697` 落地）。9-0x 历史记录（含 06-13 的 step1–step4 表）按惯例保留不动。

**事实源**：仓库根 `s1_buy_ecs.cmd` / `s2_deploy_remote.cmd` / `s3_test1_erp.cmd` / `s3_test2_erders.cmd` / `s4_release_ecs.cmd`。

## 2026-06-14 · 新增功率谱密度（PSD）分析节点 + 功率谱观察页接真数据

继 ERP（时域平均）、TFR（时频）之后补上第三类常用分析对象——**功率谱密度（PSD，power spectral density）**，即信号各频率成分的能量分布，回答"这段信号里 α/β/… 各频段功率有多少"。照 ERP/TFR 的"计算 → 存储 → 端点 → 观察页"现成链路做实。

**后端节点（`a4fab83`，镜像 TFR）**：

- 引擎 `engine/analysis/psd.py · run_psd`：用 **Welch 法**（把信号分多段各算谱再平均、降噪的标准做法）算功率谱，按 condition 选 Epochs → 跨 epoch 平均。
- IO 三件套 `save_psd_npz` / `summarize_psd` / `ensure_psd_npz_path`：存**纯 numpy `.npz`**，刻意绕开 MNE `Spectrum` 对象的跨版本 IO（吃过 MNE 版本 / numpy 真值的亏，背景见下面 06-13 TFR 一条）。
- dispatcher 登记 `eeg/analysis/psd` → `_execute_psd_output`（镜像 TFR、但无基线步）+ `_derived_psd_filename`；节点 JSON `eeg_analysis_psd.json`；`previews` 映射 psd → `/observe/psd`。`data_type='psd'`、validator 兼容集、store 的 file_role 映射在 TFR 时已预埋。

**后端端点 + 前端 live（`dbf11ee`，镜像 TfrPage 双模式）**：

- `pipeline/psd_view.py · build_psd_lines` 直接读 `.npz`（不依赖 MNE、转 dB 让折线可读），新端点 `GET /studies/{id}/outputs/{oid}/psd`。
- 前端 `PsdPage.vue` 改**双模式**：路由带 `study_output_id` 时进 live、拉真实 PSD（单通道 频率→功率 dB 折线 + 频带平均 + 通道切换），无参保留原 mockup 设计稿；补 `StudyOutputPsd` 类型、`getStudyOutputPsd` API。

验证：后端 `py_compile` + JSON 校验 + pyflakes、前端 `vue-tsc`(0 错) 均过；真实 PSD 计算（云端 MNE 1.7.1 的 `compute_psd` / `.npz` 存取 / 注册全链路 + 折线渲染）待云端 `s3_test1_erp` 验证。**至此真实分析节点 = ERP + TFR + PSD 三类。**

**事实源**：后端 `engine/analysis/psd.py`、`engine/io.py`、`pipeline/{dispatcher,previews,psd_view}.py`、`pipeline/nodes/eeg_analysis_psd.json`、`routers/study_outputs.py`；前端 `views/PsdPage.vue`、`api/pipelines.ts`、`types/index.ts`。

## 2026-06-14 · 新增独立 Baseline 基线校正节点 eeg/epoch/baseline

**基线校正**＝用刺激前一小段（基线窗）的均值，把每个 trial 拉回同一起跑线、消掉试次间的直流漂移，是 ERP / 时频分析前的标准预处理。此前 ELYS 的 Epoch 节点 `baseline=None`、ERP 直接平均，**缺这一步**。

兑现 6-01 的**节点粒度判据**（06-13 落档）——基线是 ERP / TFR 都要、跨分析类型复用的独立科研决策，按判据**单列成独立节点**，而非塞进 Epoch 或 ERP 内部（正是 6-01 裁决示例里"独立 Baseline 节点"的落地）。

改动（`cdb3e6d`）：新增 `eeg/epoch/baseline` 节点（`epochs → epochs`），对 Epochs 应用 `apply_baseline`（基线窗默认 `[起点, 0]`，可接在分段之后、ERP / 时频之前）；引擎 `engine/analysis/baseline.py · run_baseline` + `__init__` 导出 + dispatcher 登记 / 执行（复用 `_save_epochs_dataset`）+ 节点 JSON `eeg_epoch_baseline.json`。`apply_baseline` 是 MNE 老牌稳定 API、跨版本不变；纯增量、不动现有节点。

验证：本地 `py_compile` 过；真机执行待云端 `s3` 调试验证。

**事实源**：`engine/analysis/baseline.py`、`engine/analysis/__init__.py`、`pipeline/dispatcher.py`、`pipeline/nodes/eeg_epoch_baseline.json`。

## 2026-06-14 · 全栈死代码盘查清理：前端符号 + 死 CSS + 后端孤立函数 + DB 死列 + 孤立脚本/文档

应「回家前做一次详细死代码检查」的要求，对前端 / 后端 / 数据库 / 部署 / 测试做了一轮系统盘查：确认零引用的直接删，存疑 / 需决策的列表留存。横跨两个提交——`2d69c11`（前端符号级 + 后端孤立函数 / 死 import，与一笔 deploy 配置一起提交）、`daca5c8`（死 CSS + DB 死列 + 孤立脚本 / 失配文档）。

**前端 · 符号级（`2d69c11`）**

- **拆分巨石组件遗留的未用 import**：`PipelinePage.vue` 约 39 个（`datasetApi` + 15 个类型 + 14 个常量 + 10 个格式化件，均为抽 composable 后忘删）、`DatasetsPage.vue` 13 个。
- **死 computed / 死函数**：`PipelinePage` 的 `upstreamCandidates` / `selectedNodeLinks`（逻辑已迁 `useGraphConnections`）、`Dashboard` 的 `activeExecutions`（模板实用 `prioritizedExecutions`）、`BidsUploadPanel` 的 `batchProgressPercent`、`LoadDataPanel` 的 `hasAnyExcludes`、`StudyDetailPage` 的 `statusLabel` / `statusPillClass`、`DatasetsPage` 的死常量 `fileRoleOptions`。
- **死导出**：`pipelineFormatters.ts` 的 `categoryLabel`、`types/index.ts` 的 `ExecutionManifestSummary` / `StudyOverview`（全 src 零 import）。

**前端 · 死 CSS（`daca5c8`，共 −550 行）**

- `DatasetsPage.css`（−332）：删旧「文件管理器」`df-*` 整块（已被维护中心 `dmt-*` 取代）、旧「概览」tab 样式（`data-overview-card` / `data-stat` / `task-chip`）、`version-timeline__meta`、`import-scope`（独立块 + 共享选择器 + @media）。
- `style.css`（−218）：删一批命名明确的死类——`dropzone` / `fake-dropzone` / `quick-action(s)` / `import-step(__num)` / `bids-batch-progress` 容器 / `bids-dataset-note` / `bids-group__entities` / `btn--accent` / `input--lg` / `nav-item__dot` / `badge--count` / `page-stat__hint` / `import-scope`；`stat__icon` 做**外科式删除**——`.stat` 卡片本身被 `StaticWorkbenchPage`(mockup) 在用，只删它从不出现的 `__icon` 部分、保留卡片。

**后端（`2d69c11` + `daca5c8`）**

- 删两个孤立引擎 IO 函数：`read_tfr_from_data_info`（真实读 TFR 走 `tfr_view.py` 直接 `read_tfrs`）、`summarize_mne_object`（dispatcher 直接调具体的 `summarize_raw/epochs/evoked`），及 `engine/__init__.py` 对应导出。
- 删整文件 `engine/testing.py`（`make_synthetic_raw` 全仓零引用）。
- 死 import / 局部变量：`cache.py` 的 `datetime`、`topology.py` 的 `Iterable`、`dashboard_summary.py` 的 `timedelta`；`tfr.py` 的 `mne = _mne()`、`executor.py` 的 `node_id`。

**数据库（`daca5c8`）**

- 删 `DatasetFile.source_file_id` 死自引用——0 读 0 写，文件血缘实际由独立的 `dataset_file_derivations` 表承载。ORM（`study.py` 的列 + `idx_dataset_files_source_file` 索引 + `source_file` / `derived_files` 两个自引用关系）与 schema（`03_datasets.sql` 的列定义 + 注释 + 索引）一起删。**保留**指向 `dataset_file_derivations` 的 `source_derivations` / `derived_derivations`（那才是活的多源血缘）。

**部署 / 文档（`daca5c8`）**

- 删孤立 `deploy/apply_local_profile.sh`：默认读的 `profiles/local.env` 磁盘上不存在、无任何脚本调用、本地开发已由 `start_local.cmd` + `docker-compose.local.yml` 承担；顺手把引用它的 `deploy_remote.ps1` 报错文案与 `7-40` 旁白改指向现行本地栈。
- 重写 `elys_scripts/README.md`：原文引导双击不存在的 `step3_elys_debug.cmd`、描述不存在的 `02_diag/` 目录；改对齐真实结构（`02_erd_ers`、各项目 `setup.py` + `run.py`、根 `step3_test1_erp.cmd` / `step3_test2_erders.cmd`、`common/ui.py`）。

**方法 / 校验**：每类删除前用类名 token / 词边界正则在全 src（含 Vue `<template>` 段）精确数引用次数，仅 0 引用才删；CSS 用行范围脚本删、逐行 eyeball、EOL 按文件保留（`DatasetsPage.css` CRLF / `style.css` LF 均未翻）。`vue-tsc`(0 错) / `vite build`（CSS 结构闸门） / `vitest` 58/58 / `py_compile` + `compileall` / `mkdocs build` 全过。

**未实施 · 待拍板（列表留存，本轮未动）**：

- **需决策**：`types/api.ts`（openapi 生成但零消费——接通取代手写类型还是删生成管线）；`usePipelineEditor` 的 `useEditorContext` + `provide/inject`（疑为进行中的 PipelinePage 子组件拆分预埋的扩展点）；`StudyOutput.lifecycle_state` 的 `published` / `withdrawn` 死状态（列被读但全后端从不写非默认值，属「结果生命周期对齐数据集 v2」待拍板）；3 个「后端活 / 前端没接」端点（`POST /recordings/import-task`、`/recordings/import-async`、`/studies/{id}/outputs/gc`）；`Role.name_en` / `is_system` 与 `User.institution` / `is_verified`（只 seed / 只序列化、应用不读写）；`deploy_remote.ps1` 的 `prod-hybrid` / `local` 占位 profile（`prod-hybrid.env` 不存在）。
- **疑似漏写（当 bug 复查、未删）**：`dispatcher.py` 的局部 `key`（旁边 `components_by_key` 字典疑应按数据集 `key` 分组却漏了）、`dataset_lifecycle.py` 的 `notified_count`（通知数算了从不读 / 返回 / 记日志，疑漏 log）。

**事实源**：前端 `views/{PipelinePage,DatasetsPage,Dashboard,StudyDetailPage}.vue`、`components/{BidsUploadPanel,LoadDataPanel}.vue`、`composables/pipeline/pipelineFormatters.ts`、`types/index.ts`、`views/DatasetsPage.css`、`style.css`；后端 `engine/{io,__init__,testing(删),analysis/tfr}.py`、`pipeline/{cache,topology,executor}.py`、`services/dashboard_summary.py`、`models/study.py`、`database/schema/03_datasets.sql`；部署 / 文档 `deploy/{apply_local_profile.sh(删),deploy_remote.ps1}`、`elys_scripts/README.md`、`wiki/docs/7-40`。

## 2026-06-14 · Dashboard 改左右两栏：总览精简并入「进行中」、最近动态移进右栏

按 6-01 设计纲领（受众 = 医生 / 心理学家、纲领 = 让脑电处理显得简单而非复杂）重排工作台首页 `Dashboard.vue` 的整体布局。原结构是上下三段：顶部一条横向**四卡**数字带（数据集 / 研究 / 分析流程 / 分析任务）、中间「我的研究 | 进行中」两栏、底部通栏「最近动态」。四卡那条偏"工程仪表盘"味，且「分析流程 / 分析任务」两卡与左栏研究列表信息高度重叠。

改后是清爽的**左右两栏**（`dashboard-grid`，左宽右窄）：

- **左栏：我的研究**——只留近期研究项列表（最多 5 个，带三段阶段进度条）。依据用户判断：私有数据集与研究项基本一一对应，列出研究项即等于覆盖了数据集，左栏不必再单列数据集。
- **右栏：一个窄列叠三块**——
    - **总览（精简）**：原顶部四卡收进右栏、且砍到只剩**数据集 / 研究**两个计数（`counts.datasets` / `counts.studies`，各带一行状态摘要），**并入「进行中」面板顶部**、之间一条分隔线，二者合成同一个面板。砍掉的「分析流程 / 分析任务」两个计数彻底移除（不是隐藏）。
    - **进行中**：活跃运行队列原样保留（`active_executions`，优先级排序、警示横幅、展开 / 收起均不动）。
    - **最近动态**：从底部通栏**移入右栏**，叠在合并面板下方。

**实现**：纯前端**模板 + 样式**改动，所有计数取值、跳转、`dashboard/summary` 数据流、computed 全部复用，**后端零改**。删 `page-stat-strip` 四卡及其作用域样式；竖排 `.overview-stat` 列表换成两个数横排的 `.overview-mini`（并入 `live-runs` 面板顶部）；`activity-panel` 从 grid 之后的通栏挪进右栏容器 `.dashboard-col`。顺手清掉因砍卡而失效的孤儿 computed（`pipelineSummary` / `executionSummary` 及只喂前者的 `activePipelineCount` / `draftPipelineCount`）与 `.page-stat` / `.overview-stat` 作用域样式。

遗留 / 真机待看：右栏现在更长（合并面板 + 最近动态都在右），与左栏研究列表常**高度不齐**（`align-items:start` 的正常表现，非 bug）；最近动态进窄栏后那行比宽栏挤，当前默认显示 8 条（`ACTIVITY_LIMIT`），嫌长可调。

本地静态校验：全项目 `vue-tsc`（0 错）过；本机无运行环境、Dashboard 需后端聚合摘要才渲染（无后端会落 `errorMessage` 态、两栏不挂载），故本地起不了真机预览，交互待云端部署验收。

另把原本在右上角的「开始新分析」主按钮改成内容区顶部一条**满宽 CTA 横条**（`.start-analysis`，加号 + 标题 + 副标题 + 整条可点跳 `/studies`），右上角只留刷新。CTA 文案随后在 `460f056` 调到位——标题「开始新分析」→「快速开始新分析」、副标题由三步「上传数据 → 选意图 / 模板 → 看结果」改四步「上传数据 → 选意图 → 确认参数 → 看结果」（加「确认参数」，对齐三层向导「草稿优先 / 确认参数」设计）。

**事实源**：`views/Dashboard.vue`（`start-analysis` / `dashboard-grid` / `dashboard-col` / `overview-mini` / `live-runs` / `activity-panel`）。

## 2026-06-14 · 数据集详情页 tab 重构：删「概览」、权限独立成「发布与共享」、默认看数据

按 6-01 设计纲领（受众 = 医生 / 心理学家、纲领 = 让脑电处理显得简单而非复杂）重排数据集详情页的 tab。原结构是「概览 / 导入 / 数据文件」三 tab、默认落「概览」，而那个「概览」（`DatasetOverviewTab.vue`，419 行）其实是个**超载控制台**：版本发布卡 + 可见范围开放按钮 + 授权成员面板 + 删除危险区（这些是权限 / 生命周期，确实有用），外加一堆**重复信息**（被试 / 记录数概要本就在左栏卡片里、导入目标面板与「导入」tab 重复、决策面板、技术信息折叠）。一进数据集就撞一墙版本 / DOI / 生命周期文字，与「显得简单」相悖。

重排后结构：**数据文件（默认）· 上传 · 发布与共享**。

- **默认改看「数据文件」**：`activeTab` 初值 + 5 处选中 / 返回逻辑由 `'overview'` 改 `'data'`。顺带补上空态出口——新数据集落到这页原本只有「请先准备导入目标」一句话、没有按钮，给两个空态各加一个「去上传」主按钮（`activeTab = 'import'`）。
- **「导入」tab 改名「上传」**：对非工程背景用户更亲切（上传文件 vs 导入），仅改标签。
- **删「概览」、权限 / 生命周期独立成「发布与共享」tab**：把「概览」里**删了就没的**那四件套——可见范围开放（开放为共享 / 申请公开）、版本发布 / 撤回 / 丢弃 / 紧急下架、授权成员增删、删除整个数据集——抽进新的 `DatasetShareTab.vue`（由 `DatasetOverviewTab.vue` 改名而来、`git mv` 保历史），丢掉那些重复 / 可弃的块（数据概要、导入目标、决策面板、技术信息折叠），419 → 约 270 行。被丢的概要信息在左栏卡片和「数据文件」维护中心里都还在，没有信息损失。
- **删 3 个孤儿 tab 组件**：`DatasetRecordsTab` / `DatasetFilesTab` / `DatasetTechnicalTab` 被 import、被模板 `v-else-if` / `v-else` 挂着，但 `activeTab` 永远不可能取到 `'records'` / `'files'` / `'technical'`（维护中心上线后被取代的旧版）——够不到的死代码，连同 `DatasetsPage` 里 tab 抽取后遗留的未用 import（`BidsUploadPanel` / `TechnicalFold`）一并清掉。
- **类型联合收窄**：`DatasetWorkbenchTab` 由 `'overview'|'import'|'data'|'records'|'files'|'technical'` 收成 `'data'|'import'|'share'`，三处定义同步（`DatasetsPage.vue` / `datasetContext.ts` / `useDatasetImportTarget.ts`）。

遗留（本轮未动）：「概览」删除后，`provide` 仍下发但已无 tab 消费的 `datasetDecisionSummary` / `dataView` / `copyStatus` / `copyTechnicalValue` 成了软死代码，可后续随 `DatasetContext` 接口一起清，避免本轮连锁改动。

本地静态校验：全项目 `vue-tsc`（0 错）过；CRLF 未翻转（`--ignore-cr-at-eol` 比对与普通 diff 一致）；真机交互待云端部署验收。

**事实源**：`components/datasets/DatasetShareTab.vue`（原 `DatasetOverviewTab.vue` 改名）、`DatasetMaintenanceTab.vue`、`views/DatasetsPage.vue`、`composables/datasets/{datasetContext,useDatasetImportTarget}.ts`。

## 2026-06-14 · ERD/ERS 用例2 调试修补：TFR 基线/量纲、Epoch 事件显示、候选计数

跑通运动想象 ERD/ERS 测试用例（用例2，握拳 vs 放松）过程中暴露并修掉的一串问题。ERD/ERS（事件相关去同步 / 同步）看的是运动想象时 α/β 节律功率相对**基线**的百分比变化（负=去同步 ERD、正=同步 ERS），所以「基线窗取哪段、百分比怎么算」直接决定结果对不对。

- **TFR 基线归一化的量纲修正（改了显示数值，重要）**：`percent` 模式下 MNE 返回的是**分数**（−0.21 即 −21%），此前被当成「已乘 100 的百分数」直接显示、数值差了 100 倍；改成 ×100 才是真百分比（`tfr_view.py`）。
- **TFR 基线窗自检**：把「基线窗落在 epoch 之外 / 起止区间反了」在交给 MNE 之前翻译成可操作的中文报错，并让 `baseline_tmin` 缺省取 epoch 起点，不再把 `None` 甩给 MNE 后报看不懂的错（`tfr.py`）。
- **Epoch 节点 condition chip 显示规则字典**：节点存的 `conditions` 既可能是前端勾选的「分组名字符串」，也可能是脚本 / API 写的 `{name,pattern,mode}` 规则字典（如 `{name:fist,…}`）；后者此前被 `String(dict)` 渲染成 `[object Object]`，改成取 `name` 显示。仅对齐展示、不在前端还原 pattern/mode（chip 编辑仍以字符串名为准，纯脚本造的规则字典建议在 UI 里只读）（`useEventSelectEditor.ts`）。
- **ERP/TFR 候选 condition 回填真实事件数**：下游节点候选不再硬编码 `count=0`，而是回查上游 LoadData 分组显示真实试次数（分组名字符串能命中；脚本 / API 的友好名字典匹配不到则回落 0，但不再把「有 40 试次」误显成 0）；顺手把分组聚合抽成 `loadDataGroupCounts()` 共享件复用。
- **用例配置 / 文档校准 + 本机真值对拍脚本入库**：`02_erd_ers` 把「锚 cue 指令切分（避开 `label/0,1` 在 trial 起止撞同采样点 → MNE 报事件不唯一）、Epoch 窗取 `-3.5~4`（cue 落 MI 窗内部、基线须落 MI 起点之前）」等坑写进 README；新增 `erders_reference.py`（独立 MNE 直接出 ERD/ERS 图、不经后端，用来对拍 pipeline 产物是否正确），改名去掉 `local_` 前缀正式入库。

本地静态校验：`py_compile`（`tfr.py` / `tfr_view.py`）+ 全项目 `vue-tsc`（0 错）均过；真机时频数值待云端验证。

**事实源**：`engine/analysis/tfr.py`、`pipeline/tfr_view.py`、`composables/pipeline/useEventSelectEditor.ts`、`elys_scripts/projects/02_erd_ers/`。

## 2026-06-13 · 6-01 增设「设计纲领：受众与渐进披露三层」+ 节点粒度判据

把多次零散讨论里反复出现的**产品设计哲学**正式沉淀进设计原则页，作为 6-01 所有具体 UI 细则的上位框架。此前 6-01「核心设计原则」全是给「会分析的人」写的战术规则（数据可追溯、角色权限、三态处理…），缺一个回答「这软件到底给谁看、默认露多少」的纲领；而受众与「厚做到薄」的取向，恰恰是此前「UI 偏工程」与「工作流节点该拆多细」两类问题的共同根。

**新增内容**（在 §1 之前插入**无编号** `## 设计纲领` 段，仿页面已有的无编号 `## 一句话` 惯例，既有 6 个编号小节一字未动、不改号）：

- **受众**：最终用户是医生 + 心理学家 / 神经科学家（非工程师）；一句话纲领 = 让脑电处理显得「简单、fancy」而非复杂。
- **三层界面，同一套后端**：薄（临床 / 不懂细节者：上传 → 答问 → 看结果）/ 中（科研人员：看节点图、改关键参数）/ 厚（专家：全参数、可下钻近 MNE）。两条纪律——概念先行自上而下（先定薄层的词，厚层是其展开，别先堆厚层再贴薄皮）、领域语言不用文件系统语言。
- **Overleaf 原则**：底层源码永远可达（「懂了有细节」是有下限的承诺）；协作内建——文件管理 + 远程协作是区别于 MNE 的「另一半护城河」，与分析模块同等重要。
- **节点粒度判据**：工作流节点按「科研决策边界」拆、不按 MNE 函数边界拆；两问法（用户决策 vs 实现细节 / 是否跨分析类型复用）→ 做参数 / 做独立节点 / 藏内部。裁决示例：时频分析 = TFR 节点（average 作参数）+ 独立 Baseline 节点 = **2 个节点**，不是 1 也不是 3。可靠性来自三层收敛：科研人员决策粒度 = 未来 LLM 编排 pipeline 的粒度；**节点图 ≈ 结构化的论文 Methods 段**。
- 远期愿景（标 <span class="elys-badge elys-badge--planned">规划 · 北极星</span>）：对话生成 pipeline + 自动起草论文 Method/Results，落地冻结待用户访谈；其当下约束 = 每个节点带 **provenance（来历溯源）元数据**，现在留位几乎零成本、事后补昂贵。

纯文档变更；`mkdocs build --clean` 通过。

**事实源**：`wiki/docs/6-01-设计原则与UI约定.md`「设计纲领：受众与渐进披露三层」段。

## 2026-06-13 · 新增时频分析（TFR）节点 + 时频观察页接真数据

脑电分析里继 ERP（时域平均）之后最常用的一类结果是 **时频分析（time-frequency / TFR）**——把每个 trial 的信号在「时间 × 频率」两个维度展开，看某段时间里哪些频段能量升高/降低（如运动想象的 α/β 频段 ERD/ERS）。此前仓库只有半成品脚手架（`data_type='tfr'`、file_role 映射、validator 端口兼容、`/observe/tfr` 路由 + 纯静态设计稿 `TfrPage.vue` 都已就位，但没有真正能算时频的节点、也没接真实数据）。本轮照 ERP/Evoked 的现成链路把整条 TFR 做实。

**后端（计算 + 存储，对标 evoked）**：
- 引擎 `engine/analysis/tfr.py · run_tfr(epochs, params)`：按 condition 选 Epochs（与 ERP 同口径、绕 MNE 层级标签直接按数字 event id 过滤）→ `epochs.compute_tfr(method='morlet', average=True)` 算总功率（含诱发+诱导）→ 可选 `apply_baseline`。参数：`fmin/fmax`（默认 4–40 Hz，自动夹到奈奎斯特以下）、`n_freqs`（默认 30 等距频点）、`n_cycles_factor`（n_cycles = 频率 × 系数，默认 0.5）、`decim`（时间抽取省内存，默认 4）、`baseline_mode`（默认 logratio，展示时 ×10 → dB）、`baseline_tmax`、可选 `channels`。
- IO 三件套（`engine/io.py`）：`save_tfr_h5`（落 MNE 的 HDF5，强制 `-tfr.h5` 后缀）、`read_tfr_from_data_info`（兼容 `read_tfrs` 单对象/列表两种返回）、`summarize_tfr`（轻量摘要，不含功率立方）。
- dispatcher：注册 `eeg/analysis/tfr` → `_execute_tfr_output`（镜像 `_execute_evoked_output` 的 condition 展开 + 逐条保存 + GC），新增 `_derived_tfr_filename`（产出合法 `-tfr.h5` 名）。`always_per_condition`，每个 (epochs, condition) → 一个 AverageTFR artifact。
- 节点定义 `pipeline/nodes/eeg_analysis_tfr.json`：输入 `epochs`、输出 `tfr`、`preview_type='tfr'`、紫色（区别于 ERP 绿）。
- 新端点 `GET /studies/{id}/outputs/{oid}/tfr`（`pipeline/tfr_view.py · build_tfr_heatmap`）：读 `-tfr.h5`，返回某通道的 freq×time 功率矩阵（按 `produced_by_params.baseline_mode` 换算 dB/%/z 单位）+ 全通道名 + δ/θ/α/β/γ 刺激后频带均值 + 建议色阶上界（|值| 98 分位）。默认通道取刺激后能量变化最强者。

**前端（视图，双模式）**：
- `TfrPage.vue` 此前是用 `pseudoRand` + 数学函数伪造的纯设计稿。改为：路由带 `study_output_id` + `studyId`（artifact 预览面板「打开观察页」即带）时进 **live 模式**——`onMounted` 拉 `/tfr`、渲染真实热图（复用原有 SVG 热图 + 三套色标）、通道可切换（重新拉取）、坐标轴/频带功率/色标/标题全部由真实数据驱动、单位随基线模式变（dB/%/z）；无参时保留原 mockup 不动（`ObserveTabs` 直链进来仍可看设计稿）。
- 配套：`api/pipelines.ts · getStudyOutputTfr`、类型 `StudyOutputTfr`/`TfrBandStat`、`useArtifactPreview.ts` 加 `tfr` 指标分支（channels/freqs/fmin/fmax/tmin/tmax）、`previews.py · observe_route_for_data_type` 映射 `tfr`→`/observe/tfr`。

**验证**：本地无运行环境，做静态校验——后端 7 文件 `py_compile` 全过、节点 JSON 解析 OK、全项目 `vue-tsc` 0 错。真机时频计算（compute_tfr 的内存/耗时、h5 读写、热图端点）待云端 `step3_elys_debug` 验证。归一化方式当前由节点参数固定，观察页左栏「归一化」单选在 live 模式下只读显示（不回改结果）。

**事实源**：`backend/app/engine/analysis/{tfr.py,__init__.py}`、`engine/io.py`、`pipeline/{dispatcher.py,previews.py,tfr_view.py}`、`pipeline/nodes/eeg_analysis_tfr.json`、`routers/study_outputs.py`；`frontend/elys-web/src/{views/TfrPage.vue,api/pipelines.ts,types/index.ts,composables/pipeline/useArtifactPreview.ts}`。

## 2026-06-13 · 仓库根工作流 cmd 脚本按步骤编号改名（step1–step4）+ 文档引用同步

仓库根的四个工作流入口脚本（双击即用的 cmd 包装层）按「买机 → 部署 → 调试 → 释放」的实际操作顺序加步骤前缀（3ae3d95），资源管理器里按文件名排序即是操作顺序，不用再记哪个先跑：

| 旧名 | 新名 | 作用 |
|---|---|---|
| `buy_ecs.cmd` | `step1_buy_ecs.cmd` | 购买计算服务器（Spot 抢占式实例） |
| `deploy_remote.cmd` | `step2_deploy_remote.cmd` | 一键部署到入口 + 计算服务器 |
| `elys_debug.cmd` | `step3_elys_debug.cmd` | 云端端到端调试（转调 `elys_scripts/start.cmd`） |
| `release_ecs.cmd` | `step4_release_ecs.cmd` | 释放计算服务器（停止计费） |

只改仓库根包装层的名字，转调的底层脚本（`elys_project/deploy/*.ps1`、`elys_scripts/start.cmd`）均不变。文档侧同步更新全部功能性引用：`wiki/docs/2-10`（部署脚本表）、`2-80`（deploy 目录注释）、`7-10`（schema 升级方式）、`elys_scripts/README.md`、`elys_scripts/projects/01_erp_basic/README.md`、`AGENTS.md`、`CLAUDE.md`、`design/STYLE-MIGRATION-2026-06-05.md`。9-0x 月度页里旧名的历史记录按惯例保留不改。

**事实源**：仓库根 `step1_buy_ecs.cmd` / `step2_deploy_remote.cmd` / `step3_elys_debug.cmd` / `step4_release_ecs.cmd`。

## 2026-06-12 · 后端 pipelines.py 拆 router（3435→2451，6 子文件）+ 一批无悔工程投入落地（check 门 / get_db / OpenAPI 类型 / vitest）

接 `日志/6_工程债评审260612/工程债评审与重构路线.md`，把后端另一大「上帝 router」`pipelines.py` 按子领域拆分，并补齐一批无悔工程化投入。**全部云端验证或本地闭环、对外 API 路径零变化**。

**拆 router**（`pipelines.py` 3435→2451 行，路径全不变 = 对前端零影响）：
- 新增 `routers/pipeline_node_specs.py`：节点规格 `list_node_specs`（零依赖，第一刀验证模式）。
- 新增 `routers/study_outputs.py`：研究项结果数据 7 端点（list/get/update/batch/preview/timeseries/download）+ 3 个 retention helper（含 `apply_study_output_retention_action` 的 commit-then-raise 事务逻辑原样保留）。
- 新增 `routers/pipeline_load_data.py`：LoadData 解析 `resolve`。
- 新增 `routers/pipeline_definitions.py`：工作流 CRUD + edit-lock 8 端点。
- 新增 `routers/_pipeline_shared.py`：跨子领域共享 helper（`get_study_for_*` / `get_pipeline_or_404` / `pipeline_to_response` / `count_nodes` / `study_output_to_response`）。
- `pipelines.py` 剩 **Tasks + Executions 执行引擎核心**（cancel/retry/inline/lineage）——两者经 `retry_async_task → retry_pipeline_execution` 循环 import 血肉相连、本地验不出（compileall/pyflakes 都不执行 import），**有意保留不拆**；`cleanup`/`gc`（依赖共享 `create_and_dispatch_file_task`）与 execution-outputs 视角也留此。

**配套无悔投入**：
- **check 门**：`check.cmd`→`elys_project/deploy/check.ps1`（后端 compileall + 前端 `vue-tsc` + vitest）三件套，挂进 `deploy_remote.ps1` 开头作硬门（`-SkipCheck` 逃生）。
- **get_db 事务兜底**：`get_db()` yield 后 `commit`、except `rollback`，向后兼容现有 89 处手写 commit；云端 `elys_debug` 端到端验证零回归。
- **OpenAPI 类型管线**：`gen-api-types.ps1` 从云端活后端拉 `openapi.json` 生成 `src/types/api.ts`(6841 行) + npm `gen:api-types`（IP 跟随部署 profile）。手写 `types/index.ts` 退役待渐进。
- **vitest 前端单测**：50 个（`pipelineFormatters` 27 + `datasetsFormatters` 23），接入 check。

**方法 / 护栏**：本地验证流水线 = marker 脚本精确切代码 + **pyflakes**（抓 compileall 抓不到的「漏 import NameError」，本批真拦下一个会崩部署的 `count_nodes`——它实为 Pipelines+Executions 共享、已提共享层）+ 悬空残留检查 + 按文件行尾保存（`pipelines.py` 是 CRLF、新文件 LF，不混换行符）+ compileall + check 三件套。每块拆完一次部署 `curl` 逐端点验证（health + list/create/get pipelines + study-outputs + executions 全 200 零回归）。

**事实源**：`backend/app/routers/{pipelines,pipeline_node_specs,study_outputs,pipeline_load_data,pipeline_definitions,_pipeline_shared}.py`、`main.py`、`routers/__init__.py`、`database.py`；`check.cmd`、`elys_project/deploy/{check.ps1,deploy_remote.ps1}`；`frontend/elys-web/{gen-api-types.ps1, src/types/api.ts, src/composables/**/*.spec.ts}`；评审快照 `日志/6_工程债评审260612/`。

## 2026-06-12 · DatasetsPage 巨石组件拆分完成：脚本逻辑抽成 6 个 composable（机械搬运、零行为改变）

接 `日志/6_工程债评审260612/工程债评审与重构路线.md` 的结论——`DatasetsPage.vue` 是前端两大「上帝组件」之一（评审时 4721 行、动手时已涨到 5071 行，P1 债），按评审 §3 ④「沿用已开始的 composable 拆法」执行。镜像 PipelinePage 已沉淀的 `composables/pipeline/` 那套，新建 `composables/datasets/`，把 `<script setup>` 的逻辑按子领域分 6 批抽出，**模板与样式一字未动、行为零改变**。

- **批 1 `datasetsFormatters.ts`（297 行，34 个纯函数）**：日期 / 时长 / 文件体量格式化、可见范围与质控状态的标签与配色、文件角色归类、错误消息提取、`sanitizeCode`/`queryString` 等。全无状态，镜像 `pipelineFormatters.ts`，零响应式风险的第一刀。
- **批 2 `useDatasetCatalog.ts`（79 行）**：目录主轴——`datasetAssets`/`selectedDatasetAssetId`/搜索筛选/`assetStats`/`loadDatasetAssets`。自包含叶子，是其余 composable 的依赖底座（`selectedDatasetAsset` 经 options 注入）。
- **批 3 `useDatasetFiles.ts`（129 行）**：working 文件索引——搜索 / 角色筛选 / 分页 / `assetBuckets` 两桶汇总；依赖 `selectedDatasetAssetId`。`FILE_INDEX_PAGE_SIZE` 移此导出。
- **批 4 `useDatasetRecordings.ts`（235 行）**：采集记录——列表 / 按被试分组 / 每条记录文件懒加载 / 2 桶拆分；依赖 `selectedDatasetAsset` 与页面 `recordsStudyContext`。`DatasetRecordingRow`/`RecordsStudyContext` 接口随之移入并导出。
- **批 5 `useDatasetLifecycle.ts`（591 行，最大）**：数据集生命周期 v2（3-25）整块——发布 / 撤回 / 紧急下架 / 新建未发布 / 丢弃（5 个弹窗）+ 授权用户（`dataset_members`）+ 可见范围开放 / 转公开 + 整集删除 + 全部 `hasXxx`/`canXxx` 判定。这些动作互相耦合（版本载入后拉授权、开放为共享后拉授权、共用 `isAssetOwner`/`lifecycleMessage`），故合成一个；owner 判定内部自取 auth store，`showPageNotice` 经 options 注入。
- **批 6 `useDatasetImportTarget.ts`（500 行，最绕）**：导入目标准备——建集（bootstrap，连带自动建处理工作空间）/ 为已有数据集挂载（mount）+ study 自动配对的全部退避逻辑（code 冲突重试、复用既有 mount），产出 `targetSummary`/`uploadContext`（喂 BidsUploadPanel）/`recordsStudyContext`（喂采集记录）/`technicalInfoItems`。`route` 内部自取，导航 ref（`activePanel`/`activeTab`）与 `loadDatasetAssets`/`loadSelectedAssetFiles` 经 options 注入。
- **批 7 收尾**：抽离后页面有 30+ 个 import 失效（10 个类型、5 个 API、19 个 formatter、`nextTick`/`useRoute`/`RecordsStudyContext`），逐个核验出现次数后清除；`const route` 死声明一并删。

留在 `.vue` 里的是「指挥层」：import + 6 个解构 + 页面级提示 `pageNotice`/`showPageNotice` + 跨簇胶水 `datasetDecisionSummary` + 全部 watcher + `onMounted`/`onUnmounted` + 导航 / 编排函数（`selectDatasetAsset`/`openCreatePanel`/`returnToDatasetWorkbench`/`reloadAll`/`handleUploaded`/`copyDoi`/`copyTechnicalValue`）。watcher 与生命周期钩子刻意全留页面，保证响应式与副作用时序不变。

**度量**：`<script setup>` 1714 → **395 行（−77%）**；整文件 **5071 → 3751 行**；新增 `composables/datasets/` 6 文件共 1831 行。

**方法 / 护栏**：每批纯机械搬运（函数体与 computed 表达式逐字照搬、不夹带任何逻辑「优化」）→ `npm run typecheck`（`vue-tsc --noEmit`）必绿才进下一批。批 1、批 2 落地后由用户云端真机点验通过，批 3–6 + 收尾为同一会话内连续完成（用户授权无人值守、明日验收），全程仅靠 `vue-tsc` 兜底。注意 `.vue` 是 CRLF、`composables/` 目录是 LF，改 .vue 全程未混换行符。**所有 6 批 + 收尾后 `vue-tsc` 全仓零错误**；运行时（建集 / 挂载 / 导入、版本发布 / 撤回 / 删除、文件 / 记录浏览）待云端真机验证。

> 为何这么做：评审 §2.2 指出上帝组件的债是「核心功能改动副作用范围大」+ 前端 0 测试、回归只能靠真机（已被咬过：手选 UI 误删、Teleport 崩渲染器）。composable 化把各子领域隔离，改一处不再波及全文件；同时为未来「任务向导」简化入口铺垫。**特意机械搬运、不动模板 / 不拆子组件 / 不抽 CSS**——后两者会引入 `vue-tsc` 看不见的视觉回归，留待有真机验证的会话再做。

**事实源**：`frontend/elys-web/src/views/DatasetsPage.vue`、`frontend/elys-web/src/composables/datasets/{datasetsFormatters,useDatasetCatalog,useDatasetFiles,useDatasetRecordings,useDatasetLifecycle,useDatasetImportTarget}.ts`（均新增）；评审快照 `日志/6_工程债评审260612/`。

## 2026-06-11 · 修工作台「最近活动」死链：activity_target_url 漏改 /workflow → /pipeline

真机反馈：工作台首页「最近活动」里 Execution / Pipeline 条目的链接是 `/studies/{id}/workflow?pipeline_id=..&execution_id=..`，点进去落空——`/workflow` 段早在术语统一（workflow→pipeline）时改名 `/pipeline` 且不留重定向，但后端 `services/dashboard_summary.py` 的 `activity_target_url` 是这次漏网的链接构造点。把 pipeline / execution 两类的 4 处 `/workflow` 全改 `/pipeline`（query 参数不变）。`py_compile` 过；前端直接用后端返回的 url、无前端 `/workflow` 构造点。

**事实源**：`backend/app/services/dashboard_summary.py`（`activity_target_url`）。

## 2026-06-11 · 回退 P4 LoadData 前端重写（误删 task#61 手选 UI，真机反馈后还原）

真机部署后用户反馈：P4 把 `LoadDataPanel.vue` 重写成「四维度 chip + 实时预览」模型时，**误删了原有右栏「文件列表」+ 底部「Selected File」手选 UI**——而那是 task #61 的有意设计（手选放入 Selected 的列表才是 LoadData 真正输出）。

- **处理**：把 `LoadDataPanel.vue` 与 `PipelinePage.vue` 还原到 P4 之前（`git checkout 74e60bc -- 两文件`），`vue-tsc` 通过。用户原面板（Include/Exclude 左栏 + 文件列表右栏 + Selected File 底栏）原样回来。
- **认知修正**：原「前端永远 explicit」**不是 bug**，是 task #61 有意为之——未勾文件时 filter 会让后端返回挂载全集、污染下游 Epoch/ERP 事件下拉，故特意用手选 Selected File 当输出。我把它当 bug「修」掉并连带删 UI，是误判 + 过度激进。
- **filter 跟随式选择（plan §2.7）若仍要做**：须**叠加**在现有手选 UI 之上、作为可选模式，**绝不动右栏/Selected**；待与用户重新定方案。
- **不受影响**：后端 `load_data` 的 filter 能力一直都在、本就没改；P1 的 `file_role` → `fif` 改名仍在；P5 转公开审核（前后端）独立、保留。

**事实源**：`frontend/elys-web/src/components/LoadDataPanel.vue`、`views/PipelinePage.vue`（均还原至 74e60bc）。

## 2026-06-11 · 存储重构 P5 前端落地：转公开审核（owner 申请公开 + admin 审核页，镜像撤回审核）

P5 后端已在 `c90c5ee` 落地（`dataset_publicization_requests` 表 + service + owner/admin 端点 + 调试期 auto-approve），本轮补**前端**。纯前端 + 镜像现成的撤回审核那套，风险低，`typecheck` 全过。

- **owner 侧（`DatasetsPage.vue`）**：可见范围「开放为公开」按钮改为「**申请公开**」——`submitOpenVisibility` 在 `target==='public'` 时改调 `datasetAssetApi.requestPublicization(assetId)`（升 shared 仍走旧 `open-visibility`）；按调试期 auto-approve 返回 `decision==='auto'` 即时提示「已自动通过、已公开」，否则提示「已提交待审核」。弹窗文案补「先审后开、调试期自动通过」。顶部加 admin「转公开审核」入口跳 `/admin/publicizations`。
- **admin 侧（新增 `AdminPublicizationsPage.vue`）**：镜像 `AdminWithdrawalsPage.vue` —— 列 `GET /dataset-publicizations/pending`、approve/rejected + admin_notes 走 `POST /{id}/review`；通过升 public（不可逆）、驳回保持 shared。路由 `/admin/publicizations`（`AdminPublicizations`）。
- **api / 类型**：`datasetVersions.ts` 加 `datasetPublicizationApi { listPending, review }`；`datasetAssets.ts` 加 `requestPublicization`；`types` 加 `DatasetPublicizationRequestRecord` / `PublicizationDecision('approved'|'rejected'|'auto')` / `PublicizationRequestBody` / `PublicizationReviewRequest`。
- **顺手清 P1 残留**：删 `datasetAssets.ts` 里指向已删端点的死绑定 `buildRawBids`（P1 删了 `raw-bids-build` 后端，前端这条漏网）。

**测试**：前端 `vue-tsc` 全过。运行时（申请→auto 升 public、admin 审核队列、驳回保持 shared）待云端 `elys_debug` 验证。

**事实源**：`frontend/elys-web/src/views/DatasetsPage.vue`、`views/AdminPublicizationsPage.vue`（新增）、`router/index.ts`、`api/datasetVersions.ts`、`api/datasetAssets.ts`、`types/index.ts`；后端 `c90c5ee`（未改，仅佐证）；3-25 §4.2 / 方案 §2.8 / P5。

## 2026-06-11 · 存储重构 P4 落地：LoadData selector filter/explicit 双模式 + 实时预览（前端，静态验证过）

> ⚠️ **本条已回退**（见上「回退 P4 LoadData 前端重写」）：此次重写误删了 task #61 的手选右栏 UI，真机反馈后 `LoadDataPanel`/`PipelinePage` 已还原到 P4 前。下文为当时记录，保留作溯源。

承接 P1，推进 P4 —— 修掉「前端永远把选择塌缩成固定 ID、数据集后续加被试不自动纳入」的真 bug。前端 `typecheck` 通过，运行时待云端。

**核心认知**：后端 `resolve_load_data_selection` **本就支持 filter 模式执行时实时展开**（`_load_filter_datasets` 查 `recordings` 按 BIDS 四元组匹配），塌缩纯属前端行为。所以 P4 **是前端改动**，后端零改。

**改了什么**：

- **`LoadDataPanel.vue` 重写**（1299 行 → ~440 行）：丢掉旧「includes/excludes 键盘导航 listbox + 手动 Selected File 钉选」范式，换成方案 §2.7 的**四维度模型**——被试 / Session / 任务 / Run 各「全部 or 多选」chip（空集 = 全部，与后端 `_normalize_filter_value` 空列表→all 一致）+ **filter/explicit 模式切换** + 快捷场景按钮（全部数据 / 单被试·全条件 / 全被试·单任务）+ `require_fif` 开关。保存按所选模式：filter 存规则（`dataset_ids` 恒空），explicit 把当前匹配冻结成固定 id。
- **实时匹配预览走客户端**：面板已持有全量 `studyDatasets`（`datasetApi.list` 返回的 recordings，旧面板的候选列表也用它），故预览（「匹配 N 条」+ 前 20 条 + 「及另 M 条」+ qa 徽章）在客户端即时算，**不新增后端预览端点**——更快、面更小、风险更低。
- **`PipelinePage.vue` 集成**：① `ensureLoadDataParams` 不再硬塞 `selection_mode='explicit'`（旧 task #61 口径），改为尊重已存模式、缺省 `filter`；② `resolveLoadDataPreview` 与 `fetchEventLabelsForAllLoadData` 的「`dataset_ids` 空就跳过后端」闸门放开 filter 模式（filter 本身即选择，恒不带 id 也要解析展开），explicit 模式仍要求非空 id。
- **节点 spec `eeg_data_load.json`**：本就声明 filter/explicit + dataset_filter 四维 + dataset_ids，默认 filter，无 `expand_mode` 用户面项——无需改。

**未做**：执行粒度（逐条/整批）仍由引擎按节点类型自动判，不暴露给用户（聚合类节点 P2 再说）。

**测试**：前端 `vue-tsc` 全过（含与 `PipelinePage` 的 `LoadDataParams`/`LoadDataFilter` 类型对齐：面板类型镜像 PipelinePage，`DatasetFilterValue=string|null`，modelValue/emit 双向可赋值）。运行时（filter 实时展开、explicit 冻结、预览计数、下游事件标签随 filter 刷新）待云端 `elys_debug` 验证。

**事实源**：`frontend/elys-web/src/components/LoadDataPanel.vue`（重写）、`views/PipelinePage.vue`（`ensureLoadDataParams` / `resolveLoadDataPreview` / `fetchEventLabelsForAllLoadData`）；后端 `pipeline/load_data.py`（filter 展开，未改，仅佐证）；方案文档 §2.7 / §3.3 / P4。

## 2026-06-11 · 存储重构 P1 落地：数据集目录两层 + file_role 精简 + LoadData 源角色（静态验证过，待云端）

承接同日「存储与版本重构方案定稿」（见下一条），把原列为蓝图的 **P1（目录两层 + file_role 精简）** 实际落地——本机静态校验全过（`compileall app` + 9 个测试文件 `py_compile` + 前端 `npm run typecheck`），运行时正确性待云端 `elys_debug` 验证。**这条取代下一条里「P1 仍是蓝图 / storage.py 已回滚」的说法。**

**后端 IO 链路改齐（一次性，避免「解析新格式 / 写入旧格式」半截崩）**：

- **目录两层**：数据集物理根从 `datasets/{asset_id}/versions/{label}/...` 收敛为 `datasets/{asset_id}/`，下直接是 `sourcedata/original_uploads/`（asset 级原始存档）+ `BIDSdata/sub-/[ses-/]eeg/`（canonical FIF）。canonical FIF 取代旧 `derivatives/elys-canonical-fif/sub/ses/task/run/upload-NNN/`，**去掉 task/run/upload 嵌套**（同被试多 task 共享 `eeg/` 目录，靠文件名 stem 区分）。URI 方案统一为 `elys://datasets/{asset_id}/{logical_path}`（不含 `versions/` 段）。涉及 `datasets.py`（`dataset_asset_root`/`dataset_fif_root_uri` 取代 `dataset_version_root`/`dataset_version_storage_uri`、`canonical_fif_logical_dir` 去嵌套、`dataset_storage_reference_for_path`/`dataset_logical_path_from_storage_uri`/`resolve_study_relative_path` 解析去 `versions/`）、`storage.py`（`dataset_asset_root` + `_resolve_dataset_uri` 去 `versions/`，`dataset_version` 字段恒 None）、`dataset_bootstrap.py`、`dataset_lifecycle.py`（working 版 storage_uri = `BIDSdata`）。
- **FIF 文件名 `_eeg.fif`（Q6）**：`generate_canonical_fif` 与 `ensure_canonical_fif_targets_are_free` 由 `_raw.fif` 改 `_eeg.fif`；commit 阶段改**逐文件落入共享 `eeg/` 目录 + 同名覆盖**（不再整目录 move、不再因目录已存在 409）。
- **file_role 精简（§2.3）**：canonical FIF 行 `canonical_fif`→**`fif`**；provenance 行 `canonical_fif_provenance`→**`fif_provenance`**；泛化 `sidecar`→按类型 **`fif_eeg_json` / `fif_channels` / `fif_events`**（provenance 键在 sidecar 循环跳过，避免与 `fif_provenance` 重复）。`create_dataset_file_records` **删掉** `raw_source` / `raw_bids_data` / `raw_bids_*` sidecar 冗余写入（raw_bids 降纯逻辑：BIDS 实体查 recordings 表、原始位置查 `original_upload` 行）。读侧同步改名：`load_data.py` `CANONICAL_LOAD_FILE_ROLE="fif"`、`executor.py` 默认角色与候选（`raw_source`→`original_upload`）、`file_browser.py` FIF 预览判定、`file_tasks.py` `canonical_fif_rebuild` 查询。
- **溯源 provenance 重整**：合成的 `SourceRawBIDS` / `SourceEvents` / `SourceChannels`（假 raw_bids 路径）退役，改记真实 `SourceBIDSEntities`（sub/ses/task/run 四元组）+ `SourceOriginalUpload`；删 `raw_bids_eeg_dir` / `raw_bids_file_stem` / `raw_bids_*_logical_path` / `raw_bids_data_metadata` / `brainvision_component` / `RAW_BIDS_SIDECAR_ROLES` / `DATASET_RAW_BIDS_PREFIX` 等死代码。
- **下线 raw_bids 构建任务**：删 `create_raw_bids_build_task` 端点 + `run_raw_bids_build` worker；`pipelines.py` `FILE_TASK_TYPES` 去 `raw_bids_build`；`/bids-tree` 端点与 `DatasetFileTreeResponse.prefix` 缺省由 `raw_bids` 改 `None`（展示全部逻辑路径）。

**前端（去 raw_bids 痕迹，filter 重构留 P4）**：`BidsUploadPanel.vue` 导入结果删恒为 0 的「Raw BIDS」行；`DatasetsPage.vue` 文件统计删恒为 0 的「BIDS 逻辑视图」（canonicalFif 仍按 `fif` 子串匹配新角色）。

**测试对齐两层口径（9 文件）**：`test_dataset_upload_storage_paths`（重写 EDF/BrainVision 两测到新角色集 + 去 `versions/working` 路径）、`test_storage_service`（dataset URI 解析到 asset 根、`dataset_version` 恒 None）、`test_file_browser`（BIDSdata 树 + `fif_*` 角色）、`test_execution_manifest` / `test_load_data_execution_input_snapshots`（`canonical_fif`→`fif`）、`test_pipeline_execution_routes`（`FILE_TASK_TYPES` 字面量去 `raw_bids_build`）、`test_dataset_bootstrap_service` / `test_dataset_lifecycle`（storage_uri→`BIDSdata`）、`test_dataset_qa_mock_run`（`build_dataset_file_tree` 默认 prefix None）。

**未做（仍待云端）**：P3 发布物理动作（`mv BIDSdata→ver{label}` + 清单 + GC）、P4 LoadData selector 双模式持久化 + 预览端点 + `LoadDataPanel.vue` 重构、P5 前端审核页。`study_output_store.py` 的 `file_role="canonical_fif"` 映射属**输出域**（StudyOutputFile，非 dataset_files），不在 §2.3 精简范围，本轮不动。

**事实源**：`backend/app/routers/datasets.py`、`pipeline/load_data.py`、`pipeline/executor.py`、`services/storage.py`、`services/dataset_bootstrap.py`、`routers/pipelines.py`、`tasks/file_tasks.py`、`schemas/dataset.py`；前端 `components/BidsUploadPanel.vue`、`views/DatasetsPage.vue`；方案文档 `日志/4_存储与版本重构260610/00_存储与版本重构方案.md` §2.1–2.3 / §3 / P1。

## 2026-06-11 · 存储与版本重构方案定稿 + PII 强制脱敏 + 转公开审核（部分实施 + 蓝图）

连日关于「上传数据怎么存、版本怎么发、文件怎么选、PII/伦理怎么管」的设计长谈，收敛成完整方案并落档 `日志/4_存储与版本重构260610/00_存储与版本重构方案.md`。本轮实施其中**独立、可静态验证**的部分，高危 IO 重构留**精确蓝图**待云端逐阶段做。

**6 项决策（用户逐条拍板）**：

- ① 数据集物理目录收敛**两层**：`sourcedata/original_uploads/`（asset 级原始存档，永不改）+ `BIDSdata/sub-/ses-/eeg/`（canonical FIF，BIDS 命名，`_eeg.fif`）。canonical FIF 取代旧 `derivatives/elys-canonical-fif/`（去 task/run/upload 嵌套）；`raw_bids/` 降为**纯逻辑索引**（不落物理、不登记 file_role，BIDS 实体映射查 recordings 表 + original_upload 行）；`derivatives/` 本义留给科学产物（StudyOutput 在 studies/，不在数据集）。
- ② 版本管理用 **DB 清单表 `dataset_version_files`**（version↔file 多对多）：物理文件存一份（storage_uri 指真实目录），版本成员关系走清单；发布 v+1 时改动文件落新物理行、未改动文件复用旧 file_id（逻辑链接，无 OS symlink）。发布 `mv BIDSdata/→ver{label}/` + 新建空 BIDSdata；同名 FIF 改 trigger 重转 → 新版本目录新物理文件、logical_path 同 / storage_uri 异，旧版本可重现。
- ③ 转 FIF **强制 `raw.anonymize()`** 脱敏：在**导入时**（非发布时）擦文件头 subject_info + 日期，FIF「出生即干净」；sourcedata 原件不擦（事实源、限 owner）；身份映射靠 sub-001 假名 + owner 侧登记表，不进 FIF。发布时脱敏声明（勾选+伦理+版权）保留作人工担责层，两阶段互补。
- ④ 用户界面按用途区分「上传格式（EDF/BrainVision）/ FIF格式」，不透磁盘路径。
- ⑤ LoadData 用 **filter 过滤器模型**选文件（被试/session/任务/run 维度，全部 or 多选 + 实时预览 + 快捷场景），selector 持久化 filter（不再前端塌缩成固定 ID 列表）；执行粒度（逐条 per_recording / 整批 batch）由引擎按节点类型**自动判**，非用户选项。
- ⑥ 旧物理结构 DROP 重建、不写迁移（调试期无真实数据）。

**关键认知 publish ≠ expose（Q1 定稿）**：系统两条正交轴（发布态 unpublished→published→withdrawn × 可见范围 private→shared→public）。发布只冻结 + 铸 DOI（私有也铸），不曝光；真实曝光只在 shared→public。故 **发布 = 完全自助（A）**、**转公开 = 先审后开（B，shared→public 需管理员批准）**，调试期默认 auto-approve。被否的纯 A / 纯 B 都把审核错装在不曝光的「发布」步——发布不曝光，审它挡不住风险又添阻力。

**本轮已实施（`py_compile` + `compileall app` 全过）**：

- **P2 转 FIF 强制脱敏**：`datasets.py` 新增 `anonymize_raw_for_import(raw)`（`raw.anonymize()` + 兜底 `info._unlock()` 清 `subject_info`），在 `generate_canonical_fif` 的 `raw.save()` **之前**调用。
- **P5 转公开审核（全后端）**：新表 `dataset_publicization_requests`（镜像 `dataset_withdrawal_requests`）+ ORM `DatasetPublicizationRequest`；`dataset_lifecycle.py` 加 `request_publicization`（owner 申请，调试期 `PUBLICIZATION_AUTO_APPROVE=True` 即时升 public + `decision='auto'`）+ `review_publicization_request`（admin 审核 approved/rejected）；`dataset_versions.py` 加 owner 端点 `POST /dataset-assets/{id}/publicize-request` + admin `GET /dataset-publicizations/pending` & `POST /{id}/review`，经 `routers/__init__` + `main.py` 注册；`open-visibility` 端点拦 `public` 引导走申请。
- **DB schema + ORM 地基**：`03_datasets.sql` 加 `dataset_version_files` + `dataset_publicization_requests` 两表 + 索引 + 目标态注释（file_role 精简 / dataset_files.storage_uri 新方案 / dataset_versions.storage_uri）；`models/study.py` 加 `DatasetVersionFile` + `DatasetPublicizationRequest` + `models/__init__` 导出。

**留蓝图待云端逐阶段实施（高危 IO 重构，无测试不宜盲改半截）**：

- ~~**P1 目录两层 + file_role 精简**~~ → **已于本日落地**，见上一条「存储重构 P1 落地」。下文保留原蓝图描述供溯源：贯穿 `datasets.py` 路径函数（`dataset_version_root`/`dataset_storage_uri`、canonical_fif 路径、raw_bids 删物理写入、`generate_canonical_fif` commit 改逐文件覆盖、`create_dataset_file_records` 删 raw_bids/raw_source 写入 + canonical_fif→fif 改名）、`storage.py` URI 解析（去 `versions/` 段）、`dataset_bootstrap.py`、`dataset_lifecycle.py`、`load_data.py`。改一半（解析新格式 / 写入旧格式）会让导入崩，须一次改齐 + 云端验证。
- **P3 发布物理动作**：`publish_dataset_version` 补 `mv BIDSdata→ver{label}` + 写清单（changed 物理 + unchanged 链接）+ 更新 `version.storage_uri` + 版本目录 GC 条件。依赖 P1。
- **P4 LoadData**：selector filter/explicit 双模式持久化（后端执行时展开）+ 匹配预览端点 + 前端 `LoadDataPanel.vue` 重构。
- **P5 前端**：申请按钮 + admin 审核页（镜像 `AdminWithdrawalsPage.vue`）。

**为什么分这样交付**：P1/P3/P4 是跨 ~10 文件、相互纠缠的核心导入/转换/存储 IO 链路；本机无运行环境，只能 `py_compile`（纯语法），无法验证运行时正确性。无测试下盲改全量极易留隐藏 bug，且「改一半」会让后端直接崩。故先把独立、可静态验证、合规价值高的 P2/P5 + DB/ORM 地基做扎实，IO 重构以精确蓝图（方案文档 §2–§5 带文件/行号）交付，待能部署测试时逐阶段实施验证。

**测试**：`py_compile` 全部改动文件 + `compileall app` 通过；运行时（脱敏实际生效、转公开端点、auto-approve 升 public）待云端 `elys_debug` 验证。

**事实源**：`日志/4_存储与版本重构260610/00_存储与版本重构方案.md`（完整方案 + 蓝图 + 决策记录）；`backend/app/routers/datasets.py`（P2 + open-visibility 拦截）、`services/dataset_lifecycle.py`、`routers/dataset_versions.py`、`schemas/dataset_lifecycle.py`、`models/study.py`、`models/__init__.py`、`routers/__init__.py`、`main.py`、`database/schema/03_datasets.sql`。

## 2026-06-11 · 修复输出 register 撞 sha256 唯一索引：死行就地复活（06-10 残留项收口）

接 06-10「修复输出回收站两缺陷」的已知残留（出处 `日志/3_元素档案260610/06_输出_StudyOutput.md` §3.3）：唯一索引 `idx_study_output_sha256 (study_id, sha256) WHERE sha256 IS NOT NULL` **不区分死活**——回收站（`deleted_at`）/ 已清盘（`purged_at`）行同样占位；而 `StudyOutputStore.register` 做 content-addressed 去重时只查活跃行、未命中即 INSERT。后果：同 study 内某输出进回收站或被 GC 清盘后，重跑工作流产出字节相同的产物 → 查不到活跃行 → INSERT 撞唯一索引 → IntegrityError → 节点执行失败。

**方案权衡（二选一，选 b）**：

- a) 索引加 `WHERE deleted_at IS NULL`：允许死行与活跃行共存同 sha——但只是把雷挪了个坑：用户恢复回收站行（清 `deleted_at`）时若活跃行已存在，flush 照样撞同一索引，IntegrityError 从 register 转移到 restore，还得再给恢复路径加预检；且结果页 / 回收站会出现同内容重复行。
- b) **register 查重不分死活、命中死行就地复活**（采用）：保住「同 `(study_id, sha256)` 恒 ≤1 行」强不变式，06-10 刚修的 restore 409 守卫、GC 删盘前共享检查、所有查询口径全部不动。复活的物理前提天然成立——`register` 被调用前同 sha 文件刚被写回同一 content-addressed 路径（未清盘行文件本来就在；已清盘行文件刚被重新 move 回来），与 06-10 封掉的「用户手动恢复 purged 行」（文件没回来的幽灵行）语义上是两回事。

**修复**：`_register_study_output` 查重去掉 `deleted_at IS NULL` 过滤；命中死行 → 清 `deleted_at`/`purged_at`，`keep`/`cache_eligible`/`retention_expires_at` 重置为本次 `apply_save_settings` 决策值（keep=true 则 TTL 清 NULL），血缘 `produced_by_*` 与用户层 `display_name`/`tags` 保留原值（与活跃行复用同口径）。schema 不动。

**顺带修复两处预存在问题**：

- 测试设施欠账：P5 给 register 加 `db.query(...)` 去重后，`test_study_output_store_study_storage.py` 的 `FakeDb` 没有 `query` 方法，两个存量测试在现行代码下本就应红——补 `_FakeColumn`/`FakeQuery` 最小查询桩。
- `StudyOutputSummary.to_dict()` 的兼容键 `storage_path` 用 `storage_uri.split("/", 3)` 截取，对 `elys://studies/{id}/outputs/...` 会把 `{study_id}/` 段留在头上——dispatcher/cache 按 `study_root / storage_path` 解析时拼出双 study_id 死路径（现网未炸是因为 `_artifact_path` 优先走 `storage_uri` 正确解析，`storage_path` 只是 fallback，但它污染 data_info 的 `fif_path`/`logical_path` 等键）。改为直接输出 `self.logical_path`（register 登记的就是相对 study_root 的 `outputs/...`）。

**测试**：补 3 条——同 sha 活跃行复用不重插 / 回收站行复活（生命周期按本次重算、keep=true 清 TTL）/ 已清盘行复活（先物理删文件模拟 GC，断言重跑后文件重新落盘 + `purged_at` 清空）。本机无 pytest / 后端依赖，用一次性 runner（stub `pydantic_settings` + 复刻 tmp_path/monkeypatch）自跑 **5/5 通过**（含修好的 2 条存量）；`py_compile` + `compileall app` 过；真实 pytest 与节点重跑行为验证需云端。

**wiki 同步**：[3-45](3-45-StudyOutput.md) §6 register 口径改写（查重不分死活 + 复活语义 + 与手动恢复 purged 行的区别）。

**事实源**：`backend/app/pipeline/study_output_store.py`、`backend/app/pipeline/contracts.py`、`backend/tests/test_study_output_store_study_storage.py`。

## 2026-06-10 · 术语三处定名（工作流 / 保存 / 结果）+ save 面板简化为单 checkbox + 研究项三页全宽

真机走查暴露同一概念多套叫法、save 面板沿用旧五值下拉太重、研究项内三页宽度不齐。一次性把术语收口到「中文业务词 + 英文代码标识」双轨，并落地用户拍板的几处界面决策。

**术语三处定名（全局：代码 / URL / wiki 一致）**：

- **工作流 / `pipeline`**：用户面中文一律「工作流」，代码 / URL / 路由名 / tab key 一律英文 `pipeline`。研究项内 tab 路径 `/studies/:id/workflow`→`/pipeline`、路由名 `StudyWorkflow`→`StudyPipeline`、tab key 与模板里 4 处硬编码 `${id}/workflow` 路径全部改齐。**旧 `.../workflow` 链接不留重定向**——开发期系统，做彻底（顶层旧 `/pipeline`·`/results` 带 `?studyId` 的升级重定向另作他用、保留）。
- **保存 / 不保存**（取代「保留 / 不保留」）：save 节点 / 结果页那个「要不要这个产物」的二元开关，露出文案从「保留 / 不保留」改为「保存 / 不保存」（`keep` 字段名与语义不动，只改文案）。**「保留」仍用于系统语义**——永久保留 DOI / 上传证据 / Execution 记录、GC 后 DB 行留追溯等，不在此次改名内。
- **结果**（取代「派生数据」，路 A）：用户面中文统一「结果 / 分析结果」，**代码标识 `StudyOutput` / `study_outputs` / `/outputs` 不动**、「输出」作同义词保留（用户拍板①不改）；BIDS 语境的「文件派生关系」（`dataset_file_derivations`）是另一概念，不误伤。

**save 面板简化**：节点检查器的保存设置，从旧五值 retention 下拉（current/pinned/cached/temporary/discard）+ 缓存标注，简化为**单个「保存」checkbox**——叶子（终）节点默认勾、中间节点默认不勾，强制取消时提示「最终结果默认保存，不需要请删除该节点」；缓存是系统行为、不再向用户暴露。

**研究项三页全宽**：数据 / 工作流 / 结果三个 tab 统一撑满全宽（去限宽版心）——画布本需全屏，数据 / 结果靠左限宽显单薄，一并放开。

**结果页藏纯临时文件**：结果页只列「保存 / 缓存 / 回收站」三类，纯临时（`keep=false` 且 `cache_eligible=false` 且未删）产物不展示——系统中间态，不占用户视野。

**历史页（9-00/9-01/9-02）术语处理**：名词引用（「数据 / 工作流 / 派生数据」列表、「缓存复用已删派生数据」等）直接换「结果」；但**记录「派生数据→输出」这次改名本身的叙述**改用代码标识 `derived_dataset` 指代旧名——否则盲替换会变成「结果抽象、统一改输出」这种与现首选词「结果」自相矛盾的语义反转（共修 4 处：9-00 P2 条目 + 9-02 三处）。keep 的「保留 / 不保留」作为当时露出文案的忠实记录，历史页不动。

**Why**：一个概念多套叫法，会让不写代码的脑电研究员和未来接手的大模型反复对照；统一双轨后，看 UI 是业务语言、读代码是稳定标识符，两边各自自洽。save 面板「一个勾」对齐「用户只决定留不留、缓存系统自理」的心智模型。

**影响哪些页面**：[3-45](3-45-StudyOutput.md) / [4-30](4-30-Study输出与Artifact.md) / [5-30](5-30-Execution管理.md)（keep 露出文案「保留」→「保存」）、[6-00](6-00-前端页面总览.md)（pipeline 路由总表订正）。代码层见提交 `49c9bb3`（工作区打磨 + workflow→pipeline + keep 保存化）、`daaa9b6`+`c1bbad3`（派生数据→结果 路 A 代码 + wiki）。

## 2026-06-10 · 修复输出回收站两缺陷：恢复 / 取消保留补 TTL + 已清盘行禁止恢复

元素档案盘点（`日志/3_元素档案260610/06_输出_StudyOutput.md` §9、`00_索引.md` B1/B2）发现的两个高置信度缺陷，均在「输出」的 PATCH / batch-update 链路（`routers/pipelines.py` 的 `apply_study_output_retention_action`）。

> 黑话铺垫：**TTL（time to live）** 指行的"存活期限"（`retention_expires_at` 字段），到期由每日 cleanup 任务软删进回收站；**幽灵行** 指 DB 里是活跃状态、磁盘上却已没有对应文件的登记行。

**缺陷 1 · 恢复 / 取消保留不补 TTL（用户操作形同无效）**：

- 现象：把 keep 改 false、或把回收站行恢复（deleted=false）后，`retention_expires_at` 保持 NULL 或已过期值；而每日 cleanup（`tasks/file_tasks.py` 的 `run_study_output_cleanup`）把 NULL / 已过期都视为立即可回收——下一轮定时任务直接再次软删，用户感知是"我刚恢复它怎么又没了"。
- 修复：`save_settings.py` 新增 `retention_expiry_after_user_action(keep, cache_eligible)` 作为用户动作后的 TTL 重算口径——keep=true → NULL（永不自动清，原行为不变）；keep=false 且 cache_eligible → 缓存档 `now+7d`（与产出时口径一致）；否则给宽限期 `USER_ACTION_GRACE_DAYS = 7` 天（产出时临时档"登记即过期"保持不变，仅用户显式动作走宽限；天数对齐决策清单 P3-g~l 拍板的"删除/恢复 TTL 7 天"）。`apply_study_output_retention_action` 在显式传 keep、或恢复后行仍是 keep=false 时调用重算。

**缺陷 2 · 已清盘行不挡恢复（幽灵行）**：

- 现象：PATCH 恢复不检查 `purged_at`，可把 GC 已物理删盘的行恢复成"活跃但磁盘无文件"（预览 / 下载必挂），且该行占用 `(study_id, sha256)` 唯一索引、挡同内容输出再登记；此前仅前端禁用按钮、后端无校验，直接调 API 即可绕过。
- 修复：单条 PATCH 对 `purged_at IS NOT NULL` 行的恢复返回 **409 `OUTPUT_PURGED`**（守卫排在清 deleted_at 之前，写 `.blocked` 审计，与下游依赖拦截同款）；batch-update 仿照 deleted=true 的 blocker 预检——`deleted=false` 时先全量预检 purged 行，任一命中就在改动任何数据之前整体 **409 `OUTPUT_BATCH_PURGED`**（避免循环中 commit-then-raise 造成部分成功）。

**测试**：`test_save_settings.py` 补 3 条 TTL 口径单测（keep=true 清空 / 缓存档 7d / 非缓存宽限必须在未来）；`test_pipeline_execution_routes.py` 补 3 条源码守卫（purged 守卫先于清 deleted_at、TTL 重算两处调用、batch 预检先于套用循环），并顺带修复一条 P3 改名后已失效的旧断言（仍查 `target_status == ARTIFACT_DELETED_STATUS`，该符号全仓已不存在，该测试在现行代码下本就应红）。

**已知残留（另立任务）**：回收站 / 已清盘行本身仍占 `(study_id, sha256)` 唯一索引——重跑产出同内容文件时 `StudyOutputStore.register` 只查活跃行、未命中即 INSERT，会撞索引使节点执行失败；放宽索引条件（加 `WHERE deleted_at IS NULL`）或登记时"复活"死行，需另行权衡后定，出处见 06 档案 §3.3。——已于 06-11 修复（选"复活死行"方案），见当日条目。

**wiki 同步**：[3-45](3-45-StudyOutput.md) §3.5 补用户动作 TTL 口径与 purged 行 409、§6 按 06 档案 §9-3 给定方向改写（Save 节点已取消，原文还在描述不存在的 `save_promotion()`）；[4-30](4-30-Study输出与Artifact.md) §2/§3 从旧 `retention_status` 六档表重写为三层解耦现状（P3 改名漏网页）；[5-30](5-30-Execution管理.md) §6 操作映射表同步 keep/deleted 双轨 API。

**范围 / 验证**：后端 `python -m py_compile`（save_settings / pipelines + 2 个测试文件）通过；`test_save_settings.py` 本机自跑 33/33 通过；4 条路由源码守卫断言本机复刻验证通过；真实 pytest 与行为验证需云端（本机无运行环境）。前端无需改动（purged 行恢复按钮已禁用；新增 409 走通用错误提示）。

**事实源**：`backend/app/pipeline/save_settings.py`、`backend/app/routers/pipelines.py`、`backend/tests/test_save_settings.py`、`backend/tests/test_pipeline_execution_routes.py`；缺陷出处 `日志/3_元素档案260610/06_输出_StudyOutput.md` §9-4 / §9-7。

---

## 2026-06-10 · superadmin 口径收敛：紧急下架前端 / 测试对齐 admin（3-25 §6.4）

元素档案盘点（`日志/3_元素档案260610/07_用户与权限_RBAC.md` §9-2、`00_索引.md` M1）坐实「紧急下架到底谁能做」三处事实源打架，本条按 [3-25](3-25-数据集生命周期与发布机制.md) §6.4 现行设计收敛。

> 黑话铺垫：**事实源（source of truth）** 指"以谁为准"的那份定义。同一条权限规则散落在后端校验、前端按钮显隐、测试断言三处且互相矛盾时，权限审计就回答不了"到底谁有权"。

**收敛前的三处打架**（同一动作三个口径）：

- 后端 `services/dataset_lifecycle.py` `_ensure_admin`：**已收敛 admin**（2026-06-09 决策 A，docstring 写明）。
- 前端 `DatasetsPage.vue`：按钮仍 `isSuperadmin` 才显示——admin 看不到自己有权调的端点，superadmin 点了又被后端 403，两边都是死路。
- 测试 `tests/test_dataset_lifecycle.py`：仍断言「superadmin 可下架、admin 被拒」，与服务层行为相反（现行代码下这两条本就应已红）。

**本次改动**：

- 前端：紧急下架按钮显隐 `isSuperadmin`→`isAdmin`，删掉随之 unused 的 `isSuperadmin` computed，3 处旧注释同步改 admin 口径。
- 测试：`test_emergency_takedown_admin_can_takedown`（admin 放行）+ `test_emergency_takedown_rejects_non_admin`（负责人与 superadmin-only 用户均被拒——反向钉死「superadmin 不参与数据集生命周期」的边界）+ `requires_reason` 用例 actor 改 admin。
- wiki [7-20](7-20-后端API设计与三端调用模型.md) §4.4 两处旧表述订正（"superadmin 紧急下架 / superadmin 操作"→admin）。
- 顺带修复 P2 改名遗漏（mkdocs build 自查发现）：`mkdocs.yml` nav 仍指旧 `3-45-DerivedDataset.md` 致改名后的 [3-45 输出（StudyOutput）](3-45-StudyOutput.md) 掉出导航，nav 条目同步改名。

**seed 不动（核实结论）**：`seeds/01_roles_permissions.sql` 的 `superadmin` 按 3-25 §6.4 **保留**——定位"平台治理与管理员管理（管理员的管理者），独立于数据集生命周期"，角色描述（:7）与授权注释（:66）已写明该边界，无需再补。已知现状：superadmin 暂无任何专属端点（纯占位），其实际职责要等用户生命周期 / 管理端点立项时一并落（07 档案 §9-1 待办）。

**范围 / 验证**：后端 `python -m py_compile`（服务 + 测试文件）、前端 `npm run typecheck` 通过；真实 pytest 需云端跑（本机无运行环境）。

**事实源**：`frontend/elys-web/src/views/DatasetsPage.vue`、`backend/tests/test_dataset_lifecycle.py`、`wiki/docs/7-20`；设计依据 3-25 §3.3 / §6.4、`database/schema/03_datasets.sql` `withdrawal_admin_notes` COMMENT。

---

## 2026-06-10 · Save 重构 P5 收官：GC 物理清盘 + celery beat 定时 + 回收站

Save 重构（P1–P5）最后一块。P3 已把回收站基础（软删 `deleted_at` + 恢复 + 前端"已删除"chip）做了，本条补「物理清盘 GC」+「定时自动维护」。

> 黑话铺垫：**GC（garbage collector，垃圾回收）** 这里指后台任务定期把回收站里超期的文件真正从磁盘删掉；**content-addressed（内容寻址）** 指文件按内容哈希（sha256）存放、同样内容只存一份、被多处引用。

**三层删除语义**（用户拍板）：
- `deleted_at`（软删 / 回收站）：用户删除或 cleanup 标记，文件还在、可恢复。
- `purged_at`（物理清盘）：GC 删磁盘文件后置位，**保留期 30 天**——删后 30 天内可恢复，超期 GC 物理清盘。**只删磁盘文件、保留 DB 行**（标记 purged，血缘 / 审计可追溯"曾经有过这个输出"）。

**GC 任务 `run_study_output_gc`**：
- 清盘条件 `deleted_at < now − 30d AND purged_at IS NULL`。
- **content-addressed 安全检查**：删物理文件前查同 `sha256` 是否还有活跃（未删）行引用——有则保留物理文件、只标本行 purged（多个 execution 复用同一物理文件）；无则真删磁盘。
- 加 `POST /outputs/gc` 手动触发端点 + `study_output_gc` task 类型注册。

**定时自动维护（celery beat）**：
- `run_storage_maintenance`：每天一次全局跑 cleanup（软删过期缓存）+ GC（清盘超期回收站）；`cleanup` 改成支持全局（`study_id=None`）。
- `celery_app.py` 配 `beat_schedule`；`deploy.sh` 的 worker 加 `-B` 内嵌 beat 调度器（调试期单实例最简单）。

**前端**：`StudyOutput.purged_at`；结果页徽章加「已清盘」态、恢复按钮在 purged 后禁用（文件已删不可恢复）。

**至此 Save 重构 P1–P5 全部落地**：P1 死代码 → P4 缓存评分 → P2 输出改名 → P3 retention 三层解耦 → P5 GC + 回收站。

**范围 / 验证**：backend compileall + 前端 vue-tsc typecheck 通过；FILE_TASK_TYPES 测试断言同步。**物理删除 + beat 定时的运行正确性需云端验证（celery beat 进程起没起、真删磁盘、content-addressed 共享检查）；磁盘删除不可逆、保留期 30 天兜底。**

**事实源**：`database/schema/05_outputs.sql`、`backend/app/{models/study_output, tasks/{file_tasks,celery_app}, routers/pipelines}`、`deploy/deploy.sh`、`frontend/elys-web/src/{types, views/ResultsPage}`；方案 `日志/2_meeting260609/Save与发布-执行方案.md`。

---

## 2026-06-10 · Save 重构 P2「输出」改名 + P3 retention 三层解耦（keep / cache_eligible / 回收站）

接 P1（死代码）/ P4（缓存评分）之后，落地 Save 重构最大的两块：结果实体码名 `derived_dataset`→`StudyOutput`（中文定「输出」）、retention 五值状态机拆成三个正交维度。

> 黑话铺垫：**三层解耦** 指把一个混在一起的字段拆成几个互不影响的维度、各管一件事；**正交（orthogonal）** 指这些维度彼此独立、可任意组合（保留与否 × 缓存与否 × 是否在回收站）。

**P2 结果实体全栈改名 `derived_dataset`→`StudyOutput`（中文定「输出」）**：
- 结果实体原码名 `derived_dataset` 偏工程化，P2 统一定名「输出」（中）/ `StudyOutput`（码）/ `study_outputs`（表）/ `/outputs`（API）。
- 表 `derived_datasets`→`study_outputs`、模型 `DerivedDataset`→`StudyOutput`、store / schema / 路由 / scripts / 索引名 / DDL 文件名（`05_derived.sql`→`05_outputs.sql`）全栈对齐；保留 `dataset_file_derivations` 词族（那是「文件派生关系」表、与「输出」不同概念，不能误伤）。
- 验证：后端 compileall、前端 typecheck、grep 三模式归零均过；磁盘目录 `derived/`（牵动 ~8 测试硬编码 storage_uri 断言）留云端跑测试时收尾。

**P3 retention 三层解耦**：把 `study_outputs.retention_status`（current/pinned/cached/temporary/deleted 五值状态机）拆成：
- `keep`（bool）= 用户是否保留（leaf 节点默认 true / intermediate false，用户可在节点参数 `keep` 覆盖）。
- `cache_eligible`（bool）= 系统是否值得缓存（= P4 的 `is_cache_eligible(spec)`，与 keep 独立）。
- `deleted_at`（已有）= 回收站软删（保留下游依赖检查 + 恢复）。
- `retention_expires_at` 仅 `keep=false` 的行有 TTL：值得缓存→now+7d、不值得→now（登记即过期、交 GC 清）。

值映射：current/pinned→keep=true；cached→keep=false+cache_eligible=true；temporary→keep=false+cache_eligible=false；deleted→deleted_at。

- 后端（13 文件）：05_outputs.sql + model 拆字段；save_settings（`default_keep_for_role` + `_normalise_keep_param` + 调 is_cache_eligible）；store / contracts / cache（删死函数 `cache_retention_for_role`）；schemas（Update.keep/deleted、Cleanup 去 retention_statuses）；routers（状态机 `apply_study_output_retention_action` 改 keep/deleted 双轨、删 pin/unpin 文案与 `ARTIFACT_DELETED_STATUS`、PATCH/batch/cleanup/list/preview/download 全改）；file_tasks（cleanup 按 `keep=false AND expires<now AND deleted_at IS NULL`、软删只置 deleted_at）；manifest / summary / previews / dispatcher。
- 前端（6 文件）：结果页 / 工作流页的 5 值保留下拉 → 「保留 / 不保留」二元开关，缓存产物用 `cache_eligible` 标注（徽章显示 保留 / 缓存 / 临时 / 已删除）；ResultsPage summary chips 改「全部 / 保留 / 不保留 / 已删除」、批量 setKeep/delete；PipelinePage pin/unpin/hide action → keep/deleted；删枚举 `StudyOutputRetentionStatus` + 死接口 `ArtifactCleanupTaskRequest`。
- 测试（4 文件）：对齐 keep/cache_eligible，并修一处预存在不一致（test_file_tasks 之前 import `run_artifact_cleanup`，但函数早已改名 `run_study_output_cleanup`、测试没跟）。

**Why**：旧 `retention_status` 把三件不相干的事（用户意图 / 系统缓存 / 删除态）塞进一个枚举，pin / temporary / quarantined 名不副实、用户要在 5 个词里选；拆成两个布尔 + deleted_at 后，用户只面对「保留 / 不保留」二元，缓存全自动（P4 评分）、删除走回收站——认知负担与实现复杂度同时降。

**范围 / 验证**：P1/P4/P2 主体已云端部署验证（建库 + 创建 Execution + 缓存命中均正常）；P3 后端 compileall + 前端 vue-tsc typecheck + 全栈 grep `retention_status` 归零 + 4 测试 py_compile 均过。**P3 的 SQL 建库 + 前后端 keep/cache_eligible 全链路运行正确性需云端 psql \ir + pytest 联调；磁盘目录 `derived/`→`outputs/` 与 P5（GC celery beat + 回收站 UI）待续。**

**事实源**：`database/schema/05_outputs.sql`、`backend/app/{models/study_output, pipeline/{save_settings,study_output_store,contracts,cache,cache_policy,execution_manifest,previews,dispatcher}, schemas/study_output, routers/pipelines, tasks/file_tasks, services/study_summary}`、`frontend/elys-web/src/{types, api/pipelines, views/{ResultsPage,PipelinePage,study/StudyOverviewTab}, composables/pipeline/pipelineFormatters}`、`backend/tests/{test_save_settings,test_file_tasks,test_execution_manifest,test_dispatcher_split}`；方案 `日志/2_meeting260609/{Save保留策略-重构方案,Save与发布-执行方案}.md`。

---

## 2026-06-10 · Save 保留策略重构启动（P1 死代码清理 + P4 节点缓存评分）

**背景**：Pipeline 的「保存 / 保留」这条线攒了一堆历史债——`Execution.save_policy` 与 `Study.derived_dataset_retention_policy` 两个**从不驱动任何行为**的死配置、节点**一刀切全缓存**（不分计算贵贱与产物大小）、retention 枚举里 `pinned` / `quarantined` 名不副实或从没人写。完整诊断（14 条逻辑缺陷 + Q1–Q10 决策）见 `日志/2_meeting260609/Save保留策略-重构方案.md`。本条落地其中两步：P1 清死代码、P4 把缓存改成按 ROI 评分。

> 黑话铺垫：**死配置（dead config）** 指字段 / 选项在代码里被创建、存储、展示，却没有任何一处真正读它做决策——纯占位、还误导用户去选；**ROI（投资回报）** 这里指缓存的性价比 = 省下的重算 ÷ 占用的存储。

**P1 删死代码**：
- 砍 `Execution.save_policy`（全栈 30+ 处：DDL 列 + 索引 + COMMENT、PipelineExecution model、3 schema、2 service、manifest、routers 10 处、前端运行对话框死下拉 + 类型 + 6 文件、2 测试）——它从不驱动产物 retention（实际由 save_settings 按节点定）。
- 砍 `Study.derived_dataset_retention_policy`（自由 dict、无消费方，7 处全栈）。
- 删 derived 侧死态 `quarantined`（`05_derived.sql` 的 CHECK + 前端枚举 / pill / label）；`dataset_assets` 侧的 `quarantined` 是活的、**保留不动**。`pinned` / `temporary` 暂留，待 P3 与 keep 布尔改造一起处理。

**P4 节点缓存评分（存储优先）**：
- 新增 `pipeline/cache_policy.py`：`cache_score = compute_cost − STORAGE_WEIGHT × output_footprint`（`> 0` 才缓存），`STORAGE_WEIGHT = 2`（存储优先旋钮）；三条硬规则——interactive 节点强制不缓存（防缓存恢复跳过人工选 ICA 成分）、`explosive` 永不物化全量、缺标签默认不缓存。
- 8 个节点 spec 删一刀切 `cache.enabled`，7 个计算节点打 `compute_cost` / `output_footprint` 标签（LoadData 这类 source 节点不打 → 评分自动判不缓存）。
- `executor` 缓存门控由读 `cache.enabled` 改为调 `is_cache_eligible(spec)`；`node_hash` 移除 cache 块（缓存策略不影响算法字节，调标签 / 权重不破缓存）。
- 效果：当前 8 节点**只有 `ica/compute` 命中缓存**（expensive 3 − 2×small 1 = +1，算得最久、产物最小），其余跑完即走、省空间。

**Why**：死配置让用户在运行对话框选一个「选完就扔进黑洞」的保存策略、徒增认知负担；一刀切全缓存对滤波这种「算得快、产物大」的节点是**负收益**（存它 + 读它算哈希比重算还慢），对未来的单试次 TFR 更是 OOM 灾难。先清债、再把缓存改成按 ROI 自动判定（存储优先），是 Save 重构里最独立、最可静态验证的两步。

**范围 / 验证**：纯后端 + 前端，不碰运行环境；后端 `py_compile`、前端 `npm run typecheck`、全仓 `grep save_policy` 归零均通过；`cache_policy` 逻辑本机跑 8 节点确认仅 ICA 命中。调试期 DROP 重建、不留兼容层。**后续 P2（输出改名）/ P3（retention 三层解耦）/ P5（GC + 回收站）的关键正确性（表名 / 外键 / 部署）本机验不了，待云端验证后续推。**

**事实源**：`backend/app/pipeline/cache_policy.py`·`hash.py`·`executor.py`·`nodes/*.json`、`database/schema/02_studies.sql`·`04_pipelines.sql`·`05_derived.sql`、`backend/app/{models,schemas,services,routers}` 相关、`frontend/elys-web/src/{types,composables/pipeline,views}`；方案 `日志/2_meeting260609/Save保留策略-重构方案.md`。

---

## 2026-06-10 · 修复工作流画布卡"正在初始化" + 右键无菜单：拆掉 keep-alive 里的 Teleport

**现象**：进研究项「工作流·运行」tab，画布永远停在"正在初始化画布"、节点右键弹不出菜单、底部状态栏"节点 0"与画布里实际节点数对不上——但 LiteGraph 画布本身有节点、能拖拽加节点。控制台一串 `TypeError: Cannot use 'in' operator to search for 'value' in null` 与 `Cannot read properties of null (reading 'insertBefore')`。

**真因**：`PipelinePage.vue` 顶部把「工作流选择器 / 新建」用 `<Teleport to=".study-layout__extra">` 吊到研究项容器标题栏，而 PipelinePage 又被 StudyLayout 的 `<keep-alive>` 缓存。`<Teleport>` + `<keep-alive>` + 内含 `<select :value>` 三者相撞，触发 **Vue 渲染器**（`runtime-dom` / `runtime-core`，**非 LiteGraph**）在 patch 阶段给 null 元素写 `value` 属性、往 null 父节点 `insertBefore`，**整页 Vue 的 DOM 更新被这个崩溃冻结**。于是 `liteGraphReady` 其实早已 true、但 DOM 刷不动 → 永远"正在初始化"；右键把菜单 `open=true` 也渲染不出；节点数文字绑定同样卡死。LiteGraph 直接画在 canvas 上、绕过 Vue，所以画布节点正常——造成"画布有节点、Vue 文字全卡"的假象。

> 黑话铺垫：**Teleport** 是 Vue 把一段模板"瞬移"到 DOM 别处渲染的内置组件；**keep-alive** 是缓存组件实例、切走不销毁、切回不重建。两者叠加是 Vue 已知的脆弱组合——被缓存组件的 DOM 搬移会让 Teleport 目标在 patch 时变 null。

**改了什么**：删掉该 `<Teleport>`，工作流选择器 / 新建**就地渲染**在工作流页自己的工具条里（回到加该特性之前）；连带删 `teleportActive` ref 及其在 `onActivated` / `onDeactivated` 的开关。

**副作用**：撤掉了"把工具条吊到研究项顶栏"的视觉效果——选择器现在在工作流页工具条内、不在最顶 bar。若要保留该效果，须改 keep-alive 安全的跨层传递（provide/inject 或 store），别再用 Teleport；StudyLayout 的 `.study-layout__extra` 槽位暂闲置但无害。

**定位方法论**（留给后续）：生产构建报错是 minified 单字母栈（`Df` / `P` / `_`），曾连猜 HMR、LiteGraph 悬空连线均错。最终在 `vite.config.ts` 临时开 `build.sourcemap` 重新部署，报错才显真名、直指 Vue 渲染器，一举定位；修复确认后已关回。教训：云端 minified 报错看不懂时，先开 sourcemap 重部署拿真实栈，别耗在猜混淆名上。

**范围 / 验证**：纯前端；`npm run typecheck` + `npm run build` 通过；真机部署后画布初始化、右键菜单、节点计数全部恢复。

**事实源**：`frontend/elys-web/src/views/PipelinePage.vue`、`frontend/elys-web/src/views/study/StudyLayout.vue`。

---

## 2026-06-09 · PipelinePage 巨石组件拆分（第 1 批）：抽出常量 / 纯函数到 composables

**背景**：`PipelinePage.vue` 是工作流页（节点库 + LiteGraph 画布 + 参数检查器 + 运行 / 执行 / 结果管理）的单文件实现，已膨胀到 **7297 行 / 271 KB**——一个文件揽下画布引擎、节点编辑、数据筛选、运行调度、产物管理等五六个独立关注点，任何改动都得在七千行里翻找，是典型的「巨石组件」。

> 黑话铺垫：**巨石组件（God Component）** 指一个组件什么都干、状态与逻辑高度耦合、谁都改不动的反模式；**composable（组合式函数）** 是 Vue 3 把「一组相关的状态 + 方法」打包成 `useXxx.ts` 复用单元的写法，相当于「带状态的工具模块」，是给巨石组件拆方的主刀工具。

**拆分总思路（分批进行）**：逻辑层抽成 `composables/pipeline/useXxx.ts`、模板层抽成子组件 `.vue`；并先把被各处共享的「承重墙」状态（`definition` / 选中节点 / `nodeSpecs`）收拢成一个根 composable，其它模块向它取数据。整套计划约 6 批（常量 / 格式化 → 承重墙 → 草稿 / 布局 / 节点库等叶子 → 产物 / 执行 → 画布引擎 → 模板子组件化）；**本条只落地第 1 批，后续批次为规划。**

**第 1 批改了什么（已落地）**：

- 新增 `composables/pipeline/pipelineConstants.ts`——把 **39 个模块级常量**（节点类型标识、分类 / 端口 / 状态配色表、执行模式与保存策略选项、画布尺寸 / 缩放阈值、草稿与布局 localStorage 前缀等）按原名 `export`。
- 新增 `composables/pipeline/pipelineFormatters.ts`——把 **33 个纯展示 / 格式化函数**（状态文案、配色映射、文件大小 / 时间 / 时长格式化、端口兼容判断等）抽出；逐个验证为「纯函数」（只依赖入参与模块常量、不碰任何组件状态 `ref`），依赖到的常量从 `pipelineConstants` import。
- `PipelinePage.vue` 删掉这 72 个定义、改为两条具名 import，**所有引用处（含 template 调用）一字未动**——行为完全不变。主文件 **7297 → 7039 行**（净删 ~320 行定义、回填 ~63 行 import）。

**刻意留下没抽的**：`describeError`（勾连本地类型守卫 `isRecord`）、`artifactActionStatusText`（依赖本地 `type ArtifactAction`）、`buildArtifactPreview*` / `getPreviewSummary`（属「产物预览」域）——避免范围蔓延，留给后续对应批次。

**范围 / 验证**：纯前端内部重构，不动后端 / API / 类型签名 / 页面功能；`npm run typecheck`（vue-tsc）零错误通过。调试期，不留兼容层。

**Why**：七千行单文件的复杂度已压垮可维护性。先抽出零依赖、零风险的常量与纯函数（占行数不多但散落各处），既立刻减负、又跑通「抽取 → 删定义 → 改 import → typecheck」的安全流程，为后续动承重墙、画布等高风险批次铺垫。

**事实源**：`frontend/elys-web/src/composables/pipeline/pipelineConstants.ts`·`pipelineFormatters.ts`、`frontend/elys-web/src/views/PipelinePage.vue`。

---

## 2026-06-09 · 数据集生命周期 v2 定稿：发布≠分享、可见范围只升不降（废止 06-07 旧决策）

**背景**：起因于 DatasetsPage 把未发布态的内部英文枚举值直接暴露给用户，进而对 ELYS 数据集（`DatasetAsset` / `DatasetVersion`）的生命周期、状态、权限做了一次彻底重审，并对 wiki 与代码逐项核对。产出《数据集生命周期重构_综合报告 v2》（11 项决策闭环）+《落地实施清单》，作为新设计基线，落地后 [3-25](3-25-数据集生命周期与发布机制.md) 据此改写。

**这条作什么用**：本条只**宣告 v2 定稿并废止 06-07 旧决策**；逐项 schema / 服务层 / 路由 / 前端 / wiki 的落地改动随各自提交另记。06-07 两条旧记录（「可见范围=发表级、发布自动私有→共享」与「徽章用可见范围轴 B 方案」，见本页下方）**保留不改**，作历史留痕。

**v2 相对 06-07 变了什么（核心 5 条 + 配套清理）**：

- **发布 ≠ 分享（废止「发布自动私有→共享」）**：发布只做两件事——冻结版本文件 + 铸 DOI；可见范围**默认私有、不自动转共享**。是否对外可见，由负责人在发布后**单独**决定。06-07 的「发布即自动 `private→shared`」被废止（删 `dataset_lifecycle.py` 里那段自动跳变）。
- **可见范围「只升不降、无降级接口」（修正 06-07 B 方案的「只读徽章随发布联动」轴）**：私有→共享→公开可跳级开放，但**任何降级都没有入口**（共享/公开＝不可逆释放，类比论文发表）。可见范围从「通用 PATCH 可写」改由**专门的「开放」端点**处理（只升、仅负责人）；要可撤销的协作改用「私有 + 把人加进研究项团队」。
- **共享 = 邀请制（按用户授权），不再等于「任意登录用户可见」**：新建 `dataset_members` 表 `(asset_id, user_id, granted_by, granted_at)`，负责人**按用户**授权；`shared` 仅被授权者可读 / 可关联。`public` 才是「任何注册用户」——平台全员注册、**无匿名访问**，故公开＝全平台登录用户、无需匿名路由。私有＝负责人 + 管理员 + **主研究项成员**。
- **发布是 PII / 伦理 / 版权关口**：未发布→已发布时强制勾选「已脱敏」确认并声明伦理与版权，缺则不能发布；每次发布都要重做（每个已发布版本是独立不可变制品）。
- **撤回审核中禁止新关联**（修正旧代码「允许新挂载」）：已有关联保留，但不接受新关联；已撤回为终态墓碑，只能开新版本 v+1 前进。
- **配套清理**：发布状态枚举全栈统一为 `unpublished`（替换旧未发布枚举值，含 DB CHECK / 接口 / 前端，物理槽位名 `version_label="working"` 不在此列）；资产 `dataset_assets.status` 的 `working` / `active` 合并为 `working`；清理已被 `state` 取代的旧字段 `dataset_versions.status`；发布/授权/撤回申请收敛为**仅负责人**（管理员只保留撤回审核、紧急下架、隔离等平台治理；`superadmin` 角色保留另作平台治理用）。

**两根正交轴（概念模型）**：发布状态轴（版本级 `DatasetVersion.state`：`unpublished` / `published` / `withdraw_requested` / `withdrawn`）管「能不能改 / 删、是否冻结、是否铸 DOI」；可见范围轴（资产级 `DatasetAsset.visibility`：`private` / `shared` / `public`）管「谁能看、谁能关联」。核心一句话：**发布只冻结＋铸 DOI，分享是另一个独立动作**。

**术语锁定**：界面同时给中英两套标签，「导入 Import / 关联 Link / 加载 Load」三件事严格不混——导入＝外部文件进平台造数据集，关联＝研究项接上已有数据集（数据不搬动，原「挂载 Mount」改称），加载＝Pipeline LoadData 节点运行时读数据；专有名词中英一一对应、杜绝中英混排。

**影响哪些页面**：[3-25](3-25-数据集生命周期与发布机制.md)（状态机 + 权限矩阵据 v2 改写，为文档侧最高优先级）、[3-20](3-20-Dataset与采集记录表.md) / [3-50](3-50-协作权限状态与迁移.md) / [3-00](3-00-数据库设计总览.md)（删「发布自动 private→shared」「可手动调可见范围」，改「发布默认私有、只升不降」）、[6-05](6-05-信息分级与领域语言.md)（B 方案解耦：可见范围改「负责人显式开放」、保留两轴各自只读展示）、[5-10](5-10-Study研究项管理.md)（`primary_study_id` 与私有可见对齐、发布版现在时）、[3-45](3-45-StudyOutput.md)（DDL 补 `lifecycle_state` / `visibility` 列；结果是否随 v2 调整待确认）。

**范围 / 状态**：本条为**设计定稿宣告**；代码与文档的逐项落地按《落地实施清单》第五节依赖顺序施工（schema→seed→schemas→服务层→路由→前端→wiki→静态校验+部署），各步另记。调试期 DROP + 重建，不写迁移、不留兼容层。

**Why**：科研数据一旦对外共享 / 被引用即具备论文发表级的不可撤销性。把「开放可见范围」做成轻飘飘的下拉、或让发布顺带自动公开，都会诱发误操作并破坏引用方信任。v2 把两轴彻底解耦、把可见范围做成「只升不降的郑重动作」，并用邀请制把共享收敛到「明确授权的人」，让非法状态压根表达不出来。

**事实源**：`日志/2_meeting260609/数据集生命周期重构_综合报告.md`（设计基线）、`日志/2_meeting260609/数据集生命周期_落地实施清单.md`（施工清单）；代码落点 `backend/app/services/dataset_lifecycle.py`·`dataset_assets.py`、`database/schema/03_datasets.sql`、`schemas/dataset_lifecycle.py`·`dataset.py`、`routers/datasets.py`·`dataset_versions.py`、`frontend/elys-web/src/views/DatasetsPage.vue`。

---

## 2026-06-08 · 新增 buy_ecs：按启动模板 + 抢占式购买 ECS（cmd → ps1，用 aliyun CLI、不引 Python）

**背景**：计算服务器是阿里云按量 / 抢占实例、**公网 IP 每次部署都会变**（见 `AGENTS.md`），过去每次都得到 ECS 控制台手动开机、再把新公网 IP 抄回 `deploy/profiles/aliyun-test.env` 的 `COMPUTE_SERVER_IP`，繁琐且易抄错。用户要"按**自己制定的启动模板**、用**抢占式**买，而且不用 Python、像部署那样直接写个 `cmd`"。

> 黑话铺垫：**启动模板（LaunchTemplate）** 是预先存在 ECS 控制台里的一套实例规格（镜像 / 机型 / 安全组 / 交换机 / 磁盘），开机时引用它就不必每次填一大堆参数；**抢占式（Spot）实例** 是按市场价竞价的廉价 ECS（常比按量便宜一大截），代价是库存紧张时可能被自动释放；**DryRun** 是"演练"——只让阿里云校验参数对不对、并不真正下单扣费；**幂等（ClientToken）** 指带一个唯一令牌，网络重试时阿里云认得"这单下过了"、不会重复扣费开两台。

**改了什么**：

- 新增 `buy_ecs.cmd`（仓库根，4 行壳子，与 `deploy_remote.cmd` 同款）转手调 `elys_project/deploy/buy_ecs.ps1`；真活全在 ps1 的 `param()` 块里。
- **底层用阿里云官方 `aliyun` CLI（单个 exe），不引 Python SDK / venv**——契合"像部署那样一个 cmd 就跑"。
- **路径①启动模板（主）**：`-LaunchTemplateName` / `-LaunchTemplateId`，规格全交给模板，脚本只叠加抢占式策略 + 数量；显式给了 `-InstanceType` 等参数才覆盖模板对应项。**路径②显式配置（兜底）**：无模板时靠 `-InstanceType` / `-SecurityGroupId` / `-VSwitchId` 等逐项指定（镜像可留空，自动取最新 Ubuntu 22.04）。
- **默认演练**：不加 `-Yes` 永远只 DryRun（不下单、不花钱）；`-Yes` 才真买。抢占式默认 `SpotAsPriceGo`（随市场价自动出价），可切 `-SpotStrategy SpotWithPriceLimit -SpotPriceLimit <每小时上限价>`。每次带幂等 `ClientToken`（网络重试不会重复下单）。
- **闭环 IP**：真买后轮询到 `Running`、打印公网 IP + `ssh root@<IP>`；加 `-UpdateProfile` 直接把公网 IP 写回 `aliyun-test.env` 的 `COMPUTE_SERVER_IP`，紧接着 `deploy_remote.cmd` 就能部署。
- `-List` 列出账号里的 启动模板 / 安全组 / 交换机 / 镜像（用来填参）。
- 默认地域 `cn-shenzhen`（华南1，与现有计算服 IP 段一致）；凭证从环境变量 `ALIBABA_CLOUD_ACCESS_KEY_ID` / `_SECRET` 读，或走 `aliyun configure`，**不进仓库**。检测不到 `aliyun` CLI 时打印完整安装 / 配置指引并退出。

**范围 / 不进部署**：纯新增的本地运维脚本，不参与 `deploy.sh`，不碰后端 / 前端 / DB。`.ps1` 存为 **UTF-8 BOM**（与 `deploy_remote.ps1` 一致）——否则 Windows PowerShell 5.1 会按 GBK 解码、中文注释把引号 / 括号"吃掉"导致解析直接失败（本次踩过、已修）。

**验证**：本机无 `aliyun` CLI、也不真实下单（且真买已挡在 `-Yes` 之后）；已做 PowerShell AST 解析校验 + 运行时冒烟（参数绑定、中文不乱码、缺 CLI 的友好提示均 OK）。真实下单待有 CLI + 凭证的环境跑。

**Why**：把"按模板开一台便宜的抢占式计算服 → 拿公网 IP → 回写 profile → 部署"从"控制台点点 + 手抄 IP"压成一条命令；抢占式契合调试期"数据可重建、机器随时可弃"的特点，省钱。复用部署同款 `cmd → ps1` 模式，零额外学习成本。

**事实源**：`buy_ecs.cmd`、`elys_project/deploy/buy_ecs.ps1`；与 `deploy/profiles/aliyun-test.env`（`COMPUTE_SERVER_IP`）联动，计算服网络背景见 [2-10 部署架构与网络拓扑](2-10-部署架构与网络拓扑.md)。

---

## 2026-06-08 · 研究项再调「两层架构」：列表 master-detail 概览 + Overleaf 式工作区

**背景**：上一条「容器化」（见下）做完后，用户真机一看觉得**乱**：点研究项 → 整页跳进容器 → 默认落「概览」tab → 还得再点一下才摸到工作流，又跳又绕、"不像在干活"。用户给了两个参照——**DatasetsPage 的左右栏**（左列表 + 右详情，点左即时换、不跳页）、**Overleaf 的"点进项目就是操作界面"**。

**决策**：把研究项拆成清爽的**两层**：

- **第一层 · 研究项列表**（挑项目、看状态）：`/studies` 改 master-detail——左研究项列表、右选中项的概览卡 +「进入工作区」按钮，点选即时换右、不跳页（选中态走 URL `?study=id`，刷新可恢复）。
- **第二层 · 工作区**（点进去就干活，Overleaf 式）：点「进入工作区」整页切到 `/studies/:id`，**默认落工作流编辑器**（节点库 + 画布 + 参数，本就跟 Overleaf 三栏神似），顶部次级导航切 数据 / 工作流 / 结果。

核心修正：**概览从"全屏 tab"挪进列表页右栏；"点进去"的默认落点从概览改成工作流。**

**改了什么**：

- `StudyOverviewTab` 加 `studyId` prop（优先 prop、回退 `route.params`），从"容器子路由"降为"列表页右栏面板"，自己拉 summary + study 详情。
- `StudiesPage` 纯列表 → master-detail：右侧加回 `study-detail-panel`（标识 + 「进入工作区」`/studies/:id/workflow` + `<StudyOverviewTab>` 概览）；列表项从"点击跳容器"改回"点选高亮不跳页"（`selectedStudyId` 状态 + `?study=` 同步 + `ensureSelectedStudy` 兜底）；治理动作移右栏 head；CSS 双栏 grid（照 DatasetsPage `.dataset-workbench`）。
- `StudyLayout` 去掉「概览」tab（剩 数据 / 工作流 / 结果 3 项），`activeTab` 默认 `workflow`，面包屑改「← 研究项列表」（带 `?study=` 回选中）。
- `router`：`/studies/:studyId` 默认 redirect 从 `StudyOverview` 改 `StudyWorkflow`，删 `overview` 子路由（StudyOverviewTab 不再是子路由）。

- **真机迭代打磨（同日多轮）**：① StudyLayout 顶栏压成一行——`← 研究项列表 + 标题 + 状态 + code + 3-tab` 同排；② 用 `Teleport` 把工作流页顶部的「工作流选择器 / 新建」吊到顶栏槽位（配 `teleportActive` 开关，随 keep-alive 切 tab 显隐），跟 ← / 标题 / tab 拼成一条应用栏；③ 修 `.pipeline-page` 在容器内仍硬算 `height: 100vh - header`、没扣顶栏 bar 导致的下溢——改 `flex: 1` 填充容器剩余高度，工作流 tab 套 `.study-layout--full` 给定高度；④ `.page` 纵向留白抽成 `--page-pad-y` 令牌（默认 `--s-5` 不动其它页，study 页压到 8px）+ bar 降高，去掉顶部多余留白。

**复用**：后端 `/summary` 端点、`useStudySummary`、`StudyOverviewTab` 概览内容、`PipelinePage` / `ResultsPage` / `StudyDetailPage` 子页全留用——只挪位置、改落点，不重写。

**范围**：纯前端（视图 + 路由 + 样式），未动后端 / API；`npm run typecheck` 通过。

**事实源**：`views/StudiesPage.vue`、`views/study/StudyLayout.vue`·`StudyOverviewTab.vue`、`views/PipelinePage.vue`、`router/index.ts`、`style.css`（`--page-pad-y` 令牌）。

---

## 2026-06-08 · 研究项升级为「容器」：导航从四平级 tab 改为以研究项为容器（C 档重构）

**背景**：研究项本应是把「数据 → 工作流 → 结果」串起来的枢纽，但导航把它做成了和 数据集 / 工作流 / 结果 平级的"四选一 tab"之一——同一个研究项的信息被切到三页（`/studies` 右栏摘要、`/studies/:id` 采集记录、`/results` 完整结果），每个动作（导数据 / 配流程 / 看结果）都把用户甩出 studies 跳到另一个顶级页，那些页又各自带"先选哪个研究项"的下拉。用户一句"这个连接项把数据集 / 工作流 / 结果组织在一起、但呈现散乱"点中要害。

**决策（用户在 A/B/C 三档改进方案里拍板选最重的 C 档）**：把导航从「四平级 tab」改为「**以研究项为容器**」。顶层只留 工作台 / 数据集库 / 研究项；进入某研究项后，工作流·运行 / 结果 / 数据 / 概览 都是它内部的 4 个 tab。一个军师分歧（子页面怎么认 studyId：新建 store vs URL 路径参数）裁断为 **`route.params.studyId` 为唯一事实源**——嵌套路由下 URL 天然带 studyId，刷新 / 深链 / 前进后退零时序，store 只缓存当前研究项详情对象。

**改了什么**：

- **嵌套路由**：`/studies/:studyId` 成为容器布局，下挂 4 个子路由 `overview` / `data` / `workflow` / `results`（route name `StudyOverview` / `StudyData` / `StudyWorkflow` / `StudyResults`）。旧顶层 `/pipeline`、`/results` 改为兼容重定向（从 query 解析 studyId 落到对应子 tab，裸链兜底回 `/studies`），兜书签与未改全的深链。
- **studyId 事实源 = URL 路径参数**：新增 `stores/study.ts`（只缓存当前研究项详情对象供容器标题 / 概览复用，不当 id 源）；子页面一律 `route.params.studyId` 读 id，删掉 `utils/studySelection.ts`（容器化后跨页 sessionStorage 记忆的需求消失）。
- **容器骨架**：新增 `views/study/StudyLayout.vue`（WorkbenchShell 壳由容器独占 + 4-tab bar + `<router-view>` + 按 tab 切版心宽窄）、`StudyOverviewTab.vue`。三个子页面（PipelinePage / ResultsPage / StudyDetailPage）删各自的 WorkbenchShell 壳与 study 选择器，`selectedStudyId` 改 `computed(route.params.studyId)`；StudyDetailPage 就地降级为「数据」tab（只读挂载数据集 + 采集记录宽表）。
- **概览 tab**：原 StudiesPage 右栏工作台（研究进度 / 建议下一步 / 工作流·运行 / 结果 / 元信息）迁入概览；数据改吃**新后端聚合端点 `GET /api/v1/studies/{id}/summary`**（`services/study_summary.py` 复用 `dashboard_summary` 的 `grouped_count` / `build_active_executions` 范式，`subject_total` 用 `COUNT(DISTINCT subject)` 去重），一次返回替代原前端并发拼约 12 个请求的 N+1。
- **StudiesPage 瘦身**：从 master-detail 双栏改为纯研究项列表 / 卡片墙，点击进容器，治理动作（移入回收站 / 恢复 / 永久删除）行内化。
- **工作流 tab 进 keep-alive**：PipelinePage 含 LiteGraph 画布，容器对它 `keep-alive`，补 `onActivated`（重算画布尺寸防错位 / 糊）/ `onDeactivated`（停运行轮询防后台空转）/ `onBeforeUnmount` flush 草稿。
- **跳转链接 + 后端深链**：Dashboard / Index / StudiesPage 等的 `/pipeline?study_id=`、`/results?study_id=` 出链，以及后端 `dashboard_summary.activity_target_url` 的活动流深链，统一改为 `/studies/{id}/workflow?pipeline_id=&execution_id=`、`/studies/{id}/results`。

**范围**：可见性 / 权限语义（待定的 workspace 可见性那项）本次**不动**，专注导航 / 容器重构；不写迁移（调试期）。后端 py_compile + 前端 typecheck 通过。

**事实源**：`views/study/StudyLayout.vue`·`StudyOverviewTab.vue`、`stores/study.ts`、`composables/useStudySummary.ts`、`router/index.ts`、`data/workbenchPages.ts`、`views/{PipelinePage,ResultsPage,StudyDetailPage,StudiesPage,Dashboard,Index}.vue`、`backend/app/services/study_summary.py`·`routers/studies.py`·`services/dashboard_summary.py`·`schemas/study_summary.py`。

---

## 2026-06-08 · 版心宽度统一为令牌 `--content-w`（1600px）+ 补齐研究项详情页

**背景**：用户指着工作台问"中间这条内容宽度叫什么"——它是「内容容器最大宽度 / 版心」。最近「四页居中」已把工作台 / 数据集 / 研究项 / 结果做成 1280 居中，但**研究项详情页漏了**（仍撑满全宽，一进详情就突然变宽、最跳），且 1280 写死、`ResultsPage` 还自己又造了一层 1600 容器，整体不统一。

**改了什么**：

- 新增 CSS 令牌 `--content-w`（`style.css` 的 `:root`）作为版心宽度的**单一事实源**；`.page--narrow` 由写死 `1280px` 改为 `var(--content-w)`，改一处全站生效。
- 取值定为 **1600px**（用户拍板）：典型屏 1920 左右各留约 160px，2K / 4K 自动居中留白、不再拉宽。
- `StudyDetailPage` 补 `:narrow="true"`，与其余四个 live 页对齐。
- `ResultsPage` 删掉自造的重复容器 `.results-page { max-width:1600px; margin:0 auto }`，统一交给外壳 `.page--narrow`。
- 段落级阅读行宽（Results 副标题 760、Studies 描述 980 等）保留——那是文字可读性，与版心是两回事。

**范围**：工作流页（`/pipeline`）与未实现的预览页按用户要求**不动**。纯前端 CSS / 模板，未碰逻辑 / API；`npm run typecheck` 通过。当前标准沉淀到 [6-01 §1.8](6-01-设计原则与UI约定.md)。

**事实源**：`frontend/elys-web/src/style.css`（`--content-w` / `.page--narrow`）、`views/StudyDetailPage.vue`、`views/ResultsPage.vue`。

---

## 2026-06-07 · 决策：数据集可见范围（私有 / 共享 / 公开）是「发表级」严肃动作

**决策**：数据集的可见范围（`dataset_assets.visibility`）不是一个能随手切换的开关，而是**类比学术论文发表的严肃决策**。它只有三档，且只能通过郑重的发布 / 撤回流程流转，**UI 不提供随意下拉**。

**三档语义**：

| 档位 | 取值 | 谁能看 / 用 | 怎么进入 |
|---|---|---|---|
| **私有** | `private`（默认） | 仅**主研究项**可见、可挂载使用 | 新建数据集即私有 |
| **共享** | `shared` | **其他研究项**可挂载 / 引用（跨 Study 解锁） | 由「**发布版本**」流程触发：发布某版本时自动 `private→shared` |
| **公开** | `public` | 全平台可见 | 比"发布=共享"更进一步的 deliberate 动作（确认弹窗），**留作后续**，当前无 UI 入口 |

**流转规则**：

- **开放（私有→共享）= 发布**：必须走「发布版本」流程（强制 SemVer 版本号、发布后该版本文件不可改、要改开新版本），等同于"把这版数据正式发表出去"。发布成功后可见范围自动升到 `shared`。
- **收回 = 撤回，不是回退**：已发布版本要下架，走「申请撤回 → 管理员审核」流程（紧急情况由超级管理员紧急下架，事后补审计）；`withdrawn` 是终态，要继续工作只能开新版本前向演进。**可见范围不因撤回自动降级回私有**——已发布过即视为"曾公开"，其他研究项可能已引用。
- 因此**不存在"把已发布数据一键改回私有"的随意操作**；这条铁律就是不给 UI 下拉控件的原因。

**UI 落地**（同日实现，详见下方"可见范围轴"与"交互回退"两条）：数据集徽章**只读展示**当前可见范围（私有/共享/公开）+ 解释性 tooltip；改变可见范围一律经由发布 / 撤回流程，不出现随意下拉。

**Why**：科研数据一旦对外共享 / 被他人引用，就具备了和论文发表同等的不可随意撤销性（下游引用稳定性、学术诚信、DOI 不可逆）。把"开放可见范围"做成一个轻飘飘的下拉，会诱发误操作并破坏引用方的信任；交给版本发布这个本就郑重、可追溯、带版本号的流程，才与这件事的严肃程度匹配。

**相关事实源**：`backend/app/services/dataset_lifecycle.py`（发布自动 `private→shared`、撤回不降级）、`database/schema/03_datasets.sql`（`visibility` CHECK 三档）、`wiki/docs/3-25-数据集生命周期与发布机制.md`（draft/published/withdrawn 生命周期与跨 Study 引用门控）。

---

## 2026-06-07 · DatasetsPage 真机复验三补：数据概要四件套 + 创建于 X + 可见范围交互回退

**背景**：上一轮"1、2 同意，3 走 B 方案"中，实际只落了第 3 项（可见范围徽章），漏做了第 1（数据概要四件套）、第 2（列表时间口径）；且 B 方案初版给可见范围加的下拉控件，真机一看太随意，被用户否掉。本轮一并补齐 + 回退。

**改了什么**：

- **数据概要补全为四件套（被试 / 记录数 / 任务 / 时长）**：
  - 后端新增 `recording_count` 聚合（`services/dataset_assets.py` 的单查询里加 `COUNT(Recording.id)`；`schemas/dataset.py`、`routers/datasets.py::dataset_asset_to_response`、`_EMPTY_ASSET_STATS` 同步补字段，响应用 `s.get(...,0)` 兜底防 KeyError）。
  - 前端 `types` 加 `recording_count`；列表卡 + 概览卡都加"N 条记录"（`list` 图标）；概览四格定为 被试 / 记录数 / 任务 / 时长，"最近导入"移到卡片下方一行小字（L2，不占四格）。
- **列表卡时间改「创建于 X」**：原先是 `last_imported_at \|\| updated_at \|\| created_at` 三选一兜底、且无标签，含义不明；按用户要求锁定为稳定的"创建于 X"（取 `created_at`）。
- **可见范围交互回退（去随意下拉）**：用户指出"私有/共享/公开是像论文发表一样严肃的事，不该随手下拉选择"。撤掉 B 方案初版加的下拉控件（连同 `changeVisibility` / `canEditVisibility` / `hasPublishedVersion` / `isUpdatingVisibility` 及配套 CSS、`DatasetAssetVisibility` 导入一并删除）；徽章保留**只读展示 + 解释性 tooltip**，可见范围交由「发布版本」郑重流程联动（私有→发布后自动共享）、收回走撤回流程。后端 PATCH 能力保留备用（管理员/未来 deliberate 动作），但 UI 不再提供轻率入口。

**未做 / 后续**：`public`（全平台公开）作为比"发布=共享"更进一步的 deliberate 动作（确认弹窗），留待后续；`Dashboard.vue` 数据集仍按 status 统计，口径统一仍挂待办。

**验证**：本机无 Node / 真 Python，前端 typecheck、后端 py_compile、mkdocs build 待云端；本轮后端为单查询加一个 COUNT + 字段透传、前端为模板 + 一个纯函数 + 删除冗余控件，DB 无新改动（visibility 三档化在上一条已落）。

**Why**：补齐研究员判断"能不能用"的黄金四件套（尤其记录数让"总时长"不再被误读），用稳定的创建时间做列表身份锚点；并尊重"可见范围=发表级严肃决策"，把它交还给郑重的发布流程，而不是一个随手下拉。

---

## 2026-06-07 · 数据集状态徽章改用「可见范围」轴（6-05 B 方案）：跨 DB/后端/前端/wiki

**背景**：真机讨论中用户指出数据集列表徽章的"草稿"对研究员偏闷，更想要"私有/共享/公开"这类可见范围语言。挖代码发现系统其实有**三套重叠的状态轴**：资产 `dataset_assets.status`（working/active/archived…内部记账）、版本 `dataset_versions.state`（draft/published/withdrawn 发布生命周期）、可见范围 `dataset_assets.visibility`（private/workspace/shared/public）。当前列表徽章读的是轴①却套了轴②的措辞（working→"草稿"），语义拧巴。经确认走 **B 方案**：徽章改用**可见范围轴**，并把可见范围**简化为三档**（去掉用户费解、且实际从不出现的 `workspace`），同时**加 owner 手动改可见范围的控件**。

**改了什么（四层）**：

- **数据库**（`database/schema/03_datasets.sql`）：`dataset_assets.visibility` 的 CHECK 从 `private/workspace/shared/public` 改为 `private/shared/public`，补列注释。调试期走 DROP 重建。
- **后端**（`schemas/dataset.py` · `services/dataset_assets.py`）：`DatasetAssetVisibility` Literal 去 `workspace`；`can_read_dataset_asset` 与 `list_visible_dataset_assets` 的"可见即可读"集合从 `{workspace,shared,public}` 改为 `{shared,public}`。发布自动 `private→shared`（`dataset_lifecycle.py`）保持不变。**列表/详情/PATCH 早已带 visibility，无需新增端点。**
- **前端**（`views/DatasetsPage.vue` · `types/index.ts` · 组件）：
  - 列表 + 详情徽章从 `getStatusClass/Label(asset.status)` 改为 `getVisibilityClass/Label(asset.visibility)`；删除不再使用的 `getStatusClass/getStatusLabel`；新增 `getVisibilityClass`（私有→灰中性 / 共享→主色蓝 / 公开→成功绿）。
  - 筛选下拉、`assetVisibilityFilter`、顶部统计卡（`assetStats`）全部从 status 轴改为 visibility 轴（零值卡仍不渲染）。
  - 概览徽章去掉旧"可见性由发布状态自动联动、不单独展示"的注释，改为展示可见范围。（初版加了 owner 可改的下拉控件，**同日据用户反馈撤掉**——见下一条同日记录："可见范围交互回退"。）
  - `types` 的 `DatasetAssetVisibility` 去 `workspace`。
- **wiki**：`3-50` 枚举表、`3-20` 字段表、`3-00` 原则行、`6-05` §4.1 字段分级 + §6.2 改动清单全部对齐三档 + 写明"徽章用可见范围轴、资产 status 降 L3、发布状态仍在版本卡按 `state` 展示（两轴各自展示）"。

**未做 / 留作后续**：`Dashboard.vue` 仍按 `status` 统计数据集（草稿/已发布数），与本页可见范围口径暂不一致，作为后续统一口径的待办（本次只动数据集页，不扩散）。

**验证**：本机无 Node / 真 Python 运行环境，前端 `npm run typecheck`、后端 `py_compile`、`mkdocs build` 均待云端部署校验；本次后端改动为从集合/Literal 删值、前端为模板 + 计算属性 + 一处新方法、DB 为 CHECK 收窄，风险可控。

**Why**：让不写代码的研究员看到的徽章是"私有/共享/公开"这种直接回答"谁能看/能不能被引用"的可见范围语言，而不是平台内部记账轴；同时把三套重叠状态轴的职责厘清（status=内部 L3、state=发布生命周期在版本卡、visibility=用户徽章），并给 owner 一个真正能掌控可见性的入口。

---

## 2026-06-07 · DatasetsPage 真机验收三修：去重复按钮 + working 术语 + 图标语义

**背景**：上条 DatasetsPage 信息分级续修部署后，拿真机截图复验，用户红笔圈出三处新问题，按"好做直接做"原则先清掉。

**改了什么**（`frontend/elys-web/src/views/DatasetsPage.vue` + `components/IconLine.vue`，纯前端）：

- **去重复的"新建数据集"按钮**：页头右上已有主"新建数据集"，工作台详情头里又有一个同动作（`openCreatePanel`）的 `btn--primary`，截图里同屏出现两次。删详情头那个，仅保留创建模式下的"返回当前数据集"。
- **`working` 内部术语领域语言化**（§9）：版本卡 / 版本时间线 / 发布·撤回·紧急下架弹窗标题里直接显示的版本标识 `working` 对用户翻成"工作版本"（新增 `formatVersionLabel`，已发布版本仍保留其 SemVer 号如 `1.0.0`；内部取值不变）。
- **列表行 / 数据概要卡图标语义修正**：原先用 `target`（靶心）当"被试"、`play`（播放三角）当"时长"，牛头不对马嘴显得丑。给 `IconLine` 补语义正确的 `users`（人）/ `clock`（钟）并替换；任务仍用 `clipboard`。
- **"总时长"语义澄清**（用户反馈：指标有用但看不懂是"总和"）：列表行裸"2分"→"共 2 分" + 悬停"全部采集记录的总时长"；概览卡"总采集时长"格补悬停"全部采集记录时长之和"。指标保留（聚合的总时长对数据稽核有意义），仅消除"单条/平均/总和"的歧义。

**未做（转入详细分析，待决策）**：图标双系统统一管理（`AppIcon` + `IconLine` 两份字典）、工作台标签页该暴露哪些（文件索引 / 技术信息的 L3 归属）、概览页"已有数据如何清晰呈现"（数据概要应前置为主角）——见对话分析，方向定了再落 9-02 + 6-05。

**验证**：纯前端文案 / 模板 / 图标 path，未触类型签名 / API；本机无 Node 环境，typecheck 与真机效果待部署校验。

**Why**：把真机暴露的"同屏重复操作、内部术语见人、图标语义错配"这三处低成本痛点先清掉，更大的图标架构与信息架构问题留作专项决策，不夹带。

---

## 2026-06-07 · 研究项列表与详情合并（master-detail 方案 B）：工作台一页看全 + 详情页专做采集记录

**背景**：`/studies`（StudiesPage）右侧详情面板早已长成一个"迷你详情页"——研究进度、已挂载数据集、建议下一步都在里面；而 `/studies/:id`（StudyDetailPage）又把同一套"进度 + 挂载 + 工作流 + 运行"重画一遍。两页职责高度重叠，用户在列表页就能看进度、点"进入工作流"，再点进详情页价值不明，且同套逻辑两处维护。经确认走**方案 B（合并）**：承认 master-detail 模式，把详情页内容并进列表页右面板，详情页瘦身成「采集记录深表格」专页。

**改了什么**（纯前端，未动后端 / API / 表结构）：

- **`StudiesPage.vue` 右面板升级为完整工作台**：`StudySummary` 摘要结构新增 `pipelines` / `executions` 两个数组（原本只存 count，现保留列表本体，按 `started_at` 倒序）；新增「工作流与运行」卡片，并排展示工作流列表（名称 / 版本 / 节点数 / 状态）与运行记录列表（第 N 次运行 / 模式 / 保存策略 / 状态）；新增「结果」区，按选中研究项**懒加载**（watch 选中项 + 摘要 `loaded` 状态，就绪后才拉前 5 次运行的 derived datasets，避免首屏自动选中时 executions 未到）。新增 `pipelineStatusLabel / executionStatusLabel / executionModeLabel / savePolicyLabel / retentionStatusLabel / artifactPillClass / formatFileSize` 等文案映射。
- **`StudyDetailPage.vue` 瘦身为采集记录专页**：删掉已被工作台吸收的四张统计卡、概览（角色 / 成员 / 配额）、工作流列表、运行记录列表、结果、动态、快捷入口等区块；只保留页头（含返回工作台）+ 引用的数据集 + 采集记录宽表。脚本相应删掉 pipelines / executions / artifacts / members / activity 的加载与一众标签函数，`loadStudy` 简化为只拉 study + mounts + recordings。
- **入口语义对齐**：列表页右面板「查看主页」按钮改名「采集记录明细」，指向瘦身后的详情页。
- **路由不变**：`/studies`、`/studies/:id` 两个路由都保留，详情页改为专职。

**范围说明**：仅改两个 Vue 文件；未动类型签名以外的内容（`StudySummary` 是页面内 interface，新增字段属本页私有）、未动任何后端 / DB / API。

**验证**：本机无 Node 运行环境（与项目设定一致），`npm run typecheck` 待云端部署时跑；改动逐项核对字段（`DerivedDataset.display_name/file_size/retention_status`、`PipelineExecution.execution_seq/execution_mode/save_policy/status`、`Pipeline.version/node_count/status` 均已确认存在），类型层面已自查。

**Why**：一处看全一个研究项的"数据 / 工作流 / 运行 / 结果 + 下一步"，消除两页重叠与双份维护；采集记录这种宽表、深表才下沉到独立详情页。延续 6-20「列表页=生命周期 + 决策导航」的定位，但把"决策所需的上下文"做厚到不必跳页。

---

## 2026-06-06 · StudyDetailPage 领域语言化：四张英文统计卡 + 全页中英混排清账

**背景**：拿 [6-05 信息分级与领域语言](6-05-信息分级与领域语言.md) §2 现状落差点名的欠账收尾——研究项详情页 `StudyDetailPage.vue` 顶部四张统计卡标题直接是英文 "Data / Pipelines / Artifacts"，与「领域语言」原则、§9 反例（中英混排 / 暴露内部对象名）直接冲突。借此机会把整页用户可见文案过了一遍。

**改了什么**（`frontend/elys-web/src/views/StudyDetailPage.vue`，纯前端文案 / 模板）：

- **四张统计卡标题领域语言化**（§2 / §4.2 / §9）：按卡片实际统计含义命名——`Data`（统计 recordings）→「采集记录」、`Pipelines`→「工作流」、`运行记录`（原已中文）保留、`Artifacts`→「结果」；`Recording / 采集记录` 的副标题去英文。
- **全页中英混排 / 内部对象名清账**（§9）：页头 `Study 研究项 / Study 主页 / Study ID`、区块标题 `Overview / Data / Quick Links / Pipelines / Artifacts / Activity`、子区 `Dataset Mounts / Recordings`、按钮与快捷入口 `导入 Dataset / Dataset 导入`、空态与错误文案里的 `Dataset / Recording / Pipeline / Artifact / API` 等内部对象名，全部改为研究员业务语言（数据 / 概览 / 快捷入口 / 工作流 / 结果 / 动态 / 引用的数据集 / 采集记录…）。
- **"Execution #N" / "运行 #N" 按 §9 反例改**：统计卡下方运行列表与动态流的 `运行 #{seq}` 统一改「第 N 次运行」。
- **后端裸枚举接中文映射**：新增 `qaStatusLabel / executionModeLabel / savePolicyLabel / retentionStatusLabel` 四个纯文案映射函数，并把 `roleLabel` 的 `Owner/Editor/Viewer` 改中文；模板里 `execution_mode / save_policy / retention_status / qa_status / role` 不再直接吐后端枚举值；`active/inactive`、`unknown`、`status unknown`、`current` 等裸状态值也改中文。
- **BIDS 表头中文化**：采集记录表头 `Session / Task / Run`→「会话 / 任务 / 轮次」，`canonical FIF`→「标准化 FIF」（FIF 作为研究员熟悉的格式名保留）。

**范围说明**：未动状态模型 / API / 类型签名——四个新增函数只是字符串字典映射，未改任何 interface / 变量 / 函数签名；HTML 锚点 id（`#overview` 等）保持不变。`研究项标识`（studyId）仍以纯文本显示在概览卡，按 §4.2 应折进 TechnicalFold，留待后续结构性调整。

**验证**：本机无 Node 运行环境（与项目设定一致），`npm run typecheck` 待云端部署时跑；本次改动全为模板文案 + 字典字符串值，类型层面无风险。

**Why**：让不写代码的脑电研究员在研究项详情页看到的是「采集记录 / 工作流 / 结果 / 第 3 次运行」这类业务语言，而非 Data / Pipelines / Execution #3 等平台内部记账——延续 DatasetsPage 那轮"拿 6-05 当规格反查 UI"的清账思路。

---

## 2026-06-06 · DatasetsPage 信息分级续修：领域语言 + 去碎片状态

**背景**：拿 [6-05 信息分级与领域语言](6-05-信息分级与领域语言.md) 当规格复查 `DatasetsPage.vue`，发现上轮迁移做了一半——概览页已有"数据概要"卡 / TechnicalFold / DOI 按钮 / 研究项链接，但截图仍暴露三类欠账：顶部 4 张状态卡 3 个恒为 0（碎片噪音）、"Records / Recording 总数"等内部对象名直接见人、同一 `working` 状态在顶部卡叫"草稿中"而列表徽章叫"工作中"（§9 反例的中英混排 + 双说法）。

**改了什么**（`frontend/elys-web/src/views/DatasetsPage.vue`，纯前端文案 / 模板）：

- **顶部 stat 区去碎片**（§6.2）：状态卡（草稿 / 已发布 / 已归档）改为 `v-if="count"`，零值不渲染，只留"数据集总数" + 当前真有数据的状态；调试期常见的"3 个 0"噪音消失。
- **状态用词统一**（§4.1 / §9）：`getStatusLabel` 与筛选下拉的 `working→"工作中"/"草稿中"` 双说法收敛为 `working→草稿 / active→已发布 / archived→已归档`，与顶部 stat 条同口径。
- **"采集记录"标签页去内部对象名**（§2 现状落差 / §9）：tab 标签 `Records→采集记录`；面板标题、说明、4 张统计标签（`Recording 总数→采集记录数`、`QC 通过→质控通过`、`查询上下文→所在工作空间`等）、空态 / 加载 / 错误文案、行内 `source format→原始格式`、`QC 状态→质控状态` 全部领域语言化；保留被试 / 会话 / 运行等 BIDS 通用术语（研究员日常用语）。
- **L3 标签对齐**：技术信息折叠区 `Dataset Asset ID→数据集 ID`（与概览页 TechnicalFold 同名）；`Study ID / mount_name` 等纯复制用技术字段保留（L3 受众=管理员 / 调试）。

**范围说明**：未动状态模型与 API，未重构共享的 `page-stat-strip` 全局类（仅页面内加 `v-if`）；`StudyDetailPage.vue` 四张英文统计卡（Data / Pipelines / Artifacts）仍是欠账，留待后续。

**验证**：本机无 Node 运行环境（与项目设定一致），`npm run typecheck` 待云端部署时跑；本次改动全为模板文案 + 字典字符串值，未触及任何类型签名 / 变量 / 函数签名，类型层面无风险。

**Why**：延续"拿 wiki 当规格反查代码"的找茬思路落实到 UI 信息分级——让不写代码的脑电研究员看到的是"草稿 / 采集记录"这类业务语言，而非 working / Records 等平台内部记账。

---

## 2026-06-05 · 找茬收尾：4 个未深验候选逐个核实（3 个坐实并修、1 个确认非 bug）

**背景**：上一条「代码逻辑找茬」留了 4 个"看着可疑但没逐行坐实"的候选。本次逐个 Read 代码核实，是真 bug 才改、不误报。

**核实结论 + 处理**：

- ✅ **修 · 异步导入 worker 不复检 asset/mount 状态**（`tasks/file_tasks.py::run_dataset_import`）：同步导入在请求内由 `resolve_upload_dataset_asset` 校验"asset 状态可上传 + asset 已 active 挂载到本 Study"，但异步 worker 从 payload 直接按 id 取 asset、没复检——请求受理到 worker 执行有时间窗，期间 asset 被归档/隔离/删除、或 mount 被停用，worker 仍照写。已在 materialize 前补 `ensure_dataset_asset_uploadable` + `ensure_dataset_asset_active_mounted`，与同步路径对齐（不合规则任务 failed、带原因）。
- ✅ **修 · canonical FIF legacy 镜像复制失败留孤儿目录**（`routers/datasets.py::generate_canonical_fif`）：转完 FIF 先 `shutil.move` 到 canonical 目录并置 `committed=True`，再逐个复制镜像到 legacy 兼容目录。若镜像复制中途失败（磁盘满/权限），原 `except` 因 `committed=True` 不清理 → canonical 目录留盘、但上层 `db.rollback()` 致 DB 查无 → 孤儿目录（且会以 409 挡住后续同 seq 重试）。已把 legacy 镜像包进 try，失败时回滚 canonical + legacy 两个目录再 raise，做成 all-or-nothing。
- ✅ **修 · 紧急下架后遗留"永远 pending"的撤回申请**（`services/dataset_lifecycle.py::emergency_takedown_version`）：对 `withdraw_requested` 版本紧急下架时，新建一条 `decision='emergency'` 申请、但原来那条等审批的 `decision=NULL` 申请不动 → 它永远留在管理员 `/pending` 列表、点 review 又因版本状态已变而 409。已在下架时把同版本的 pending 申请一并标 `decision='emergency'` + reviewed 字段关闭（`emergency` 是 CHECK 允许值）。
- ❌ **非 bug · `get_next_upload_seq` 探测目录硬编码 `working` label**：探测用 `build_canonical_fif_base_path_for_asset`（默认 `version_label="working"`）、落盘用 `build_canonical_fif_base_path(target_version,...)`（取真实 label）——两者确实取不同字段。但核实导入链路：`_build_import_context` 里 `target_version = get_or_create_working_dataset_version(...)`（`datasets.py:3170`），即导入**永远**针对 working 版本，`target_version.version_label` 恒为 `"working"`，探测与落盘一致、当前根本触发不到（且有 409 兜底）。属"发布-rename 真启用后才可能冒头"的潜伏点，且现行 import 路径写死 get-or-create-working、不会走到，故**不改工作代码**，记此备查。

**验证**：3 个改动文件 `python -m py_compile` 通过。无本地运行环境，真实跑通待部署云端。

**Why**：把上一条留的尾巴收干净——异步/同步导入校验对齐、FIF 落盘原子化、紧急下架不留脏 pending；同时坐实第 4 个是不可达的潜伏点、避免为它改动工作代码（不误报、不过度修）。

---

## 2026-06-05 · 代码逻辑找茬：修 8 处后端/前端真实 bug（锁恢复 / retention / superadmin / 缓存 / 乐观锁 / 单活跃 / batch 事务 / refresh 单飞）

**背景**：延续 wiki 全章核对的思路，反过来拿 wiki 描述的"应有行为"当规格去读代码找**逻辑错误**（不是文档措辞）。5 个子代理按子系统并行深挖，再逐条亲自 Read 代码复验（子代理有误报前科，下文标了两处被纠正的高估）。共坐实并修复 8 处。

> 黑话铺垫：**乐观锁**指保存时带上"我看到的版本号"，服务端发现版本对不上就拒绝(409)、防止后写覆盖先写；**单飞(single-flight)** 指同一时刻多个请求要做同一件事(刷 token)时只真正做一次、其余共享结果；**CHECK 约束**是数据库给某列限定的合法取值集合，写了集合外的值会直接报错拒绝写入。

**改了什么（按严重度）**：

- 🔴 **锁恢复分支 500 + 运行锁泄漏**（`routers/pipelines.py`）：创建 Execution 抢运行锁失败后的"清理 stale 锁再重试"分支里，重试调用没接收返回值——若并发窗口里别的运行刚好结束、这次重试**成功**，`execution_lock` 变量未赋值会在后面 `str(execution_lock.id)` 抛 `UnboundLocalError`(500)，且刚拿到的锁引用丢失、泄漏到 10 分钟 TTL（期间该 Pipeline 全部 409）。改成赋值写法，三条路径都正确收敛。
- 🔴 **`retention_status='none'` 撞数据库 CHECK**（`pipeline/save_settings.py`）：用户给节点设 `retention: none`（或旧 NodeSpec 的 `temporary`/`trash`）时返回 `"none"`，但 `derived_datasets.retention_status` 的 CHECK 六值里没有 `none` → 写库报错、整个节点产物登记失败、execution 挂掉。改成映射到合法的 `temporary`（expires 为空 → 下次 cleanup 立即可回收，语义一致）。
- 🔴 **`superadmin` 角色不存在致紧急下架永远 403**（`services/dataset_lifecycle.py`）：`_ensure_superadmin` 要求 `has_role("superadmin")`，但 seed 只建 `admin`/`pi`（还主动删了 researcher/reviewer）。从零自建的环境里没人能持有 superadmin → 紧急下架对所有人 403、整条路径在实测里是死的。改成一并放行 `admin`（seed 里 admin 拥有全部权限）。
- 🟠 **缓存命中复用已删结果**（`pipeline/cache.py`）：cleanup 把过期 cached 数据标 `deleted` 但**保留物理文件**，而缓存查找只按 job_id 取、只校验"文件在 + sha256 对"，没排除 deleted → 重跑会把用户视角已删除的结果当命中复用。加 `deleted_at IS NULL` 过滤，与 content-addressed dedup 路径口径对齐。
- 🟡 **保存遇 409 乐观锁冲突死循环**（前端 `PipelinePage.vue`）：保存版本冲突后只显示报错、不刷新本地版本号 → 再点保存仍发旧 `expected_version`、永远 409，直到手动重载才逃。改成 409 时拉最新版本号刷新（保留画布改动），提示用户确认后可重新保存。
- 🟡 **`single_active_pipeline_run` 形同虚设**（`routers/pipelines.py`）：策略默认 `true`、能存能改，但创建 execution 时从没读过它，并发控制只有 per-pipeline 运行锁（挡不住同 Study 不同 Pipeline 并发）。补上按 `run_policy` 的 Study 级前置检查（已有活跃运行则 409）。**这是行为变化，见下**。
- 🟡 **batch 改 retention 部分提交无回滚**（`routers/pipelines.py`）：批量删除时，单条 helper 命中依赖 blocker 会"先 commit 再抛 409"，导致前几条改动已落库、后面抛错且无回滚。改成删除前全量预检 blocker、命中就在动数据之前一次性 409。
- 🟡 **前端 401 refresh 无单飞**（前端 `api/client.ts`）：并发 401 各自发一次 `/auth/refresh`。当前后端 refresh 无状态、不吊销旧 token，所以**不会**把人踢下线（子代理"几乎必现被踢"的判断已纠正），只是多刷几次；但将来若加 refresh token 轮换/黑名单就会踩雷。加模块级单飞 Promise，两个 client + 并发请求共享一次刷新。

**⚠️ 行为变化（single_active）**：修复后默认"同一研究项同一时刻只允许一个运行"（之前默认 true 但没强制，所以可能一直在并发跑多个）。经用户确认**目前保持此限制**，将来要并行并发时再放开（去研究项设置把 `run_policy.single_active_pipeline_run` 改 false）。做法是前置查询 + 409（同 Pipeline 仍由运行锁兜底）；跨 Pipeline 硬互斥需引 Study 级锁，留待后续。

**验证**：后端 4 个改动文件 `python -m py_compile` 全通过；前端 `npm run typecheck`（vue-tsc）退出 0。无本地运行环境，真实跑通待部署云端。

**未做 / 留待**：① 两个设计问题待定语义后再改——BIDS 四元组唯一性是"Study 级"还是"Asset 级"（唯一索引 + 去重查询都不含 `dataset_asset_id`，致同 Study 第二个数据集无法导入同名被试同任务）、`workspace` 可见性现等同 `public`（全平台登录可见，工作区概念未实现）。② 四个未深验候选待逐个核实再决定修否：`get_next_upload_seq` 探测目录硬编码 `working` label（发布-rename 启用后才冒头）、异步导入 worker 不复检 asset/mount 状态、canonical FIF legacy 镜像复制失败留孤儿目录、紧急下架后遗留"永远 pending"的撤回申请。

**Why**：调试期最怕"层间不一致"（retention 枚举、缓存 vs dedup 的 deleted 过滤、seed vs 代码的 superadmin）和"并发边界"（锁恢复、单飞、batch 事务）这类隐性 bug——平时不显、真用起来才崩。借这次找茬一次性收掉能站住的 8 处。

---

## 2026-06-05 · 滤波节点统一为 eeg/filter/apply + 新增 visible_when / advanced 两个 NodeSpec 原语

**背景**：FIR / Butterworth / Notch 三个滤波节点高度重复——在 MNE 里带通/高通/低通本就是一个 `raw.filter()`（FIR/IIR 只差一个 method 参数），notch 是独立 `notch_filter()`。按"节点按用户意图组织、不按 MNE 机制拆"的原则（用户是医生 / 心理学研究者，想'滤波'而非先选 FIR/IIR），把三者合并成一个面向用户的统一滤波节点。

**改了什么**：

- **新增节点 `eeg/filter/apply`（Filter）**：一个节点覆盖 带通 / 高通 / 低通 / 工频陷波；`method` 选 FIR（默认零相位）/ IIR Butterworth / 谱拟合（仅陷波）；`phase`（零相位/最小相位因果）、`order`、`trans_bandwidth` 收进「高级设置」。engine 侧 `engine/preprocess/filters.py` 三个 `run_*` 合并为一个 `run_filter`（按 filter_type 分派 `filter()` / `notch_filter()`）；`dispatcher.py` 单 handler `_execute_filter`（按类型推 save_descriptor）；删旧的 `eeg_filter_fir/butterworth/notch.json` 及对应 handler（调试期不留兼容）。
- **新增两个 NodeSpec 原语（所有节点通用）**：
    - `visible_when`：`{控制字段:[允许值]}`，按其它参数取值条件显示某参数（陷波才显示 notch_freq、IIR 才显示 order）。validator 的必填校验改为"可见才必填"，前端 `PipelinePage.vue` 按可见性渲染。
    - `advanced`：`true` 的参数折进前端「高级设置」折叠区，普通用户默认只看核心参数。
- **配套**：`data_type=filtered_raw`；`validator.py` 增 `validate_filter_params`（filter_type 合法 / 谱拟合仅陷波 / 带通 l<h）；前端 `types/index.ts` 的 `NodeProperty` 加 `visible_when` / `advanced`；调试脚本 `01_erp_basic/run.py` 改用新节点并加一个 `notch` 陷波步骤（链路成 LoadData→带通(IIR)→陷波 50Hz→重参考→Epoch→ERP）；三处测试 `test_pipeline_validator_executor.py`（重写）/ `test_node_hash.py` / `test_save_settings.py` 清掉旧 `eeg/filter/butterworth`·`run_butterworth_filter` 字样、改用 `eeg/filter/apply`·`run_filter`。

**设计依据**：见 [5-25 工作流节点路线图](5-25-工作流节点路线图.md) §1（节点解剖 + 两原语）；核心原则"按用户意图组织节点、对 MNE 的封装是另一层、科学决策（高通截止 / 相位）用大白话上浮且进追溯记录"。

**验证**：后端 `py_compile`（filters / dispatcher / validator / test / run.py）通过；模拟 registry 校验现 8 个节点全部合法、`eeg/filter/apply` 在列、旧 3 节点已无；用真实节点 JSON 跑 visible_when + 必填逻辑（带通/高通/低通/陷波/IIR 五场景）零误报；前端 `npm run typecheck`（vue-tsc）通过。无运行环境，真实运行待部署后跑一条含 Filter 节点的 Pipeline（`01_erp_basic` 已改用）。

**Why**：把"按算法拆"（FIR/IIR/Notch 三节点）改成"按用户意图组织"（一个 Filter + 渐进式呈现），临床 / 科研用户少记三个节点；`visible_when` / `advanced` 是一次性投入、后续所有节点复用的 UI 基建。

---

## 2026-06-05 · 修复 storage.py `study://` URI 解析 bug（dispatch 误写成 `project://`）

**背景**：上一条 wiki 核对中发现的代码真 bug——`services/storage.py` 的 `resolve_uri` 解析端没有 `study://` 分支，而生成端（`studies.py` / `datasets.py` / `cache.py`）大量产出 `study://{study_id}/...`。本次核实并修复。

> 黑话铺垫：**storage URI / scheme** 指给文件一个抽象地址（`scheme://netloc/path`，如 `study://202605000001/fifdata/x.fif`），由 `StorageService` 翻译成真实磁盘路径，业务层不直接拼物理路径；**dispatch** 指 `resolve_uri` 按 scheme 分流到不同解析函数。

**根因**：`resolve_uri` 的 dispatch 把 legacy 分支的 scheme 写成了 `project`（line 58），但**全后端没有任何地方生成 `project://`**（仅存在于 `_old/` 旧版与 wiki/会议日志）——是一次改名只改了 dispatch、漏改回 `study`。佐证：`_resolve_legacy_study_uri` 的 docstring（line 124）、生成端、`tests/test_storage_service.py`（line 48/62/104/105 直接 `resolve_uri("study://…")` 并断言成功）用的全是 `study://`，即这些测试此前必然 FAIL。

**为什么不按 DDL 注释统一到 `elys://studies/`**（纠正上一条的设想）：核实发现两者解析到**不同物理根**——`study://` 走 legacy resolver → `STUDIES_DIR/{id}`（= `study.data_root`，`studies.py:247` 设定，也是数据集导入 `resolve_study_relative_path` 实际写文件的地方）；`elys://studies/` 走新根 → `STUDIES_STORAGE_ROOT/{id}`（另一个目录）。把生成端改成 `elys://studies/` 等于把读取目标从有数据的 data_root 挪到空目录，是一次物理迁移而非 bug 修复。命名上 `legacy_*` / `_resolve_legacy_study_uri` 也表明 `elys://studies/`（新 Study 存储）是迁移目标、`study://`（data_root）是仍在用的现状。故采用最小正解：让解析端重新认 `study://`。

**改了什么**：
- `services/storage.py`：dispatch（line 58）`project` → `study`；`resolve_path` 的 scheme 白名单（line 81）`{"project","elys"}` → `{"study","elys"}`（否则 `study://` 会被当裸文件路径）；`_resolve_legacy_study_uri` 返回的 `scheme="project"` → `"study"`。死 scheme `project://` 直接删除不留兼容（调试期、零产出）。
- `database/schema/03_datasets.sql:197`：`dataset_files.storage_uri` 的 COMMENT 原写"例如 `elys://studies/...`"与现状不符，改为如实说明——当前导入写 legacy `study://{study_id}/{relative_path}`（解析到 data_root）、新 Study 存储用 `elys://studies/{study_id}/{relative_path}`（解析到 STUDIES_STORAGE_ROOT）。

**验证**：`py_compile` 通过；本地无 pytest/依赖，用桩掉 `app.config` 的内联脚本复刻测试断言——`study://` 解析到 legacy 根✓、带 resolver 走 data_root✓、`resolve_path("study://…")` 不再误当裸路径✓、`elys://studies/` 不受影响✓、`../`/`%2e%2e/`/`file://` 穿越仍被拒✓、`project://` 现报"Unsupported"✓，全 PASS。

**Why**：任何被存成 `study://…` 的 URI 在读取链路（`resolve_uri` / `resolve_path`）都会落到"Unsupported storage URI"或被误当裸路径，影响数据集导入后的文件读取、缓存命中引用、Study 文件浏览。

**留待**：把 legacy `study://`/`data_root` 整体迁移到新 `elys://studies/`/`STUDIES_STORAGE_ROOT`（含物理搬迁与生成端、测试改写）是独立的存储统一任务，本次不做。

---

## 2026-06-05 · wiki 全章逐页核对：以代码为准成批修订 ~40 页

**背景**：用户要求"逐个页面检查 wiki 与当前代码是否自洽、有没有出入、有没有没交代清楚的地方"。文档欠债是调试期最大的隐形坑——读代码见 A、读文档见 B，接手的人和大模型都会被带偏。本次按「P0 代码为准」做一次全章核对（5 个子代理并行比对各章 vs 对应代码区，再逐条亲自 Read schema DDL / routers / views / `style.css` / `config.py` 抽验"实锤"才动笔）。

> 黑话铺垫：**P0 事实源**指判断"是否已实现"以当前代码 `elys_project/` 为准、文档与之冲突一律改文档（见 [0-40](0-40-冲突处理与权威来源.md)）；**内容寻址**指文件按内容哈希 `sha256` 决定存放路径（同内容只存一份、天然去重）；**inline 执行**指无 Celery worker 时 Pipeline 直接在 API 进程内同步跑完，而非派后台任务。

**改了什么（按系统性主题，波及 ~40 页）**：

- **目录命名**：全章把旧 `runs/`+`artifacts/` 纠正为代码实际的 `executions/`+内容寻址 `derived/{sha256[:2]}/{sha256}/`（`services/studies.py` 的 `STUDY_STORAGE_DIRECTORIES` 为准；2-40 还把常量值抄错，一并改）。波及 [2-40](2-40-文件系统与数据目录.md) [4-00](4-00-文件管理总览.md) [4-10](4-10-存储原则与后端选择.md) [4-30](4-30-Study输出与Artifact.md) [4-50](4-50-清理策略与迁移.md) [5-10](5-10-Study研究项管理.md)。
- **改名残留 ArtifactStore → DerivedDatasetStore**：`pipeline/artifacts.py` 不存在，实际是 `derived_dataset_store.py`。波及 [7-00](7-00-后端架构总览.md) [7-30](7-30-后端分层与数据契约.md) [7-40](7-40-工作流执行与后台任务.md) [5-30](5-30-Execution管理.md) [3-45](3-45-StudyOutput.md)。
- **已删 ORM 类**：`Dataset`/`DatasetUpload` → `Recording`/`RecordingVersion`（`models/study.py` 中前者已不存在）。波及 [7-30](7-30-后端分层与数据契约.md) [2-20](2-20-服务分层设计.md) [2-80](2-80-代码工程结构.md) [0-10](0-10-术语与统一约束.md)。
- **lock_type**：`run` → `execution`（`02_studies.sql` CHECK 实为 `edit/execution`）。波及 [3-05](3-05-数据库模块与功能关联.md) [5-10](5-10-Study研究项管理.md)。
- **计算服 IP 去硬写**：2-10/0-10 把废弃旧机 `8.135.57.42` 写死，改为"以 `deploy/profiles/*.env` 的 `COMPUTE_SERVER_IP` 为准、对外用 `data.elysbrain.site`"（计算服按量实例、IP 每次部署变，见 AGENTS.md）。
- **数据库字段级硬伤**（[3-xx](3-00-数据库设计总览.md)）：3-30 删两个查无列 `settings_json`/`definition_hash`（配置在独立 `study_settings` 表、真实列是 `node_count`）；3-20 删 `recording_versions.status` 非法值 `quarantined`、补回真实唯一索引 `uq_recordings_bids_entities`、file_role 标准/兼容角色归位；3-05 角色权限举例全改（seed 只有 `admin`/`pi`、权限是冒号式 `study:read`）；3-50 `recordings.qa_status` 无 CHECK 且 `converted` 属版本表、`pipeline_definitions.status` 四值均已落地；3-40/4-70 旧列名 `dataset_id`/`dataset_upload_id` → `dataset_asset_id`/`recording_id`/`recording_version_id`；3-45 索引名 `idx_derived_run` → `idx_derived_execution`。
- **不存在的工具/文件**：删对 `file_backfill.py` / 旧目录迁移路线图的引用（调试期不写迁移）、`00_current_schema.sql` → schema/ 6 文件。波及 [4-00](4-00-文件管理总览.md) [4-40](4-40-数据选择器与文件索引.md) [4-50](4-50-清理策略与迁移.md)。
- **未落地却写成已实现 → 标「规划」**（按用户选定口径）：[4-60](4-60-raw_bids视图与零复制策略.md) 整页零复制（硬链接/reflink/`storage_objects` 表）改为顶部现状声明 + 各小节标规划，现状只有纯数据库逻辑视图；3-10/3-20/4-40 的 `recording_*_summaries` 摘要表标规划。
- **inline 执行 fallback 文档化**：补 `PIPELINE_EXECUTION_MODE`（`should_execute_pipeline_inline`）——无 worker 时 API 进程内联同步执行，是本机主路径。波及 [7-00](7-00-后端架构总览.md) [7-40](7-40-工作流执行与后台任务.md) [2-60](2-60-任务队列与异步架构.md) [2-50](2-50-API设计总览.md)。
- **前端偏差最大**（[6-xx](6-00-前端页面总览.md)）：核心页 `ImportPage.vue` **不存在**，主数据页是 `/datasets`+`DatasetsPage.vue`（alias `/import`）；补登记两个真实页 `/observe/waveform`(WaveformDetailPage，接真实 timeseries) 与 `/admin/withdrawals`(撤回审核)；Dashboard 卡片纠正为四对象「数据集/研究项/工作流/运行记录」、StudyDetail 卡片纠正为「Data/Pipelines/运行记录/Artifacts」；6-05 §5.1 色值整表换成 Steel 钢蓝迁移后的实值（`--c-primary #3F5E8F` 等）；补 `TechnicalFold` 的 `hint`/`defaultOpen` props、`LoadDataPanel`/`IconLine` 组件；6-40 标注"`/observe/*` 全静态"被 waveform 打破。
- **后端文件清单**（[2-20](2-20-服务分层设计.md) [2-80](2-80-代码工程结构.md)）：补 `engine/`、`tasks/` 整层、9 个 router、~16 个 service、10 个节点（含 `eeg_filter_notch`、删幽灵 `eeg_output_save_result`）、auth 无 logout；DB 事实源统一为 schema/ 6 文件。
- **反向过期**：[1-00](1-00-需求分析总览.md)/[1-20](1-20-用户角色与使用场景.md) 把已接的执行器写回"仅 LoadData / M4-M12 规划"，对齐 index/8-00（M4/M5 部分接入、滤波→ICA→Epoch→ERP 可跑通）。
- **软坏链 / 渲染**：[5-00](5-00-研究管理总览.md) §2 的中文弯引号 `class=”elys-flow”` 破坏 `md_in_html` 致 mermaid 不渲染，已修；`M0-xx`/`M3-xx` 失效编号链接（[2-70](2-70-安全边界与合规设计.md) [1-20](1-20-用户角色与使用场景.md) [1-40](1-40-非功能需求.md) [1-50](1-50-阶段路线图.md) [8-00](8-00-功能模块总览.md)）重指到真实页或标规划。

**期间代码更新，已连带对齐**：核对中后端落地了**异步导入**（`/recordings/import-async` + `materialize_recording_import` + worker `run_dataset_import` 已真做导入，见本月同日「数据导入异步化」条）。原先写"`dataset_import` 是骨架/同步线程内完成"的 [4-00](4-00-文件管理总览.md)/[4-20](4-20-Dataset文件与导入转换.md)/[7-40](7-40-工作流执行与后台任务.md)/[2-60](2-60-任务队列与异步架构.md)/[2-50](2-50-API设计总览.md) 全部改为现状（`canonical_fif_rebuild` 仍是 stub、已注明）。另核出一处子代理误报并纠正：「cached 默认 7 天过期」其实由 `pipeline/save_settings.py`（leaf=current、intermediate=cached+7d）落地，是对的，未误删。

**验证**：`mkdocs build --clean` 通过——无失效内部链接、无漏配 nav（仅剩跨目录代码链接 WARNING，常态）。所有"实锤"均核到具体文件行（schema DDL / routers / models / views / style.css / config.py）才改，未臆测。

**代码真 bug（本轮发现、已于同日修复）**：`services/storage.py` 的 `resolve_uri` 只解析 `project://`/`elys://datasets`/`elys://studies`，但 `studies.py`/`datasets.py`/`cache.py` 生成端产出的是 `study://` 前缀——解析端无对应分支，这些 URI 读取会落到"Unsupported storage URI"。修复见同日「修复 storage.py `study://` URI 解析 bug」条（结论：dispatch 把 `study` 误写成 `project`，改回即可；不是统一到 `elys://studies/`——那俩解析到不同物理根）。

**Why**：调试期"读代码见 A、读文档见 B"会持续误导接手的人与大模型、并让总览页与子页互相打架。一次全章核对以代码为准收口，把目录命名 / 改名残留 / 虚构字段 / 规划当已实现 / inline 执行未交代等系统性欠债清掉。

**未做 / 留待**：本轮 wiki 核对只改文档与提 bug；提出的 storage.py `study://` 解析 bug 已于同日单独修复（见上方同日条）。6-05 领域语言原则被 UI 英文卡名违背（属代码欠账、已在 6-05 标注现状）留待代码侧修复。

---

## 2026-06-05 · 数据导入异步化：新增 /recordings/import-async + Celery worker + 客户端轮询

**背景**：调试用的 ERP 测试链（`elys_scripts/projects/01_erp_basic/setup.py`）上传 BrainVision 三件套时"屏幕干等几十秒、看不出卡在哪"。根因：当时唯一能用的导入端点 `POST /studies/{id}/recordings/import`（`backend/app/routers/datasets.py`）是**全同步**——一个 POST 里走完 存盘 → 校验 → `generate_canonical_fif`（BrainVision→FIF，MNE 转换，重活）→ 写库 才返回，客户端从上传到转换全程阻塞且零反馈。异步框架其实早搭了一半（`POST /recordings/import-task` + Celery + `/tasks/{id}` 状态 / 事件 / SSE 端点），但那个 worker `run_dataset_import` 是空壳（返回 `import_performed: False`，注释写"staged upload workers 启用前同步上传仍是唯一路径"），且配套的 staged 上传端点从不存在。

> 黑话铺垫：**multipart/form-data** 是浏览器传文件那种"带分隔线拼多文件"的请求体编码；**流式上传**指不把整包先攒进内存再发、而是从盘一块块读一块块发（才数得出进度）；**Celery** 是把慢活丢给独立 worker 进程的后台任务队列，HTTP 请求秒回、前端再轮询进度；**canonical FIF** 是平台把各种原始格式统一转成的 MNE `.fif` 标准件。

**改了什么**：

- **抽出共享核心 `materialize_recording_import`**（`routers/datasets.py`）：把"对已落盘文件做 校验 → 转 canonical FIF → 写库 → audit → commit"这段重活抽成一个函数，**同步端点与异步 worker 共用，单一事实源**；另抽 `_build_import_context`（鉴权 / 解析 asset / 重复 409 / 算 job_dir / 写 archiving manifest 等请求阶段前置）。同步老端点 `/recordings/import` 重构为调这两个 helper，**对外行为 / 响应不变**（纯去重，约 200 行内联逻辑收敛掉）。
- **新增端点 `POST /studies/{id}/recordings/import-async`**：请求内只把文件 archive 到 job_dir 就**秒回 task_id**，转 FIF 的重活派发给 Celery；权限、重复 409 等快判校验仍在请求内同步做掉，客户端立刻能拿到 409。
- **实装 worker `run_dataset_import`**（`tasks/file_tasks.py`）：从 `task.payload_json` 重建上下文（study / asset / version / duplicate / 各 BIDS label / job_dir / archived「扩展名→绝对路径」映射，全部从 payload 取——**不让 worker 重算 `upload_seq` / `job_id` 致路径漂移**）、读暂存文件、调 `materialize_recording_import`，全程 `record_task_event` 上报进度（10 暂存 → 30 校验 → 45 转换中 → 100 完成），recording 元信息写进 `result_json`。worker 用**惰性 import** 引 `routers.datasets`（顶层 import 会把 router 层拉进 worker 进程并可能造成循环 import）。
- **客户端 + 测试链**（`elys_scripts/`）：`common/elys_client.py` 新增 `import_recording_async`（手写流式 multipart 带上传进度条 → 拿 task_id → 轮询 `GET /studies/{id}/tasks/{task_id}` 直到 succeeded/failed），同步 `import_recording` 也顺手加了"上传进度条 + 转换计时"；`01_erp_basic/setup.py` 上传改走异步路径。上传体验由"一行干等几十秒"变成"上传进度条 + 轮询出的服务端阶段"两段可见。

**验证**：四个改动文件均 `py_compile` 通过；worker 里惰性 import 的符号（`materialize_recording_import` / `recording_to_response` / `get_active_study_dataset_mount_for_asset`，以及 `app.models` 的 `Study` / `User` / `Subject` / `DatasetVersion`）逐一核实可导入。无本地运行环境，真实跑通待部署到云端——**前提是计算服已有 Celery worker 消费 `CELERY_WORKFLOW_QUEUE`**（Pipeline 执行已走该队列、file 任务路由同队列，故 worker 大概率在跑；否则任务永远 `queued`、客户端轮询到超时）。

**Why**：兑现 [7-00](7-00-后端架构总览.md) §5「Dataset 导入异步化」一项，根治"大文件 / 重转换时上传请求长阻塞且无反馈"；顺带把同步端点 200 行内联逻辑抽成共享核心，消除"同步 / 异步两份转换逻辑各自漂移"的隐患。

**未做 / 留待**：真正的客户端直传对象存储（presigned / staged upload，免经 API 进程中转大文件）仍是后续；worker 转换失败只标任务 `failed`（`error_json` 带因）、未回写 manifest 的 failed 状态（任务状态为权威信号）；自动重试场景下 canonical 目录的幂等（当前 `max_attempts=1` 无自动重试，暂不触发）。

---

## 2026-06-05 · 工作流节点纠偏：坐实 10 节点执行器已接 + 新增 Notch 节点 + 立 5-25 节点路线图

**背景**：盘点工作流节点时发现 wiki 与代码（P0）严重背离——[5-20](5-20-Pipeline工作流管理.md) / [6-30](6-30-工作流与执行.md) / [index](index.md) / [8-00](8-00-功能模块总览.md) 均写"10 个节点仅 LoadData 接了执行器、其余规划中"，但 `backend/app/pipeline/dispatcher.py` 早已注册全部 9 个 handler，`backend/app/engine/**` 有对应真实 MNE 实现，本月「study_locks CHECK 修复」条自己也记了"执行 LoadData→Epoch→ERP → succeeded"。即工作流其实已能跑通"预处理 → ICA → Epoch → ERP"整条链。

**核实结论（以代码为准）**：`dispatcher.py` 的 `_handlers` 注册 9 类节点执行器（LoadData、FIR / Butterworth 滤波、resample、rereference、Compute / Apply ICA、Epoch、ERP），ICA Apply 还实现了 `waiting_user_input` 人机交互暂停、Epoch / ERP 支持按 condition 拆分多产物。文档所列的第 10 个节点 `eeg/output/save_result` 在代码中查无（无 `nodes/*.json`、无 handler），属幽灵条目。

**改了什么**：

- **新增节点 `eeg/filter/notch`（工频陷波）**：作为"加节点全流程"的模板与 M4 补强——`backend/app/pipeline/nodes/eeg_filter_notch.json`（规格）+ `engine/preprocess/filters.py` 的 `run_notch_filter`（MNE `notch_filter`，支持基频 × 谐波或显式频点）+ `dispatcher.py` 注册 handler + `data_type` 映射。现内置节点为 10 个、全部已接执行器。
- **新增 [5-25 工作流节点路线图](5-25-工作流节点路线图.md)**：节点级事实清单（一个节点涉及哪 10 层代码、端口类型系统、已实现 10 节点表）+ 补全规划（批次 A 质控 / 预处理、B 时频 PSD/TFR、C 微状态 / 连接 / 溯源、D 统计 / 出图），并入 nav。
- **纠偏 4 页**：[index](index.md) 现状一句话 + 模块状态表（M4 预处理 / M5 时频改"部分接入"）+ 主线流程图；[5-20](5-20-Pipeline工作流管理.md) §7 / §8 删除"butterworth 无 handler"等过时表述；[6-30](6-30-工作流与执行.md) §1.3 节点库表全部改"已接"+ 加历史更正提示框、§1.4 限制说明、§四规划表；[8-00](8-00-功能模块总览.md) 一句话现状 + M3 / M4 / M5 状态 + 主线箭头。

**验证**：后端 `py_compile`（filters.py / dispatcher.py）通过；模拟 registry 校验 10 个节点规格全部合法、`eeg/filter/notch` 在列；`mkdocs build --clean` 自查通过。无运行环境，真实运行待部署到云端跑一条含 Notch 的 Pipeline。

**Why**：调试期最怕"读代码见执行器、读文档见规划"对不上，误导接手的人与大模型低估进度。以代码为准一次性坐实节点现状，并立一页节点路线图承接后续 M4-M12 节点补全。

---

## 2026-06-05 · 前端整体风格迁移到 Steel 钢蓝主题（去花哨、降饱和；内容与交互不变）

**背景**：原全局样式 `frontend/elys-web/src/style.css` 是「v2（ported from demo）」——主色亮蓝 `#2E6BFF` + 青绿 `#00C2A8` 双跳色、主按钮蓝光晕、`.stat` 装饰圆斑、登录 / 落地 / 模块页大量蓝青径向渐变背景、头像与标题文字渐变。用户反馈"太鲜艳 / 活泼 / 廉价、缺冷静科技感"，要求**整体迁移到更克制的钢蓝（Steel）**，但**显示内容与交互方式一律不变**。

**做法（只动样式层，零 `.vue` 改动）**：在唯一的全局样式 `style.css` 上「换肤 + 去装饰」——
- **令牌降饱和**：`:root` 的 L1 品牌 / L2 语义 / L3 中性全部改钢蓝系（主色 `#2E6BFF`→`#3F5E8F`、删青绿副色、品牌渐变改同色系、语义色降彩度、中性灰转冷）。全站组件都引用 `var(--c-*)`，故 21 页一次性换肤。
- **去花哨**：主按钮光晕→极轻阴影；`.avatar` 渐变→品牌令牌；`.auth-stage`/`.hero`/`.module-hero` 蓝青渐变背景→纯色面；`.hero h1 .grad` 文字渐变→实色；脉冲动画 / 上传「处理中」蓝残留→钢蓝。
- **补字阶令牌** `--fs-display…--fs-micro`（不强改现有组件字号，避免动版式）。

**为什么这样迁**：用户强调「内容与交互不变」。新设计系统 kit 组件（分段统计卡 / 数据表 / tag2…）若套到生产页会改变信息结构与交互，属违例。故 production 迁移走「全局令牌换肤 + 去装饰」最稳：一处 CSS、全站生效、行为零风险。像素级贴近 mockup 的组件级重排留作后续按页评审。

**验证**：本地 `npm run build`（vite）通过——21 页全编译、CSS 打包无误（仅 PipelinePage 体积告警，既有）；顺带刷新 `dist/`（清掉 `ProjectsPage`/`ImportPage` 等旧迭代残留）。无运行环境，视觉待部署后核。另修了一个**本地构建早已损坏**的环境坑：`main.ts` 依赖 `@fontsource-variable/inter` 未装，`npm install` 补上。

**捋前端（顺带）**：`style.css` 3277 行，堆叠 v1 业务 / v2 demo / landing / module-* 多代迭代；抽查 `study-card`/`module-stage`/`import-scope`/`pipeline-mock` 等遗留类仍在 Index/Studies/StaticWorkbench/Datasets 使用（非死代码），`net-edge`/`task-dock` 疑似孤儿，建议后续用 PurgeCSS 做正式死 CSS 审计。仓库**无 git**，已把 `src/` 快照备份到 `_backups/elys-web-src-20260605`。

**未做（按指示 / 留待）**：observe 等"死页面"只吃全局换肤；datasets 状态列统一、组件级贴 mockup、死 CSS 审计、6-01/6-05 设计文档与令牌漂移对账，均列后续。完整说明见 `design/STYLE-MIGRATION-2026-06-05.md`。

---

## 2026-06-05 · 核实收尾：3-30 的 current_execution_id 确认为未落地设计项、对齐 current 保留档机制

**背景**：上一条"文档欠债收尾"留了个待核实尾巴——3-30 [Study与Pipeline表](3-30-Study与Pipeline表.md) 把 `current_execution_id` 当 `pipeline_definitions` 的真实列写进字段表，但 6-04 那批刷新只判"疑似过时"、没坐实。本次单独核实并对齐。

**核实结论**：`current_execution_id` 在 `elys_project/` 全仓 grep 零命中——`PipelineDefinition` ORM 模型（`backend/app/models/study.py`）与建表脚本（`database/schema/04_pipelines.sql`）均未定义此列，确认是从未落地的设计项，不是改名 / 删列遗留。

**改了什么**：[3-30](3-30-Study与Pipeline表.md) 字段表删掉 `current_execution_id` 行；正文把"当前认可结果"改写清楚——该语义当前由 `current` 保留档承载（Execution `save_policy='current'` → 产物 `derived_datasets.retention_status='current'`，再经 `produced_by_execution_id` 反查 Execution，见 [4-30](4-30-Study输出与Artifact.md)），专用指针列与规划端点 `POST .../set-current-execution`（[2-50](2-50-API设计总览.md) 标"规划"）仍是设计项。措辞对齐 [3-50](3-50-协作权限状态与迁移.md) 第 7 条（上一批已先行修正的参照口径）。

**未连带改（已核对）**：[2-50](2-50-API设计总览.md) 的 `set-current-execution` 已标"规划"、[3-10](3-10-数据库原则与对象关系.md) 的"当前认可 Execution"是逻辑对象职责描述——两处都没把它写成已落地的真实列，无需同改。另：3-30 字段表的 `definition_hash` 同样在代码 / DDL 查无（真实列是 `node_count`），与本次同源、属范围外，单独记此备查。

**Why**：把"疑似过时"坐实成"确认查无 + 文档对齐"，关掉上条留下的待核实项；免得"读代码无此列、读文档却列为真实字段"继续误导接手的人和大模型。

---

## 2026-06-05 · wiki 文档欠债收尾：13 页对齐 6-04 Recording 新口径

**背景**：A+B 重构（见下方 6-04 条）后端早已 `Dataset`→`Recording`、表 / 端点全改，但一批 wiki 页仍停在旧口径——首页用裸 `Dataset` 当核心对象，数据库章（3-00 / 3-05 / 3-10 / 3-50）还在讲"写入仍走旧 `datasets` 表、提供 `recordings_view` 兼容视图"，与已落地代码、甚至与同章 [3-20](3-20-Dataset与采集记录表.md) 自相矛盾。本次以代码为准（grep + Read `models/study.py` / `routers/datasets.py` 核出权威新口径）成批刷新。

**两批共刷新 13 页**：

- 第一批（门户 + API / 文件事实页）：[index](index.md)、[2-00](2-00-系统架构总览.md)、[3-20](3-20-Dataset与采集记录表.md)、[4-20](4-20-Dataset文件与导入转换.md)、[2-50](2-50-API设计总览.md)
- 第二批（数据库章 + 零星残留）：[3-00](3-00-数据库设计总览.md)、[3-05](3-05-数据库模块与功能关联.md)、[3-10](3-10-数据库原则与对象关系.md)、[3-50](3-50-协作权限状态与迁移.md)、[3-25](3-25-数据集生命周期与发布机制.md)、[2-10](2-10-部署架构与网络拓扑.md)、[1-40](1-40-非功能需求.md)、[1-10](1-10-项目背景与产品定位.md)

**统一口径**：`recordings` / `recording_versions` 为真实主表（由原 `datasets` / `dataset_uploads` 改名，无 `recordings_view` 之类兼容视图）；`DatasetFile` 外键为 `recording_id` + `recording_version_id`；采集记录 API 在 `/studies/{id}/recordings/*`，`/datasets/*` 仅剩 `/mounts`。首页新增「业务名 ↔ 代码类名 ↔ 中文」对照表、技术栈与"前端有页面 ≠ 后端有能力"提示。设计页 [3-25](3-25-数据集生命周期与发布机制.md) 的"命名并存矛盾"按"重构前 / 已解决"就地标注，保留设计主体。

**连带修正的过时 / 虚构内容**：3-20 删掉代码里并不存在的数据库唯一约束；2-50 改正 Study / 成员端点方法（`PATCH`→`PUT /{id}/settings`）；3-00 修一句 Project→Study 一刀切误伤的 `Study→Study` 自指坏句；3-50 改正错字段名 `qc_status`→`qa_status`、撤下"规划写成已实现"的 `current_execution_id`；1-40 删虚构表名 `dataset_derivatives`。

**验证**：`mkdocs build --clean` 通过（仅剩 3 条跨目录代码链接 WARNING，常态）；grep 全 wiki 确认 `recordings_view` / 旧端点只余"否定式说明 / 已迁移"语境，无裸残留。

**待核实（范围外，已另记）**：1-40 的 `schema_compat.ensure_schema_compatibility()` 在代码中查无，疑为过时实现引用——与本次 Recording 改名无关，留待单独核实。（3-30 的 `current_execution_id` 已于 2026-06-05 单独核实：确认代码 / DDL 查无、属从未落地的设计项，文档已对齐，见本月顶部同日条目。）

**Why**：文档欠债会让新人 / 大模型"读代码见 `Recording`、读文档见 `Dataset`"对不上，数据库章总览还与子页打架。趁刚立的"重要改动记 9-00"规矩把欠债一次收口。

---

## 2026-06-05 · 部署链路修复 + 观测性精简（deploy.sh / deploy_remote.ps1）

**背景**：aliyun-test 重新部署连续失败，逐层排查时发现部署脚本里一串"同类问题"（错误不进日志、校验引用旧表名、失效的清理代码等），一并修掉，并按用户反馈把"给人看的屏幕"做了两轮精简。

**两个会中断部署的真 bug**：

- `database/schema/05_derived.sql`：两处 `CREATE INDEX ... idx_derived_executionON ...` 索引名与 `ON` 间漏空格，psql 语法错误，DB bootstrap 卡在 05。
- `deploy.sh` 重置自检：`SELECT COUNT(*) FROM datasets` 查的是 A 重构后已不存在的旧表（现为 `dataset_assets`），`relation "datasets" does not exist` + `set -e` 致部署中断；改查 `dataset_assets`。

**健壮性 / 可观测性 / 提速**：

- **日志自包含**：`log_*`（步骤 / OK / FAIL）现也写进日志文件，重置自检查询的 stderr 也导入日志——此前"屏幕只有标记、日志只有命令输出"两边割裂，是"日志莫名断在某行、查无此错"的根因。
- **进程清理修对**：停服后"等待 + 强杀残留"原匹配 `elys-backend\|elys-worker`（systemd 单元名，不在进程命令行），实测命中不到真实 `uvicorn` / `celery`；改按 venv 路径匹配。
- **前端依赖增量**：保留 `node_modules`、`package-lock.json` 未变则跳过 `npm install`（对齐后端 venv/requirements 策略，省 ~10–40s/次；实测已生效）。
- **深度重置开关**：新增 `-DeepReset`（及细粒度 `-ResetVenv` / `-ResetNodeModules`）。默认部署已清全部数据（DB/存储/数据根）但**保留** venv/node_modules 提速；`-DeepReset` 连这两层也从零重建。`deploy.sh` 配套加 `--reset-node-modules`，banner 重置摘要新增 `Venv` / `NodeModules` 字样，深度重置时一眼可见。

**观测性精简（屏幕给人看、细节沉日志）**：

- 远程步骤每步多行框 → 一行，且**自带服务器名**（`▸ [计算服/入口服 N/8] 标题`，本地为 `▸ [本地 N/6]`）——否则 compute / entry 两段 `[N/8]` 嵌在本地 5/6、6/6 里、编号相同极易混淆；每台远程块用 `▌▌▌ … ▌▌▌` 分段头醒目隔开。远程头部 22 行 → 2 行；删除重复的"阶段预告 / 6 阶段路线图"。
- 本地侧：`[1/4]~[4/4]` 配置罗列 → 4 行摘要；`[LOCAL N/6]` 多行框 → 一行。（开头大 logo **保留**——它是"部署起点"路标，长滚动/截图里靠它一眼定位每轮从哪开始，非噪音。曾误删后按反馈复原。）
- 活人感心跳 20s → 6s 首次冒头并显示已用时；`apt-get update` 加刷新提示；`pip` / `npm` / 前端构建加超时（卡死及时中断、不无声干等）。

**验证**：`deploy.sh` 经 `bash -n`、`deploy_remote.ps1` 经 PowerShell 解析均通过；**aliyun-test 实测重新部署跑通**（run `20260605_001615`，compute + entry 均 `SUCCESS` 8/8，后端 / worker / Nginx / PG / Redis 全 UP，本地 + 数据 + 入口 API 健康检查 OK，重置自检 `0/0/0`，前端"跳过 npm install"生效）。

**Why**：调试期部署靠手动反复重跑，最痛的是"出错却定位不到"（错误不进日志）和"屏幕信息过载 / 慢步骤无反馈"。一次性把部署可观测性补齐——屏幕精简给人看、细节沉到日志给排查、慢步骤有活人感与超时。

---

## 2026-06-05 · 修复 study_locks.lock_type CHECK 漏改导致所有 Pipeline 运行 409

**问题**：点「运行」一律 409「该工作流正在被运行」，但 `study_locks` / `pipeline_executions` 表全空。根因：`study_locks.lock_type` 的 CHECK 约束仍是早期值 `('edit', 'run')`，而运行锁代码用 `lock_type='execution'`（`pipeline_runs → pipeline_executions` 改名时漏改这条 CHECK）。INSERT 撞 CHECK → `acquire_study_lock` 把任何 `IntegrityError` 都当"锁冲突"抛 → 前端显示"正在被运行"，把真实约束错误盖住，极难定位。

**修复**：`database/schema/02_studies.sql` 的 CHECK 改为 `('edit', 'execution')`。顺带把部署默认 `PIPELINE_EXECUTION_MODE` 由 `auto` 改 `celery`、worker ping 超时 `0.5s → 3.0s`（`auto` 的 0.5s 广播 ping 探不到活着的 worker，会误回退 inline 同步执行、拖长请求并占锁）。修复后运行链路打通：派发到 Celery worker → 执行 LoadData→Epoch→ERP → `succeeded`。

**加固**：`acquire_study_lock` 不再把所有 `IntegrityError` 一律当锁冲突——只有真正撞唯一锁约束（同资源已有活跃锁）才报"正在被运行"；外键 / CHECK / 非空等约束错误照实抛出（500 + 真实报错），避免下次再被"正在被运行"盖住真因。

**与 A+B 重构无关**：属更早 Pipeline/Execution 改名的遗留；A+B（Dataset→Recording / ArtifactStore→DerivedDatasetStore）部署后导入 / 采集记录 / LoadData / 运行均已实测跑通。

---

## 2026-06-04 · 修复 Project→Study 遗留文档坏链 + 3-45 DerivedDataset 补进 nav

**决策方向**：Project→Study 改名（见 [9-01](9-01-2026年5月.md) 的 2026-05-24 条）要求"统一、彻底"，但文档侧有文件名 / nav 没跟上，点击即 404。本次按"领域对象 Study 统一为'研究项'、但不误伤指代产品立项的'项目'"原则收口，并补回一个漏配 nav 的正式页。

- **6-20 页统一为「研究项」**：文件 `6-20-项目与数据管理.md` → `6-20-研究项与数据管理.md`，`mkdocs.yml` nav 标题同步改「研究项与数据管理」。该页 H1 与 4 处入链（[index](index.md) / [6-00](6-00-前端页面总览.md) / [6-01](6-01-设计原则与UI约定.md) / [6-10](6-10-展示与认证.md)）早已用"研究项"，只有文件名 + nav 滞后，故改文件名而非改 4 处链接。
- **1-60 坏链改回「项目背景」**：[1-60 产品边界](1-60-产品边界.md) 里的 `[1-10 研究项背景与产品定位](1-10-研究项背景与产品定位.md)` 改回 `[1-10 项目背景与产品定位](1-10-项目背景与产品定位.md)`。此处"项目"指产品 / 立项本身（商业方案 / 投标 / 路演语境），**不是 Study**，不参与 Project→Study 改名——避免一刀切误改。
- **3-45 补进 nav**：[3-45 结果（DerivedDataset）](3-45-StudyOutput.md) 此前不在 nav（build 报 "not included in nav"），但属数据库设计章正式页、被 15+ 页引用，故按编号置于 3-40 与 3-50 之间。

**验证**：`mkdocs build --clean` 通过；上述坏链与 nav 缺失 WARNING 全部消失，仅剩 3 条指向 `elys_project/backend/...py` 的跨目录代码链接 WARNING（常态、可忽略）。

**Why**：改名要彻底但要分清两种"项目"——领域对象 Study（研究项）统一口径，产品 / 立项语义的"项目"保留；同时让所有正式内容页都进 nav，避免页面只能靠站内链接到达。

---

## 2026-06-04 · 协作规范补充：日志记录约定 + 新对话上手引导 + 术语铺垫 + AI 上手文件

**决策方向**：固化三条协作习惯，并给新会话一个零配置的上手入口。

- **更新日志记录约定**：重要改动（命名 / API / 表结构 / 章节 / 关键决策）必须记 9-00——标题级周概览进 [9-00 周报总览](9-00-更新日志.md)，明细按自然月落 9-0x 月度页。
- **新对话上手引导**：[0-30](0-30-人机协作规则.md) 新增「上手第一步：先搞清进展」一节（先读 9-00 → 月度页 → index）并附可复制开场白；[9-00](9-00-更新日志.md) 顶部加 `新对话从这里开始` 提示框。
- **术语铺垫规范**：[0-20](0-20-文档编写规范.md) 新增第 8 节，术语 / 黑话可用但首次出现要补一句大白话，区别于面向终端用户的 [6-05](6-05-信息分级与领域语言.md)。
- **AI 上手文件**：仓库根新增 `AGENTS.md`（Codex / Trae 通用上手指南，单一事实源）与 `CLAUDE.md`（Claude Code 自动读，`@AGENTS.md` 引用）；二者均不含密码 / IP 等敏感信息。

**Why**：用户希望跨会话、跨工具（Claude Code / Codex / Trae）都能快速接上进度、不必每次口头交代，并让文档对非全栈成员更友好。

---

## 2026-06-04 · A+B 重构（Dataset→Recording / ArtifactStore→DerivedDatasetStore）

**决策方向**：一次性消除后端两层语义错位——①原 `Dataset` ORM 实际承载 BIDS 单次采集（Recording）却与用户视角的"数据集"（DatasetAsset）撞名；②`derived_datasets` 已取代旧 `pipeline_artifacts` 但类名 / 目录 / 字段仍残留 `artifact`。调试期无真实数据，全栈彻底改名、不留兼容别名、不写迁移（DB 走 DROP + 重建）。

**方向 A：Dataset → Recording**

- ORM：`Dataset`→`Recording`、`DatasetUpload`→`RecordingVersion`；字段 `current_upload_id`→`current_version_id`、`upload_seq`→`version_seq`、`dataset_files.dataset_id`→`recording_id`、`recording_versions.recording_id`（原 `dataset_uploads.dataset_id`）、`dataset_upload_id`→`recording_version_id`；反向 relationship 统一为 `Study/Subject/DatasetAsset.recordings`
- API 端点迁移：`import` / `import-task` / `{rid}/qa` / `qa/mock-run` / `qa/review` 由 `/studies/{id}/datasets/*` 迁到 `/studies/{id}/recordings/*`；删除与 recording_router 重复的 list / files 旧端点；`/datasets/mounts/*` 保留（Mount 关于 DatasetAsset，不动）
- Schema：删 `DatasetResponse` / `DatasetListResponse` / `DatasetUploadResponse`，改用 `RecordingResponse` / `RecordingListResponse`，新增 `RecordingUploadResponse`
- 前端：删 `Dataset` / `DatasetListResponse` / `DatasetUploadResponse` / `LegacyRecording` 类型与 `recordingApi.listLegacyDatasets` 回退；`datasetApi` 五个方法路径改指 `/recordings/*`；bootstrap `next_upload.upload_endpoint` 与 DatasetsPage 上传入口改 `/recordings/import`

**方向 B：ArtifactStore → DerivedDatasetStore（前序已落地，本次补完测试）**

- 物理目录 `artifacts/`→`derived/`；`study_settings.artifact_retention_policy`→`derived_dataset_retention_policy`；旧 `pipeline-artifacts` 路由由 `/derived-datasets/*`（`update_derived_dataset` + `apply_derived_dataset_retention_action`）取代
- 测试同步：`PipelineArtifact`→`DerivedDataset`、`upstream_artifact_id`→`upstream_dataset_id`、`input_kind`/`dependency_kind` 取值更新、删除已失效的 `PipelineArtifactActionRequest` 与 pin/unpin/hide 旧路由测试

**修复（接线后暴露的旧 bug）**：`recording_to_response` 误读了 `recordings` 表不存在的列 `bids_subject_id` / `current_version_seq`（它们分别在 `subject` / `current_version` 关系上）。`datasetApi.list` 切到 `/recordings` 后该路径首次被真正执行，触发 500（浏览器显示为 "Network Error"）。改为从关系读取，并给 `list_recordings_for_study` 加 `joinedload(subject, current_version)`。

**验证**：后端全部改动 `py_compile` 通过、`\bDataset\b`/`\bDatasetUpload\b`/`ArtifactStore` 零代码残留（仅保留指代 DatasetAsset/DatasetFile 的用户可见文案）；前端 `vue-tsc --noEmit` 通过。建议部署后在云端跑一遍 pytest 复核测试桩字段。

**待刷新文档**：~~3-20 / 4-20 / 2-50 等仍以 `Dataset` 指代单次采集、以 `/datasets/import` 为上传入口~~ → **已于 2026-06-05 全部刷新**（连同数据库章 3-00 / 3-05 / 3-10 / 3-50 / 3-25、首页 index、2-00 及零星残留页，共 13 页），见本月「wiki 文档欠债收尾」条。

**Why**：`Dataset` 一词在代码里同时指"单次采集"和"用户数据集"，是长期 bug 温床；`artifact` 与 `derived_dataset` 并存增加认知负担。趁调试期无真实数据一次性收敛语义，避免后续雪球越滚越大。

---

## 2026-06-04 · 后端 API 设计与三端调用模型立项 + L2 端点收敛

**决策方向**：API 同时面向 Web UI / SDK Scripts / 未来 LLM Agent 三端，引入 L1/L2/L3 分层思路。当前 L2 资源 API 是健康的，主要问题是命名边界模糊和缺少任务级抽象层（L3）。同时对当前 L2 做一轮"孤儿端点 / 重复别名"清理。

**API 端点收敛**：

- 删 `POST /pipelines/{id}/run` 单数别名（与复数 `/executions` 完全重复）
- 删 `POST /auth/logout` 占位端点（JWT 无状态，登出即前端清 token；前端 `authApi.logout` + `WorkbenchShell.handleLogout` 同步改为只清本地）
- 删 `POST /dataset-assets` 裸建端点（强制走 `bootstrap` 保证 dataset 必有配对 study）
- `POST /tasks/{id}/retry` 对 `pipeline_execution` 类型从"409 redirect"改为"内部 dispatch 到 Execution retry"，返回新 task；前端无感

**Dead Code 清理**：

- 后端：`DatasetAssetAccessError` / `is_pre_release` / `require_study_delete` 三个全项目零调用的孤儿
- 前端：`datasetFileApi` 整条死链 + 关联 3 个 type interface（`DatasetFileMetadata` 等）
- 全栈：`legacy_dataset_id` / `legacy_dataset_upload_id` 字段链（DB view + ORM + Pydantic + router + TS types）

**文档层**：新增 [7-20 后端 API 设计与三端调用模型](7-20-后端API设计与三端调用模型.md)（资源拓扑图 + API 领域地图两张 mermaid + L1/L2/L3 分层 + 12 领域盘点 + 7 个未决议项）；更新 [7-00 后端架构总览](7-00-后端架构总览.md) 与 [2-50 API 设计总览](2-50-API设计总览.md)。

**Why**：用户提出"未来要同时支持网页交互 / 代码交互 / 大模型语言交互"三端，需要从架构层做规划。同时清掉一批"看起来能用但实际不能用"或"完全冗余"的端点，让 L2 表面更干净。

## 相关页面

- [9-00 周报总览](9-00-更新日志.md)
- [9-01 2026 年 5 月](9-01-2026年5月.md)
