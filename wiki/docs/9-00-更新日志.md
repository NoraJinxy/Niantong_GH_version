# 9-00 更新日志（周报总览）

> 按周给出最近系统级 / 文档级变更的**标题级**概览。详细记录按自然月落到月度页（[9-01](9-01-2026年5月.md) 起）。规范 / 总览页只写当前状态，带日期的变更只进本栏。

<div class="elys-meta" markdown>

频度
: 每周一行概览；明细按月写进 9-0x 月度页

记什么
: 决策、页面新增 / 重写、状态语义调整、API 端点收敛的"标题"

不记什么
: 具体 diff、字段级清单（看月度页或 git）

更新
: 2026-06-18（**feat(pipeline): 工作流节点自动蛇形布局 + 适应屏幕**：脚本程序化建的 pipeline 把节点全钉在同一行（9 节点 ≈ 2900px 横线），画布一屏只看得到 3~4 个，且原「居中」只对单节点、无 fit-all → 永远看不全、也难看。新增纯函数 `computeSnakeLayout`（按 Kahn 拓扑最长路径层深排序 + 蛇形折行：奇数行反向使「上行末」与「下行首」上下相邻、连线最短，列数按画布宽高比自适应）+ `fitGraphToView`（按包围盒缩放到全部可见，镜像 `centerOnNode` 变换）+ 加载时对退化布局（单行 / 单列）自动整理一次（不标脏）；画布按钮「居中」→「整理布局 / 适应」、右键菜单同步。前端对任意来源工作流都生效、旧脚本无需改；`vue-tsc` 0 错 + 5 组布局用例全过；详见 [9-02](9-02-2026年6月.md)）

: 2026-06-18（**perf(pipeline): 运行「卡一下」优化 —— 轮询渐进退避 + worker 探测缓存**：全员命中缓存仍卡几秒，主因是「点运行→看到完成」链路的周边开销、非节点执行——①前端轮询第一拍干等 1.5s（`useRunExecution.ts`）改为「立即首拍 + 前几拍快(300/700ms)再回落 1.5s」渐进退避，缓存命中运行 0.3–0.6s 即可见完成、长任务仍走常规间隔不压后端；②auto 模式每次 ping Celery worker（≤0.5s）加短 TTL 可用性缓存（新增 `CELERY_WORKER_PING_CACHE_TTL_SECONDS=5`），TTL 内连续运行跳过 ping、worker 挂掉最多 TTL 秒自愈（比写死 celery 模式安全）；详见 [9-02](9-02-2026年6月.md)）

: 2026-06-18（**feat(数据集导入): 批量上传自动区分被试编号（subject）避免互撞**：一次选多个 BDF 一起导入时，旧逻辑认不出 BIDS `sub-` 标签就把 subject 全兜底填 `01` → 四元组彼此相同、自撞触发判重弹窗。`BidsUploadPanel.vue` 把单文件推断升级为「批级」消歧：先按一批文件名的公共前后缀差异抠出区分编号（`P01..P06`→`01..06`），抠不出再顺序兜底 `01、02…`，并 best-effort 拉当前数据集已有 recordings 避开已占四元组；唯一性口径同后端 `DATASET_EXISTS`，自动改动有页脚提示、用户可手改。纯前端 `vue-tsc` 0 错 + 8 组纯逻辑用例全过，真机待部署；详见 [9-02](9-02-2026年6月.md)）

: 2026-06-18（**fix(pipeline): 上游命中缓存致紧邻下游节点必然重跑一次**：`input_hash` 同时哈希上游产物的 `data_infos` 与 `artifacts`，而「新鲜计算」与「缓存恢复」两条路径在溯源 / 表示层字段上不一致（data_info 的 `processing.cached`、artifact 的 `artifact_type`/`source_dataset_id` —— 后者初版漏堵）→ 下游 `node_hash` 漂移、cache miss（实测 LoadData→Epoch→TFR：Epoch 命中后 TFR 仍重跑 22s）。把两个签名函数收敛成**只认内容身份**（content hash + 存储定位 + id + data_type），从根上消除整类表示层漂移；补 4 条回归测试；详见 [9-02](9-02-2026年6月.md)）

: 2026-06-18（**fix(#39): 三个观察页 PNG 导出重绘逻辑修复**：PSD / Waveform（ERP）导出时原抓 facet 小格 canvas（portrait/方形）→ 现 `TimeCourseCanvas` 暴露 `getExportCanvas()`，在隐藏 div 以 2400px 宽横版重建 uPlot 再导出，tick 密度、量程、图例均与论文图一致；TFR（HeatmapCanvas）上轮已改为 `getExportCanvas(3×)`；详见 [9-02](9-02-2026年6月.md)）

: 2026-06-18（**feat(observe): 频域/时域图例可交互 + 焦点联动（issue #40）**：游标读数列表升级为交互式图例，并与右栏「◎ 焦点」开关联动——**焦点关=纯读数**（鼠标移动只刷新读数、图线零强调）、**焦点开=互动图例**（行/图线 hover 临时高亮、click 锁定，● 标记已锁行、其余行降 32% 透明）；点任意读数行/明细行在焦点关时一键开焦点+锁定（选项①）。`TimeCourseCanvas` 加 `line-hover` emit（overlay 实时检测最近曲线）、线宽归一单档（2.6/0.7，移除首版 `highlightLocked` 强弱双档——根因是误把"是否有锁定"当"当前线是否锁定"致悬停/锁定线宽不一致）；两页 `effectiveFocus = focusEnabled ? (hovered||locked||自动缺省) : ''` 焦点门控，自动缺省对齐为 PSD α 最强 / 时域峰值线；详见 [9-02](9-02-2026年6月.md)）

: 2026-06-18（**fix(observe): 三观察页「复制」按钮 HTTP 环境静默失效**：`copyStats()` 用 `navigator.clipboard?.writeText()` 在非 HTTPS 云端服务器上短路为 undefined，按钮无反应；改为先判断 clipboard 可用性、不可用时降级 `execCommand` textarea 方式，两条路径均触发「✓ 已复制」反馈；三页同源改；详见 [9-02](9-02-2026年6月.md)）

: 2026-06-18（**fix(PSD): IAF 标记改为按 condition 各自计算**：PSD 观察页多条件分面时，IAF 竖线原来所有格共用同一 readout stat（某通道全局 α 最强），改为每格取该 condition 下同名通道的 α 峰——`useFacetGrid.FacetCell` 补 `segs` 字段，`PsdPage` 的 `psdMarkers` 改为 `cellPsdMarkers(cell.segs)` 函数，多 seg 叠加格降级到全局 readout stat；详见 [9-02](9-02-2026年6月.md)）

: 2026-06-17（**重部署全量重置**：FastAPI lifespan 钩子，每次重启 `TRUNCATE` 26 张业务表（CASCADE）+ 清空磁盘 `ELYS_STORAGE_ROOT`，账号表保留；调试期每次重部署彻底清零，测试脚本从零重建；**Pipeline 缓存 Fix 4：`_artifacts_for_job` 补 execution_outputs 回退查询**，dedup 场景的缓存命中从 O(n) 候选迭代降为 O(1) 直接 JOIN，同时验证 16 个节点 spec 标签全部正确、Bad Channels 两模式均产出 FIF；**Group 分析节点 P1+P2：Group Merge + Grand Average PSD**：新增后端节点 `eeg/group/merge`（N-to-1 堆叠 dispatcher 新路径）+ `eeg/group/average`（mean ± SEM），io.py 补 `load/save_group_psd_npz`、`save_psd_grandavg_npz`、`summarize_*`；run.py 接入两节点；节点 JSON 各一；详见 [9-02](9-02-2026年6月.md)）

: 2026-06-16（新增「试次剔除」节点 Reject Trials；同日：PSD 接受连续数据 raw→PSD、坏道顺序软护栏、时域看图页绘图手势、坏道节点 RANSAC、数据集上传页布局重构、数据管理两层 UX、调整归类 relabel、时频观察页推翻重做 HeatmapCanvas；修 PSD Condition 选择器始终空白；pipeline 缓存策略重构：拆分「查缓存跳过」与「长期留存」两个门控，修 3 个节点 compute_cost 枚举错误，无变化重跑现在会跳过计算；Fix 3：中间节点产物留 7 天防 GC 立即清除 + _artifacts_are_valid 改 file_size 快速校验避免全文件 SHA256 阻塞；修画布左键拖动偶发弹出右键菜单）

</div>

!!! tip "新对话从这里开始"
    接手 ELYS 任务前，先扫本页（按周看最近改了什么）→ 再点进最近的月度页（每条改动的来龙去脉）→ 然后看 [首页 index](index.md) 的主线流程图和模块状态。完整上手顺序见 [0-30 人机协作规则](0-30-人机协作规则.md) 的「上手第一步」。

## 月度详细记录

- [9-01 · 2026 年 5 月](9-01-2026年5月.md)
- [9-02 · 2026 年 6 月](9-02-2026年6月.md)

## 周概览

### 2026-06-17 → 详见 [9-02](9-02-2026年6月.md)

- **三观察页统一分面引擎 + 轴范围手势**：把时域(ERP)看图页最后一份 inline 手写的 facet 分面逻辑（`cells`/`facetStyle`/`gridCols`/`cellHide*` 百余行）收口到 `useFacetGrid` 组合式引擎，与 PSD/TFR 同源（本页只留 `buildCell` 注入数据访问、行为零改）；并统一三视图「轴范围手势」语言——**平滚=缩自变量轴、Ctrl+滚轮=调度量轴、拖拽=选区间、双击锁定 / 右键解锁**：PSD 补 Ctrl+滚轮调 dB 窗、TFR 补 Ctrl+滚轮调色阶 `zmax`（`HeatmapCanvas` 加 `amp` 事件、与 `TimeCourseCanvas` 同契约），三页「操作提示」同步补行。顺带 TFR 色彩映射扩到 12 套 + 选择器改渐变色卡下拉（去「发散/顺序」抽象术语、对标时域/频域「配色」）、删 TFR 通道列表「能量最强」tag、时域 Y 轴改非对称上下限手输（ERP 成分有正有负，对齐 PSD dB 范围输入）。纯前端 `vue-tsc` 0 错，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **数据集维护中心三栏滚动统一**：数据详情「数据文件」tab 的中间记录表此前 `overflow:hidden`、无高度上限，记录一多就撑高整页、把工具栏/筛选/右栏选中态一起卷出屏——补 `.dmt-main` 内部滚动（`max-height` 绑视口高、记录少不出条）、表头 `sticky` 钉顶、分组小标题二级钉头；并把三栏散落的高度上限收成 `.dmt-body` 单一变量 `--dmt-chrome`（三栏同底边、调一处），左右两栏也从「垂到屏外翻页」改「缩进视口自滚」（06-16 的 sticky 安全网保留）。纯 CSS，真机待部署（详见 [9-02](9-02-2026年6月.md)）

### 2026-06-16 → 详见 [9-02](9-02-2026年6月.md)

- **PSD 地形图支持任意频率区间 + 跟随游标实时联动**：左栏「频段」下拉改为三模（频段 / 自定义 Hz 区间 / 跟随鼠标），自定义输入 lo–hi Hz 后地形图取该范围内 `power[]` 均值，跟随鼠标模式游标移到谱线上时地形图用最近频率 bin 实时更新（双击锁定）；副标题随模式切换显示当前频率标注。纯前端无后端改动。`vue-tsc` 0 错，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **时频观察页（TFR）推翻重做：对齐时域/频域三栏壳 + 新写热图画布**：把时频页从「`WorkbenchShell` 里伪造数据的 SVG 热图」整体推翻，重写成与 PSD/时域同款的 `ov-page` 全屏三栏壳，并新写原生 Canvas 热图组件 `HeatmapCanvas`（`ImageData` 插值面）+ 配色模块 `heatmapColor`（RdBu 发散=ERD/ERS、Viridis 顺序=绝对功率）。核心差异源于时频比时域/频域多一维——功率是 (时间,频率)→颜色的 2D 面：故**不能多通道同图叠加**（每通道一张面、恒分面）、按 (数据集×通道) **并发取单通道**（复刻 PsdPage 的 allSettled、**零后端改动**）、拖拽=2D ROI、游标=各图@某(t,f)、PSD 的「Y(dB) 量程」变「色阶 ±zmax」（跨格共享）；滚轮缩时间/双击锁定/右键解锁手势与两页一致。临床向：红=ERS 蓝=ERD、t=0 刺激线、右栏峰值 ERD/ERS + 频段功率变化条。**同日应用户反馈补频段地形图**：新增后端 `GET /tfr/topo` 端点(全通道在 时窗×频窗 的均值 + 真实 montage 坐标) + 复用 TopoStrip(选中频段×时窗 / 框选 ROI 跟随)；数据集多选改**自动发现**同研究项下其它 TFR 产物（`listStudyOutputs` 拉取追加进列表，免手动拼 URL）。全项目 `vue-tsc` 0 错、后端 `py_compile` 过，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **「调整归类」relabel：在线改 BIDS 实体（被试/会话/任务/轮次）**：补全数据管理两层里"组织可调"的另一半——记录抽屉里直接改标签、不必为标签错误重新上传。关键：BIDS 实体烧死在物理文件名 + `recordings`/`recording_versions`/`dataset_files` 三表路径里，改标签 = 真·重排。唯一性**拍板为 Study 级**（`uq_recordings_bids_entities` 不含 asset），冲突检查同口径（同研究项撞车 409）。后端新 `relabel_recording` 服务（规范化→排重→按新实体重算 canonical FIF/sidecar 路径并 `shutil.move`→同步三表路径→重指 Subject）+ `PATCH /recordings/{id}` 端点，**原始上传/sourcedata 不碰**（不可改存档）；前端抽屉「归类」加「调整」内联表单接 PATCH、撞车显示人话。`py_compile` + `vue-tsc` 0 错，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **数据管理「两层」落地：记录抽屉重组 + 上传决策换人话**：接上传页重构，和用户敲定数据感知的两层心智——①「原始数据（你上传的）」像档案、按第几次上传存档、**不可删改**；②「归类」（被试/会话/任务/轮次标签）**可随时调、决定用哪次**；系统的"标准化/派生.fif"降为一句状态、不再当概念。落地：记录抽屉（`DatasetMaintenanceTab`）把「两份数据（原始+标准化）」重组为「归类标签 chips + 原始数据上传历史（第 N 次·当前/历史，当前那次展开列**真实源文件名**让可溯源落地）+ 一行『分析数据已可分析』」，接 `recordingApi.listVersions`（`useDatasetRecordings` 加版本懒加载、接口不可用静默退化兜底）；重新上传弹窗从「保存为新版本」黑话改成「『sub·task』已经有数据了 → 作为新一次数据上传」（后端 upload_seq + 当前指针机制未动、纯文案）。在线「调整归类」写接口（依赖四元组唯一性规则）按说定留下一步。`vue-tsc` 0 错，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **数据集「上传」页布局重构（去伪按钮 + 单一主线）**：应用户反馈「按钮太多、不知道点哪个，顶部『继续上传』点了没反应」——查实该按钮非死代码，但只把页面滚到下方本就可见的上传面板（=零效果），且全页三个并列蓝色主按钮抢焦点、真正入口『选择数据』埋在第 5 层。按 [6-01](6-01-设计原则与UI约定.md) 渐进披露重排为「选数据 → 导入」一条主线：删顶部「可以上传了/继续上传」横条（信息与下方面板重复、按钮只滚屏）、拖拽区提到最顶当主入口、目标信息三卡压成一行、三个散落折叠区合并成一个「高级选项」；连带清死代码（`scrollToUploadPanel`/`uploadPanelRef`/`uploadStudyName`/已失效的「关联名称」输入）与死 CSS。`vue-tsc` 0 错，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **PSD 增 FFT 单段法（SSVEP）+ 产物带频段绝对/相对功率**：应用户 SSVEP（稳态视觉，盯单个刺激频率点如 12Hz）需求，PSD `method` 加 `fft`——单段整段周期图，频率分辨率拉满（=1/时长），Welch 分段平均反而牺牲分辨率。并让引擎在**线性域积分**算 δ/θ/α/β/γ 的绝对（µV²）+ **相对功率**（频段/全谱）进产物 preview——补上「相对功率」这个临床硬伤，且用严格积分（非显示层 `_band_stats` 的 dB 域均值）。纯后端引擎 + spec，避让并行会话正在重写的 PSD 显示层。`py_compile` + JSON 过，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **修 PSD 节点 Condition 选择器始终空白**：`useEventSelectEditor` 里 `isConditionFromEpoch` 只判 ERP / TFR，遗漏 PSD，导致 Epochs 上游含多 condition 时 PSD 面板 Condition 区永远为空、用户无从选择、后端校验必失败。补 `PSD_NODE_TYPE` 常量（`pipelineConstants.ts`）并加入 `isConditionFromEpoch` 判断，三行改动，`vue-tsc` 0 错，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **新增「试次剔除」节点 `eeg/epoch/reject`（Reject Trials）**：补三大分析的质量门——分段后、ERP / 时频 / 频域前自动剔掉含大伪迹的坏 epoch（脏试次不剔会污染平均结果）。**一个节点两种方法**（`method` 下拉，同 Filter FIR/IIR、Bad Channels LOF/RANSAC 同构）：`threshold`（峰峰值 / 平坦阈值，MNE 内置 `drop_bad`，**零新依赖**，默认）+ `autoreject`（交叉验证自动学每通道阈值，Jas 2017，懒加载、`requirements` 加 `autoreject==0.4.3`，云端重部署即 pip install）。`epochs→epochs`、保留上游 condition，剔除溯源（剔了几个 / 剔除率 / 判据）写进结果 preview 非黑箱。后端执行器 13→14、节点数 14。`py_compile` + JSON 校验过，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **PSD 节点接受连续数据（raw→PSD，开通静息态频域）+ Bad Channels 顺序软护栏**：（P2）频域分析此前被 `eeg/analysis/psd` 入口写死只吃 Epochs 卡住——无事件的静息态数据切不了 Epoch 就到不了 PSD，等于「频域分析对静息态不可用」。新增端口类型 `spectral_source`（同收 `eeg_data` + `epochs`），引擎 `run_psd` 按有无 `event_id` 分流（连续整段算一条 / Epochs 按 condition），dispatcher 加 raw 分支，节点 `condition` 改选填（仅 Epochs 需要）；litegraph 0.7.x 靠逗号类型列表 `epochs,eeg_data` 让画布原生放行 `Filter`/`LoadData`→`PSD`（否则新端口类型会被它的字符串相等连线校验直接拦死）。（P4）`validator` 加 `NODE_ORDER_HINT` 软警告：Bad Channels 用插值 / RANSAC 但上游没接 Ch Loc Assign 时，**校验期**就提示（不阻断，因部分数据自带 montage），把「缺电极坐标运行期才炸」提前。`py_compile` + JSON + `vue-tsc` 0 错，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **时域看图页补绘图手势（滚轮缩时间 / Ctrl+滚轮调幅 / 操作提示）+ 收口一批观察页交互打磨**：给 `WaveformDetailPage` 补 Niantong 对标里时域查看器仅剩的「绘图手势」缺口——滚轮绕游标缩放时间轴、Ctrl+滚轮调幅度（`TimeCourseCanvas` 内闭环）、工具条「操作提示」悬浮片列全 6 手势、状态条缩放视窗 + 一键复位；缩放态由父层广播给**所有** facet 子图保证同窗、不破坏 06-16 早些做的共享 facet 轴，纯前端视觉缩放不回后端取数（连续 raw 流式浏览明确不在本页做、归其它部件）。同次提交（`5945f85`）一并收口这两个文件此前未落库的观察页打磨（双击锁定游标 / 右栏焦点·对比卡·折叠明细表 / 期刊·色盲配色 / 叠加维度 + 共享 facet 轴 / 全屏 / Ctrl·Shift 多选 / 游标 emit 合帧等）。`vue-tsc` 0 错，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **坏道节点 `eeg/preproc/bad_channels` 补 RANSAC（pyprep/PREP）检测算法**：给 06-15 的自动坏道节点加第二种检测法——一个节点两种算法（`method` 下拉切 LOF/RANSAC，同 Filter 的 FIR/IIR、ICA 的 fastica/Picard）。RANSAC=pyprep `find_bad_by_ransac`（随机抽好电极、按位置互相预测，预测不合判坏，比 LOF 激进，需电极坐标→先接 Ch Loc）；新增依赖 `pyprep==0.7.1`（净增 pyprep+psutil、不拉 sklearn、不与锁定的 mne1.7.1 冲突、Python≥3.10 云端3.11✓，**云端重部署即 pip install**）。LOF/RANSAC 参数按 `method` 条件显隐。`py_compile`+JSON 校验过，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **地形图改薄板样条插值 + 电极中性标记（对标 EEGLAB/MNE/FieldTrip）+ 多通道配色铺满 + 游标抖动修补**：`TopoStrip` 地形图色面从 IDW 加权平均（把场压扁 + 电极牛眼伪影）改 **薄板样条**（穿过电极真值、最小化弯曲能量，对标三大软件的 v4 / cubic 画法），电极从「按值填色」改 **中性标记**（底色表达数值、点只标位置，消除「点色与底色对不上」）；同批顺带 `channelColor` 多通道配色按总数铺满（连续色 viridis/parula 取样成渐变、离散色循环复用自身颜色，修「全选 63 通道换色板不生效」），修地形图游标 ms 位数变化致卡片左右抖（固定 cap 宽 + `tabular-nums`）。纯前端 `vue-tsc` 0 错，真机待部署（详见 [9-02](9-02-2026年6月.md)）

### 2026-06-15 → 详见 [9-02](9-02-2026年6月.md)

- **新增自动坏道检测 + 插值修复节点 `eeg/preproc/bad_channels`（Bad Channels）**：补 Niantong 对标第二个真洞——自动找坏电极并换通道，科学性对标 MNE（检测 = `find_bad_channels_lof` 局部离群因子、修复 = 球面样条 `interpolate_bads`）。用户只设参数（处理方式 检测并插值 / 仅标记、阈值、近邻数），跑完把"自动判了哪些坏道 / 是否插值"写进结果 preview 作溯源（非黑箱改数据）。引擎 `run_bad_channels` 复用 raw→raw 通用执行路径（顺手给该路径加"引擎可回传溯源元数据"的向后兼容钩子）；插值依赖电极坐标，须先接 Ch Loc Assign。`scikit-learn` / `mne 1.7` 依赖已在 requirements；后端执行器至此 13 个。`py_compile` 过，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **时域看图页配色扩到 16 套 + 配色选择器改色卡点选 + 工具条/全屏微调**：曲线分类色板从 elys/NPG/Wong 三套补齐成 **16 套四组**（品牌 `elys 风格` / 期刊 NPG·AAAS·Lancet·NEJM·JAMA / 色盲安全 Wong·Tol Bright / 通用 Tableau·D3·seaborn·ColorBrewer Set1·2 + 连续色 parula·viridis），命名去专有软件印记（`MATLAB lines`→`Scientific Lines` 等、组名不暴露具体软件牌子）；附商用结论（颜色数值不可版权化、非专利，ColorBrewer/seaborn/viridis 全开放许可，期刊 5 套+Scientific Lines+parula 仅借数值、风险极低，"洁癖版只留全开放许可"开关经拍板暂不做）。配色选择器从原生下拉改念通式**内联色卡点选行**（每套直接画色卡预览、规避左栏滚动裁切）；工具条「右栏」开关归位到最右、与「左栏」左右对称，全屏时隐去顶部信息条让绘图区吃满。纯前端 `vue-tsc` 0 错，真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **研究项页面去黑话 + 信息架构重排（面向临床用户）**：照 6-01「显得简单」纲领体检「研究项」页（`StudiesPage` + 右栏 `StudyOverviewTab`）——①前台全局 `数据资产`→`数据集`；②结果状态不再暴露内部「缓存/临时」，对用户统一收敛成「保存 / 不保存 / 已删除」（`StudyOverviewTab`/`ResultsPage`/共享 `pipelineFormatters` 三处口径对齐，删结果详情「系统缓存」行、「更多」菜单改「清理未保存的结果」）；③研究项页瘦身归位（叙事顺序保留工程师序「数据→流程→运行→结果」，不重排卡片）——左栏列表行的挂载标签 + 数据/工作流/运行三计数收敛成单枚「阶段」药丸，删与决策卡重复的头部「进入工作区」、权限从研究进度网格移进「元信息」、存储配额下沉进「技术信息」折叠、「使用的数据集」隐藏数据集版本标签（`vworking` 等后台架构）；④顺手 `挂载`→`关联`、`重放`→`重跑`、研究项页「审计信息」→「运行详情」（admin/生命周期的真实审计语境不动）；⑤结果数据类型（`evoked` 等 MNE 术语）经共享 `formatDataType` 映射成大白话（ERP 波形 / 时频图 / 功率谱…，研究概览 + 结果页显示、逻辑与路由仍用原值）。`节点`、工作流 `v1` 版本号、BIDS 结果名按用户保留、顶部导航栏本轮不动。纯前端，`vue-tsc` 0 错 + `vitest` 28 过；真机待部署（详见 [9-02](9-02-2026年6月.md)）
- **新增 `execution_outputs` 关联表：修「重跑/缓存命中产物显示 0」**：节点重跑或缓存命中时运行面板把产物数显示成 0（分析其实成功、文件也在）——根因是结果按 `(study_id, sha256)` content-addressed 去重、一条物理结果只一行且 `produced_by_*` 永远指首产执行，而面板按 `produced_by_execution_id` 统计。引入 execution↔output 多对多关联表（每次执行产出/复用记一条 created/reused 边），运行面板与执行详情改从关联表统计、血缘溯源仍走 `produced_by_*`；写路径覆盖新建/去重/缓存三处，前端零改。部署须 DROP+重建库（详见 [9-02](9-02-2026年6月.md)）
- **照 Niantong 对标补强：新增通道定位节点 + 地形图坐标兜底 + Picard**：对标隔壁 Niantong-eeg-analysis 盘点 elys 该补什么（原则=抄能力不抄颗粒度，故不拆 filter/PSD），补两个真洞之一「通道定位」——地形图坐标在无内嵌 montage（电极位置）时按通道名兜底匹配 MNE 标准帽（10-05/1020），并新增预处理节点 `eeg/preproc/channel_location`（Ch Loc Assign：auto/10-20/10-05 选帽 + 自动规范通道名）；投影助手抽出 `montage_layout` 供时域 / ICA 共用。顺手给 ICA compute 加 Picard 算法选项（补 scikit-learn / python-picard 依赖；scikit-learn 是既有 fastica 本就缺的）（详见 [9-02](9-02-2026年6月.md)）
- **ICA 成分交互审阅做成真实功能**：ICA 此前能算不能审（`IcaPage` 是纯设计稿）；新增后端 `ica_inspect`（每成分 地形图 / 时域 / Welch 频谱 / 去除前后对比）+ 两个 output 级只读端点 `GET /outputs/{id}/ica-components[/{index}]`，前端 `IcaPage` 重建为真实审阅台（电极散点地形图网格 → 点选详情 → 标记剔除 → 有工作流 job 上下文时提交剔除决策，接已有 `waiting_user_input` / `decision` 握手）（详见 [9-02](9-02-2026年6月.md)）

### 2026-06-08 ~ 06-14 → 详见 [9-02](9-02-2026年6月.md)

- **观察作图与缓存架构方案落档 + 时域看图改用 uPlot 渲染（P1 增量①）**：把六类观察页"图怎么画 / 数据怎么缓存"的整体架构落成 6 篇方案（`日志/10_…`，以隔壁 Niantong-eeg-analysis 静态 HTML 旧形态为只读参照），锁定一期 uPlot 探索面 / 二期 FigureSpec+D3+headless 出图、真实栈 uPlot+原生 Canvas/SVG+Web Worker、缓存默认开；代码首刀把时域看图页 `WaveformDetailPage` 从手写 SVG 改 uPlot Canvas 渲染（新增 `TimeCourseCanvas` 宿主组件 + `channelColor` 分类色板），引入 uplot 依赖（详见 [9-02](9-02-2026年6月.md)）
- **新增功率谱密度（PSD）分析节点 + 功率谱观察页接真数据**：继 ERP/TFR 后补第三类分析对象，镜像 TFR 链路——引擎 `run_psd`（Welch 法、按 condition 跨 epoch 平均）+ 存纯 numpy `.npz`（绕 MNE 跨版本 IO）+ dispatcher / 节点 JSON / 新端点 `GET /outputs/{id}/psd`，前端 `PsdPage` 双模式接真实折线；真实分析节点至此 = ERP+TFR+PSD（详见 [9-02](9-02-2026年6月.md)）
- **新增独立 Baseline 基线校正节点 `eeg/epoch/baseline`**：补上"ERP 此前无基线校正"的缺口，对 Epochs 应用 `apply_baseline`；兑现 6-01 节点粒度判据——基线是跨分析类型复用的独立决策，单列成节点而非塞进 Epoch/ERP（详见 [9-02](9-02-2026年6月.md)）
- **4-00 文件管理总览补「对标产品」节**：记下文件 / 目录管理的两个外部对标——[OpenNeuro](https://openneuro.org/)（基于 BIDS 的数据仓库 / 版本化 / 公开共享 + DOI 标杆，对标 ELYS 数据集存储布局 + 「发布≠转公开」）、[fw-heudiconv](https://fw-heudiconv.readthedocs.io/)（启发式规则把原始数据映射进 BIDS 四元组，对标导入转换 sourcedata→BIDSdata）；均为参考非依赖（详见 [9-02](9-02-2026年6月.md)）
- **仓库根工作流 cmd 脚本前缀 step→s（s1–s4）**：接 06-13 的「step1–step4」编号改名，进一步把前缀 `step` 缩成单字母 `s`（用户偏好：让这 5 个工作流脚本在仓库根按首字母 `s` 抱团、与其它文件区分）——`step1_buy_ecs`/`step2_deploy_remote`/`step3_test1_erp`/`step3_test2_erders`/`step4_release_ecs` → `s1`/`s2`/`s3_test1_erp`/`s3_test2_erders`/`s4`。`git mv` 保留历史；脚本内部转调的 `deploy/*.ps1`、`start.cmd`、`step_banner.ps1 -Step N`（步骤号是概念非文件名）均不变。全仓 14 处活引用一次同步（deploy 三 ps1 的用户可见提示、AGENTS/CLAUDE/wiki 2-10·2-80·7-10/两份 elys_scripts README/erders_reference/日志），字节级 ASCII 替换不碰中文/换行；顺修 `01_erp_basic/README` 引用不存在的 `step3_elys_debug.cmd`→`s3_test1_erp.cmd`。9-0x 历史记录按惯例不动（详见 [9-02](9-02-2026年6月.md)）
- **全栈死代码盘查清理（前端符号 + 死 CSS + 后端孤立函数 + DB 死列 + 孤立脚本/失配文档）**：应「回家前做一次详细死代码检查」的盘查，系统清掉一批确认零引用的死代码——前端清拆分巨石组件（PipelinePage/DatasetsPage）遗留的约 53 个未用 import、多页死 computed / 死导出（Dashboard/BidsUpload/LoadData/StudyDetail/types/pipelineFormatters），删 `DatasetsPage.css` 旧「文件管理器」`df-*` 块与旧「概览」样式、`style.css` 一批命名明确的死类（dropzone/quick-action/import-step/bids-* 等，共 −550 行 CSS）；后端删两个孤立 IO 函数（`read_tfr_from_data_info`/`summarize_mne_object`）+ 整文件 `engine/testing.py` + 几处死 import/局部变量；DB 删 `DatasetFile.source_file_id` 死自引用列（文件血缘实走 `dataset_file_derivations` 表，ORM + `03_datasets.sql` 同步）；部署删孤立 `apply_local_profile.sh`（依赖的 `local.env` 已不存在、被 `start_local.cmd` 取代）、重写失配的 `elys_scripts/README`。每类删除前用类名 token / 词边界精确验零引用（含 Vue 模板段），`vue-tsc`(0 错)/`vite build`/`vitest` 58/`compileall`/`mkdocs` 全过；存疑 / 需决策项（types/api.ts 接通、`lifecycle_state` 死状态、几个前端没接的端点、疑似漏写的 `key`/`notified_count` 等）列表留待逐项拍板（提交 2d69c11 符号级 + daca5c8 CSS/DB/脚本，详见 [9-02](9-02-2026年6月.md)）
- **Dashboard 改左右两栏 + 总览精简并入「进行中」+ 最近动态移入右栏**：照 6-01「显得简单」纲领重排工作台首页——删掉顶部横向四卡数字带（数据集 / 研究 / 分析流程 / 分析任务），改左右两栏：左栏只留「我的研究」研究项列表（私有数据集 ≈ 研究项，列研究项即覆盖数据集），右栏一个窄列叠三块——「总览」精简到只剩**数据集 / 研究**两个计数并进「进行中」面板顶部（分析流程 / 分析任务两卡与左栏重叠，删）、「进行中」活跃运行队列、「最近动态」时间线（原底部通栏移入右栏）；右上角「开始新分析」按钮也改成内容区顶部一条满宽 CTA 横条。纯前端模板 + 样式（复用现有 computed / 跳转 / API，后端零改），死掉的 `.page-stat` / `.overview-stat` 样式与 `pipelineSummary` / `executionSummary` 等孤儿 computed 一并清。`vue-tsc`（0 错）过；本机无运行环境（Dashboard 需后端才渲染、本地起不了真机预览），真机待云端部署验收；同步 6-20 §1.1/§1.2/§1.3（详见 [9-02](9-02-2026年6月.md)）
- **数据集详情页 tab 重构：删「概览」、权限独立成「发布与共享」、默认看数据、「导入」→「上传」**：照 6-01 设计纲领（受众医生 / 心理学家、显得简单）重排数据集详情页——默认 tab 从超载的「概览」改为「数据文件」（新数据集空态补「去上传」CTA）、「导入」tab 改名「上传」；把「概览」里独有的权限 / 生命周期（可见范围开放 · 版本发布 / 撤回 / 丢弃 · 授权成员 · 删除数据集）抽成独立「发布与共享」tab（`DatasetOverviewTab`→`DatasetShareTab`，丢掉与左栏 / 上传 tab 重复的数据概要 / 导入目标块，419→约 270 行）；顺手删 3 个够不到的孤儿 tab 组件（Records / Files / Technical，被维护中心取代的旧版）+ 遗留未用 import，类型联合收窄成 `data|import|share`。`vue-tsc`(0 错) 过、CRLF 未翻，真机待云端（详见 [9-02](9-02-2026年6月.md)）
- **ERD/ERS 用例2 调试修补：TFR 基线 / 量纲、Epoch 事件显示、候选计数**：跑通运动想象 ERD/ERS 测试用例时修掉一串问题——TFR `percent` 基线量纲 ×100（MNE 返回分数非百分数、此前数值差 100 倍）、基线窗越界 / 区间反了改成可操作中文报错且 `baseline_tmin` 缺省取 epoch 起点；Epoch 节点 condition chip 认出脚本 / API 写的 `{name,pattern,mode}` 规则字典并显示 `name`（不再糊成 `[object Object]`）；ERP/TFR 下游候选 condition 回查 LoadData 分组回填真实试次数（不再硬编码 0）、顺手抽 `loadDataGroupCounts()` 共享件；`02_erd_ers` 用例配置 / 文档校准（cue 指令切分、Epoch 窗 `-3.5~4`）+ 本机真值对拍脚本 `erders_reference.py` 改名入库。`py_compile` + `vue-tsc`(0 错) 均过，真机数值待云端验证（详见 [9-02](9-02-2026年6月.md)）
- **6-01 增设「设计纲领：受众与渐进披露三层」+ 节点粒度判据**：把反复讨论的产品设计哲学沉淀进设计原则页，作为所有 UI 细则的上位框架——受众=医生/心理/神经科学家（非工程师）、纲领=显得简单·fancy 而非复杂；三层界面（薄/中/厚，同一后端）+ Overleaf 原则（源码可下钻、协作=区别于 MNE 的另一半护城河）+ **节点粒度判据**（按科研决策边界拆而非 MNE 函数边界，TFR=节点〔average 作参数〕+独立 Baseline=2 个，节点图≈结构化 Methods 段）；远期「对话生成 pipeline + 自动写论文」标规划·北极星，当下只兑现「节点带 provenance 元数据」的零成本约束。仿页面已有无编号 `## 一句话` 惯例插入，既有编号小节不动（详见 [9-02](9-02-2026年6月.md)）
- **新增时频分析（TFR）节点 + 时频观察页接真数据**：照 ERP/Evoked 的「计算→存储→预览→观察页」链路补齐第五类分析对象。新增 `eeg/analysis/tfr` 节点（Epochs → Morlet 小波 → AverageTFR，每 condition 一张时频图，存 MNE 的 `-tfr.h5`）、引擎 `engine/analysis/tfr.py`、IO 三件套（`save_tfr_h5`/`read_tfr_from_data_info`/`summarize_tfr`）、dispatcher `_execute_tfr_output` 与文件名助手、新端点 `GET /studies/{id}/outputs/{oid}/tfr`（按通道返回 freq×time 功率热图 + δ/θ/α/β/γ 频带统计，按基线模式换算 dB/%/z 单位）。前端把原本纯静态设计稿的 `TfrPage.vue` 改成双模式——路由带 `study_output_id` 时拉真实热图（通道切换 / 色标 / 频带功率 / 动态坐标轴），无参时保留原 mockup；并补 `getStudyOutputTfr` API、`StudyOutputTfr` 类型、artifact 预览面板的 `tfr` 指标分支、`observe_route` 映射 tfr→`/observe/tfr`。`data_type='tfr'`、file_role、validator 端口兼容均早已预埋。本地 py_compile + 节点 JSON 校验 + 全项目 vue-tsc(0 错) 均过，真机时频计算待云端验证（详见 [9-02](9-02-2026年6月.md)）
- **仓库根工作流 cmd 脚本按步骤编号改名（step1–step4）+ 文档引用同步**：`buy_ecs.cmd`→`step1_buy_ecs.cmd`、`deploy_remote.cmd`→`step2_deploy_remote.cmd`、`elys_debug.cmd`→`step3_elys_debug.cmd`、`release_ecs.cmd`→`step4_release_ecs.cmd`，按「买机→部署→调试→释放」实际操作顺序排号、文件名自释义；只动仓库根包装层，转调的 `deploy/*.ps1` 与 `elys_scripts/start.cmd` 不变；wiki（2-10/2-80/7-10）、两份 elys_scripts README、AGENTS.md、CLAUDE.md、design 迁移文档的功能性引用一次改齐，9-0x 月度页历史记录按惯例不动（详见 [9-02](9-02-2026年6月.md)）
- **DatasetsPage 巨石组件拆分完成（composable 化，6 批一气呵成）**：`DatasetsPage.vue`（5071 行单文件）镜像 PipelinePage 的拆法，新建 `composables/datasets/` 把脚本逻辑按子领域抽成 6 个 composable——`datasetsFormatters`（纯格式化 / 标签配色）、`useDatasetCatalog`（目录主轴）、`useDatasetFiles`（working 文件索引）、`useDatasetRecordings`（采集记录）、`useDatasetLifecycle`（版本 + 授权 + 开放 / 转公开 + 删除）、`useDatasetImportTarget`（建集 / 挂载 + study 自动配对）。纯机械搬运、模板与样式一字未改、行为零变化（避开 P4 LoadData 重写误删 UI 的坑）；脚本段 1714→395 行（−77%）、整文件 5071→3751 行，顺带清掉 30+ 个因抽离而失效的死 import。每批 `vue-tsc` 绿、CRLF 未混；运行时待云端真机验证（详见 [9-02](9-02-2026年6月.md)）
- **修工作台「最近活动」死链（真机反馈）**：首页活动流里 Execution/Pipeline 条目链接还是旧 `/studies/{id}/workflow?...`，而术语统一已把 `/workflow` 改名 `/pipeline` 且不留重定向 → 点进去落空。后端 `dashboard_summary.py` 的 `activity_target_url` 是漏网构造点，4 处 `/workflow`→`/pipeline`（详见 [9-02](9-02-2026年6月.md)）
- **回退 P4 LoadData 前端重写（真机反馈）**：P4 把 `LoadDataPanel` 重写成四维度模型时误删了 task #61 的手选右栏 UI（文件列表 + Selected File，那才是 LoadData 真正输出）。真机部署后用户反馈，已把 `LoadDataPanel.vue`/`PipelinePage.vue` 还原到 P4 前（74e60bc），typecheck 过。认知修正：原「前端永远 explicit」是 task #61 有意设计（避免 filter 未勾时返回全集污染下游事件），不是 bug；plan §2.7 的 filter 跟随式选择若要做须叠加在手选 UI 之上、不动右栏。后端 filter 能力未动、P1 file_role 改名仍在、P5 转公开审核不受影响（详见 [9-02](9-02-2026年6月.md)）
- **存储重构 P5 前端落地：转公开审核（owner 申请公开 + admin 审核页）**：P5 后端已在 c90c5ee 落地，本轮补前端、镜像现成的撤回审核那套。owner 侧 `DatasetsPage` 把「开放为公开」改「申请公开」（`target='public'` 走 `requestPublicization`，调试期 auto-approve 即时升 public）；新增 admin `/admin/publicizations` 审核页（`AdminPublicizationsPage.vue`，列 pending + approve/驳回）；api/类型补 `datasetPublicizationApi` + `requestPublicization` + `DatasetPublicizationRequestRecord` 等。顺手删 P1 残留的死绑定 `buildRawBids`（指向已删的 raw-bids-build 端点）。前端 `typecheck` 全过，运行时待云端（详见 [9-02](9-02-2026年6月.md)）
- **存储重构 P4 落地：LoadData selector filter/explicit 双模式 + 实时预览（前端）**：修掉「前端永远把选择塌缩成固定 `dataset_ids`、数据集后续加被试不自动纳入已配置 pipeline」的真 bug。关键认知：后端 `resolve_load_data_selection` 本就支持 filter 执行时实时展开（查 `recordings` 按 BIDS 四元组匹配），塌缩纯属前端行为，故 P4 是纯前端改动、后端零改。`LoadDataPanel.vue` 重写（1299→~440 行）为方案 §2.7 四维度模型（被试/Session/任务/Run 各「全部 or 多选」）+ filter/explicit 模式切换 + 快捷场景 + 客户端实时预览（面板已持有全量 recordings，不新增后端端点）；`PipelinePage` 的 `ensureLoadDataParams` 不再硬塞 explicit、resolve 闸门放开 filter 模式。前端 `typecheck` 全过，运行时待云端（详见 [9-02](9-02-2026年6月.md)）
- **存储重构 P1 落地：数据集目录两层 + file_role 精简 + LoadData 源角色**：把上一条列为「高危 IO 蓝图」的 P1 实际改齐——数据集物理结构由 `datasets/{asset}/versions/{label}/...` 收敛为 **两层** `sourcedata/` + `BIDSdata/sub-/[ses-/]eeg/`（canonical FIF 去 task/run/upload 嵌套、文件名定 `_eeg.fif`），URI 去 `versions/` 段（`elys://datasets/{asset}/{logical}`）；`raw_bids` 降纯逻辑（删物理写入与 `raw_bids_*`/`raw_source` 冗余角色、删 raw_bids 构建任务与端点）；**file_role 精简** `canonical_fif`→`fif`、泛化 `sidecar`→`fif_eeg_json/channels/events`、`canonical_fif_provenance`→`fif_provenance`，读写两侧（`datasets.py`/`load_data.py`/`executor.py`/`file_browser.py`/`file_tasks.py`/`storage.py`/`dataset_bootstrap.py`/`dataset_lifecycle.py`）一次改齐；溯源 `SourceRawBIDS`→`SourceBIDSEntities`+`SourceOriginalUpload`。前端去 raw_bids 痕迹（导入结果/文件统计的恒 0 行）。9 个后端测试对齐两层口径。`compileall app` + 测试 `py_compile` + 前端 `typecheck` 全过；运行时（导入转换链路、pipeline 读 `fif` 角色）待云端 `elys_debug` 验证。P3 发布物理动作 / P4 LoadData selector+前端 / P5 前端审核页仍待云端（详见 [9-02](9-02-2026年6月.md)）
- **存储/版本重构方案定稿 + PII 强制脱敏 + 转公开审核落地**：围绕「上传数据怎么存、版本怎么发、文件怎么选、PII/伦理怎么管」整体设计并落档（`日志/4_存储与版本重构260610/`）。6 项决策拍板：①数据集物理目录收敛**两层** `sourcedata/` + `BIDSdata/`（canonical FIF 取代 `derivatives/elys-canonical-fif`，raw_bids 降为纯逻辑索引）；②版本管理用 DB 清单表 `dataset_version_files`（一物理文件一行 + 逻辑链接复用旧版本，发布时 `mv BIDSdata→ver{label}`）；③转 FIF **强制 `raw.anonymize()`** 脱敏（出生即干净，sourcedata 留 owner）；④用户界面区分「上传格式 / FIF格式」；⑤LoadData **filter 过滤器模型**选文件（执行粒度逐条/整批由引擎按节点自动判，非用户选项）；⑥旧结构 DROP 重建不迁移。**关键认知 publish≠expose**：发布=自助（冻结+DOI，private/shared），转公开=先审后开（shared→public 唯一需审，调试期 auto-approve）。**本轮已实施并 py_compile/compileall 全过**：P2 转 FIF 强制脱敏、P5 转公开审核全后端（新表 + ORM + service + owner/admin 端点 + 注册）、DB 两张新表。**P1 目录两层 / P3 发布物理动作 / P4 LoadData+前端属高危 IO 重构，方案文档已是精确蓝图，待云端逐阶段实施验证**（详见 [9-02](9-02-2026年6月.md)）
- **修复输出 register 撞 sha256 唯一索引（06-10 残留项收口）**：唯一索引 `(study_id, sha256)` 不分死活，而 `StudyOutputStore.register` 去重只查活跃行——输出进回收站 / 被 GC 清盘后重跑工作流产出同内容文件，INSERT 撞索引致节点执行失败。两案权衡选「死行就地复活」（保「同 sha 恒 ≤1 行」不变式、schema 不动；加 `WHERE deleted_at IS NULL` 只会把 IntegrityError 挪到恢复路径）：查重不分死活，命中死行清 `deleted_at`/`purged_at`、keep/cache_eligible/TTL 按本次 save 设置重算，血缘与用户改名保留——复活前同 sha 文件刚被写回同一 content-addressed 路径，purged 行的磁盘文件也已回来。顺带修两处预存在问题：store 测试 `FakeDb` 缺 `query`（P5 后两存量测试本就应红）、`to_dict()` 兼容键 `storage_path` 截 URI 多留 `{study_id}/` 段（dispatcher/cache 拼双 study_id 死路径，改输出 `logical_path`）。补 3 条复活单测，本机 runner 自跑 5/5、compileall 过，云端待验（详见 [9-02](9-02-2026年6月.md)）
- **术语三处定名 + save 面板简化 + 研究项三页全宽**：把多套叫法收口到「中文业务词 + 英文代码标识」双轨——①**工作流 / `pipeline`**（URL `/studies/:id/workflow`→`/pipeline`、路由名 `StudyWorkflow`→`StudyPipeline` + 4 处硬编码路径，旧链接不留重定向）、②**保存 / 不保存** 取代 keep 开关的「保留 / 不保留」（`keep` 字段不动、「永久保留」等系统语义不在内）、③**结果** 取代「派生数据」（路 A：码 `StudyOutput` 不动、「输出」留作同义词、BIDS「文件派生关系」不误伤）。配套：save 面板五值下拉 → 单「保存」checkbox（藏缓存）、结果页藏纯临时文件、研究项内三页全宽。历史页名词引用换「结果」、但改名叙述用代码标识 `derived_dataset` 指代旧名防语义反转（修 4 处）；同步 6-00 路由总表、3-45/4-30/5-30 keep 文案。typecheck 过（详见 [9-02](9-02-2026年6月.md)）
- **输出回收站两缺陷修复（恢复不补 TTL + 已清盘行可恢复）**：元素档案盘点（06 输出 §9）坐实——①恢复 / 取消保留后 `retention_expires_at` 留 NULL / 已过期值，下一轮每日 cleanup 立即再次软删、用户操作形同无效，现按用户动作口径重算 TTL（缓存档 7d / 非缓存宽限 7d，`save_settings` 新增 `retention_expiry_after_user_action`）；②PATCH 恢复不看 `purged_at`，可把 GC 已删盘行恢复成"活跃但磁盘无文件"的幽灵行，现后端单条 409 `OUTPUT_PURGED`、batch 全量预检整体 409（此前仅前端禁按钮）。补 6 条测试 + 修 1 条 P3 后失效旧断言；py_compile + 单测自跑过，行为验证待云端；残留"死行占 sha256 唯一索引挡同内容再登记"另立任务（详见 [9-02](9-02-2026年6月.md)）
- **superadmin 三处口径收敛为 admin（紧急下架）**：元素档案盘点坐实同一动作三个事实源打架——后端 06-09 已收敛 admin，但前端按钮仍 superadmin-only（admin 看不到按钮、superadmin 点了被 403，两边都是死路）、测试仍断言旧口径（现行代码下本就应红）；按 3-25 §6.4 统一：前端紧急下架按钮改 `isAdmin`（删 unused `isSuperadmin`）、3 条测试翻转为「admin 放行 / superadmin 与负责人被拒」、wiki 7-20 两处旧表述订正；seed 里 `superadmin` 角色按设计**保留**作平台治理（管管理员的人），边界注释已在位（详见 [9-02](9-02-2026年6月.md)）
- **Save 保留策略重构启动（P1 死代码 + P4 缓存评分）**：清掉两个死配置 `Execution.save_policy` / `Study.derived_dataset_retention_policy`（被创建 / 存储 / 展示却从不驱动行为）+ derived 侧死态 `quarantined`；节点缓存从「一刀切全开」改为按 ROI 评分（`cache_score = compute_cost − 2×footprint`，存储优先），当前 8 节点**仅 `ica/compute` 命中缓存**；新增 `pipeline/cache_policy.py`、`node_hash` 移除 cache 块。后端 py_compile / 前端 typecheck / grep `save_policy` 归零均过；P2 输出改名 / P3 retention 三层解耦 / P5 GC+回收站待云端验证后续推（详见 [9-02](9-02-2026年6月.md)）
- **Save 重构 P2「输出」改名 + P3 retention 三层解耦落地**：结果实体全栈定名「输出 / StudyOutput / study_outputs / /outputs」（旧码名 `derived_dataset`）；`retention_status`（5 值状态机）拆成 `keep`（用户是否保留，二元开关）+ `cache_eligible`（系统是否缓存，= P4 评分）+ `deleted_at`（回收站软删）三个正交维度——用户只面对「保留 / 不保留」，缓存全自动、删除走回收站。后端 13 文件 + 前端 6 文件（5 值下拉→二元开关 + 缓存标注）+ 测试 4 文件，compileall / vue-tsc / 全栈 grep `retention_status` 归零 / py_compile 均过，顺带修一处预存在测试不一致（`run_artifact_cleanup`→`run_study_output_cleanup`）。P1/P4/P2 已云端验证；P3 全链路运行 + 磁盘目录 `derived/`→`outputs/` 收尾 + P5（GC+回收站）待续（详见 [9-02](9-02-2026年6月.md)）
- **Save 重构 P5 收官：GC 物理清盘 + celery beat 定时 + 回收站**：P3 的回收站基础(软删 deleted_at + 恢复)之上补物理清盘——加 `purged_at`(GC 删磁盘文件后置位、DB 行留追溯)，GC 任务清 deleted_at 超 30 天的、content-addressed 去重(同 sha256 有活跃引用则保物理文件)；`run_storage_maintenance` + beat_schedule 每天全局跑 cleanup+GC，deploy.sh worker 加 `-B` 内嵌 beat；前端徽章「已清盘」+ 恢复按钮 purged 禁用。**至此 Save 重构 P1–P5 全部落地**（P1 死代码→P4 缓存→P2 改名→P3 解耦→P5 GC）。保留期 30 天 / 只删磁盘留 DB 标记 / beat 自动(用户拍板)；compileall + typecheck 过、物理删除+beat 运行需云端验（详见 [9-02](9-02-2026年6月.md)）
- **工作流画布卡"正在初始化" + 右键无菜单修复**：根因是 keep-alive 缓存的 PipelinePage 顶部 `<Teleport>`（内含 `<select :value>`）崩了 Vue 渲染器、冻结整页 DOM 更新（`liteGraphReady` 早已 true 但 DOM 刷不动；LiteGraph 直绘 canvas 故画布节点正常、造成假象）；删掉 Teleport 改就地渲染即恢复。附方法论：云端 minified 报错先临时开 `build.sourcemap` 重部署拿真实栈、别猜混淆名（详见 [9-02](9-02-2026年6月.md)）
- **PipelinePage 巨石组件启动拆分（第 1 批）**：`PipelinePage.vue`（7297 行单文件工作流编辑器）按「逻辑抽 composable、模板抽子组件」分批瘦身；首批把 39 个模块级常量 + 33 个纯展示 / 格式化函数抽到 `composables/pipeline/pipelineConstants.ts`·`pipelineFormatters.ts`，主文件删定义改 import（7297→7039 行），引用处与行为不变、typecheck 通过；承重墙 `usePipelineEditor`、画布 `useLiteGraphCanvas` 等后续批次为规划（详见 [9-02](9-02-2026年6月.md)）
- 数据集生命周期 **v2 定稿**（11 项决策闭环，废止 06-07 旧决策）：①**发布≠分享**——发布只冻结版本+铸 DOI，可见范围默认私有、不再自动 `private→shared`；②可见范围**只升不降、无降级接口**，从通用 PATCH 移出、改专门「开放」端点（仅负责人），逆转 06-07 B 方案的「徽章随发布联动」轴；③**共享=邀请制**（新建 `dataset_members` 按用户授权），`public` 才是任意注册用户（平台无匿名）；④发布加 **PII/伦理/版权关口**；⑤撤回审核中**禁新关联**。配套：发布状态枚举全栈统一 `unpublished`、`dataset_assets.status` 的 `working`/`active` 合并、清旧 `dataset_versions.status`、发布/授权/撤回收敛为仅负责人、术语「导入/关联/加载」锁定。3-25 据此改写（文档侧最高优先级），同步 3-20/3-50/3-00/6-05/5-10/3-45（详见 9-02）
- 新增 `buy_ecs` 购机脚本（`buy_ecs.cmd` → `elys_project/deploy/buy_ecs.ps1`，底层用 aliyun CLI、不引 Python，与 `deploy_remote` 同款 cmd→ps1）：按「自己制定的启动模板(LaunchTemplate) + 抢占式(Spot)」开计算服——默认 DryRun 演练（`-Yes` 才真买）、幂等下单、轮询到 Running 打印公网 IP，`-UpdateProfile` 把公网 IP 回写 `aliyun-test.env` 的 `COMPUTE_SERVER_IP` 直接闭环 `deploy_remote`；默认地域 cn-shenzhen，凭证走环境变量不进仓库（详见 9-02）
- 研究项升级为「容器」（C 档重构，用户在 A/B/C 三档改进方案里拍板选最重的）：导航从「数据集 / 研究项 / 工作流 / 结果」四平级 tab，改为「以研究项为容器」——顶层只留 工作台 / 数据集库 / 研究项，进入某研究项后 工作流·运行 / 结果 / 数据 / 概览 成为其内部 4 个 tab（嵌套路由 `/studies/:studyId/{overview,data,workflow,results}`）；studyId 由 URL 路径承载（新 `stores/study.ts` + `route.params`，删 `utils/studySelection.ts` 跨页记忆）；原右栏工作台迁为「概览」tab + 新后端 `GET /studies/{id}/summary` 聚合端点消除 12 连发 N+1；PipelinePage 进 keep-alive 防 LiteGraph 画布重建；StudiesPage 双栏瘦身为纯列表、治理动作行内化；旧 `/pipeline`·`/results` 路由保留重定向兜底（详见 9-02）
- 研究项再调「两层架构」（用户真机觉得容器 4-tab 绕，参考 DatasetsPage 左右栏 + Overleaf"点进项目即操作界面"）：`/studies` 改 master-detail（左研究项列表 + 右概览卡 +「进入工作区」，点选高亮不跳页、选中走 `?study=`）；点「进入工作区」整页落**工作流操作界面**（Overleaf 式），工作区顶部次级导航只剩 数据 / 工作流 / 结果，**概览从"全屏 tab"挪进列表右栏、"点进去"默认落点从概览改工作流**，删 `overview` 子路由；纯前端、typecheck 通过（详见 9-02）
- 版心宽度统一为 CSS 令牌 `--content-w`（取值 1600px，用户拍板）：列表 / 总览页的「内容容器最大宽度」收敛到单一事实源，`.page--narrow` 不再写死 1280；补齐漏网的研究项详情页（`:narrow`）、删掉 Results 自造的重复容器；工作流页与未实现预览页不套。当前标准进 [6-01 §1.8](6-01-设计原则与UI约定.md)（详见 9-02）

### 2026-06-01 ~ 06-07 → 详见 [9-02](9-02-2026年6月.md)

- 决策：数据集可见范围（私有/共享/公开）= 发表级严肃动作——只三档、只经发布/撤回流程流转（发布自动私有→共享、撤回不降级、公开留后续），UI 只读徽章、不提供随意下拉（详见 9-02）
- DatasetsPage 真机复验三补：数据概要补全为四件套（被试/记录数/任务/时长，新增后端 `recording_count` 聚合）、列表时间锁定「创建于 X」、可见范围交互回退（撤掉随意下拉，徽章只读 + 交还「发布版本」郑重流程联动）（详见 9-02）
- 数据集状态徽章改用「可见范围」轴（6-05 B 方案，跨 DB/后端/前端/wiki）：厘清三套重叠状态轴（资产 status=内部 L3 / 版本 state=发布生命周期在版本卡 / visibility=用户徽章），徽章/筛选/统计改读 `visibility` 且简化为三档（去 `workspace`，DB CHECK 走 DROP 重建），概览加 owner 手动改可见范围控件（已发布禁降级回私有）；Dashboard 口径暂留待统一（详见 9-02）
- DatasetsPage 真机验收三修：去掉与页头重复的"新建数据集"按钮、版本标识 `working`→"工作版本"、列表 / 概要卡图标语义修正（`target`/`play`→`users`/`clock`）；纯前端，更大的图标双系统统一 / 标签页信息架构留作专项分析（详见 9-02）
- 研究项列表与详情合并（master-detail 方案 B）：`/studies` 右面板吸收详情页的工作流 / 运行 / 结果摘要，一页看全单个研究项的"数据 + 工作流 + 运行 + 下一步"；`/studies/:id` 瘦身为「采集记录深表格」专页，消除两页职责重叠与双份维护；路由不变、未动后端 / API（详见 9-02）
- StudyDetailPage 领域语言化：拿 6-05 §2 当规格清研究项详情页欠账——四张统计卡英文标题 `Data/Pipelines/Artifacts`→「采集记录/工作流/结果」，全页 `Study/Dataset/Recording/Pipeline/Execution/Artifact` 等内部对象名 + `Overview/Activity` 等英文标题 + `active/unknown` 裸枚举一并领域语言化，"Execution #N"按 §9 反例改"第 N 次运行"；纯前端文案、未动类型签名 / API（详见 9-02）
- DatasetsPage 信息分级续修：拿 6-05 当规格清三类欠账——顶部状态卡零值不渲染（去碎片）、"Records/Recording"内部对象名全改"采集记录"、`working` 状态"工作中/草稿中"双说法统一为"草稿/已发布/已归档"；纯前端文案，未动状态模型 / API（详见 9-02）
- 代码逻辑找茬：拿 wiki 当规格反查代码，先修 8 处真实 bug——锁冲突恢复分支 500+锁泄漏、`retention='none'` 撞 CHECK、`superadmin` 缺失致紧急下架永远 403、缓存复用已删结果、前端保存 409 乐观锁死循环、`single_active_pipeline_run` 未强制（**已按确认默认强制 Study 级单活跃运行**）、batch 改 retention 部分提交无回滚、前端 401 refresh 单飞；再把 4 个未深验候选逐个坐实，又修 3 处（异步导入不复检 asset/mount、FIF legacy 镜像失败留孤儿目录、紧急下架遗留死 pending 申请）、1 个确认非 bug（`get_next_upload_seq` 探测 label，导入恒针对 working 版本、触发不到）；后端 py_compile + 前端 typecheck 通过；仅余 2 个设计问题（BIDS 唯一性 Study/Asset 级、workspace 可见性）待定语义（详见 9-02）
- 滤波节点统一为 `eeg/filter/apply`：FIR / Butterworth / Notch 三节点合一（带通 / 高通 / 低通 / 陷波 + FIR / IIR / 谱拟合），按"按用户意图组织、不按 MNE 机制拆"；新增通用 `visible_when`（条件显示）/ `advanced`（高级折叠）两个 NodeSpec 原语；删旧 3 节点、改调试脚本与测试，后端 py_compile + 前端 typecheck 通过（详见 9-02）
- 修复 `storage.py` 的 `study://` URI 解析 bug：`resolve_uri` dispatch 误把 legacy scheme 写成全后端零产出的 `project://`，致生成端产出的 `study://` 全部解析失败；改回 `study`、删死 scheme、订正 DDL COMMENT（详见 9-02）
- wiki 全章逐页核对：以代码为准（P0）成批修订 ~40 页，清掉系统性文档欠债——目录命名 `runs/artifacts`→`executions/derived`、改名残留 `ArtifactStore`/`Dataset` ORM、3-xx 虚构字段（`settings_json`/`definition_hash`/非法 `quarantined`）、计算服 IP 去硬写、前端 `ImportPage`→`DatasetsPage` 且补 waveform/撤回审核两页、4-60 零复制整页标「规划」、补 inline 执行与异步导入现状、修 5-00 弯引号破坏 mermaid + `M0-xx` 软坏链；`mkdocs build` 通过
- 数据导入异步化：新增 `POST /recordings/import-async`（请求内落盘秒回 task_id + Celery worker 后台转 FIF + 客户端轮询任务状态），根治调试测试链上传"长阻塞无反馈"；抽出同步 / 异步共用的转换核心 `materialize_recording_import`，同步老端点重构为调它（对外行为不变）；客户端 `elys_client` 加 `import_recording_async`、ERP 测试链改走异步（详见 9-02）
- 工作流节点纠偏 + 新增 Notch 节点 + 节点路线图：核实代码后更正"仅 LoadData 有执行器"的过时表述（实为 10 节点全接执行器、预处理→ICA→Epoch→ERP 可跑通），移除幽灵节点 `eeg/output/save_result`；新增工频陷波节点 `eeg/filter/notch`（JSON + engine + dispatcher 全流程）；新增 [5-25 工作流节点路线图](5-25-工作流节点路线图.md)，并刷新 index / 5-20 / 6-30 / 8-00（详见 9-02）
- 前端整体风格迁移到 **Steel 钢蓝**主题：全局 `style.css` 令牌降饱和换肤 + 去装饰渐变 / 光晕 / blob，21 个页面一次换肤，**类名 / 显示内容 / 交互一律不变**；新增字阶令牌；本地 `npm run build` 实测通过（详见 9-02）
- 部署链路修复 + 观测性精简：修两个会中断部署的 bug（`05_derived.sql` 索引漏空格、重置自检查旧表 `datasets`→`dataset_assets`）；日志自包含 / 进程清理按 venv 匹配 / 前端 npm 增量；部署屏幕精简（大 logo→4 行、每步一行、心跳提前、慢步骤加超时），aliyun-test 实测跑通
- A+B 重构：`Dataset`→`Recording`、`ArtifactStore`→`DerivedDatasetStore` 全栈改名收敛语义
- 后端 API 三端调用模型立项 + L2 端点收敛（清孤儿端点 / 重复别名）
- 协作规范补充：更新日志记录约定 / 新对话上手引导 / 术语铺垫规范 + 新增 `AGENTS.md`·`CLAUDE.md` 上手文件
- 文档收口：修复 Project→Study 遗留坏链（6-20 文件 + nav 统一「研究项」、1-60 坏链修回「项目背景」），并把漏配的 3-45 DerivedDataset 补进 nav
- 修复运行 409：`study_locks.lock_type` CHECK 漏改（`run`→`execution`）致所有 Pipeline 运行被误判"正在被运行"；改 CHECK + 部署默认 `PIPELINE_EXECUTION_MODE=celery`，运行链路实测跑通
- wiki 文档欠债收尾：13 页（首页 / 2-00 / 数据库章 3-xx / 2-50 等）对齐 6-04 Recording 新口径，消除文档与代码、3-00 与 3-20 的矛盾（两批 workflow，详见 9-02）
- 核实收尾：坐实 3-30 `current_execution_id` 为从未落地的设计项（代码 / DDL 查无），字段表删列、正文对齐到 `current` 保留档机制（`save_policy` / `retention_status='current'`），关闭上条遗留的待核实项

### 2026-05-25 ~ 05-31 → 详见 [9-01](9-01-2026年5月.md)

- UI 信息分级原则与领域语言落档（新增 6-05）
- 数据集生命周期与发布机制设计（draft/published/withdrawn，新增 3-25）

### 2026-05-18 ~ 05-24 → 详见 [9-01](9-01-2026年5月.md)

- 文档目录与 MVP 四核心对象、Dataset-first 导入、Pipeline/Execution 全链路收口
- 数据库 schema 拆 6 文件 + 停用 Alembic、Project→Study 改名、结果合并为 derived_datasets、域名备案、`cancelled`→`canceled`、1-30 降级为路线图视角

## 记录规则

- 本页只放每周**标题级**概览；明细按"自然月"写进 9-0x 月度页（跨月的周，条目按实际日期归月）
- 月度页每条用"日期 · 主题"标题 + 简短"为什么 / 影响哪些页面"，具体 diff 交给 git
- **规范 / 总览页只写当前状态；带日期的"什么时候改了什么"只进本栏**
- 密钥、密码、服务器账号和敏感配置不写入
- 涉及"以后不再使用某种做法"的决策，必须同步到对应规则页

## 相关页面

- [0-20 文档编写规范](0-20-文档编写规范.md)
- [0-40 冲突处理与权威来源](0-40-冲突处理与权威来源.md)
