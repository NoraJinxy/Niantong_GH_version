# ELYS 项目 · AI 协作指南

念析 ELYS（EEG anaLYSis）是面向科研级脑电的 Web 分析平台。本文件是任何大模型（Claude Code / Codex / Trae 等）接手本仓库前的统一上手说明。详细规范在 `wiki/docs/`，本文件只给最关键的几条。

## 上手第一步：先搞清当前进展

接手任何任务前，先按顺序扫一遍，别上来就改代码：

1. `wiki/docs/9-00-更新日志.md` —— 周报总览，按周看“最近在改什么”。
2. 最近的月度页 `wiki/docs/9-0x-*.md`（如 `9-02-2026年6月.md`）—— 每条改动的“为什么 / 影响哪些页面”。
3. `wiki/docs/index.md` —— 主线流程图与模块状态，掌握整体盘子。
4. `wiki/docs/0-00-协作规范总览.md` 与 `0-30-人机协作规则.md` —— 协作规则与按任务类型的阅读路径。

## 事实源优先级

- **P0 = 当前代码 `elys_project/`**：判断“是否已实现”以代码为准。
- **P1 = `wiki/docs/`**：判断“应该怎么做 / 为什么这么设计”。
- 两者冲突时以代码为准，并据此修正文档。

## 项目当前阶段：调试期

- 数据库里全是调试数据，**没有真实有效数据**。
- 可以大胆改 schema / 重命名 API / 调整字段语义；可以直接删 dead code、旧表、旧字段。
- **不写数据迁移**（已停用 Alembic，升级走 DROP + 重建）、**不留兼容层 / 废弃标记**、不考虑向后兼容。

## 改完代码要做的事

- **重要改动（命名、API、表结构、章节调整、关键决策）必须在更新日志记一条**：标题级周概览进 `wiki/docs/9-00-更新日志.md`，明细按自然月落到 `9-0x` 月度页。规范 / 总览页只写当前状态，带日期的“什么时候改了什么”只进 9-0x。
- 同步对应总览页：数据库→`3-00`、文件→`4-00`、研究管理→`5-00`、前端→`6-00`、后端→`7-00`、模块状态→`8-00`。
- 改完 wiki 跑 `cd wiki && mkdocs build --clean` 自查（跨目录代码链接的 WARNING 是常态，可忽略）。

## 写文档的语气

术语 / 工程黑话可以用、也鼓励用，但第一次出现时顺带一句大白话铺垫，别甩词不解释——读者也包括非全栈背景的成员和未来接手的大模型。细则见 `wiki/docs/0-20-文档编写规范.md` 第 8 节。

## 开发与部署

- 本地 Windows 写代码 → 从仓库根跑 `s2_deploy.cmd` → 在阿里云双服务器（入口 + 计算）上调试，**没有本地运行环境**。常用组合：`s23.cmd`（部署 + 测试）、`s123.cmd`（买机器 + 部署 + 测试）；单独买 / 放计算服是 `s1_buy_ecs.cmd` / `s4_release_ecs.cmd`。
- 部署前可本地静态校验：跑仓库根 `check.cmd` 一键过（后端 `py_compile` + 前端 `typecheck` + vitest）；`s2_deploy.cmd` 上传前也会自动跑这道校验门禁（加 `-SkipCheck` 跳过）。真实 pytest / 起服务只能在云端。
- 计算服务器是阿里云按量实例、**IP 每次部署都会变**，但 IP 单一事实源是 `deploy/profiles/*.env` 的 `COMPUTE_SERVER_IP`（`s1_buy_ecs.cmd` 买机器时自动写回）；`elys_scripts/common/config.py` 等调试脚本自动跟随，**无需手改**。
- 测试账号、密钥、服务器密码等**不写进本仓库**：非密配置（IP / 端口 / OSS 桶名）在 `deploy/profiles/*.env`；SSH 部署 key 在 `~/.ssh/elys_deploy_ed25519`、OSS AccessKey 在 `~/.elys/oss.env`、阿里云 ECS 凭证（买 / 放机器用）在 `~/.aliyun/config.json`（均在仓库外用户目录），root 密码不落盘。位置详见 wiki `2-10` §6。

## 多会话共享工作树：git 安全

本仓库常有多个大模型会话**同时**在跑，它们**共用同一个工作目录和同一个 git 暂存区（index，即 `git add` 把改动暂存待提交的地方）**。“未提交的改动”是唯一不受 git 历史保护的东西——任何一个会话一次手滑的 git 操作，就能把所有会话 + 用户手改的未提交内容一并冲掉。铁律：

- **勤提交。** 一小段工作就 commit 到当前分支；未提交 = 唯一有风险，已提交的永远能用 `git reflog` 捞回。
- **共享树上禁用 `git stash` 与 `git reset --hard`。** 要干净树跑 typecheck / 测试，先把当前改动 commit 到临时分支，别 stash。`git stash && <可能失败的命令> && git stash pop` 尤其致命：中间命令返回非零会被 `&&` 短路、pop 不执行，改动滞留 stash；之后 pop 一冲突再误 `git stash drop`，整包改动就孤立丢失。
- **只提交自己的文件。** 用 `git commit --only <显式路径>`，**绝不 `git add -A` / `git add .`**（会把别的会话已 stage 的活一并裹走）；提交前先 `git status` / `git log -1` 核实 HEAD。
- **真丢了能救。** `git fsck --no-reflogs --dangling` 找游离的 stash/commit → `git branch <名> <hash>` 先锚住防 gc → 再 `git stash apply <hash>` 或三方合并取回。

## 不要做

- 不要把规划能力写成“已实现”（状态语义见 `wiki/docs/0-00-协作规范总览.md`）。
- 不要在规范 / 总览页里嵌入带日期的变更记录（统一进 `9-00`）。
- 不要为“兼容旧逻辑”保留死代码或写迁移脚本（调试期，直接改）。
- 不要把任何密码 / 密钥 / 服务器账号写进仓库文件。
