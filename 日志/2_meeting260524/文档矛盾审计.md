# 文档矛盾审计

文档生成时间：2026-05-24 +08:00

## 背景

对 `wiki/docs_v2` 全套文档与代码现状（截至 2026-05-23 派生数据集 10 步重构完成后）做一次一致性审计，识别文档之间、文档与代码之间不一致的地方。

审计依据：
- `wiki/docs_v2/0-50-变更记录.md`（截至 2026-05-24 的最新决策）
- `日志/2_meeting260523/派生数据集模型设计与实施方案.md`（10 步实施记录）
- `elys_project/database/migrations/20260523_0004_derived_datasets.sql`
- `elys_project/backend/app/models/`、`routers/`、`pipeline/` 实际代码

## 一、问题清单（按优先级排序）

下面每条都标注了：**位置**、**实际情况**、**建议处理方式**，便于逐条讨论。

---

### 🔴 高优先级——会误导实际开发

#### 问题 ① `1-50` 路线图 Phase 1 引用了已废弃的表名

- **位置**：`wiki/docs_v2/1-50-阶段路线图.md` § 3 Phase 1 目标表
- **原文**：
  > 派生产物（`derivatives/preprocessing/`）+ **`dataset_derivatives` 写入**
- **实际情况**：
  - `dataset_derivatives` 已被 `0-50` 2026-05-24 审计确认为"僵尸表"，需 DROP
  - 真正落地的派生产物表是 `derived_datasets`（2026-05-23 迁移 `0004` 建立）
- **建议**：
  - 把 `dataset_derivatives` 改为 `derived_datasets`
  - 同步更新文档头部"更新"时间到 2026-05-24
  - 链接指向 `3-45-DerivedDataset.md`
- **状态**：✅ 已处理（2026-05-24，随问题 ⑪ 一并完成）

---

#### 问题 ② `1-30` 功能需求矩阵 M5 引用了已删除的表

- **位置**：`wiki/docs_v2/1-30-功能需求矩阵.md` M5 时频分析"下一步关键需求"列
- **原文**：
  > PSD / TFR 节点规格、执行器、**`analysis_results` 表启用**
- **实际情况**：
  - `analysis_results` 表已在 2026-05-23 迁移 `0004` 中被 DROP，永久退出
  - 派生数据现统一登记在 `derived_datasets`
- **建议**：
  - 把 `analysis_results` 表启用 → 写入 `derived_datasets`
  - 同步更新文档头部"更新"时间
- **状态**：✅ 已处理（2026-05-24）

---

#### 问题 ③ `2-50` API 设计总览里 Artifact 端点描述与实际代码不符

- **位置**：`wiki/docs_v2/2-50-API设计总览.md` § 2 资源命名表
- **原文**：
  > Artifact: 标准路径 `/studies/{study_id}/artifacts`，当前兼容路径 `/projects/{project_id}/pipeline-artifacts`
- **实际情况**：
  - `/pipeline-artifacts/*` 全部端点已在 5-23 第 5 步被**删除**
  - 现行路径是 `/projects/{id}/derived-datasets`
- **建议**：
  - 把 Artifact 行整体改为 DerivedDataset：标准 `/derived-datasets`、当前 `/projects/{project_id}/derived-datasets`
  - 或者直接删除该行，因为 `3-45` 已经有详细的 8 个端点清单
- **状态**：✅ 已处理（2026-05-24，范围扩大）
- **执行说明**：原审计只点了资源命名表 1 行，实际处理时发现 `2-50` § 15 / § 16 共有 13 行旧端点残留，全部一并修复；同时清理了 `1-20` / `3-00` / `3-10` / `4-50` / `7-40` 等 5 份文档里同类的散落引用。

---

#### 问题 ④ `2-60` 任务队列时序图 API 路径错误

- **位置**：`wiki/docs_v2/2-60-任务队列与异步架构.md` § 7 Pipeline Run 异步链路 Mermaid 图
- **原文**：
  ```
  FE->>API: POST /studies/{id}/pipelines/{pid}/run
  ```
- **实际情况**：
  - 按 `2-50` 权威口径，标准入口是 `POST /projects/{project_id}/pipelines/{pipeline_id}/runs`
  - 关键差异：`/projects` 而非 `/studies`、`/runs` 而非 `/run`
- **建议**：
  - 图里改为 `POST /projects/{id}/pipelines/{pid}/runs`
  - 或在图下方加注释说明 `studies` 是目标语义、`projects` 是当前兼容路径
- **状态**：✅ 已处理（2026-05-24，与 ②③⑤ 同批）

---

#### 问题 ⑤ `2-60` 标准任务类型表还用旧名 `artifact.cleanup`

- **位置**：`wiki/docs_v2/2-60-任务队列与异步架构.md` § 3 标准任务类型表
- **原文**：
  | Task Type | 说明 |
  |---|---|
  | `artifact.cleanup` | 清理 temporary/cached 文件 |
- **实际情况**：
  - 代码里已改名为 `derived_dataset_cleanup`（5-23 第 5 步）
  - `file_tasks.py` 同时兼容 `artifact_cleanup` 字面量，是过渡兼容，不是正式名
- **建议**：
  - 改为 `derived_dataset.cleanup`（保持点号风格与其它任务类型一致）
  - 同步更新 §13 当前代码到标准模型映射表里的相关条目
- **状态**：✅ 已处理（2026-05-24，范围扩大）
- **执行说明**：除 `2-60` § 3 / § 13 / footer 外，同步清理了 `4-00` / `4-50` / `5-00` / `5-30` / `7-40` 等 5 份文档里 9 处把 `artifact_cleanup` 当作"当前代码状态"的描述。`artifact.preview` 任务类型也一并改为 `derived_dataset.preview`。

---

### 🟠 中优先级——概念混乱，影响理解

#### 问题 ⑥ `3-45` 文档内部矛盾——"五档"实为六档

- **位置**：`wiki/docs_v2/3-45-DerivedDataset.md` § 3.5 生命周期
- **原文**：标题写 "`retention_status` **五档**"
- **实际情况**：表格内列出六档：`current`、`pinned`、`cached`、`temporary`、`deleted`、`quarantined`，建表 SQL 也是六档
- **建议**：标题改为"六档"
- **历史关联**：5-23 设计方案文档（`派生数据集模型设计与实施方案.md`）也写的是"五档"，可能是因为最初设计是五档，后来加了 `quarantined` 但标题没改。要注意同步检查相关文档。
- **状态**：✅ 已处理（2026-05-24）
- **执行说明**：除 `3-45` § 3.5 外，`4-30` § 3 也有同样的"五档"描述，一并改为"六档"。`派生数据集模型设计与实施方案.md` 是历史会议记录，不动。

---

#### 问题 ⑦ `canceled` / `cancelled` 拼写混用

- **位置**：`wiki/docs_v2/2-60-任务队列与异步架构.md` § 5.1 `async_tasks.status` 字段说明
- **原文**：
  > 当前约束为 `queued` · `running` · `succeeded` · `failed` · **`canceled` · `cancelled`** · `retrying`
- **实际情况**：`0-50` 2026-05-24 已经把这列为"枚举与约束不一致"问题
- **建议**：
  - 短期：文档里明确"目标统一为 `cancelled`（英式），`canceled` 是历史遗留"
  - 长期：按 `0-50` 建议做数据迁移 + 约束更新 + 代码全局替换
- **状态**：✅ 已处理（2026-05-24，方向**反转**为美式 `canceled`）
- **方向决策**：用户命名原则=美式英语；`0-50` 当年的"用 cancelled"建议被推翻
- **执行说明**：
  - **代码层（11 文件）**：后端 8 个 + 前端 3 个全部 `cancelled` → `canceled`；函数名 `mark_pipeline_run_canceled`、常量 `RUN_CANCELED_STATUS`、JSON 字段 `canceled_by` / `canceled_at`、审计 action `pipeline.run.canceled` / `async_task.canceled`、task event 类型 `run_canceled` 等全部美式化。
  - **数据库层**：新增迁移 `20260524_0006_normalize_cancelled_to_canceled.sql`（UPDATE 旧数据 + 收紧 3 张表 CHECK 约束）；同步更新 `00_current_schema.sql`。
  - **文档层（8 文件）**：7 份内容文档 + 0-50 新增决策记录条目（历史审计原文保留作可查记录）。
  - **验证**：全量 grep 后剩余 `cancelled` 引用都在合法位置（迁移文件自身 / compat 一次性脚本 / 0-50 历史记录）。

---

#### 问题 ⑧ `1-30` 功能需求矩阵的 M3 状态未同步最新进展

- **位置**：`wiki/docs_v2/1-30-功能需求矩阵.md` M3 工作流行
- **原文（更新时间 2026-05-21）**：
  > 当前状态：部分接入
  > 下一步关键需求：9 类节点执行器、异步运行、进度推送、节点缓存
- **实际情况**：
  - 5-22 第 16 步：Task cancel/retry、Run cancel/retry、Task events/SSE 后端接入完成
  - 5-23 派生数据集 10 步重构完成（合并 `pipeline_artifacts` + `analysis_results`）
  - `1-30` 自称是"**模块状态唯一真相**"，但实际落后 3 天
- **建议**：
  - M3 下一步关键需求更新：去掉已完成的"进度推送"（SSE 已接入），保留"节点执行器、节点缓存"
  - 补充新增的"派生数据集模型重构完成"作为已实现项的说明
  - 同步更新文档头部"更新"时间
- **状态**：✅ 已处理（2026-05-24，**采用方向 A**：降级 1-30 为"模块路线图"）
- **方向决策**：维护 18 份文档 131 个状态徽章在实践中行不通；与其追求"唯一真相",不如降低 1-30 的承诺级别。
- **执行说明**：
  - 头部说明：从"模块状态的唯一真相"改为"模块路线图视角"，加 ⚠️ 提示"不要求与代码实时同步"。
  - meta 块：定位改"模块路线图（按模块视角看能力范围与下一步）"；事实源改"路线讨论 + git log"；新增"更新频度：按需"。
  - §1 一句话：补"M3 工作流已扩到异步运行 + 派生数据集合并"。
  - M3 行：已实现项扩到 11 项（含 5-22/5-23 进展）；下一步聚焦"节点执行器扩展 + 节点缓存 + 协作式取消"。
  - §4 维护规则完全重写：从"任何模块状态变化必须先改本页"改为"路线图视角入口"，明确"遇到矛盾时以代码为准"。
- **未处理项**：其它 17 份文档的 118 个状态徽章按方向 A 保持"自治"（各自维护，矛盾时以代码为准），不强求与 1-30 同步。

---

### 🟡 低优先级——不影响功能，但积累了技术债

#### 问题 ⑨ `4-50` 清理策略文档状态不明

- **位置**：`wiki/docs_v2/4-50-清理策略与迁移.md`
- **背景**：5-23 第 10 步日志记录："4-50 内容与 4-30 / 5-30 的 retention 段高度重叠，当前不动，等后续再决定是否合并"
- **当前问题**：
  - 文档没有任何"已过时"标注
  - 读者不知道该信 4-50 还是 4-30 / 5-30
- **建议**：
  - 选项 A：删除 4-50，内容合并到 4-30 / 5-30
  - 选项 B：保留 4-50 但只作为索引页，指向 4-30 / 5-30 的相应段落
  - 选项 C：保留独立内容，但加显式 cross-link 说明边界
- **状态**：✅ 已处理（2026-05-24，采用方案 C 整页重构）
- **决策依据**：详细分析后发现 4-50 有 4 节是独有内容（§ 2 跨 Dataset/Study 的文件生命周期表、§ 4 缓存清理优先级、§ 6 存储迁移 10 步、§ 7 MinIO 未来规划），删除会丢失重要信息。
- **执行说明**：
  - 重新定位标题为"清理策略与存储迁移路线图"，文档顶部加边界说明（与 4-30 / 5-30 各自负责什么）。
  - § 5 标记为"历史快照"——这部分是 2026-05 已完成的字段重命名记录。
  - § 6 迁移顺序表加状态列：1-9 步标 ✅（已完成），第 10 步标 ⏳（未做：`PROJECTS_DIR` → `ELYS_STORAGE_ROOT`）。
  - 同步清理 7 处旧名引用。

---

#### 问题 ⑫（新增）派生数据集重构遗留：表名 `pipeline_artifacts` / 列名 `upstream_artifact_id` 在多份文档残留

- **背景**：2026-05-24 处理问题 ②③④⑤ 时，所用 grep 模式只匹配了 `/pipeline-artifacts`（端点路径）和 `analysis_results`，**漏掉了 `pipeline_artifacts` 表名本身**（不带前缀斜杠）和 `upstream_artifact_id` 列名。
- **覆盖范围**：8 份文档中 26 处残留。
  - `3-00-数据库设计总览.md` ER 图（2 处）
  - `3-50-协作权限状态与迁移.md` retention 字段 + 迁移计划（4 处）
  - `4-30-Study输出与Artifact.md` 发布流程 Mermaid + 字段表 + 回归关注点（4 处）
  - `4-50-清理策略与迁移.md` 触发条件 + 文件生命周期表 + 兼容表 + 迁移步骤（7 处）
  - `4-70-文件角色URI与追溯关系.md` 文件角色定义 + schema 段（5 处）
  - `5-00-研究管理总览.md` 事实源列表（1 处）
  - `5-20-Pipeline工作流管理.md` worker 流程 Mermaid + 实现表（2 处）
  - `7-40-工作流执行与后台任务.md` ArtifactStore 描述（1 处）
- **状态**：✅ 已处理（2026-05-24，与 ⑥⑨ 同批）
- **教训**：以后做"全量清理"类工作时，grep 模式要覆盖**多种引用形态**——表名（裸名）、列名、端点路径、Pydantic schema 名、ORM 类名都要分别 grep 一遍。

---

#### 问题 ⑩ 测试文件（`tests/`）未同步——代码层面遗留

- **位置**：`elys_project/backend/tests/` 下 5 个测试文件
  - `test_pipeline_run_routes.py`
  - `test_run_manifest.py`
  - `test_file_backfill.py`
  - `test_file_tasks.py`
  - `test_run_dependencies.py`
- **背景**：5-23 第 10 步日志记录："这些测试仍引用 `PipelineArtifact` / `AnalysisResult` / `upstream_artifact_id` 等旧名"
- **影响**：
  - 跑测试套件会报 ImportError
  - 现状是"代码已切换、测试未切换"的不一致
- **建议**：
  - 单独一个 PR 把这 5 个测试迁移到 `DerivedDataset`
  - 或者评估 `file_backfill.py` 是否还需要保留（已是死代码），直接删除测试
- **状态**：待讨论

---

#### 问题 ⑪ `dataset_derivatives` 僵尸表本身未 DROP

- **位置**：`elys_project/database/schema/00_current_schema.sql` 第 627–639 行
- **背景**：`0-50` 2026-05-24 已识别这是僵尸表
- **影响**：
  - schema 里存在但没有任何业务代码读写
  - `project.py` 的 `DatasetDerivative` ORM 模型 + `Dataset.derivatives` relationship 也是孤儿
- **建议**：
  - 新建迁移 `DROP TABLE IF EXISTS dataset_derivatives CASCADE`
  - 同步从 `project.py` 删除 ORM 模型和 relationship
- **状态**：✅ 已处理（2026-05-24）
- **改动清单**：
  - 新建 `database/migrations/20260524_0005_drop_dataset_derivatives.sql`
  - 新建 `backend/alembic/versions/20260524_0005_drop_dataset_derivatives.py`
  - 删除 `database/schema/00_current_schema.sql` 中的表定义（原 627-639 行）和索引（原 688 行）
  - 删除 `backend/app/models/project.py` 中的 `DatasetDerivative` 类和 `Dataset.derivatives` relationship
  - 删除 `backend/app/models/__init__.py` 中的 import 和 `__all__` 导出
  - 同步处理问题 ①：`1-50` 路线图 Phase 1 目标更新（`dataset_derivatives` → `derived_datasets`，状态"规划"→"部分接入"）
  - 在 `0-50` 变更记录追加 2026-05-24 条目
- **遗留**：`database/migrations/20260521_0001_compat_v1_to_current.sql` 第 726/799 行仍 CREATE 此表，但 `20260524_0005` 紧接其后 DROP；效果一致。compat 文件本身已被本日审计第 ⑩ 条标记需单独处理，故本次不动它。

---

## 二、按主题归纳

便于一次性集中处理：

### 主题 A：派生数据集重构的文档跟进
- 问题 ①、②、③、⑤、⑥、⑨、⑩、⑪

### 主题 B：API 路径与命名规范化
- 问题 ③、④

### 主题 C：状态文档的时效性
- 问题 ⑧（核心）+ ①、②（连锁）

### 主题 D：枚举与约束统一
- 问题 ⑦

---

## 三、建议讨论顺序

1. **先讨论 ⑪**（DROP 僵尸表）→ 这是其它问题的前置
2. **然后 ①、②、③、⑤**（高优先级文档同步）→ 一次会议可以全部改完
3. **再讨论 ④、⑥**（小修小补，但要细心）
4. **⑧** 单独评估 → 需要决定 `1-30` 到底维护到什么粒度
5. **⑦** 长期决策 → 涉及数据迁移，不要轻易动手
6. **⑨、⑩** 视情况安排

---

## 四、记录规则

- 每条问题在讨论结束后，把"状态：待讨论"改为"状态：已处理 + 关联 commit/迁移文件 + 日期"
- 涉及代码改动的，关联到对应日志或 PR
- 涉及文档改动的，同步更新 `wiki/docs_v2/0-50-变更记录.md`
