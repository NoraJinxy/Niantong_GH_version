# ELYS · 事件归一化(condition 统一模型)· 修改计划

> 把 `00_数据导入原则.md` 里"事件可切分"这一件事落成代码。**2026-06-12 决策后已大幅收窄范围。**

| 项 | 内容 |
|---|---|
| 文档性质 | **实施计划**(含已落地/待做状态) |
| 日期 | 2026-06-12(决策后修订) |
| 关联 | 规范 `00_数据导入原则.md`;协作边界 `日志/6_工程债评审260612/` |
| 事实源 | P0 当前代码;"已落地"项含本地实测,**云端验证待部署窗口** |

---

## 0. 决策(2026-06-12,用户拍板)

- **TFR / 时频节点:本轮不建。**(产品现仅 epoch + ERP 时域叠加,无频谱节点。)
- **参数模型:统一成一个 `conditions`** —— 取代 `event_id`,exact / contains / 正则一套引擎。
- **前端:极简** —— 文本框填规则 + 全局 mode 下拉 + 只读模板提示;不做 chip 池、不做实时命中数。
- **全砍(本轮不做):** 导入 QC 五项、litmus 导入警告、HED / badge、导入侧 condition 派生、`datasets.py` 改动。

## 1. 本轮要做(收窄后)

| 文件 | 动作 | 状态 |
|---|---|---|
| `engine/analysis/event_conditions.py` | 匹配引擎(exact/contains/regex)。`summarize_event_vocabulary` 留着,本轮**不接入**(可作前端模板提示来源) | ✅ 已落地 |
| `engine/analysis/epoching.py` | 分组已通;**待小改**:读参数由 `event_groups`/`event_id` 收敛为单一 `conditions` | 🔄 待小改 |
| `pipeline/nodes/eeg_epoch_segment.json` | `event_id` → `conditions`(list of `{name,pattern,mode}`);+ 全局 `match_mode`(默认 `exact`,若采全局下拉方案) | ⬜ 待做 |
| `pipeline/nodes/eeg_analysis_erp.json` + 前端 ERP 选择器 | 读上游 Epoch 的 `conditions` 名字(不再读 `event_id`) | ⬜ 待做 |
| 前端 `composables/pipeline/useEventSelectEditor` | 极简:文本框(一行一规则 `name=pattern`)+ mode 下拉 + 只读模板提示;产出 `conditions` | ⬜ 待做 |

> 后端核心(匹配 + 分组)已在隔离模块落地并本地实测(econ:2 condition × 40 epoch、py_compile 过、diff 无 CRLF);本轮新增主要是**参数改名 + 节点 spec + ERP 连带 + 极简前端**。

## 2. 不做(本轮明确排除)

TFR/时频节点;导入 QC 五项;litmus 导入警告;HED/badge;导入侧 condition 派生;`routers/datasets.py` 改动。

## 3. 能力边界(诚实)

| 能力 | 本轮后 |
|---|---|
| 按 condition 切分 | ✅ 全范式(econ 可切握拳/放松) |
| ERP 叠加 | ✅ 听觉/视觉/认知 ERP 范式 |
| MI / SSVEP 的 ERD-ERS / 频谱 | ❌ 待 TFR 节点(econ 这轮能 epoch,产品里仍出不了 ERD/ERS) |

## 4. 待定小项(动手前确认)

- **极简 mode 表达**:全局下拉(**建议**,默认 `exact`)vs 行内算符(`==`/`=~`)vs 智能默认。
- **chip 池去留**:去掉 + 模板提示(**建议**)vs 保留 chip + 加文本框。
- **参数名 `conditions`** 确认(牵动节点 spec + `epoching.py` + ERP 选择器)。

## 5. 验证 + 工作树

- 本地:`py_compile`(后端)+ `vue-tsc`(前端类型);`compileall` 已过(停掉的会话无语法破损)。
- 云端:`elys_debug`(后端)+ 真机点检(前端,我看不到渲染,**前端预计 1–2 次部署来回**)。
- 提交:**只提交本功能自己的文件**,不混入停掉会话的半拆未提交件。

## 6. 风险 / 回滚

统一模型是**破坏性改名**(调试期、无真实数据,可接受;会让旧存的 pipeline 的 Epoch 参数失效)。回滚 = 还原节点 spec + `epoching.py` + ERP 选择器 + 删 `event_conditions.py`。
