# 数据集重构 · 开发计划（后端轨道）

> 生成 2026-06-08 · 轨道：**后端**（routers/datasets.py · routers/dataset_versions.py · services/dataset_lifecycle.py · services/dataset_assets.py · services/studies.py）
> 设计事实源：`日志/2_meeting260607/文件管理目标方案.md`（28 节）。本计划是**待办**，不是现状；每条任务标注 ✅已实现 / 🚧需改 / 🆕新增。
> 硬约束：调试期 → 可 DROP 重建、不写迁移、不留兼容层 / 死代码；判断"是否已实现"以代码为准（P0）；本机无运行环境，只做静态校验（`python -m py_compile`）。

---

## 0. 现状速读（精读后的事实校对，作为下面任务的依据）

精读了 4 个文件，确认现状如下（行号以当前代码为准）：

- **FIF 双写**：`generate_canonical_fif`（datasets.py:1929）落 `derivatives/elys-canonical-fif/…` 后，再 `shutil.copy2` 一套到 legacy fifdata（datasets.py:2091-2104，`ConversionParams.legacy_mirror=True`）。`recordings.fif_path` 回填的是 **legacy** `conversion["fif_path"]`（datasets.py:3037 / 3067），`dataset_files.canonical_fif` 指 derivatives → 指针分裂。
- **legacy fifdata 写入入口**：`build_fif_base_path`（datasets.py:893，`study.data_root/fifdata/…`）+ `ensure_fifdata_dataset_files`（datasets.py:1798，在 fifdata 写 `dataset_description.json`/`participants.*`）+ `ensure_fif_targets_are_free`（datasets.py:1769）。这些都在 `materialize_recording_import`（datasets.py:2988-3009）里被调。
- **一文件多行**：`create_dataset_file_records`（datasets.py:1418）每个原件写 `raw_source`(1464) + `original_upload`(1480) + `raw_bids_data`(1503) 三行；每个 sidecar 写 `sidecar`(1592) + `raw_bids_{channels,events,eeg_json}`(1618) 两行。配套 `add_dataset_file_derivation_record`（datasets.py:1394，`transform_name="import.generate_canonical_fif"`）把 raw_bids 行当 source。
- **versions/{label} 目录层**：`dataset_version_root`（datasets.py:990 = `DATASETS_STORAGE_ROOT/{asset}/versions/{label}`）、`dataset_version_storage_uri`（datasets.py:986 = `elys://datasets/{asset}/versions/{label}`）、`dataset_storage_reference_for_path`（datasets.py:1195，要求 `parts[1]=="versions"`）、`dataset_logical_path_from_storage_uri`（datasets.py:1211）、`resolve_study_relative_path`（datasets.py:1318）全靠这层。原件落点 `make_import_job_dir`（datasets.py:1125）走 `dataset_version_root/sourcedata/original_uploads/upload-{seq}`（**未按被试归组**）。
- **原件未按被试归组**：`dataset_upload_logical_dir`（datasets.py:994）= `sourcedata/original_uploads/upload-{seq}`，缺 `sub-XXX` 层（§6.2 / F-1 要求 `original_uploads/sub-XXX/upload-{seq}`）。
- **发布**：HTTP 端点在 `routers/dataset_versions.py`（不在 datasets.py）。`POST /api/v1/dataset-versions/{id}/publish`（dataset_versions.py:125）→ `publish_dataset_version`（dataset_lifecycle.py:266）。**问题**：dataset_lifecycle.py:352-356 发布时把 `visibility private→shared` 自动升级——与目标方案 §4.4「发布≠公开、发布不动 visibility」**冲突**；且**不写 `releases/v{semver}.json`**。撤回 / 紧急下架 / 审核（request_version_withdrawal / review_withdrawal_request / emergency_takedown_version）齐全可复用。
- **可见性 PATCH**：`PATCH /dataset-assets/{id}`（datasets.py:2530）已能改 visibility（2554），但**无发布状态闸**——未发布也能设 shared/public（违反 §4.2「shared/public 仅已发布后」）。`disclosure_level` 字段：**模型无**（DatasetAsset 模型 study.py:190-227 无此列）、**schema 无**（DatasetAssetUpdate 无此字段）。
- **访问闸现状**：`get_dataset_asset_for_user`（dataset_assets.py:231）+ `can_read_dataset_asset`（dataset_assets.py:~195）已是「元数据闸」（owner/创建者/admin/shared+public 可读）。但 **download（datasets.py:2759）/ bids-tree（datasets.py:2403）/ files 列表 只过元数据闸，无「数据闸」**（不校验 disclosure_level=data）。
- **archive 打包下载**：**无**（datasets.py 无 `/archive` 端点）。
- **addenda**：**无**（无 `dataset_addenda` 表 / 无端点）。
- **QC**：`/recordings/{id}/qa` + `/qa/mock-run` + `/qa/review`（datasets.py:2789-2908）齐全，沿用。
- **legacy study data_root**：`services/studies.py:23 LEGACY_PROJECT_DIRECTORIES`（含 source_uploads/fifdata/upload_staging/validation/bids_exports/…）由 `create_legacy_study_directories`（studies.py:97）+ `create_study_storage_directories`（studies.py:140）创建。`study.data_root = STUDIES_DIR/{id}`（studies.py:247）。导入仍把 `temp_root = study.data_root/upload_staging`（datasets.py:3244）。
- **附带 BUG（顺手修）**：datasets.py:2449 `can_write_dataset_asset(asset, current_user)` **参数顺序写反**（签名是 `can_write_dataset_asset(user, asset)`，dataset_assets.py:205）。当前 user 被当 asset、asset 被当 user，`asset.status` 会取到 User 对象 → import-task 端点的写权限校验逻辑是坏的。

---

## 1. 任务清单（按依赖排序）

> 依赖链：先收口磁盘/索引写入（T1→T4）→ 再去 versions 层（T5）→ 状态轴与 schema（T6→T7）→ 访问闸（T8）→ 发布清单 + 派生能力（T9→T11）→ 清理 legacy 存储（T12）→ 顺手 bugfix（T13）。

### T1 🚧 去 FIF 双写：`generate_canonical_fif` 单写 derivatives
- **改哪**：`routers/datasets.py`
  - `generate_canonical_fif`（1929）：删 `legacy_fif_base` 形参 + 所有 `legacy_*` 局部变量（1958-1963）；删镜像 `shutil.copy2` 段（**2087-2104**）；返回 dict 删 `fif_dir`/`fif_path`/`sidecar_paths`(legacy) 段（2116-2123）与 `qa_report.fif_conversion.legacy_fif_path`（2138）。
  - provenance（2028）：删 `legacyFifDir`/`legacyFifPath`/`legacySidecars`（2047-2048, 2055-2060）；`ConversionParams` 删 `legacy_mirror`（2078）。
  - `materialize_recording_import`（2911）：删 `fif_base = build_fif_base_path(...)`（2988）、`ensure_fif_targets_are_free(fif_base)`（2997）、`ensure_fifdata_dataset_files(...)`（2999）、`legacy_fif_base=fif_base` 实参（3009）。
  - 删函数 `build_fif_base_path`(893)、`make_fif_version_dir`(949)、`ensure_fif_targets_are_free`(1769)、`ensure_fifdata_dataset_files`(1798)。`get_next_upload_seq`（1144）删 `make_fif_version_dir(...).exists()` 分支（1167）。
- **验收点**：导入一条 BrainVision → 磁盘 FIF 只出现在 `derivatives/`，`study.data_root/fifdata/` 不再被创建/写入（验收清单第 1 条）；`py_compile` 通过；无残留 `legacy`/`fifdata` 符号（grep 仅剩 studies.py 待 T12 处理）。
- **风险**：`conversion` 返回 dict 字段被多处消费——`create_dataset_upload_record`（1737-1739 写 RecordingVersion.fif_dir/fif_path/sidecar_paths）、`materialize_recording_import`（3037/3067/3117/3140-3141 写 recordings.fif_path、manifest）。必须连同 T2/T3 一起改，否则 KeyError。RecordingVersion.fif_dir/fif_path/sidecar_paths 列需改指 canonical（不可悬空）。

### T2 🚧 `recordings.fif_path` + `RecordingVersion.*` 指向 derivatives 唯一副本
- **改哪**：`routers/datasets.py`
  - `materialize_recording_import`：`dataset.fif_path = conversion["fif_path"]`（3037、3067）改为 `conversion["canonical_fif_path"]`；manifest 的 `fifPath/fifDir/sidecars`（3138-3141）改用 canonical 字段。
  - `create_dataset_upload_record`（1704）：`fif_dir/fif_path/sidecar_paths`（1737-1739）改写 `conversion["canonical_fif_dir"]`/`["canonical_fif_path"]`/`["canonical_sidecar_paths"]`。
- **影响表**：`recordings.fif_path`、`recording_versions.fif_dir/fif_path/sidecar_paths`。
- **验收点**：`recordings.fif_path` 与 `dataset_files.canonical_fif.storage_uri` 指向同一 URI（验收清单第 3 条）；下游用 `fif_path` 解析能下到真身（file_browser.resolve_dataset_file_path）。
- **风险**：Pipeline / Execution 输入快照若直接读 `recordings.fif_path`（4-70 §7），路径语义从 legacy 变 derivatives——属预期（legacy 不再产），但要确认无别处 hardcode `fifdata`。

### T3 🚧 `create_dataset_file_records` 一文件一行：删冗余角色
- **改哪**：`routers/datasets.py`，`create_dataset_file_records`（1418）
  - 原件循环：删 `raw_source` 行（1457-1472）、删 `raw_bids_data` 行（1497-1519）；只留 `original_upload`（1473-1488，并去掉 metadata 里 `compat_role`/`standard_role`）。
  - sidecar 循环：删 `raw_bids_{role}` 整段（1603-1639）；只留 `sidecar` 行（1585-1602）。
  - 删 `add_dataset_file_derivation_record` 里所有以 raw_bids 行为 source 的派生写入（1651-1699），改为以 `original_upload` 主行为 source（仅保留 canonical_fif / provenance 的来源记录，sidecar 派生可一并删——sidecar 与 FIF 同次生成，无需 N×M derivation 行）。
  - 删辅助：`raw_bids_data_logical_path`(1054)、`raw_bids_sidecar_logical_path`(1071)、`raw_bids_eeg_dir`(1037)、`raw_bids_file_stem`(1045)、`raw_bids_data_metadata`(1101)、`brainvision_component`(1092)、常量 `RAW_BIDS_SIDECAR_ROLES`(158)、`DATASET_RAW_BIDS_PREFIX`(156) —— **除非** bids-tree 投影仍要复用（见 T10，建议把投影需要的命名函数迁去 file_browser 或就地保留，二选一，避免野引用）。
- **影响表**：`dataset_files`（角色集合精简为 original_upload/canonical_fif/canonical_fif_provenance/sidecar）、`dataset_file_derivations`。
- **验收点**：sub-093 三件套 `dataset_files` 从 9 行降到 3 行（原件）；无 `raw_source`/`raw_bids_*` 角色行（验收清单第 2/5 条）；"原始上传"计数=真实物理原件数。
- **风险**：`generate_canonical_fif` 的 provenance 里 `SourceRawBIDS`/`SourceEvents.logical_path`/`SourceChannels.logical_path`（2031-2041）也依赖这几个 raw_bids 命名函数——T1 已动 provenance，需同步把这些字段改成基于 `original_upload` 的真实 logical_path 或直接删（provenance 只需指回原件 + sidecar 真实路径）。bids-tree（T10）原本吃 raw_bids 行，删行后必须改投影来源，**T3 与 T10 强耦合，同批改**。

### T4 🚧 原件按被试归组 `original_uploads/sub-XXX/upload-{seq}`
- **改哪**：`routers/datasets.py`
  - `dataset_upload_logical_dir`（994）签名加 `bids_subject_id`，返回 `sourcedata/original_uploads/{sub}/upload-{seq:03d}`。
  - `make_import_job_dir`（1125）的 asset 分支（1134-1135）传入 `bids_subject_id`。
  - `get_next_upload_seq`（1144）调用 `make_import_job_dir` 处补 subject（已有 bids_subject_id 在作用域）。
- **验收点**：磁盘出现 `datasets/{asset}/sourcedata/original_uploads/sub-093/upload-001/`（§6.2 / §16）；多被试不串目录。
- **风险**：`get_next_upload_seq` 的"目录已存在则 +1"兜底依赖路径一致——subject 层加进去后旧老路径不存在（调试期 DROP 重建，无历史数据，安全）。

### T5 🚧 去 `versions/{label}/` 目录层：数据挂 asset 根
- **改哪**：`routers/datasets.py`（这是改动面最大的一项，因为 storage_uri / 路径解析全绕它）
  - `dataset_version_root`（990）→ 改为 `DATASETS_STORAGE_ROOT/{asset_id}`（去掉 `/versions/{label}`）。建议重命名 `dataset_asset_root(asset_id)`，删 `version_label` 形参。
  - `dataset_version_storage_uri`（986）→ `elys://datasets/{asset_id}`（去 `/versions/{label}`）。
  - `dataset_storage_reference_for_path`（1195）：判定从 `parts[1]=="versions"` 改为「相对 asset 根后整段即 logical_path」（parts[0]=asset_id，logical=parts[1:]）。
  - `dataset_logical_path_from_storage_uri`（1211）、`resolve_study_relative_path`（1318 的 `elys://datasets/` 分支 1320-1324）、`dataset_storage_uri`（998）同步去版本段。
  - `build_canonical_fif_base_path*`（922/935）、`canonical_fif_logical_dir`（908）、`make_import_job_dir`（1134）的 root 取用改 `dataset_asset_root`。
  - 删 `WORKING_DATASET_VERSION_LABEL` 在路径层的用途（仅 DB version_label 仍可叫 working，路径不再含它）。
- **验收点**：working/ 目录不再被写入（验收清单第 4 条）；新导入磁盘布局= `datasets/{asset}/{sourcedata,derivatives,qc,releases}`（§9 总图）；`file_browser` 解析 `elys://datasets/{asset}/…` 仍能定位。
- **风险**：**最高**。`storage_uri` 是已落库字段，改格式属 breaking——调试期 DROP 重建可接受，但务必全仓 grep `versions/` 与 `elys://datasets`，确认 `services/file_browser.py`、`services/dataset_bootstrap.py`（含 `ensure_dataset_version_storage`/`dataset_version_storage_uri` 的二次定义，dataset_lifecycle.py:148-151 import 了它）、worker `tasks/file_tasks.py` 全部跟改。**与 dataset_bootstrap.py 是跨文件契约，需对齐（见末节契约）**。

### T6 🆕 DB：`dataset_assets.disclosure_level` + 状态铁律约束
- **改哪**：`models/study.py`（DatasetAsset，190）+ schema 重建脚本 `seeds/03_*`（DROP 重建，不写迁移）
  - 加列 `disclosure_level VARCHAR(16) NOT NULL DEFAULT 'metadata'`（取值 metadata/data，§4.3 / F-10 默认保守）。
  - 加 CHECK（DB 层，§4.5）：`dataset_versions.state='draft' ⟹ asset.visibility='private'` 难以纯列级 CHECK 表达（跨表），落地为 **service 层强制**（T7/T8）+ 可选 asset 级 CHECK `visibility in ('private','shared','public')`、`disclosure_level in ('metadata','data')`。
  - `models/study.py` DatasetAsset 加 `disclosure_level = Column(String(16), nullable=False, default="metadata")`。
- **同步**：`schemas/dataset.py`：`DatasetAssetResponse` 加 `disclosure_level`；`dataset_asset_to_response`（datasets.py:251）回填 `asset.disclosure_level`。
- **验收点**：bootstrap 出来的 asset `disclosure_level='metadata'`；响应体含该字段；`py_compile` + schema 重建无报错。
- **风险**：DatasetAssetResponse 被前端契约消费（6-00），新增字段属加法，安全；但 `seeds/03` DROP 重建顺序（27 节 runbook：01_auth→02_studies→03_datasets→…）要保持。

### T7 🚧 发布只冻结、不动 visibility（修 publish 副作用）
- **改哪**：`services/dataset_lifecycle.py`，`publish_dataset_version`（266）
  - 删 visibility 自动升级段（**352-356**：`if asset.visibility == "private": asset.visibility = "shared"`）。发布只改 `version.state/published_at/published_by/version_doi` 与 `asset.current_version_id/concept_doi`，**保持 visibility=private**（§4.4 铁律 2「发布≠公开」、§4.5「发布接口不动 visibility」）。
  - audit metadata 里 `asset_visibility`（379）保留留痕（值现在恒为发布前值）。
- **验收点**：发布后 asset.visibility 仍为发布前值（默认 private）；要公开需另调 PATCH（T8）。
- **风险**：`_ensure_version_mountable`（dataset_assets.py:267）允许「published 任何 Study 可挂」——发布后 visibility 仍 private 时，其它 Study 能否挂？按 §23 表「发布→任何研究项可挂具体 release 版本」，挂载锁版本不锁 visibility，**保持现有 _ensure_version_mountable 逻辑**（published 即可挂），与 visibility 解耦。需在计划里点明这是有意为之，不要因为 T7 误加 visibility 闸到挂载。

### T8 🚧+🆕 可见性/粒度 PATCH：仅发布后允许；未发布设 shared/public → 403
- **改哪**：`routers/datasets.py` `update_dataset_asset`（2530）+ `schemas/dataset.py` `DatasetAssetUpdate`（63）
  - schema 加 `disclosure_level: Optional[Literal['metadata','data']] = None`。
  - 端点逻辑：当 `visibility` 被设为 `shared`/`public` 或 `disclosure_level` 被设时，校验该 asset **存在已发布版本**（查 `DatasetVersion.state=='published'` for asset；或 `asset.current_version.state=='published'`）。未发布 → `403`（§4.5「未发布想设 shared/public → 403」、§20 轴 B/C「未发布调用 → 403」）。
  - 写 `asset.disclosure_level`（2554 visibility 旁）。
  - audit metadata 补 `disclosure_level`（2572 旁）。
- **验收点**：未发布 asset PATCH visibility=shared → 403；已发布后可在 private/shared/public + metadata/data 间切换且**零文件变动**（验收清单第 7/8 条）。
- **风险**：bootstrap 默认 visibility 由 `DatasetBootstrapRequest.dataset.visibility`（schema 默认 private）——若前端允许 bootstrap 时传 shared，会绕过此闸。需在 `bootstrap_dataset` 或此处统一：未发布 asset 一律 private（强制覆盖），把铁律 1 钉死在 service。

### T9 🚧+🆕 两道访问闸：download / bids-tree / files 加数据闸（disclosure_level=data）
- **改哪**：`routers/datasets.py` + 建议在 `services/dataset_assets.py` 加 `can_access_dataset_data(user, asset) -> bool`
  - 新函数语义（§4.4 铁律 4「元数据闸 ≠ 数据闸」）：owner/创建者/admin 恒通；否则要求 `asset.disclosure_level=='data'` **且** `asset.visibility in {'shared','public'}` **且** 版本已发布。
  - `download_dataset_file`（2759）：在 `ensure_dataset_file_readable` 后追加数据闸；未通过 → `403 +「可申请访问」`（§4.5 / §13）。
  - `get_dataset_asset_bids_tree`（2403）数据节点、`list_dataset_asset_files`（2376）：metadata-only 访问者可看树/列表结构但 download 被拒（最简实现：列表/树照返回，仅 download 拦——与 §4.5「download 端点按 disclosure 拒绝」「数据文件原地不动」一致）。
  - `ensure_dataset_file_readable`（396）保持「元数据闸」语义不变（owner/团队/shared/public 可读元数据），**数据闸单独叠加在 download**。
- **验收点**：他人对 shared+metadata 资产能看描述/文件列表，download 返回 403 +「申请访问」；shared+data 可下载。
- **风险**：owner 自己下载不能被误拦（owner/创建者短路必须在最前）；Execution 输入快照走的是 storage_uri 直读（4-70 §7），不经此端点，**不受影响**——但要确认 worker 不复用 download 端点取数。

### T10 🚧 bids-tree 改从 original_upload + sidecar 行实时投影（删行后修来源）
- **改哪**：`routers/datasets.py` `get_dataset_asset_bids_tree`（2403）+ `services/file_browser.py` `build_dataset_file_tree`
  - T3 删了 `raw_bids_data`/`raw_bids_*` 行后，原查询（2415-2424 `prefix=raw_bids` 过 logical_path）会查空。改为：查 `original_upload` + `sidecar` 行，在投影函数里用 BIDS 命名（`sub-XXX/[ses-XX/]eeg/sub-XXX_task-YYY_eeg.{ext}`）现算树（§11 / §25「从 original_upload + sidecar 行实时投影」）。
  - BIDS 命名函数（T3 中标记的 `raw_bids_data_logical_path` 等）建议**迁入 file_browser.py 复用**，而非删掉。
  - 投影叶子节点 `storage_uri` 指回 original_upload 真身（零复制，§25）。
- **验收点**：无物理 raw_bids 目录、无 raw_bids 行时，bids-tree 仍返回正确 BIDS 树（验收清单第 9 条）。
- **风险**：与 T3 强耦合（命名函数的归属要么迁要么留，避免野引用）；前端 bids-tree 视图契约（DatasetFileTreeResponse）字段不变即可。

### T11 🆕 发布写 `releases/v{semver}.json` 冻结清单（零复制）
- **改哪**：`services/dataset_lifecycle.py` `publish_dataset_version`（266）+ 新建 helper（建议 `services/dataset_releases.py` 或就近）
  - 发布成功后，按 §12.3 结构写 `datasets/{asset}/releases/v{semver}.json`：`versionLabel/state/publishedAt/publishedBy/contentHash/versionDoi/files[]`（每个 file 列 `datasetFileId/role/logicalPath/sha256`，遍历该版本 `dataset_files` 现有行，**不复制字节**）。
  - 物理写盘需 settings（DATASETS_STORAGE_ROOT）→ service 层不直接耦合 settings 的话，按现有模式（如 create_new_draft_version 把目录创建留给路由层，dataset_lifecycle.py:145）：**路由层 `publish_version`（dataset_versions.py:130）拿到 PublishResult 后写清单**，或给 service 传 `settings_obj`。二选一，保持与 bootstrap 一致风格。
  - `content_hash` 可同步算（遍历 files sha256 合并）或留异步（§20 标"异步"）；MVP 先同步算整版本指纹写入 release + `version.content_hash`。
- **验收点**：发布产生 `releases/v{semver}.json`，磁盘数据文件零搬动（验收清单第 8 条）；清单 files 指向 append-only 的现有副本。
- **风险**：发布后描述只读 + content_hash 一致性——清单一旦写就是不可变边界；若 publish 在 commit 后写盘失败，需补偿（要么写盘进事务前置，要么失败回滚 state）。建议：先写盘成功再 commit state=published（all-or-nothing）。

### T12 🚧 删 legacy study data_root 写入：存储统一两根
- **改哪**：`services/studies.py` + `routers/datasets.py`
  - 删 `LEGACY_PROJECT_DIRECTORIES`（studies.py:23）、`create_legacy_study_directories`（97）、`create_study_storage_directories`（140）里对它的调用（165）。`study_storage_policy`（88）删 `legacy_*` 字段。
  - `study.data_root`：调试期可保留指向 `STUDIES_DIR/{id}`，但**其下不再建 fifdata/source_uploads/upload_staging 等**。导入用的 `temp_root = study.data_root/upload_staging`（datasets.py:3244）改指 `STUDIES_STORAGE_ROOT/{study}/temp` 或 asset 根下临时区（§8.2 现有 STUDY_STORAGE_DIRECTORIES 已含 temp）。
  - 全仓 grep `study.data_root` 残留写入点（datasets.py 内 `make_import_job_dir` 的非 asset 分支 1137-1141 走 data_root/source_uploads——asset 分支已是主路径，可删非 asset 分支，导入恒走 asset）。
- **验收点**：legacy `studies/{id}/` data_root 下不再出现 fifdata/source_uploads 等（验收清单第 4 条）；新建 study 只建 `STUDIES_STORAGE_ROOT/{id}/{executions,derived,exports,previews,temp,pipeline_snapshots}`。
- **风险**：`temp_root` 迁移要确保导入落盘目标目录存在（mkdir parents）；`make_import_job_dir` 非 asset 分支若有别处调用（worker），需一并清。与 studies 轨道（如有）对齐 data_root 语义。

### T13 🚧 顺手修 `can_write_dataset_asset` 参数顺序 BUG
- **改哪**：`routers/datasets.py:2449`
  - `can_write_dataset_asset(asset, current_user)` → `can_write_dataset_asset(current_user, asset)`（签名 dataset_assets.py:205 是 `(user, asset)`）。当前写反，`create_recording_import_task` 端点的写权限校验失效。
  - 全仓 grep `can_write_dataset_asset(` 复核其它调用点顺序（datasets.py:2481/2511 已是正确的 `... ` 形式需逐一核对）。
- **验收点**：import-task 端点对无写权限用户正确返回 403；`py_compile` 通过。
- **风险**：极低；纯参数纠正。属本次重构相邻代码，顺手修不扩大范围。

---

## 2. addenda（发布后补充信息 + 管理员审核）—— 范围内但建议独立小批

> 任务书列了 addenda，但它是**纯新增**（新表 + 2 端点 + 审核流），与 T1–T12 的"收口存储/状态"主线弱耦合。建议作为 T11 之后的独立提交，避免主线 PR 过大。

### T14 🆕 `dataset_addenda` 表 + 提交端点 + 管理员审核端点
- **改哪**：`models/`（新 `DatasetAddendum`）+ `routers/dataset_versions.py`（或 datasets.py asset_router）+ `services/`（复用 audit_events）
  - 表（§4.6 / §20）：`id / dataset_version_id / content(JSON 或结构化段) / submitted_by / status(pending|approved|rejected) / reviewed_by / reviewed_at / created_at`。
  - `POST /api/v1/dataset-versions/{id}/addenda`（owner，仅已发布版本，status=pending）；`POST /api/v1/dataset-withdrawals`-style 的 `POST …/addenda/{addendum_id}/review`（admin，approved 后对外可见，写 `audit_events`，复用撤回审核 admin 流程 review_withdrawal_request 的鉴权范式）。
  - 描述本体（§4.6.5 结构化 abstract/methods/usage/ethics）存 `dataset_versions.metadata_json.description{...}`；**发布后描述只读**：`PATCH 描述` 仅未发布允许（已发布 → 403）——若有改描述端点（当前 update_dataset_asset 只改 asset.description，版本级描述在 metadata_json），需在描述写入处加发布状态闸。
- **验收点**：已发布版本可提交 addendum(status=pending)；admin review approved 后可见并落 audit_events；未发布版本提交 addendum → 409/403。
- **风险**：与撤回审核共用 admin 鉴权（`actor.has_role("admin")`，dataset_lifecycle.py:488）；不要新造权限体系。描述结构化字段是前端契约（6-00），需与前端轨对齐 description 子段命名。

---

## 3. 静态校验 & 落地顺序

- 每改一批跑 `python -m py_compile`（本机无运行环境，pytest/起服务在云端）。
- 推荐提交批次：①T1–T4（去双写+一文件一行+归组，含 conversion dict 收口）→ ②T5（去 versions 层，跨 file_browser/bootstrap/worker）→ ③T6–T8（disclosure 字段+状态闸+PATCH）→ ④T9–T11（访问闸+bids-tree 投影+release 清单）→ ⑤T12–T13（清 legacy 存储+bugfix）→ ⑥T14（addenda）。
- 落地后按 §19 验收清单 + §27 runbook（DROP 重建 → 导入 sub-093 → 跑验收）在云端验证。
- 文档回改（wiki 3-25/3-50/4-20/4-70 + 9-00）属**文档轨道**，不在本后端计划范围，但本轨改动需在 9-00 记一条（命名/API/表结构变更）。

---

## 4. 对外接口 / 依赖契约

> 本轨与其它轨道 / 跨文件的契约边界，改动需双方对齐。

### 4.1 与 DB / schema 轨
- **新增列** `dataset_assets.disclosure_level VARCHAR(16) NOT NULL DEFAULT 'metadata'`（T6）——需进 `seeds/03_datasets`（DROP 重建，不写迁移）。
- **角色集合收敛**：`dataset_files.file_role ∈ {original_upload, canonical_fif, canonical_fif_provenance, sidecar, qc_report/*_summary}`（删 raw_source / raw_bids_*）。下游任何按角色查询/计数的代码以此为准（§10）。
- **状态轴**：沿用 `dataset_versions.state ∈ {draft,published,withdraw_requested,withdrawn}` + `dataset_assets.visibility ∈ {private,shared,public}` + 新 `disclosure_level`；铁律「draft⟹private」由 service 强制（T7/T8）。

### 4.2 与前端轨（6-00 / DatasetsPage）
- `DatasetAssetResponse` **新增** `disclosure_level` 字段（前端徽章粒度小标读它）。
- `PATCH /api/v1/dataset-assets/{id}` body **新增可选** `disclosure_level`；语义：未发布设 shared/public/disclosure → **403**（前端未发布态需置灰可见性控件，§4.5/§28）。
- **新增端点** `GET /api/v1/dataset-assets/{id}/archive?scope=subject|all|bids`（T11 旁；打包下载，§13）——**本计划未把 archive 列为独立 T（任务书要求"打包下载 archive 接口"）**：补为 **T11.5 🆕**，流式 zip（§26），大资产走异步任务 + 进度。前端"下载全部/按被试"按钮依赖它。
- `download` 端点对 metadata-only 访问者返回 **403 + {code, "可申请访问"}**（前端他人视角据此显示「申请访问数据」）。
- bids-tree 响应模型 `DatasetFileTreeResponse` 字段不变（仅来源从 raw_bids 行改投影，前端无感）。

### 4.3 跨文件（同后端内）依赖契约
- **storage_uri 格式变更**（T5）：`elys://datasets/{asset_id}/{logical_path}`（去 `versions/{label}`）。`services/file_browser.py`（resolve/preview/tree）、`services/dataset_bootstrap.py`（`dataset_version_storage_uri` 二次定义 + `ensure_dataset_version_storage`，被 dataset_lifecycle.py:148-151 与 datasets.py:2335 import）、`tasks/file_tasks.py`（worker 跑 materialize/raw_bids_build/canonical_fif_rebuild）必须同步同一格式——**否则路径解析全断**。
- **conversion dict 契约**（T1/T2）：`generate_canonical_fif` 返回键删 legacy（`fif_dir/fif_path/sidecar_paths` legacy 版），消费方 `create_dataset_upload_record`/`materialize_recording_import` 改读 `canonical_*` 键。worker `run_dataset_import` 若复用这些函数，需同批改。
- **publish 写盘契约**（T11）：release 清单写盘位置 `datasets/{asset}/releases/`，路由层 or service 层写（与 bootstrap「目录创建留路由层」风格对齐，dataset_lifecycle.py:145）——需在 dataset_versions.py:130 `publish_version` 与 dataset_lifecycle.py:266 之间定清职责。
- **QC 端点**：`/recordings/{id}/qa`、`/qa/mock-run`、`/qa/review`（datasets.py:2789-2908）**不动**，§28 发布后"数据详情含质控报告段"由前端聚合，后端沿用现有端点。

### 4.4 与撤回 / 生命周期既有契约（复用，不重写）
- addenda 审核（T14）复用 admin 鉴权范式（`actor.has_role("admin")`）与 `audit_events` 信道（§4.6.2「复用撤回审核的 admin 流程」），不新造审批表/权限。
- publish/withdraw/emergency/review 既有 service（dataset_lifecycle.py）齐全，本轨只**改 publish 副作用**（T7 去 visibility 升级）+ **加 release 清单**（T11），不重写状态机。
