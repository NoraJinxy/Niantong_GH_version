# 数据集重构 · 开发计划【文档 / Wiki 轨道】

> 负责人轨道：**文档 / Wiki**（不含后端 / 前端 / DB 代码落地，那是其它轨道）。
> 设计事实源：`日志/2_meeting260607/文件管理目标方案.md`（28 节，应该态）。
> UI 基准：`design/mockups/datasets-workbench-demo.html`。
> 生成：2026-06-08 +08:00。
>
> **本轨道的产物全是文档**：回改既有 wiki 让它对齐目标态、判断要不要新建一页、起草更新日志条目、把本轮设计整理成可入库稿。
>
> ⚠️ **状态语义铁律**：本轮目标方案里**绝大多数能力尚未在代码落地**（FIF 仍双写、raw_bids 仍有幽灵行、`disclosure_level` 字段不存在、发布/撤回业务流转是骨架、状态自适应 tab / 描述结构化 / addenda 全是规划）。文档里凡涉及这些的，**一律标注为「目标态 / 规划」，绝不写成「已实现」**。判断"是否已实现"以代码为准（P0）；本机无运行环境，只做 `mkdocs build --clean` 静态校验。

---

## 0. 全局背景与债务清单（写文档前必须钉死的事实）

目标方案要文档对齐的核心变化（来自 `文件管理目标方案.md` §1–§4.6、§28）：

| 主题 | 现状债（wiki 现在怎么写的） | 目标态（要回改成什么） | 设计源 |
|---|---|---|---|
| 存储根数量 | 4-00/4-20 写「三根」：`datasets/` + `storage/studies/` + legacy `studies/{id}/` data_root | **两根**：`storage/datasets/{asset}/` + `storage/studies/{study}/`，删 legacy data_root 整支 | §1 §9 §8.3 |
| FIF 双写 | 4-20 §8「canonical FIF 已实现，回写 `recordings.fif_path`」未提双写债 | 揭示**双写债**（一记录 10 文件、5 个 fifdata 镜像）+ 标目标「单写 derivatives + 单指针」 | §2 §7.2-7.3 |
| raw_bids 幽灵 | 4-20/4-60/4-70 把 `raw_bids_data` 当已登记的"文件行" | **纯逻辑视图**：删 `raw_bids_*` 存储行，bids-tree 现算；角色精简 | §2 §10 §11 §25 |
| 一文件多行 | 4-70 角色表列 `raw_source`/`raw_bids_*`/`sidecar` 并存 | **一物理文件 = 一行 = 一角色**；角色表精简到 5 类有物理 + 删 4 类幽灵 | §3 §10 |
| 目录层级 | 4-00/4-20 有 `versions/working/` 层 | **取消 `versions/{label}/` 层**：数据挂 asset 根、发布进 `releases/` | §9 |
| 状态轴 | 6-05/3-50 已收敛到"可见范围单轴徽章"，但**未表达两轴 + 粒度** | **两轴 + 粒度**：发布状态（未发布/已发布/已撤回）× 可见性（私有/共享/公共）× 可见粒度（仅描述/数据本身）；发布≠公开 | §4.1-4.4 |
| disclosure 粒度 | 全 wiki **无** `disclosure_level` 概念 | 新增「可见粒度」：元数据闸 vs 数据闸；默认 metadata | §4.3 §4.5 §13 |
| 描述可编辑性 | 全 wiki **无** "发布前可编辑 / 发布后只读 + 追加审核" 规则 | 新增：描述发布前可改、发布后只读、addenda 追加需管理员审核；描述结构化（摘要/方法/使用/伦理） | §4.6 |
| 状态自适应 tab | 全 wiki **无** 页面信息架构描述（4 tab vs 数据详情） | 新增：发布前=概览/采集记录/导入/质控（+概览发布按钮）；发布后=概览/数据详情（+管理分享按钮） | §28 |
| 打包下载 | 13 节列为 ❌ 待补 | 标目标接口 `GET /dataset-assets/{id}/archive`（规划） | §13 |

> 这张表是**所有回改任务的总账**。下面每条任务都从这里派生。

---

## 1. 有序任务清单（按依赖排序）

> 标注：✅已实现 / 🚧需改 / 🆕新增。本轨道几乎全是「🚧需改文档」与「🆕新增文档」；个别任务确认现状用 ✅。
> 依赖原则：**先出可入库设计稿（T1）→ 再回改各总览/规范页（T2–T9）→ 评估并新建生命周期页（T10）→ 最后写更新日志收口（T11）+ 静态校验（T12）**。设计稿先行，避免各页各写一套、口径打架。

---

### T1 🆕 产出可入库 wiki 设计稿草案（本轨道地基，最先做）

- **产物**：`日志/2_meeting260607/wiki草案-数据集设计.md`（**本计划同批已写**）。
- **做什么**：把 `文件管理目标方案.md` 28 节 + UI mockup 收敛成一份**结构化、可直接搬进 wiki 的设计稿**——两根存储、三条数据线、两轴+粒度状态模型、描述结构化与可编辑性、状态自适应 tab、角色精简、bids-tree 逻辑视图、对外接口契约。语气按 `0-20 文档编写规范` §8（术语第一次出现配大白话）。
- **为什么先做**：它是 T2–T11 的**单一口径来源**——各页回改、新页起草、更新日志都引用同一套措辞与状态标注，避免「同一件事三页三种说法」。
- **验收点**：
  - [ ] 覆盖目标方案全部承诺：两根存储 / 三线 / 两轴+粒度 / §4.6 描述规则 / §28 tab / 角色精简表 / 接口契约。
  - [ ] 每个"尚未落地"能力**明确标「目标态 / 规划」**，与"已实现"分区写。
  - [ ] 给出"将来切进哪些 wiki 页 / 是否新建页"的搬运指引（与 T10 一致）。
- **风险**：设计稿若把规划写成现状 → 污染后续所有页。**缓解**：每节顶部统一一行"性质：现状 / 目标态"，参照 4-60 顶部 `!!! warning 实现现状` 写法。

---

### T2 🚧 回改 `wiki/docs/4-20-Dataset文件与导入转换.md`（债最重，优先）

- **文件**：`wiki/docs/4-20-Dataset文件与导入转换.md`。
- **现状债定位**（已逐行核对）：
  - §1 标准目录用 `versions/working/` 层 + 把 `raw_bids/` 画成真实文件树（行 21-52）。
  - §2 三层表把 `raw_bids` 当"权威入口"且未点明它是逻辑视图债。
  - §5 规则行 133 写"成功导入后登记 `original_upload`、`raw_bids_data`、sidecar、canonical_fif…"——把**幽灵行**当已登记成果。
  - §7 字段对照、§8 落地状态表均按"双写 / working 目录 / raw_bids 行"描述。
- **改成什么**（对齐目标态，且区分现状/目标）：
  - 标准目录改两根布局：`datasets/{asset}/sourcedata/original_uploads/sub-XXX/upload-{seq}/` + `derivatives/canonical-fif/...` + `qc/` + `releases/`，**取消 `versions/working/`**（设计源 §6.2 §7.4 §9）。
  - 新增「现状债」小节（仿 4-60 顶部 warning）：列 FIF 双写、raw_bids 幽灵、一文件多行三债 + 标"目标方案见 §2/§10/§11"。
  - §4 canonical FIF：补「现状=双写、目标=单写 derivatives + 单指针（`recordings.fif_path` 与 `dataset_files.canonical_fif` 同指）」。
  - §5 导入流程规则：把 `raw_bids_data` 从"已登记"改为"目标删除、由 bids-tree 现算"；登记角色改为 `original_upload`(一文件一行) + `canonical_fif` + `canonical_fif_provenance` + `sidecar`。
  - §8 落地状态表：明确"双写仍在 / working 目录仍在 / raw_bids 行仍在"是**现状**，目标态另起一栏或加规划标注。
- **验收点**：
  - [ ] 全页无任何把"两根存储 / 单写 / 角色精简"写成"已实现"之处。
  - [ ] 目录树与设计源 §9 总图一致（含 `releases/`、无 `versions/working/`）。
  - [ ] 双写/幽灵/多行三债被显式点名，且链到目标方案对应节。
- **风险**：4-20 与 4-60/4-70/4-00 互引，改完目录树需同步它们（见 T3-T5），否则跨页矛盾。**缓解**：T2-T5 作为一组连改、最后统一 `mkdocs build`。

---

### T3 🚧 回改 `wiki/docs/4-70-文件角色URI与追溯关系.md`（角色精简核心页）

- **文件**：`wiki/docs/4-70-文件角色URI与追溯关系.md`。
- **现状债定位**：
  - §2 角色表（行 31-50）列 13 个角色，含 `raw_bids_data`/`raw_bids_eeg_json`/`raw_bids_channels`/`raw_bids_events`/`raw_bids_electrodes`/`raw_bids_coordsystem` 等**幽灵角色**，且行 50 写"已写入 `raw_bids_data`…等标准角色"。
  - §4 URI 规则示例用 `versions/v0001/raw_bids/...` 逻辑 URI（行 75）——含已取消的 versions 层。
  - §5 provenance 字段映射全部指向 `raw_bids_*` 角色（行 114-123）。
- **改成什么**：
  - §2 角色表改为目标方案 §10 的**精简表**：保留 `original_upload`/`canonical_fif`/`canonical_fif_provenance`/`sidecar`/`qc_report`(+`*_summary`) 5 类有物理；显式列 `raw_source`/`raw_bids_*` 为**删除（幽灵，改 bids-tree 现算）**，给删除理由。
  - §4 URI 示例去 `versions/v0001/` 层，改 `elys://datasets/{asset}/derivatives/canonical-fif/...`，对齐两根布局。
  - §5 provenance 映射：source 改指 `original_upload`（不再有 `raw_bids_data` 行）；补"目标删除 `legacy*` 字段"（设计源 §12.2）。
  - §6 派生关系示例 `canonical_fif generated_from raw_bids_data` 改为 `generated_from original_upload`（幽灵行删除后来源直接是原件）。
- **验收点**：
  - [ ] 角色表与目标方案 §10 完全一致（5 留 + 4 删，留删都有理由）。
  - [ ] 无任何"已写入 raw_bids_* 角色"的现状陈述（那是要删的）。
  - [ ] sub-093 三件套"9 行→3 行"的去重效果在页内有说明。
- **风险**：4-70 被 3-40/4-40 引用其角色清单；删角色后那两页若仍引旧角色会矛盾。**缓解**：T3 完成后用 Grep 全库搜 `raw_bids_data`/`raw_bids_channels` 残留，列入 T9 兜底清扫。

---

### T4 🚧 回改 `wiki/docs/4-60-raw_bids视图与零复制策略.md`（强化"纯逻辑视图"定位）

- **文件**：`wiki/docs/4-60-raw_bids视图与零复制策略.md`。
- **现状**：✅该页**已有** 顶部 `!!! warning 实现现状`（行 15-18），已说明只落地 §2.1 纯逻辑视图、硬链接/reflink/storage_objects 是规划——**方向正确，不推翻**。
- **改成什么（增量对齐）**：
  - 补一条：目标方案 §4/§11/F-4 已**拍板**——现阶段 raw_bids **不落 `dataset_files` 行**（连逻辑行都不建），完全由 `GET /bids-tree` 从 `original_upload`+`sidecar` 行**实时投影**（设计源 §11 §25）。这比"逻辑行 storage_uri 指回原件"更进一步：连幽灵行都消灭。
  - §6「MVP 建议」里"`dataset_files.file_role` 细分为…`raw_bids_data`、`raw_bids_*`"一句**需改**：那是旧方向，现决策是不建这些行。改为"角色精简见 4-70 + 目标方案 §10"。
  - 硬链接 / reflink / `storage_objects` 维持"留作 4-60 未来优化（F-4）"——不删，仍是规划。
- **验收点**：
  - [ ] 页内不再建议"细分 raw_bids_* 角色行"（与 4-70 精简表一致）。
  - [ ] 明确"现阶段决策 = 不建 raw_bids 任何 dataset_files 行，bids-tree 实时投影"。
  - [ ] 硬链接/对象引用仍清楚标"规划/未来优化"。
- **风险**：低。该页本就标了规划，主要是把"细分 raw_bids 角色"这一旧 MVP 建议纠偏。

---

### T5 🚧 回改 `wiki/docs/4-00-文件管理总览.md`（存储总图：三根→两根）

- **文件**：`wiki/docs/4-00-文件管理总览.md`。
- **现状债定位**：
  - §3 标准目标目录（行 48-73）有 `versions/working/` 层 + 行 75 写"当前代码仍保留 `STUDIES_DIR` 和 `studies.data_root` 兼容旧上传"——即**三根并存**现状。
  - §4 三层表 + §6 落地快照「canonical FIF…保留旧 `fif_path/fifdata` 兼容」、「Raw BIDS 逻辑视图…已完成基础登记」。
- **改成什么**：
  - §3 目录树改两根：删 `versions/working/`、删 legacy `studies/{id}/` data_root 描述；datasets 下直接 `sourcedata/` + `derivatives/canonical-fif/` + `qc/` + `releases/`。**现状仍三根**则在"一句话现状"或落地快照里标"目标两根、legacy data_root 待 DROP（规划）"，不写成已删。
  - §4 三层表把 `raw_bids` 一行从"权威入口（物理层）"改为"逻辑视图（不落盘、bids-tree 现算）"。
  - §6 落地快照：fifdata 双写、legacy data_root 标"现状保留、目标删除"。
- **验收点**：
  - [ ] 目标目录树 = 两根，且与 4-20/设计源 §9 一致。
  - [ ] "三根并存"明确标为**现状债**、"两根"标为**目标**，不混淆。
- **风险**：4-00 是文件章入口页，措辞被多页引；与 4-10（存储原则）可能有 `study://` legacy scheme 描述需呼应。**缓解**：仅改目录布局与三层定位，legacy scheme 解析细节留 4-10 不动，避免越界。

---

### T6 🚧 回改 `wiki/docs/3-25-数据集生命周期与发布机制.md`（两轴模型 + 描述可编辑性 + tab）

- **文件**：`wiki/docs/3-25-数据集生命周期与发布机制.md`。
- **现状定位**：该页是**生命周期设计页**，已有 draft/published/withdrawn 状态机（§3）、SemVer（§5.3）、撤回（§5.1）、跨 Study 引用门控（§2）——**骨架对，但缺三块本轮新增**：
  1. **可见性与发布"解耦"**：现页把"发布"和"跨 Study 可见"绑在一起（§2 设计原则 4「跨 Study 引用受发表门控」）；目标方案 §4 强调**发布 ≠ 公开**，发布只冻结、可见性单独配。
  2. **可见粒度 disclosure_level**：现页**完全没有**"仅描述 / 数据本身"两层访问闸概念。
  3. **§4.6 描述可编辑性**：发布前可改 / 发布后只读 + addenda 追加需管理员审核 / 描述结构化（摘要/方法/使用/伦理）——现页**无**。
- **改成什么**：
  - 新增「§ 状态两轴 + 粒度」节：轴 A 发布状态（未发布/已发布/已撤回）× 轴 B 可见性（私有/共享/公共）× 粒度 C（metadata/data），铁律"未发布⟹私有"、"发布≠公开"、"两道访问闸"（设计源 §4.1-4.4）。**全标"规划：业务流转未接通"**（现状只有 schema 字段 + 发布自动 private→shared 的部分逻辑）。
  - 新增「描述可编辑性与结构化」节：发布前可改 / 发布后只读 / addenda 审核 / 描述四段结构（设计源 §4.6）；DB 落点 `dataset_versions.metadata_json.description{abstract,methods,usage,ethics}` + `dataset_addenda`(或 `metadata_json.addenda[]`)，**标"字段需加、规划"**。
  - 新增「页面信息架构（状态自适应 tab）」节或链到新页（见 T10）：发布前 4 tab + 概览发布按钮；发布后 概览/数据详情 + 管理分享按钮（设计源 §28）。
  - 现有 §2 原则 4 加注：可见性与发布**解耦**（发布只冻结不公开），与目标方案 §4 对齐；`DatasetVersion.state` 沿用、`dataset_assets.visibility` 沿用、`disclosure_level` **新增字段（规划）**。
  - F-9 撤回保留为发布状态轴第 4 终态——与现页 §5.1 一致，确认沿用。
- **验收点**：
  - [ ] 两轴 + 粒度模型完整且**全标规划/字段需加**，未写成已实现。
  - [ ] "发布 ≠ 公开"、"两道访问闸"白纸黑字。
  - [ ] §4.6 描述规则（只读+追加审核+结构化四段）入页，DB 落点标规划。
  - [ ] 不破坏该页既有 draft/published/withdrawn 已落 schema 的现状陈述。
- **风险**：3-25 是**设计页**，混杂"已落 schema（Phase 1）"与"未接业务（Phase 3-4）"；新增内容若不分清，会让读者以为两轴+粒度已可用。**缓解**：严格复用该页既有的"`!!! success 已落 / !!! info 规划`"分区写法。

---

### T7 🚧 回改 `wiki/docs/3-50-协作权限状态与迁移.md`（状态枚举对齐两轴 + 粒度）

- **文件**：`wiki/docs/3-50-协作权限状态与迁移.md`。
- **现状定位**：§2 状态枚举表（行 30-41）已收敛 `dataset_assets.visibility = private/shared/public`、`dataset_versions.state = draft/published/withdrawn`（隐含）；§3 提到 `dataset_members` "MVP 可先由 Study 挂载权限覆盖"。
- **现状债**：
  - 枚举表**缺** `disclosure_level`（可见粒度）这一新增字段。
  - `visibility` 行注释写"发布时自动 `private→shared`"——对，但**未表达粒度第二闸**与"发布≠公开"。
- **改成什么**：
  - §2 枚举表新增一行 `dataset_assets.disclosure_level = metadata / data`（默认 metadata），注"可见粒度第二闸；**字段需加、规划**"（设计源 §4.3 §4.5）。
  - `visibility` 行注释补："发布只冻结、默认仍 private；公共/共享是发布后单独配（发布≠公开）"。
  - §3 Dataset 级权限：`dataset_members` 按收件人记 (范围 + 粒度)，对齐目标方案 F-7「MVP 用发布+挂载间接共享、真授权列下一阶段」——标规划。
  - §1 审计表：补"发布 / 撤回 / 紧急下架 / 可见性变更 / 粒度变更 / addenda 审核 必留痕"（设计源 §4.5 §20 §24），标"落地时覆盖、规划"。
- **验收点**：
  - [ ] 枚举表含 `disclosure_level` 且标规划。
  - [ ] `visibility` 注释体现两闸 + 发布≠公开。
  - [ ] 审计覆盖项与目标方案 §20/§24 一致。
- **风险**：低。该页是枚举速查表，主要做加行 + 注释，不动既有已落字段语义。

---

### T8 🚧 回改 `wiki/docs/6-05-信息分级与领域语言.md`（徽章：单轴→两轴+粒度小标）

- **文件**：`wiki/docs/6-05-信息分级与领域语言.md`。
- **现状定位**：§4.1 数据集字段分级 + §6.2 DatasetsPage 改动清单，**当前口径 = "B 方案：徽章用可见范围单轴 `visibility`"**（行 74、246，已落地 2026-06-07）。这是上一轮决策，**与本轮目标方案 §4.5/§15 的"双层徽章"有出入**——目标方案要 **发布状态 + 可见性 + 粒度小标** 三件，而非单轴。
- **关键判断**：这是**口径演进**，不是推翻。本轮目标方案把状态拆得更细（两轴+粒度），6-05 需从"单轴徽章"升级为"双层徽章 + 粒度小标"，但**保留**上一轮已确立的两条铁律：①可见范围是发表级严肃决策、②徽章只读不提供随意下拉（设计源 §4.5/§15 也认同"发布≠随意"）。
- **改成什么**：
  - §4.1「可见范围」行 + §6.2 徽章行：从"单轴 `visibility`"升级为「**发布状态（未发布/已发布）+ 可见性（🔒私有/👥共享/🌐公共）+ 粒度小标（仅描述/含数据）**」三件（设计源 §4.5 🖥️ + §15）；明确未发布时可见性锁"私有"置灰。
  - 新增字段分级行：`disclosure_level`（可见粒度）→ L1 粒度小标；描述结构化四段（摘要/方法/使用/伦理）→ L1/L2；发布后「出身/引用/许可」→ L1（仅发布后出现）；「文件数/标准 FIF 数」→ 概览 L1 只读（设计源 §4.6 F-13）。注意这与现页 §4.1「文件总数…→ L3」的旧规则有**精修**：目标方案 F-13 要"文件数 + 标准 FIF 数"在概览只读可见（一览概要），需在页内厘清"概览只读展示 N 文件/N FIF" vs "L3 折叠区的全套文件统计"的区别。
  - §6.2 改动清单新增「状态自适应 tab」「描述结构化卡」「分享设置面板（发布后）」「他人视角 metadata-only 申请访问」条目，**全标规划/目标态**（设计源 §28 §4.6 §15）。
  - **保留**上一轮"徽章只读 + tooltip、不提供随意下拉、可见范围随发布/撤回流程联动"的铁律（与目标方案不冲突）。
- **验收点**：
  - [ ] 徽章描述从单轴升级为"两轴 + 粒度小标"，且新增项标规划。
  - [ ] "文件数/FIF 数 概览只读" vs "全套文件统计 L3" 厘清，不自相矛盾。
  - [ ] 上一轮"不提供随意下拉"铁律保留，未被本轮覆盖掉。
- **风险**：**口径冲突高危**——6-05 与 9-02 已写死"单轴 B 方案、2026-06-07 已做"。若直接改 6-05 正文成"两轴"，会与 9-02 历史记录矛盾。**缓解**：6-05 正文写**目标态两轴**并注"由 2026-06-08 目标方案演进，单轴是过渡态"；**不改** 9-02 已有历史条目（历史就是历史），只在 9-02 新增一条 2026-06-08 记录承接演进（见 T11）。

---

### T9 🚧 回改 `wiki/docs/6-20-研究项与数据管理.md`（DatasetsPage 信息架构：加状态自适应 tab）

- **文件**：`wiki/docs/6-20-研究项与数据管理.md`。
- **现状定位**：§四「数据集管理 `/datasets`」（行 171-246）现在写的是 **Dataset-first 上传流程**（BidsUploadPanel、BIDS 自动分组、上传状态机、重传）——**完全没有** 目标方案 §28 的"状态自适应 tab（发布前 概览/采集记录/导入/质控 + 发布按钮；发布后 概览/数据详情 + 管理分享按钮）"页面信息架构。
- **改成什么**：
  - §四新增「页面信息架构（状态自适应，目标态）」小节：照搬目标方案 §28.1 状态×角色矩阵 + §28.2 关键规则（tab 跟版本状态、发布=概览按钮、管理/分享=概览按钮、导航不蒸发、质控双向可达）+ §28.3 各 tab 职责；**全标"规划，未实现"**，并链 UI 基准 `design/mockups/datasets-workbench-demo.html`。
  - §4「两桶」呈现：补"文件管理器只管 📥原始 + ✨标准 FIF 两桶，派生属研究项不进此页"（设计源 §5 §15）。
  - 现有上传流程内容（4.3-4.6）**保留**（那是真实现状），只在节首标"以下是当前 Dataset-first 上传现状；目标态页面信息架构见上方新小节"。
  - 文件总览页 `6-00`：✅ 路由表 `/datasets` 现状无需大改，仅在 5.1 节可补一句"目标态将引入状态自适应 tab（见 6-20 + 3-25）"，**标规划**——属轻量补注，可选。
- **验收点**：
  - [ ] §28 状态×角色矩阵 + tab 职责入页，全标规划。
  - [ ] 现有上传流程现状不被删，且与目标态分区清楚。
  - [ ] 链到 UI mockup 与 3-25 生命周期页。
- **风险**：6-20 现状是"上传页"，硬塞"完整生命周期工作台"会让页面定位漂移。**缓解**：用"现状 / 目标态"分区，目标态部分只描述信息架构、链到 3-25 与 mockup，不在此页展开状态模型细节（避免与 3-25 重复维护）。

---

### T10 🆕 评估并（建议）新建一页《数据集页面与生命周期设计》

- **判断结论**：**建议新建一页**。理由：
  - 目标方案的"状态自适应 tab（§28）+ 两轴粒度 UI 映射（§4.5/§15）+ 描述结构化与可编辑性（§4.6）+ 两桶呈现（§5/§15）"是一套**完整的页面级设计**，横跨 DB/后端/前端三层语义，**塞进任何单页都会撑爆**：放 3-25 会让"数据库生命周期页"混入大量前端 IA；放 6-20 会让"上传页"承载状态模型；放 6-05 会让"信息分级规范页"变专题设计。
  - 现 wiki **缺一页**专门回答"数据集这一个对象，从未发布到发布的页面长什么样、各状态看到什么"。这正是 mockup `datasets-workbench-demo.html` 对应的设计。
- **建议页号与命名**：`6-25-数据集工作台与生命周期页面设计.md`。
  - 选 **6-25**：6 章是前端页面章；6-20 是"研究项与数据管理（含 DatasetsPage 上传现状）"，6-25 紧随其后承载"数据集工作台**目标态**完整设计"，号段语义连贯（25 居 20 与 30 之间，留扩展位）。
  - 备选 6-06（紧跟 6-05 信息分级），但 6-0x 段更像"全局规范"，6-2x 更贴"具体页面"，故首选 6-25。
- **mkdocs.yml nav 插入位置**：`wiki/mkdocs.yml` 第 6 章 `6 前端页面` 组内，**`研究项与数据管理: 6-20-...` 之后、`工作流与执行: 6-30-...` 之前**插入：
  ```yaml
      - 数据集工作台与生命周期页面设计: 6-25-数据集工作台与生命周期页面设计.md
  ```
- **新页内容大纲**（全标目标态/规划）：
  1. 定位与 UI 基准（链 mockup）；2. 状态×角色矩阵（§28.1）；3. 四 tab vs 数据详情职责（§28.3）；4. 双层徽章 + 粒度小标（§4.5/§15）；5. 描述结构化与可编辑性（§4.6，含四段 + addenda 审核）；6. 两桶文件呈现（§5/§15）；7. 治理动作位置（发布/管理分享恒在概览右上）；8. 他人视角与申请访问（§4.5/§15）；9. 与 3-25/6-05/6-20/4-20 的关系。
- **验收点**：
  - [ ] 给出明确页号（6-25）+ 命名 + nav 插入坐标（6-20 与 6-30 之间）。
  - [ ] 大纲覆盖 §28 + §4.5 + §4.6 + §5/§15 且**整页标"目标态设计，未实现"**。
  - [ ] 与 3-25（状态模型事实源）、6-05（信息分级规范）分工不重叠：本页只画"数据集这一页怎么长"，状态语义引 3-25、分级规范引 6-05。
- **风险**：新页与 3-25/6-05/6-20 **职责重叠** → 四份维护。**缓解**：新页定位为"页面设计专题（聚合视图）"，状态/分级/上传细节**只链不抄**；并在 4 页互相加"相关页面"链。是否真建由用户拍板（本计划只给建议 + 落点）。

---

### T11 🆕 起草更新日志条目（9-00 周概览 + 9-02 六月明细）

- **文件**：`wiki/docs/9-00-更新日志.md`（标题级周概览） + `wiki/docs/9-02-2026年6月.md`（明细）。
- **9-00 周概览**（插入「2026-06-01 ~ 06-07」周块**之上**或新建「2026-06-08」起的承接条；按现有倒序习惯，新条放当周块顶部。**注意 06-08 属同一自然周 06-08~06-14，需新建周标题** `### 2026-06-08 ~ 06-14 → 详见 9-02`）：
  - 决策：数据集存储/状态全面重构方案落档——三根存储→两根、FIF 双写→单写、raw_bids 幽灵→纯逻辑视图、文件角色精简（一文件一行）、状态从单轴升级为**两轴 + 可见粒度**（发布状态 × 可见性 × 仅描述/数据本身，发布≠公开）、描述结构化 + 发布后只读+追加审核、页面状态自适应 tab；产出可入库设计稿 + 回改 4-20/4-70/4-60/4-00/3-25/3-50/6-05/6-20，（建议）新建 6-25 数据集工作台页面设计页（详见 9-02）。**全条强调"设计落档 + 文档对齐，代码落地属规划"**。
- **9-02 六月明细**（倒序置顶，按现页"## 日期 · 主题"格式，写 1-2 条）：
  - **`## 2026-06-08 · 数据集重构方案落档：两根存储 / 单写 FIF / raw_bids 逻辑视图 / 两轴+粒度状态 / 状态自适应 tab`**：背景（28 节目标方案 + UI mockup）、改了什么（产出设计稿 `日志/2_meeting260607/wiki草案-数据集设计.md` + 回改 8 页 wiki + 建议新建 6-25）、**未做/后续（FIF 单写、角色精简、disclosure 字段、发布/撤回业务流转、状态自适应 tab、描述结构化全是代码侧规划，本轮仅文档对齐）**、Why（让 wiki 反映目标态、消除三大债的文档欠账、为各轨道落地提供单一口径）、相关事实源（`日志/2_meeting260607/文件管理目标方案.md`、`design/mockups/datasets-workbench-demo.html`、`backend/app/routers/datasets.py`）。
  - 承接 6-05 口径演进的小条（可并入上条）：状态徽章由 2026-06-07 的「单轴可见范围」演进为目标态「两轴 + 粒度小标」，**不改 06-07 历史条目**，仅记演进方向（标规划）。
- **验收点**：
  - [ ] 9-00 只放**标题级**周概览（不堆字段级 diff），且新建了 06-08~06-14 周标题。
  - [ ] 9-02 明细按"日期·主题"格式、含"未做/后续"且把代码能力全标规划。
  - [ ] 更新日志条目里**无一处把规划写成已实现**。
  - [ ] 9-00 顶部 `<div class="elys-meta"> 更新:` 日期刷新为 2026-06-08。
- **风险**：易把"设计落档"误写成"重构完成"。**缓解**：条目统一用"方案落档 / 文档对齐 / 设计稿"等措辞，动词不用"完成重构/已单写/已精简"。

---

### T12 ✅→🚧 静态校验与跨页一致性收口（mkdocs build + 残留清扫）

- **做什么**：
  - 全库 Grep 残留：搜 `raw_bids_data`、`raw_bids_channels/events/eeg_json`、`versions/working`、`fifdata` 镜像、`三根/三个存储根` 等旧表述，凡仍把幽灵/双写/三根当现状的页面，列清单（可能波及 3-20/3-40/4-40 等本轨道外页，**只列不擅改**，交对应轨道或用户确认）。
  - 跑 `cd wiki && mkdocs build --clean`（本机有 mkdocs，见 MEMORY），确认无新坏链（跨目录代码链接 WARNING 是常态可忽略）。
- **验收点**：
  - [ ] `mkdocs build --clean` 通过（无新增 ERROR / 坏链）。
  - [ ] 新页 6-25（若建）已进 nav 且能 build 出。
  - [ ] 残留旧表述清单已产出（供后续/其它轨道）。
- **风险**：本机无 Python 运行环境但 mkdocs 可用（MEMORY 记载）；若 mkdocs 调用受 Anaconda 环境影响，回退为"仅静态人工核对坏链"。**缓解**：MEMORY 已记 mkdocs 部署方式，按其执行。

---

## 2. 对外接口 / 依赖契约

> 本轨道是**文档轨道**，本身不产出代码接口；这里登记的是「文档要忠实记录的对外契约」+「本轨道依赖其它轨道交付的事实」。

### 2.1 本轨道依赖（上游：DB / 后端 / 前端轨道的落地结论）

| 依赖项 | 来自轨道 | 文档需要的事实 | 现状 |
|---|---|---|---|
| `dataset_assets.disclosure_level` 字段最终是否新增、枚举值 | DB / 后端轨道 | 决定 3-50 枚举表、3-25/6-05 粒度描述怎么写 | 🚧 字段未加（规划），文档先标规划 |
| `dataset_versions.metadata_json.description{abstract,methods,usage,ethics}` 结构 | DB / 后端轨道 | 决定 3-25/6-25 描述结构化怎么落 | 🚧 规划 |
| `dataset_addenda`(或 `metadata_json.addenda[]`) 形态 + 审核流 | DB / 后端轨道 | 决定 3-25/6-25 "发布后追加需审核" 怎么写 | 🚧 规划 |
| FIF 单写 / 角色精简 / raw_bids 不建行 是否落地 | 后端轨道 | 决定 4-20/4-70/4-60 从"现状债"改成"已实现"的时机 | 🚧 当前仍双写/有幽灵行，文档保留"现状债 + 目标态"双写法 |
| `GET /dataset-assets/{id}/archive` 打包下载接口 | 后端轨道 | 决定 4-20 §13 表从 ❌待补 改 ✅ 的时机 | ❌ 未实现，标规划 |
| 状态自适应 tab / 双层徽章 / 分享设置面板 前端落地 | 前端轨道 | 决定 6-05/6-20/6-25 从"目标态"改"已实现"的时机 | 🚧 规划 |
| publish/withdraw/可见性变更/addenda 审核 业务流转 | 后端轨道 | 决定 3-25 状态机从"骨架"改"已接通" | 🚧 部分骨架（`publish_dataset_version` 等），文档标规划 |

### 2.2 本轨道交付物（下游：供其它轨道 / 用户消费）

| 交付物 | 路径 | 用途 | 状态 |
|---|---|---|---|
| 可入库设计稿草案 | `日志/2_meeting260607/wiki草案-数据集设计.md` | 各轨道落地 + wiki 回改的**单一口径来源** | 🆕 本批已产出 |
| 本开发计划 | `日志/2_meeting260607/开发计划-文档.md` | 文档轨道任务清单 + 验收 | 🆕 本文件 |
| 回改后的 8 页 wiki + 建议新页 6-25 | `wiki/docs/4-20,4-70,4-60,4-00,3-25,3-50,6-05,6-20,(6-25)` | 让 wiki 反映目标态、消三大债欠账 | 🚧 待执行（T2-T10） |
| 更新日志条目 | `wiki/docs/9-00, 9-02` | 记录"方案落档 + 文档对齐" | 🚧 待执行（T11） |

### 2.3 文档须忠实记录的"对外接口契约"（来自目标方案，文档不发明、只转录）

> 这些是后端轨道的接口契约，文档（4-20 §13、3-25、6-25）需如实登记其"状态"，**不得把规划接口写成已实现**：

- 访问 / 下载（设计源 §13）：`GET /dataset-assets/{id}/files`✅、`/bids-tree`✅、`/dataset-files/{id}/metadata|preview|download`✅、`/dataset-assets/{id}/archive?scope=subject|all|bids`❌规划。
- 发布状态轴（设计源 §20）：`POST /dataset-assets/bootstrap`✅、`/{id}/publish`🚧骨架、`/{id}/versions`✅、`…/withdraw`✅、`…/withdraw/review`✅、`…/emergency-takedown`✅。
- 可见性 / 粒度轴（设计源 §20，**仅已发布后允许**）：`PATCH /dataset-assets/{id}` {visibility, disclosure_level}🚧、`POST/DELETE …/members`🚧、`POST …/addenda`🚧、`POST …/addenda/{id}/review`🚧。
- 前端契约铁律：前端只拿 `dataset_file_id`，**绝不接触** `file://`/`s3://`/服务器绝对路径（设计源 §13、4-70 §4）——文档须在 6-05/6-20/6-25 强调。

---

## 3. 风险总览（文档轨道专属）

| 风险 | 影响 | 缓解 |
|---|---|---|
| **把规划写成已实现**（最高危） | 误导后续接手者/模型，违反 AGENTS.md 状态语义 | 每页/每节标"现状 vs 目标态"；动词避免"已单写/已精简/已完成"；复用 4-60/3-25 既有 warning/info 分区写法 |
| **6-05 单轴 vs 本轮两轴口径冲突** | 6-05 正文与 9-02 历史记录打架 | 6-05 写目标态两轴+注"单轴是过渡"；不改 9-02 历史、仅新增 06-08 演进条 |
| **跨页矛盾**（4-20/4-70/4-60/4-00 目录树与角色表不一致） | 读者看到三套布局 | T2-T5 连改一组、最后统一 mkdocs build + Grep 残留清扫（T12） |
| **新页 6-25 与 3-25/6-05/6-20 重复维护** | 四份维护、易漂移 | 6-25 定位"页面设计聚合视图"，状态/分级/上传只链不抄；是否真建由用户拍板 |
| **越界改到本轨道外页**（3-20/3-40/4-40 等含幽灵角色残留） | 改了别人轨道的页 | T12 只"列清单不擅改"，交对应轨道/用户 |
| **mkdocs 本机环境** | 无法 build 自查 | MEMORY 记载 mkdocs 可用；不可用则回退人工核对坏链 |
| 设计稿/计划与其它轨道（DB/后端/前端）口径分叉 | 各轨道按不同理解落地 | 三份计划共用同一设计稿（`wiki草案-数据集设计.md`）+ `文件管理目标方案.md` 为唯一事实源 |
